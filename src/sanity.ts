/**
 * Placeholder module so the toolchain (`pnpm verify`) has real source to
 * typecheck, test, and cover at 100%. Library logic lands in the MVP phases.
 */
export const sanity = (): true => true;
