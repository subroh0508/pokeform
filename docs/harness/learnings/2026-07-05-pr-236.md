---
id: learning-pr-236
title: PR #236 レトロスペクティブ (castform-normal の ja を括弧合成へ MANUAL_NAME_OVERRIDE で補正)
type: learning
status: draft
related_pr: 236
related_plan: —
generated_at: 2026-07-18T18:17:35Z
generator: pr-retrospective skill
---

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/236 / commit 24a70e8f0fcd1b106b8913a97a13eff2fe6f0f0d / merge 2026-07-05T20:40:28Z / 差分 1 files +3/-0

## ✅ Keep（継続したいこと）

- **設計済みの例外機構へ最小差分で寄せる**: 新規ロジック・分岐を足さず、`fetch-pokeapi.ts` の `MANUAL_NAME_OVERRIDE`（合成結果より優先の per-id 例外機構）へ 1 エントリ追加するだけで対処。既存の `tauros-paldea-*-breed` と同じ「ja のみ override / en は合成へ委ねる」パターンに揃え、gold-plating せずユーザー指摘の 1 件を正確に潰した。
- **バグ機序を正確に言語化して docstring へ残した**: `castform-normal` の `form_names.ja` が base 名「ポワルン」を含む「ポワルンのすがた」ゆえ **passthrough 短絡が誤発火**し括弧合成（`base名（form名）`）が効かない、という原因を docstring にコロケーション。他の「〜のすがた」形（アローラ / ガラル / ヒスイ）は実 descriptor を持つのに対しこれは base 名直付けの default form 名、という差異も明記し、なぜ独立名として不適切かを後続保守者が辿れる。
- **en を合成へ委ねる判断の根拠を記録**: `Castform (Normal)` は合成で正しく出るため ja のみ override、と docstring に理由を残した。over-override を避け、合成が正しく効く部分は機構に任せる原則を維持。
- **coverage 追従要否を機械的に判断**: `scripts/**` は `vitest.config.ts` の `include: ["src/**"]` 外で coverage 対象外、かつ `composeFormName` は既に `src/codegen` で網羅済み、という 2 点から test 追従不要を根拠付きで確定した（薄い層は明示除外の testing 規約に沿う）。
- **CI 赤の切り分けを PR 本文で事前開示**: main が author-static-data 動作確認のため languages を意図撤去した from-scratch 状態にあり、既存の missing-module 赤は本変更と無関係、反映は `pokeapi-names.yml` 再 dispatch の languages 復元 PR で full verify 緑になる、と本文へ明記。レビュー側の誤解を先回りで防いだ。

## ⚠️ Problem（詰まった点・制約）

- **PR 単独の CI が from-scratch 赤で緑にならない**: main が languages 撤去状態のため、本 PR の `pnpm verify`（CI run 28754115332）は `src/generated/languages/*.ts` の `Cannot find module`（TS2307）で exit 2 失敗。scripts のみの正しい修正でも CI 単独では正しさを示せず、変更の妥当性が本文説明とパイプライン外知識に依存した。
- **機械ゲートを `--no-verify` でバイパスせざるを得なかった**: pre-commit / pre-push の typecheck ゲートが同じ from-scratch 赤で落ちるため `--no-verify` で通過（languages 撤去コミットと同じ扱い）。ゲートバイパスが「main を意図的に赤い状態に置く運用」中は常態化し、本来ゲートで拾うべき別の破壊を見逃すリスクがある。
- **fix と反映が非同期**: override の実効（ja が `ポワルン（ポワルンのすがた）` に直る）は本 PR merge では起こらず、`pokeapi-names.yml` 再 dispatch による from-scratch fetch の languages 復元 PR を待つ必要がある。修正コミットと成果物反映が別 PR に分かれ、追跡に一手間かかる。
- **passthrough 短絡誤発火の網羅洗い出しは未実施**: 「base 名を含む default form 名」で括弧合成が効かない同型リスクは `castform-normal` 以外の default form にも潜在しうるが、今回は指摘された 1 件のみ対処。他候補の一括点検はされておらず、次に別の form で同じ whack-a-mole が起きうる。

## 🚀 Try（次回の改善案）

- **[skill] author-static-data の references に「passthrough 短絡誤発火」分岐と既知 override を明記**: `form_names.ja` が base 名を含む default form（例 `castform-normal`）は括弧合成が効かず `MANUAL_NAME_OVERRIDE` で `base名（form名）` を著述する、という分岐と既知エントリを skill から辿れるようにし、curated override の保守導線を作る。
- **[skill] 同型候補の一括洗い出し手順を追記**: passthrough 短絡が誤発火する潜在候補（base 名 prefix を持つ default form）を PokeAPI 全件 fetch から一度に列挙するチェックを author-static-data に足し、1 件ずつの whack-a-mole を避ける。
- **[rule] main を from-scratch 撤去状態に置く運用中の CI/ゲート制約を注記**: scripts のみの単発 fix PR の CI が missing-module で赤になり `--no-verify` バイパスが常態化する既知制約を data-pipeline か cli-and-io のどこかに明記し、「赤の切り分けを PR 本文で事前開示する」を既定運用として言語化する（今回の本文開示を再現可能に）。

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | fail | fail | — | tsc --noEmit（main の languages from-scratch 撤去による missing-module 赤・本変更と無関係） |
| カバレッジ | 100% | 100% | ±0 | Vitest（scripts/** は include:["src/**"] 外で対象外・composeFormName は src/codegen で網羅済） |
| Biome 違反 | 0 | 0 | ±0 | biome check |
| CI | fail | fail | — | server-side pnpm verify（run 28754115332・TS2307 languages 未生成赤・反映は pokeapi-names.yml 再 dispatch で緑） |
| 差分 files/行 | — | 1 files +3/-0 | — | gh pr diff（fetch-pokeapi.ts に override 1 エントリ + docstring 3 行） |

## 🤖 ハーネス改善提案

- [ ] `[skill]` author-static-data の references に「form_names.ja が base 名を含む default form は passthrough 短絡が誤発火 → MANUAL_NAME_OVERRIDE で括弧合成を著述」分岐と既知エントリ（castform-normal 等）を明記。
- [ ] `[skill]` author-static-data に passthrough 短絡誤発火の潜在候補（base 名 prefix を持つ default form）を全件 fetch から一括洗い出すチェック手順を追記（1 件ずつの whack-a-mole 回避）。
- [ ] `[rule]` main を from-scratch 撤去状態に置く運用中は scripts のみの fix PR の CI が missing-module で赤・`--no-verify` バイパスが常態化する制約を data-pipeline / cli-and-io に注記し、「赤の切り分けを PR 本文で事前開示」を既定運用として明記。

## 📝 harness-meta フィードバック

### 採用

### 見送り

### 保留
