---
id: learning-pr-117
title: PR #117 レトロスペクティブ (メガ関連データの決定論自動著述・ADR 0031 supersede)
type: learning
status: draft
related_pr: 117
related_plan: 03-survey-regulation-rework/phase-09
generated_at: 2026-06-14T16:43:50Z
generator: pr-retrospective skill
---

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/117 / commit 5b7e4f9 / merge 2026-06-14T16:43:50Z / 差分 15 files +338/-83。Serebii メガ form 名から `megaLinks` / メガ先種族 / per-reg `mega[]` / メガストーン `megaSpecies` を決定論で自動著述（`megaSpeciesId` 純関数）。ADR 0031 のメガ手動 linking を ADR 0033 で supersede し 0031 を archive へ退避。SKILL/references/rule を追従。

## ✅ Keep（継続したいこと）
- **「決定論変換できない」という前提を実データで再検証し、誤りと判明したら supersede した**。base slug 既知前提なら成立すると突き止め、ADR 0033 で 0031 を正面から覆した（不変ログを正しく更新）。
- **純関数 + 薄い orchestrator の層分離を踏襲**。`megaSpeciesId` / `megaStoneSpeciesId` / `megaAuthoring` をテスト済み純関数（カバレッジ100%）に置き、fs/YAML I/O は orchestrator（coverage 除外）に閉じた。正しさが層1 に宿り cross-agent で収束する。
- **クリーンスレート検証を committed HTML fixture から決定論で組んだ**。ネットワーク無しで `scrape:serebii`→使い捨てレギュ `serebii:catalog` を通し、per-reg `mega[]` / megaLinks / megaSpecies（新規 floettite→floette-mega）の自動著述と、authored 状態での git diff 空（冪等）を実証。PokeAPI 404 チェックだけ実機（HTTP 200）。
- **ADR supersede 規約を厳密に踏んだ**。0031 は status のみ更新（本文不変）・`git mv` で archive 退避・相対リンクを `../` 追従・README のアクティブ/アーカイブ表を整合・採番は active+archive の最大+1。
- **編集を小さく分割して適用**（純関数 → テスト → orchestrator → ADR → SKILL/references/rule）。SKILL.md / rule の大編集をストールさせず段階適用した。
- **両系統レビューを Agent で分担**（code-review = src/scripts・harness-review = ADR/SKILL/rule/docs）。Generator/Evaluator 独立を保ち、harness-review が archive 移動の dangling を捕捉した。

## ⚠️ Problem（詰まった点・制約）
- **ADR archive 移動の dangling リンク追従が漏れた**。rule/skill/ADR 0032/phase doc は追従したが、`.claude/skills/survey-regulation/workflows/*.workflow` のヘッダ 3 リンクが取りこぼし（本 PR で未編集のファイルが archive 移動で orphan 化）。harness-review が指摘し fix した。`.md`/`.workflow` 横断の全走査が必要だった。
- **未知 id ガードの強さをコメントで過大表現していた**。`isCatalogIdShape`（shape）を `normalizeAgainstCatalog`（membership 照合）と「同思想」と書いたが、後者の方が強い。code-review 指摘でコメントを正確化（shape ガードで membership は下流委譲）。
- **メガ著述の非対称性**（catalog megaLinks/メガ先は毎回・per-reg `mega[]` は新規種族のみ）が残る。既存尊重設計とは整合するが、メガ情報が初回種族著述より後に来ると per-reg `mega[]` を欠く不整合がありうる（non-blocking 指摘・将来の warn 明示候補）。
- **20種フル網羅のクリーンスレートはネットワーク/フィクスチャ制約で見送り**、charizard/garchomp fixture + PokeAPI 404 チェックに縮約した（lopunny の HTML fixture が無い）。決定論部分はこれで十分実証できたが、phase doc の「20種」とは縮約差がある。

## 🚀 Try（次回の改善案）
- ADR を archive へ `git mv` する PR では、**追従対象を `.md` に限らず `.workflow` / `.ts` まで含めて `git grep` 全走査**するチェックを `harness-review` checklist（または adr-new skill 手順4）へ明示追記する（今回 `.workflow` 取りこぼし）。
- 純関数のガード強度をコメントで他機構と比較する場合、**「同思想」でなく「shape ガード / membership は下流」のように強弱を明示**する書き方を [[type-conventions]] か code-review 観点に一文残す。
- CLI 表示や fixture を要する検証で**フル網羅が制約で縮約される場合、縮約理由と代替で担保した範囲を phase doc / PR に明記**する運用を徹底（learning #94/#96 のデータ完全性ノートと同系）。

## 📊 指標
| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit |
| カバレッジ | 100% | 100% | ±0 | Vitest（451/451 stmts・256/256 branches・92/92 funcs・375/375 lines） |
| Biome 違反 | 0 | 0 | ±0 | biome check（fix loop: coverage 1 分岐 → テスト追加で解消） |
| CI | pass | pass | — | server-side pnpm verify（26s・run 27505440161） |
| 差分 files/行 | — | 15 files +338/-83 | — | gh pr diff（ADR supersede + SKILL/rule 追従含む） |

## 🤖 ハーネス改善提案
- [ ] `[skill]` `harness-review` checklist（または `adr-new` 手順4）に「ADR を archive へ `git mv` する際は `.md` に限らず `.workflow` / `.ts` まで `git grep` で参照全走査し相対リンクを追従」を明示追記（今回 `.workflow` 3 件取りこぼし）。
- [ ] `[rule]` `code-review` 観点 or `type-conventions` に「ガード強度を他機構と比較するコメントは『同思想』で済ませず shape / membership 等の強弱を明示する」を一文追加。
- [ ] `[skill]` `implementation-workflow` rule の data 系注意点に「クリーンスレート検証がネットワーク/フィクスチャ制約で縮約される場合、縮約理由と代替担保範囲を phase doc / PR に明記」を追記。

## 📝 harness-meta フィードバック
### 採用
### 見送り
### 保留
