---
id: learning-pr-165
title: PR #165 レトロスペクティブ (ワークフロー系 skills を P1-P5 で圧縮)
type: learning
status: draft
related_pr: 165
related_plan: 07-rules-skills-simplify/phase-04
generated_at: 2026-06-22T17:17:42Z
generator: pr-retrospective skill
---

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/165 / commit a5c9ef342e88d37dd272d31328443a707e4ec5c3 / merge 2026-06-22T17:17:42Z / 差分 9 files +73/-154

## ✅ Keep（継続したいこと）

- **SoT 一本化（三重記述の解消）を圧縮の主軸に据えた**: supersede 手順 → `adr.md`、判定基準 → `harness-meta-criteria.md`、learning frontmatter/見出し → `retrospective-format.md`、6 基準 → `planning.md`、IW 不変条件 → `implementation-workflow.md` と、実体を rule 1 箇所へ寄せて skill をポインタ化した。冗長削減でなく「どこが正本か」を確定させる純改善になっている。
- **安全性記述を明示的に保全リスト化して圧縮した**: fix loop 上限3・worktree 0/9 ペア・未マージ強制削除禁止・auto-merge 委譲・redaction・撤去2段階・idempotent・見出し固定を「残す対象」として PR 本文に列挙し、圧縮で落とさないことを設計方針として宣言した。harness-review でも欠落ゼロが確認された。
- **skill 別の Before→After 行数 + 是正内容を表で可視化した**: 8 skill それぞれ「何行を何へ・なぜ圧縮したか」を 1 行で示し、836→755 行（-81）の内訳が追跡可能。レビュアが圧縮意図を skill 単位で照合できる。
- **adr-new の圧縮を「ポインタ化ついでの欠陥是正」に使った**: supersede を `adr.md` へポインタ化する際、旧来 skill に欠けていた「全 inbound 参照（.md/.ts・reference 式・素の番号）の追従」を明示し、dangling 防止ギャップを是正した（adr-new だけ +2 行で増加したのはこのため）。圧縮 = 単純削減でなく、整合性向上を兼ねている。
- **OVERVIEW の設計方針 5 点を「やりすぎ防止」ガードレールとして PR 本文に再掲した**: 「安全性記述を残す / trigger 精度維持 / references≠削除（dangling ゼロ） / front matter は 08 の責務で未変更」を明記し、スコープ越境（description や last_modified の改変）を自制した。
- **cross-agent symlink パリティを 8 skill 全件で確認した**: `.agents/skills/<name>` が `../../.claude/skills/<name>` を指す symlink であることを確認し、canonical 編集が Codex 側へ自動反映される構成を維持した（コピー同期の手作業ドリフトを回避）。

## ⚠️ Problem（詰まった点・制約）

- **複数 skill 一括圧縮は trigger 句の意図せぬ欠落を生みやすい**: pr-retrospective の description から口語 trigger「学びを残す」系の 1 句が圧縮で落ちた（harness-review nit）。中核 trigger は残ったが、8 ファイルを横断で削ると個々の trigger 精度への影響が見えにくく、under-trigger リスクが分散する。
- **具体例セクションの削除は progressive disclosure と情報欠落の境界が曖昧**: verify から「全緑 / 型 fail の出力サンプル」例節を削除。本文の定型記述で代替できるが、初見エージェントが出力整形を再現する手掛かりが減る。「重複 Gotcha 削除」と「有用な具体像の喪失」の線引きが判断ベースで、機械的に検証できない。
- **plan README の進捗チェックに順序逆転が残った**: Phase 4 を `- [x]` 完了にした一方 Phase 3（survey-regulation 圧縮）は `- []` 未完のまま（harness-review non-blocking）。phase-04 が依存なしで独立実施可のため設計上は許容だが、README 上の見た目の順序逆転は混乱を招く。
- **ポインタ化の正しさは「参照先 rule に実体があるか」の手動確認に依存する**: 6 本の skill→rule リンクが dangling していないこと、かつ参照先 rule に圧縮で消した内容の実体があることを、レビュアが行番号レベル（`adr.md:48-51` 等）で目視照合した。リンク解決の機械チェックはあるが「実体の同等性」までは機械化できていない。
- **圧縮 PR は diff の意味が「削除行」に偏り、レビュー負荷が高い**: -154 行のうち多くが文言短縮で、削除が「冗長除去」か「情報損失」かを 1 行ずつ判定する必要があった。機械ゲートでは一切捕れない層（仕様忠実性・安全性記述の保全）が全レビュー価値で、harness-review の目視に全面依存した。

## 🚀 Try（次回の改善案）

- **description（trigger 句）を圧縮スコープから明示的に除外する規約を立てる**: 本 PR は front matter を 08 の責務として未変更にしたが、本文圧縮の wrap 整形で description の trigger 句が落ちた。「skill 圧縮 PR では description 行を一切変更しない（trigger 改修は別 PR）」を skill-authoring に明文化すれば、under-trigger の混入を構造的に防げる。→ `[rule]`
- **skill→rule ポインタ化時の「実体存在チェック」を手順化する**: ポインタ化した内容が参照先 rule に行レベルで実在することを確認する手順を skill-authoring の圧縮節に追加。リンク dangling の機械チェックに加え、「移動先に実体があるか」を著者が自己点検するチェックリスト項目にする。→ `[skill]`
- **plan README の phase チェック順序逆転を検出する軽量チェックを足す**: 独立実施で前後する phase は許容だが、`- [x]` の後に `- [ ]` が並ぶ場合に「依存なし故の許容か」を確認するリマインダを finish-phase 手順4 に追加。→ `[skill]`
- **具体例の削除可否を判定する基準を skill-authoring に置く**: 「本文の定型記述で再現できる例は削除可 / 出力フォーマットの具体像など再現困難な例は references へ退避」の線引きを明文化し、verify のような「例削除が情報欠落寄り」になるケースの判断を揃える。→ `[rule]`

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit |
| カバレッジ | 100% | 100% | ±0 | Vitest |
| Biome 違反 | 0 | 0 | ±0 | biome check |
| CI | pass | pass | — | server-side pnpm verify |
| 差分 files/行 | — | 9 files | +73/-154 | gh pr diff |

## 🤖 ハーネス改善提案

- [ ] `[rule]` skill-authoring に「圧縮 PR では skill description（trigger 句）を変更しない（trigger 改修は別 PR）」を明文化し、本文 wrap 整形に伴う trigger 句の意図せぬ欠落（pr-retrospective で再発）を構造的に防ぐ。
- [ ] `[skill]` skill-authoring（または skill-creator）の圧縮節に「ポインタ化した内容が参照先 rule に行レベルで実在するか」の自己点検チェックリスト項目を追加し、dangling の機械チェックを超えた「移動先実体の同等性」確認を手順化する。
- [ ] `[rule]` skill-authoring に「例セクション削除の可否基準（本文で再現可なら削除可 / 出力フォーマット等の再現困難な具体像は references へ退避）」を明文化し、verify のような情報欠落寄り削除の判断を揃える。
- [ ] `[skill]` finish-phase 手順4 に「plan README の phase チェックボックスで `- [x]` の後に `- []` が並ぶ順序逆転を検知したら依存なし故の許容か確認する」リマインダを追加する。

## 📝 harness-meta フィードバック
### 採用
### 見送り
### 保留
