---
id: learning-pr-256
title: PR #256 レトロスペクティブ (author-static-data に Champions オリジナル名の Bulbapedia 手作業著述パスを定型化)
type: learning
status: draft
related_pr: 256
related_plan: —
generated_at: 2026-07-18T18:19:13Z
generator: pr-retrospective skill
---

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/256 / commit 1302bc1ab5f3103280a05901af07a6705dc768ba / merge 2026-07-16T15:04:20Z / 差分 3 files +46/-21

## ✅ Keep（継続したいこと）

- **KPT ループが 1 周して機能した**: learning #254 で摘出した skill ギャップ（`author-static-data` が PokeAPI 非存在の Champions オリジナル特性/技を扱えず、`eelevate`/`fire-mane` を場当たり的に Bulbapedia 著述した）を、本 PR が手順定型化で解消し #255 を Closes。learning → harness-meta 起票 → skill 改修の一方向フロー（ADR 0015）が具体 PR として閉じた。
- **harness-review の nit を同一 PR 内で解消**: レビューコメントの 2 nit（rule が skill 手順3 の内容を再記述している / 「現状 6 件」列挙が時点依存で陳腐化しうる）を follow-up commit（`9fb9c0b` レビュー nit 反映）で「rule を純ポインタ化・特性列挙を例示に緩和」として反映し、マージ前にレビュー往復を閉じた。最終 diff で rule 追記が純ポインタ（手順・出典は author-static-data 手順3 が SoT）になっていることを確認。
- **SoT を 1 箇所へ一本化**: 手順詳細（Bulbapedia 著述手順・`kebabId` 導出・取得元 category 表・欠落一括把握 diff）を `author-static-data` 手順3 に集約し、`author-regulation-data` は「PokeAPI 経路 / Bulbapedia 手作業パス」の分岐をポインタ + 委譲で記述、rule は純ポインタに留めた（skill-authoring の「rule は責務境界のみ・手順詳細は skill 本文」準拠）。3 箇所ドリフトの余地を最小化。
- **cross-agent パリティと再生成要否を明示判断**: `.agents/skills/{author-static-data,author-regulation-data}` は canonical への相対 symlink ゆえ自動反映、`data-pipeline.md` は body のみ変更で `paths` frontmatter 不変ゆえ rules-index 再生成不要、と根拠付きで判定（推測でなく機構理解に基づく）。
- **doc 引用の実在をレビューで裏取り**: harness-review が `kebabId`（`src/codegen/showdown/ids.ts`）・停止メッセージ `languages/<kind>: '<id>' missing ja/en`（`scripts/generate.ts:124`）・`isNonstandard: "Future"`（`scripts/showdown/dex.ts`）・列挙 6 特性の abilities.yaml 著述済を実ファイルで確認。skill が引く実装シンボル・throw 文字列が実体と一致することを機械ゲート外で担保。
- **欠落一括把握を定型として明文化**: `generate:data` が最初の欠落名で throw する挙動を根拠に、specs（seq / map）vs languages（map）の直接 diff で欠落 id 集合を一括確定する手順を skill 化（#254 で実践した whack-a-mole 回避を再現可能に）。

## ⚠️ Problem（詰まった点・制約）

- **手順の正しさは次回のデータ投入まで未検証**: 本 PR は「能力整備（手順定型化）が主眼」で実データ追加を伴わない（Champions オリジナル特性 6 件は既に辞書済・欠落ゼロ）。定型化した Bulbapedia 著述パスが実際に機能するかは、次に PokeAPI 非存在の新特性/技が showdown 更新で入るまで実地検証されない。
- **rule への手順詳細再記述が初版に混入**: 初版は rule（`data-pipeline.md`）にも取得元（Bulbapedia / 命名慣例）を再記述し、skill 手順3 と 2 箇所ドリフトの余地を作った。harness-review nit で摘出・同 PR で純ポインタ化したが、著述時に「rule は純ポインタ」を守れなかった（レビューで拾えたが著述段のチェックリスト化はされていない）。
- **時点依存のデータ状態記述が skill 本文に混入**: 「現状 6 件 = eelevate/fire-mane/... すべて著述済」の列挙は Champions が Future 特性を追加すると陳腐化しうる時点依存記述。例示明示で緩和したが、手順 skill に陳腐化しうる列挙を書く傾向（#253 の docstring blanket 断言と同根）は体系化された歯止めがない。
- **Serebii 第一優先 memory と Bulbapedia 採用の境界が暗黙**: 「Serebii first-priority Champions data」memory は解禁データ（技/持ち物/種族/メガの構造・legality）が対象、本変更は reg 非依存の名前辞書 ja/en（別 SoT）で、かつ Serebii の ability ページは ja 空ゆえ Bulbapedia を ja 源に採用。harness-review が矛盾なしと整理したが、「Serebii=解禁データ / 名前辞書 ja/en=別 SoT」の境界が memory / rule に明示されておらず、優先序列を混同する余地が残る。

## 🚀 Try（次回の改善案）

- **[skill] skill-authoring（または harness-review checklist）に「rule に手順詳細を再記述しない = rule は責務境界と skill へのポインタのみ」を著述時セルフチェックとして明記**する。今回 rule に取得元表相当を再記述した nit を、レビュー往復でなく著述段で防ぐ。
- **[skill] 手順 skill 本文に時点依存のデータ状態列挙（「現状 N 件」等）を書くときは「例示」と明示し、正本の gap 検出手順（specs vs languages 直接 diff）を併記する慣行を skill-authoring に一言添える**。#253 の docstring blanket 断言 refine と同系統の doc 陳腐化を体系的に防ぐ。
- **[rule] data-pipeline（または memory）に「Serebii = 解禁データ（roster/技/持ち物/メガ構造・legality）第一優先 / 名前辞書 ja/en は別 SoT（PokeAPI 全件 + PokeAPI 非存在の Champions オリジナル名は Bulbapedia）」の権威境界を 1 行で明示**し、取得元の優先序列と SoT レイヤの混同を防ぐ。

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit（doc のみ変更・コード不変） |
| カバレッジ | 100% | 100% | ±0 | Vitest（コード不変） |
| Biome 違反 | 0 | 0 | ±0 | biome check |
| CI | pass | pass | — | server-side pnpm verify |
| 差分 files/行 | — | 3 files +46/-21 | — | skill 2（author-static-data / author-regulation-data）+ rule 1（data-pipeline.md） |

## 🤖 ハーネス改善提案

- [ ] `[skill]` skill-authoring / harness-review checklist に「rule に手順詳細を再記述せず純ポインタに留める（手順詳細 SoT は skill 本文へ一本化）」を著述時セルフチェックとして明記し、rule×skill の 2 箇所ドリフトを著述段で防ぐ。
- [ ] `[skill]` skill-authoring に「手順 skill へ時点依存のデータ状態列挙（『現状 N 件』）を書くときは例示と明示し、正本の gap 検出手順（specs vs languages 直接 diff）を併記」を追記し、doc 陳腐化を体系的に抑える（#253 の docstring refine と統合可）。
- [ ] `[rule]` data-pipeline に「Serebii=解禁データ（構造/legality）第一優先 / 名前辞書 ja/en は別 SoT（PokeAPI 全件 + Champions オリジナル名は Bulbapedia）」の権威境界を 1 行で明示し、取得元優先序列と SoT レイヤの混同を防ぐ。

## 📝 harness-meta フィードバック

### 採用

### 見送り

### 保留
