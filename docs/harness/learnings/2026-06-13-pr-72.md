---
id: learning-pr-72
title: PR #72 レトロスペクティブ (PokeAPI を Champions legality・技メタの信頼源から外す Phase 12)
type: learning
status: draft
related_pr: 72
related_plan: 02-data-model-redesign/phase-12
generated_at: 2026-06-13T00:11:16Z
generator: pr-retrospective skill
---

<!--
この雛形は `.claude/rules/retrospective-format.md`（SoT）に準拠する。
見出し（絵文字・文言）は harness-meta が parse するため変更しないこと。
書き出し前に `.claude/rules/redaction.md` の正規表現で Secrets / PII を [REDACTED-*] 置換すること。
-->

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/72 / commit 588d1dd69b8253ad25e9b51140d7f28a11111034 / merge 2026-06-13T00:11:16Z / 差分 16 files +1515/-167

## ✅ Keep（継続したいこと）

<!-- 最低 3 / 推奨 5-10 -->

- ADR 0026 で「PokeAPI を Champions legality・技メタの信頼源にしない」決定を確定し、ADR 0012 を **supersede せず部分改訂**（ADR 0025 の前例に倣う）として扱えた。Accepted ADR 本文不変の規約を守りつつ方針更新を連番ログに残せている。
- `reversible`（PokeAPI が Champions 正式対応したら見直せる）を Consequences に明記し、不可逆判断でないことを後から辿れるようにした。決定の射程が明確。
- 技メタ SoT 移設で**生成物 `moves.ts` が等価**であることを `data/raw/move` 退避でも同一出力として確認し、回帰なしを機械的に裏付けた。dataset 的移設の安全な検証パターン。
- レビューを code-review / harness-review の独立サブエージェントでスコープ分割起動し、双方 blocking 0。harness 側は ADR 採番・可変 plan 参照なし・cross-agent symlink パリティまで確認、source 側は moves.yaml の実数値 spot-check まで踏み込めた。
- dragon-cheer の `power: 0` / `accuracy: 0` という null 規約不一致の nit を、catalog が後続 Phase（13/15 全量投入）の SoT になる**前**に修正コミット（8b47b33）で潰せた。SoT 一貫性を先回りで確保。
- 機械ゲート（型 / カバレッジ100% / Biome）をレビューで再実行せず CI に委譲し、`code-review.md` の非再実行原則を守った。

## ⚠️ Problem（詰まった点・制約）

<!-- 最低 3 / 推奨 5-10 -->

- worktree + 別ペインのワーカー claude で実装を駆動したが、ワーカーが **survey-regulation SKILL.md の単一大編集中に約47分ストール**した（トークン消費が止まり、Escape 中断 → 続行指示で復帰）。大きな skill/rule の単一編集がワーカーをハングさせる運用リスク。
- `acceptEdits` モードのワーカーでも **Bash コマンド（sed/git/gh/pnpm 等）が都度承認**を要し、read-only な inspection 系まで含めオーケストレーターの介入が多発した。並列駆動の効率を下げる承認摩擦。
- 技メタ SoT 移設の diff（moves.yaml +1318 行）が大きく、split=no の妥当性判断に説明コストがかかった（dataset 的 seed なので分割困難という planning rule の例外に該当する旨を PR 本文で明示する必要があった）。
- nit（dragon-cheer null 規約不一致）は tsc が `number | null` のため `0` を通してしまい、**型ゲートでは捕れず**人手レビューでのみ検出された。catalog のフィールド規約（変化技 = null）を型で強制できない隙間。

## 🚀 Try（次回の改善案）

<!-- rule / skill / template / ADR 単位まで具体化し、下の 🤖 改善提案へ落とす -->

- ワーカーペインでの **大きな skill / rule 編集は小さく分割して適用する**運用ノートを `skill-authoring` か `implementation-workflow` のワーカー運用節に追記する（単一大編集ストールの再発防止）。
- ワーカーペイン運用時の **read-only inspection 系コマンドの安全な許可 allowlist**（git status/log/diff・gh view・pnpm typecheck 等の非破壊系）方針を `implementation-workflow` のワーカー運用節へ文書化し、承認摩擦を減らす。
- catalog の **「変化技は power/accuracy = null」規約を機械的に検証**する手段（生成段の satisfies で status 技の null を強制する型表現、または check の追加）を検討する。tsc で捕れなかった nit を型 / ゲート側へ寄せる。
- dataset 的な大量 seed を含む PR の **split=no 判断テンプレ**（「機械的 seed で意味ある分割困難」を planning rule の例外として PR 本文へ定型記載する）を planning 系 rule / PR テンプレに足すか検討。

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit |
| カバレッジ | 100% | 100% | ±0 | Vitest（learnset 照合撤去に伴い到達不能 3 ケース除去・残テスト維持で 100%） |
| Biome 違反 | 0 | 0 | ±0 | biome check |
| CI | pass | pass | — | server-side pnpm verify（緑） |
| 差分 files/行 | — | 16 files +1515/-167 | — | gh pr diff（dragon-cheer nit 修正コミット 8b47b33 込み） |

## 🤖 ハーネス改善提案

<!-- harness-meta が parse する正規構造。未処理は [ ]、採用後に [x] + PR リンク。 -->

- [ ] `[skill]` `skill-authoring`（または `implementation-workflow`）のワーカー運用ノートに「大きな skill / rule 編集は小さく分割して適用する」を追記し、単一大編集によるワーカーストール（約47分）を防ぐ。
- [ ] `[rule]` `implementation-workflow` のワーカー運用節に、ワーカーペイン用 read-only inspection 系コマンド（git status/log/diff・gh view・pnpm typecheck 等）の安全な許可 allowlist 方針を文書化し、`acceptEdits` でも Bash 都度承認になる摩擦を減らす。
- [ ] `[rule]` `data-pipeline.md`（または catalog 規約 rule）に「変化技は power/accuracy = null」を機械検証へ寄せる方針を追記し、tsc が `0` を通す隙間を埋める。
- [ ] `[template]` PR テンプレ / planning rule に、dataset 的大量 seed を含む PR の split=no 判断（「機械的 seed で意味ある分割困難」を例外として定型記載）の項目を足す。
- [ ] `[adr]` （該当なし。本 PR の決定は ADR 0026 で起票済み）
- [ ] `[remove]` （該当なし）

## 📝 harness-meta フィードバック

<!-- 空でも見出しは維持する。harness-meta が後から各表に追記する。 -->

### 採用

<!-- 採用した提案と採用先 PR リンク -->

### 見送り

<!-- 見送り理由（harness-meta-criteria.md の見送り基準に対応） -->

### 保留

<!-- 保留理由 -->
