---
id: learning-pr-174
title: PR #174 レトロスペクティブ (docs/plan→docs/roadmap 改名 + completed/ 集約)
type: learning
status: draft
related_pr: 174
related_plan: 08-docs-restructure/phase-04
generated_at: 2026-06-26T00:00:00Z
generator: pr-retrospective skill
---

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/174 / commit e3b3de5dcf16c53b644f627594c6e513cad9c531 / merge 2026-06-25T17:13:27Z / 差分 114 files +228/-202。plan 08 Phase 4。`docs/plan/`→`docs/roadmap/` 改名 + 完了計画を `completed/` 集約し、全 inbound 参照（89 rename + 25 内容変更）を追従。`planning.md` rule / `finish-phase` skill に completed/ 集約手順を育成した。

## ✅ Keep（継続したいこと）

- **114 ファイルの改名を `git mv` + plan 対応の参照追従で完遂**: 改名・集約・相対リンク深度補正（`../../`→`../../../`）・active→completed 挿入を体系的に処理し、active 資産の dangling ゼロを達成。
- **凍結方針の線引きが一貫**: archive ADR 本文は全凍結、active ADR（0017/0020）は用語 rename 例外として path-follow という二段の保守度を [[adr]] の機械的整備例外 #2 に沿って適用。
- **dangling を 251 md/workflow の機械チェックで担保**: 残存 17 hit はテンプレ placeholder か pre-existing archive のみと確認。本 PR 起因の新規 dangling は archive/0034 の 1 件に限定し可視化。
- **改名と同時にハーネスを育成**: `planning.md` に「計画群完了時の completed/ 集約」専用節、`finish-phase` 手順 4 に集約の促しを追加し、運用を SoT 化。`paths` 更新後 `rules-index.md` を再生成しドリフトゼロ。
- **completed/ 集約を README ロールアップ表で二重可視化**: ディレクトリ構造（completed/ 配下 vs 直下）と進捗表（✅/🚧/⬜）で active/完了を両面から見えるようにした。
- **SoT 二重記述を回避**: `finish-phase` は「集約手順 SoT は [[planning]]」と明示し促しに留め、機械手順は planning.md 専用節に一意化。

## ⚠️ Problem（詰まった点・制約）

- **archive ADR の plan 参照が改名で clickable 404 化**: `archive/0034:21` の `[Phase 10](../../plan/03-...)` が唯一の実リンク dangling に。archive 本文不変方針での据え置きと、[[adr]] 機械的整備例外 #1（可変 plan 参照の脱リンク）のどちらも in-policy で、判断が著者裁量に委ねられた（non-blocking）。
- **literal grep が相対形 `../plan/` を取りこぼした**: `docs/harness/README.md` の相対参照が literal `docs/plan` トークン走査から漏れ、別途追従が必要だった。改名 PR の grep パターン網羅性の難しさ。
- **README の completed/ 運用節が planning.md の節構造と微妙に食い違う**: 「renumber チェックリストと同種」と案内したが planning.md 側は独立専用節。リンクは解決するが読み手の探索コストが残る（nit）。

## 🚀 Try（次回の改善案）

- 改名 PR の dangling 検出は literal トークン（`docs/plan`）だけでなく**相対形（`../plan/`・`../../plan/`）も同時に走査**する grep パターン集を harness-review-checklist の「改名・移動 PR」観点に固定する（PR #174 の `../plan/` 取りこぼし由来）。
- archive ADR の可変 plan 参照は、改名時に **[[adr]] 例外 #1（脱リンク）を既定の推奨**として明文化し、「凍結 or 脱リンク」の都度判断を減らす（archive/0034 の唯一の dangling を構造的に解消）。
- README から他 rule の節を案内するときは「同種」でなく**節名を直接引用**する運用を harness-review の SoT 一貫性観点に補足（nit 由来）。

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit |
| カバレッジ | 100% | 100% | ±0 | Vitest（185 tests） |
| Biome 違反 | 0 | 0 | ±0 | biome check |
| CI | pass | pass | — | server-side pnpm verify 緑 |
| 差分 files/行 | — | 114 files +228/-202 | +26 | 89 rename + 25 内容変更 |

## 🤖 ハーネス改善提案

- [ ] `[rule]` harness-review-checklist の「改名・移動 PR」観点に、literal トークン + 相対形（`../plan/`・`../../plan/`）の両方を走査する dangling grep パターン集を固定（PR #174 の `../plan/` 取りこぼし由来）。
- [ ] `[rule]` [[adr]] に「改名で archive ADR の可変 plan 参照が clickable 404 化する場合、例外 #1（脱リンク）を既定の推奨とする」を明文化し都度判断を削減（archive/0034 由来）。
- [ ] `[rule]` harness-review の SoT 一貫性観点に「他 rule/doc の節を案内するときは『同種』でなく節名を直接引用」を補足（PR #174 nit 由来）。

## 📝 harness-meta フィードバック

### 採用

### 見送り

### 保留
