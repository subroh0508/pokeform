---
id: learning-pr-29
title: PR #29 レトロスペクティブ (01-mvp Phase 3 ステータス調整の壁打ち)
type: learning
status: draft
related_pr: 29
related_plan: 01-mvp/phase-03
generated_at: 2026-06-07T01:30:00Z
generator: pr-retrospective skill
---

<!--
この雛形は `.claude/rules/retrospective-format.md`（SoT）に準拠する。
見出し（絵文字・文言）は harness-meta が parse するため変更しないこと。
-->

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/29 / commit 210b658 / merge 2026-06-07T01:07:24Z / 差分 14 files +750/-0。`pokeform stat`（実数値・性格補正・ポイント配分・耐久指数）と、耐久/火力指数・確定数判定用ダメージ式・ポイント逆算（素早さ抜き / 確定耐えフロンティア・往復整合）を domain 純関数で実装し 01-mvp を完了。`implementation-workflow` Phase 0〜6 を fix loop 0 回・レビュー blocking 0 件で通過。

## ✅ Keep（継続したいこと）

- **逆算結果の往復整合をテストで固定**: `solveOutspeed` / `solveSurvival` の出力配分を `calcHp` / `calcStat` / `maxDamage` に通し直して目標ライン充足を検証（`stat-tuning.test.ts`）。逆算（探索）と実数値計算（確定式）の整合を機械的に担保し、「plausible だが calc-stats とズレる」落とし穴を回避（code-review が明示評価）。
- **ダメージ式の floor 順序を game-spec と一致させた**: 素ダメージ `floor(floor(C×威力×攻撃/防御)/50)+2` → STAB `floor(×1.5)` → 相性 `floor(×倍率)` → 乱数 `floor(×0.85)` の段階順を rule と突き合わせて実装。1 ずれの温床を断った。
- **層分離の徹底**: domain 純関数（calc-stats / stat-indices / damage / stat-tuning・100% カバレッジ）と薄い io / cli（カバレッジ除外）を分け、指標・逆算ロジックを純関数側に寄せた（[[testing]] の「ドメイン完全網羅 / 薄い層除外」に忠実）。
- **スコープを確定数判定に絞った**: ダメージ計算を「確定耐え / 確定数の判定に必要な範囲（乱数上下限・STAB・相性のみ）」に限定し、本格計算（乱数16段・急所・補正）は将来 `02-<slug>` へ明示的に逃がした。MVP の肥大化を防止。
- **Generator/Evaluator 独立の遵守**: 実装（本ワーカー）とレビュー（`code-review` / `harness-review` を Agent で並行起動）を分離し、両結果を PR コメントに残してから停止。自己採点で代替しなかった。
- **クロスエージェントパリティ**: 新規 skill `stat-tuning` を canonical + `.agents/skills` 相対 symlink（mode 120000）で追加し、既存 12 skill と同一パターンを維持（harness-review 確認済み）。

## ⚠️ Problem（詰まった点・制約）

- **io 層が不正入力を黙認する**: `load-individual.ts` がポイントの範囲・合計（`isValidPointAllocation` 既存）を検証せず `as PointAllocation` で流し込み、`nature.up === down` の衝突も `as NatureSpec` キャストで握り潰す。不正 YAML でも `stat` が誤った実数値を黙って表示する（code-review non-blocking ×2）。[[cli-and-io]] の「問題検出時は非0終了」と乖離。
- **rule が宣言上の正本（architecture.md）に先行した**: `game-spec.md` に耐久/火力指数・ダメージ式・逆算の定義を追記したが、同 PR では `architecture.md` を同期せず Phase 3 / `stat` / 逆算が「将来」のまま残り、rule が事実上の SoT になった（harness-review non-blocking）。rule 自身の「ADR + architecture.md + 本 rule をセット更新」留意と矛盾。**本 follow-up PR で同期**したが、マージが同期コミットより先行して反映漏れが起き、follow-up が必要になった。
- **`STAT_KEYS` の 3 重定義**: 同じ 6 能力キー配列が `cli/commands/stat.ts` / `domain/stat-tuning.ts` / `codegen/normalize.ts` に個別定義（code-review nit）。固定6能力ゆえ実害は小さいが将来のドリフト源。
- **`buildTuningReport.feasible` が「合計 ≤66」判定**: game-spec の「合計 = 66」より緩い。余剰を後で振る「予算」セマンティクスとしては意図的だが、仕様の「ちょうど66」とは差がある（code-review non-blocking・意図的と判断し未修正）。
- **マージ運用と完了処理の順序**: オーケストレーターのマージが finish-phase（README 進捗 / architecture.md 同期）より先に走り、進捗・同期が main 未反映のまま残った。Phase 8 の doc 反映を別 follow-up PR に切り出す必要が生じた。

## 🚀 Try（次回の改善案）

- **io ローダで既存バリデータを使う**: `load-individual.ts`（および将来の単体ロード経路）が `isValidPointAllocation` と `up !== down` を検証し、不正時は `console.error` + 非0終了（または明示警告）にする。表示専用コマンドでも「問題検出時は非0終了」（[[cli-and-io]]）を満たす。
- **`STAT_KEYS` を `src/types/stats.ts` に一元化**: 6 能力キー配列を型定義側に置き各所が import。能力追加時のドリフトを防ぐ。
- **rule と architecture.md の同期を完了ゲートに織り込む**: rule が `architecture.md` を正本と宣言しつつ数式を追記する場合、**同一 PR** で architecture.md を同期する（finish-phase / harness-review の点検項目に「rule の正本宣言と本文の同期」を明記）。本 PR のような「rule 先行 → follow-up 同期」を構造的に防ぐ。
- **完了処理（README 進捗 / doc 同期）をマージ前に取り込む運用**: マージがオーケストレーター主導の場合、進捗・同期コミットをフィーチャー PR に含めてからマージするか、follow-up PR を定型化する（どちらを採るか implementation-workflow Phase 7/8 で明確化）。

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit |
| カバレッジ | 100% | 100% | ±0 | Vitest（197 stmt / 85 branch / 37 func / 165 line・69 tests） |
| Biome 違反 | 0 | 0 | ±0 | biome check |
| CI | pass | pass | — | server-side pnpm verify |
| 差分 files/行 | — | 14 files +750/-0 | +14f | gh pr diff #29 |

## 🤖 ハーネス改善提案

<!-- harness-meta が parse する正規構造。未処理は [ ]、採用後に [x] + PR リンク。 -->

- [x] `[rule]` `cli-and-io`: 個体の単体ロード（`load-individual`）でもポイント範囲・合計（`isValidPointAllocation`）と性格 up≠down を検証し、不正は非0終了 or 明示警告する、と io 層の入力検証方針を明記する（表示専用コマンドの黙認を防ぐ）。
- [x] `[rule]` `type-conventions`: 6 能力キー配列 `STAT_KEYS` を `src/types/stats.ts` に一元化し各所が import する、と単一ソース化を指針化する（cli/stat・domain/stat-tuning・codegen/normalize の 3 重定義の解消）。
- [x] `[skill]` `finish-phase` / `harness-review`: 「rule が `architecture.md` を正本と宣言しつつ数式・仕様を追記する場合、同一 PR で architecture.md を同期しているか」を点検項目に追加する（rule が正本に先行する乖離の再発防止）。
- [x] `[skill]` `implementation-workflow`: オーケストレーター主導マージ時、README 進捗・doc 同期をフィーチャー PR に含めてからマージするか follow-up PR にするかを Phase 7/8 で明確化する（本 PR で進捗・同期が main 未反映になった件）。
- [x] `[adr]` （新規不要）確定数判定に限定したダメージ式・近似指数は既存 [[game-spec]] / ADR 0010（tsc 検証）の範囲内で、可逆な実装判断のため新規 ADR は起こさない。

## 📝 harness-meta フィードバック

<!-- 空でも見出しは維持する。harness-meta が後から各表に追記する。 -->

### 採用

- harness-meta batch 2026-23（PR `harness/meta-batch-2026-23`）で rule 反映:
  - io 層検証（D1）→ `cli-and-io.md` 終了コード節に「単体ロードでも `isValidPointAllocation` + up≠down を検証し問題検出時は非0終了」を追記。
  - `STAT_KEYS` 一元化（D2）→ `type-conventions.md` 整合節に `src/types/stats.ts` への一元化指針を追記。
  - architecture 同期点検（D3）→ `implementation-workflow.md` Phase 4 セルフ検証に点検項目を追加（pr-24 と統合）。
  - README/doc 同期（D4）→ `implementation-workflow.md` Phase 8 に follow-up PR / 単発スキップ方針を追記（pr-22/pr-34 と統合）。
  - `[adr]` placeholder は no-op として処理済み（[x] 化）。

### 見送り

### 保留
