---
id: learning-pr-155
title: PR #155 レトロスペクティブ (スクレイパーを責務別に分解し種族ページの技メタ副産物抽出を除去)
type: learning
status: reviewed
related_pr: 155
related_plan: 05-move-master-scraper-refactor/phase-03
generated_at: 2026-06-21T08:15:54Z
generator: pr-retrospective skill
---

<!--
この雛形は `.claude/rules/retrospective-format.md`（SoT）に準拠する。
見出し（絵文字・文言）は harness-meta が parse するため変更しないこと。
書き出し前に `.claude/rules/redaction.md` の正規表現で Secrets / PII を [REDACTED-*] 置換すること。
-->

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/155 / commit 62025816cc98db12906b7620d956c1b1d0714803 / merge 2026-06-21T08:15:54Z / 差分 15 files +739/-674。plan 05 Phase 3 = 意味保存リファクタ。`parse.ts` を `parseSpeciesBase` / `parseMoves`（覚える技名一覧のみ）/ `parseMegas` へ分解、`to-catalog.ts` を `catalog-fields` / `per-game-fields` / `regulation-fields` の 3 ファイルへ分離、`serebii-to-catalog.ts` の `transcribeSpecies` を `writeSpeciesCatalog` / `writeSpeciesPerReg` へ責務分解。Phase 2 で技専用ページができたので種族ページからの技メタ副産物抽出を除去し技メタ SoT を `parseMoveMaster` に一本化。`generate:data` 差分ゼロで意味保存を証明。役割分割境界は ADR 0037 設計メモ 3 で確定済み。

## ✅ Keep（継続したいこと）

- **`generate:data` 差分ゼロで意味保存を機械的に証明した**: リファクタ前後で `pnpm generate:data` を実行し `git diff src/generated` が空であることを確認。`generate.ts` は committed YAML を読み scraper を呼ばないため、純構造変更が出力に波及しないことを担保した。意味保存リファクタの正しさを主観でなく機械で言い切れる。
- **役割分割境界を ADR 0037 設計メモ 3 で先に確定してから実装した**: `parse.ts` 3 分割 / `to-catalog.ts` 3 ファイル分離 / script の catalog・per-reg 分解が設計メモと 1:1。境界設計を設計 phase（#153）で前倒ししたため Phase 3 で分割粒度の再決定が起きなかった。
- **テストを分割後の関数へ過不足なく追従させカバレッジ 100% を維持した**: `to-catalog.test.ts`（305 行）を 3 テストファイルへ再配置し、`parse.test.ts` / `schema.test.ts` を分割後関数へ追従。`parseMoves` 出力に技メタが含まれないことを明示するテストを追加して退行に強くした。code-review でも各純関数が削除元と逐語一致と確認。
- **技メタ副産物除去で SoT を技専用ページへ一本化した**: `ParsedMove` を `{name, id}` へ縮小、`validateSpecies` の技 type/damageClass 欠落検査を除去（種族ページの技は id 形のみ検査）、species 経路から move-specs 書き込みを除去。技メタの唯一 SoT が `parseMoveMaster` / `validateMoveMaster` になり、二重 SoT を解消（ADR 0037 設計メモ 3 と整合）。
- **ライブ手順 SoT の追従漏れを同 PR で是正した**: `serebii-sourcing.md`（ライブ手順 SoT）の exit code 4 記述を実態へ正確化。doc-data 乖離を分割と同じ PR で潰した。
- **dangling 参照の残存を切り分けて追従要否を判定した**: `git grep "to-catalog"` の残存が ADR 0033 / 0037 のみ（不変ログ / 設計メモの歴史的記述で追従不要）と確認し、production コード・skill ライブ手順への dangling import が無い（`moveStatsFields` / `movesIn` / `megasIn` 参照ゼロ）ことを機械確認。

## ⚠️ Problem（詰まった点・制約）

- **新 export 関数 `parseSpeciesBase` / `parseMegas` に専用ユニットテストが無い**: `export` 化したが production の外部呼び出し元は無く（`parseSpeciesPage` 内部利用のみ）、`parseMoves` だけ単体テストがあり他 2 つは `parseSpeciesPage` 経由で transitive カバー。境界（メガ手前 cut の有無）を直接突くテストが無く、対称性が崩れている（code-review nit 1）。
- **テスト用途主体の export が意図不明瞭**: `regulation-fields.ts` の `sortedUnion` / `megaSpeciesId` は production からの直接呼び出しが限定的（`megaSpeciesId` は `megaAuthoring` 経由のみ）。移設前からの状態だが、テスト用途主体の export であることがコメントで示されておらず意図が読み取りにくい（code-review nit 2）。
- **意味保存の証明が `generate.ts` の committed-YAML 依存に立脚している**: 差分ゼロが成立するのは `generate.ts` が scraper を呼ばず committed YAML を読むため。逆に言えば scraper 経路の挙動変化は `generate:data` では絶対に検出できず、parse/schema の正しさは専ら fixture テストに依存する。本 PR では fixture が厚いが、構造上の死角として残る。
- **+739/-674 と大きめの構造変更で逐語一致レビューが必要だった**: 15 ファイルの分割・移設で各関数が削除元と等価かを diff 上で逐語照合する必要があり、レビュー負荷が高い（code-review が早期 return 化・else フラット化の等価性まで確認）。

## 🚀 Try（次回の改善案）

- **`[rule]` testing に「責務分割で新規 export した純関数は、transitive カバーに頼らず境界を直接突く単体テストを足す」原則を明記**する。`parseSpeciesBase` / `parseMegas` のような分割副産物の export が無テストで残る非対称を防ぐ（code-review nit 1 の制度化）。
- **`[rule]` testing / type-conventions に「テスト用途主体の export は理由をコメントで一言示す」運用を明記**する。`megaSpeciesId` のような外部直接呼び出しの無い export の意図を可読化（code-review nit 2）。
- **`[skill]` code-review checklist に「意味保存リファクタは `generate:data` 差分ゼロを証拠として要求しつつ、scraper 経路の挙動は fixture テストでしか担保されない死角を確認する」観点を追加**する。committed-YAML 依存の証明の限界を観点化。
- **`[skill]` code-review / harness-review に「to-catalog 等のファイル名を ADR / 設計メモが歴史的記述として参照している場合は不変ログのため追従不要、production / skill ライブ手順の dangling のみ追従要」と dangling 切り分け基準を明記**する。本 PR の判定を再利用可能にする。

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit |
| カバレッジ | 100% | 100% | ±0 | Vitest（分割後関数へテスト追従） |
| Biome 違反 | 0 | 0 | ±0 | biome check + check:yaml-style |
| CI | pass | pass | — | server-side pnpm verify |
| 差分 files/行 | — | 15 files | +739/-674 | gh pr diff（generate:data 差分ゼロで意味保存） |

## 🤖 ハーネス改善提案

<!-- harness-meta が parse する正規構造。未処理は [ ]、採用後に [x] + PR リンク。 -->

- [ ] `[rule]` testing に「責務分割で新規 export した純関数は transitive カバーに頼らず境界を直接突く単体テストを足す」原則を明記する（PR #155 code-review nit 1・分割副産物 export の無テスト非対称防止）。
- [ ] `[rule]` testing / type-conventions に「テスト用途主体の export は理由をコメントで示す」運用を明記する（`megaSpeciesId` 等の意図可読化・code-review nit 2）。
- [ ] `[skill]` code-review checklist に「意味保存リファクタは `generate:data` 差分ゼロを証拠に要求しつつ scraper 経路は fixture テストでしか担保されない死角を確認する」観点を追加する。
- [ ] `[skill]` code-review / harness-review に「ファイル名を ADR / 設計メモが歴史的記述として参照する場合は不変ログのため追従不要・production / skill ライブ手順の dangling のみ追従要」の dangling 切り分け基準を明記する。

## 📝 harness-meta フィードバック

<!-- 空でも見出しは維持する。harness-meta が後から各表に追記する。 -->

### 採用

（本ラウンドでの採用なし）

### 見送り

- `[rule]` testing に責務分割 export の直接単体テスト原則（#155-57）→ 単発・code-review nit 由来。現状の fixture + transitive で実害低。同種が反復すれば testing.md へ昇格候補。
- `[rule]` testing / type-conventions にテスト用途 export のコメント運用（#155-58）→ nit。現状の可読性で許容、見送り。
- `[skill]` code-review に意味保存リファクタの fixture 死角確認（#155-59）→ 単発。既存 code-review で個別に確認可能で skill checklist 追記は見送り。
- `[skill]` code-review / harness-review に dangling 切り分け基準（#155-60）→ 採用済みの adr.md 役割境界（#153-56）と方向が重なり原則は一部担保。単発のため本ラウンドは見送り。

### 保留
