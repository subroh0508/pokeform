---
id: learning-pr-77
title: PR #77 レトロスペクティブ (data/ 全 YAML ブロックスタイル強制ゲート新設)
type: learning
status: draft
related_pr: 77
related_plan: 02-data-model-redesign/phase-15
generated_at: 2026-06-13T02:57:13Z
generator: pr-retrospective skill
---

<!--
この雛形は `.claude/rules/retrospective-format.md`（SoT）に準拠する。
見出し（絵文字・文言）は harness-meta が parse するため変更しないこと。
書き出し前に `.claude/rules/redaction.md` の正規表現で Secrets / PII を [REDACTED-*] 置換すること。
-->

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/77 / commit 12802fbda55bc18b7ce4701e6d733205325155c0 / merge 2026-06-13T02:57:13Z / 差分 12 files +670/-110

## ✅ Keep（継続したいこと）

<!-- 最低 3 / 推奨 5-10 -->

- **AST ベース検出で文字列値中の括弧の誤検出を回避**: `src/domain/yaml-block-style.ts` の `findFlowCollections` が `parseDocument` + `node.flow` で flow コレクションを再帰検出。正規表現を使わず、文字列値中の `[` / `{` を誤検出しない設計。code-review が実機で確認済み。
- **生成物等価を実証してマージ**: `pnpm generate:data` 後 `git diff --stat data/generated/` が空であることを worktree で実機確認し、species.yaml の flow→block 再整形が値不変であることを担保。「再整形は値に影響しない」を主張でなく実機で裏取りした。
- **新ゲート種別を ADR 0028 で線引き**: 「検証は tsc のみ（ADR 0010）」の例外＝型で表現できないスタイル lint を `verify` / `.githooks` に置く線引きを ADR として記録。後から「なぜスタイル lint だけ別軸か」を辿れる。
- **検出ロジックを純関数 1 箇所に集約しゲート二重化を回避**: 検出は `findFlowCollections` 1 箇所。CLI（`check-yaml-style.ts`）と pre-commit は薄い配線で同一純関数へ委譲。harness-review が「ゲート二重化なし・ADR 0013 違反でない（pre-commit は既存設計通り個別チェック列挙）」を明示確認。
- **ワーカーへ「編集を小さく分けよ」と最初に指示しストールなく完走**: worktree + 別ペインのワーカー駆動で、前回 Phase 12 の 47 分ストール教訓を活かし、最初に編集分割を指示。今回はストールなく完走できた。
- **code / harness 独立起動レビューで両者 blocking 0**: ソース（src/scripts/data）と harness 資産を別エージェントでレビューし、責務を分けつつ双方 blocking 0 で健全性の純改善を確認。
- **M-A 全量（Phase 16）の手前に仕組みを確定**: 後追いの一括再整形事故を防ぐため、全量投入前にゲート + 既存整形を先行させる順序付けが妥当だった。

## ⚠️ Problem（詰まった点・制約）

<!-- 最低 3 / 推奨 5-10 -->

- **`check:yaml-style` で存在しない path 入力時に ENOENT が uncaught**: `src/cli/commands/check-yaml-style.ts:21` の `statSync` が存在しない path でメッセージなしのスタックトレースで落ちる。`check:regulation` 等の兄弟コマンドと同挙動で本 PR 起因の退行ではないが、横並びの hygiene として clean な非0終了に寄せる余地がある（code-review nit）。
- **`yamlFiles` が `resolveInputFiles` と軽微重複**: `check-yaml-style.ts:18-28` の path 解決が既存 `src/io/resolve-paths.ts` の `resolveInputFiles` とほぼ同形。差分は `ignore: ["**/raw/**"]` のみで、既存ヘルパが `ignore` を受けないため自前実装は正当だが、将来 `resolveInputFiles` に `ignore` を渡せれば重複を畳める（code-review nit）。
- **スタイル lint という新カテゴリのゲートを今後どう増やすか方針未確定**: 今回 flow 排除という単一スタイル lint を追加したが、インデント幅・キー順・引用符など他のスタイル規約を将来追加する際の置き場所・増やし方の方針が未定（スコープ外として明示されているが、増設パターンが定まっていない）。
- **オーケストレーションで `acceptEdits` でも Bash 都度承認が継続**: worktree + 別ペインのワーカー駆動で、`acceptEdits` モードでも Bash 実行の都度承認が必要な反復が続いた。PR #72 / #73 と同様の課題で、harness-meta 集約対象。
- **pre-commit コメントに「02 Phase 15」表記が残る**: `.githooks/pre-commit` のコメントに phase 番号表記が残り、将来 phase renumber 時に陳腐化しうる（harness-review nit）。ADR 本文ではないため規約違反ではないが、`[[data-pipeline]]` 参照のみに寄せる余地あり。

## 🚀 Try（次回の改善案）

<!-- rule / skill / template / ADR 単位まで具体化し、下の 🤖 改善提案へ落とす -->

- CLI check コマンド群（`check:yaml-style` / `check:regulation` 等）で存在しない path 入力時に ENOENT スタックトレースでなく clean な非0終了 + メッセージを返すよう、`resolveInputFiles` 側で path 不在を扱う方針を rule 化（`cli-and-io.md` の「問題検出時は非0終了」に path 不在の扱いを追記）。
- `resolveInputFiles` に `ignore` パターン引数を持たせ、`check-yaml-style` の自前 path 解決を畳む（src リファクタ・薄い層なので強制せず候補として残す）。
- スタイル lint（block 強制のような型で表現できない data YAML 規約）を今後増やす際の置き場所・配線パターンを `data-pipeline.md` に「スタイルゲート増設手順」として明文化する。
- worktree + 別ペインワーカー駆動での `acceptEdits` 下 Bash 都度承認の反復を harness-meta で集約処理（PR #72 / #73 と反復）。承認削減の settings / 運用方針を判定する。
- `.githooks/pre-commit` 等のコメント中の phase 番号表記を `[[data-pipeline]]` 等の安定参照へ寄せ、phase renumber 耐性を上げる（任意）。

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit |
| カバレッジ | 100% | 100% | ±0 | Vitest（Branches 122/122） |
| Biome 違反 | 0 | 0 | ±0 | biome check |
| CI | pass | pass | — | server-side pnpm verify（+ check:yaml-style） |
| 差分 files/行 | — | 12 files +670/-110 | — | gh pr diff |

## 🤖 ハーネス改善提案

<!-- harness-meta が parse する正規構造。未処理は [ ]、採用後に [x] + PR リンク。 -->

- [ ] `[rule]` `cli-and-io.md` に「check 系コマンドは存在しない path 入力時も ENOENT スタックトレースでなく clean な非0終了 + メッセージを返す」を追記し、CLI 全体の path 不在挙動を揃える。
- [ ] `[rule]` `data-pipeline.md` に「スタイルゲート増設手順」節を追加し、型で表現できない data YAML スタイル規約（block 強制等）を増やす際の検出純関数 + verify/.githooks 配線パターンを明文化する。
- [ ] `[skill]` worktree + 別ペインワーカー駆動で「最初に編集を小さく分けよと指示」する運用を `implementation-workflow` のオーケストレーション節へ Keep として取り込む（Phase 12 の 47 分ストール対策の定着）。
- [ ] `[skill]` `acceptEdits` 下でも Bash 都度承認が続く反復（PR #72 / #73 / #77 で反復）を harness-meta で集約し、承認削減の settings / 運用方針を判定する。
- [ ] `[adr]` （アーキ決定としては ADR 0028 で本 PR の決定は記録済み。追加の ADR 化候補なし。）
- [ ] `[remove]` （撤去候補なし。）

## 📝 harness-meta フィードバック

<!-- 空でも見出しは維持する。harness-meta が後から各表に追記する。 -->

### 採用

<!-- 採用した提案と採用先 PR リンク -->

### 見送り

<!-- 見送り理由（harness-meta-criteria.md の見送り基準に対応） -->

### 保留

<!-- 保留理由 -->
