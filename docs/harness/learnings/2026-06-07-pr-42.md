---
id: learning-pr-42
title: PR #42 レトロスペクティブ (02 Phase 2 レギュレーション解禁の per-regulation 一本化)
type: learning
status: draft
related_pr: 42
related_plan: 02-data-model-redesign/phase-02
generated_at: 2026-06-07T16:56:31Z
generator: pr-retrospective skill
---

<!--
この雛形は `.claude/rules/retrospective-format.md`（SoT）に準拠する。
見出し（絵文字・文言）は harness-meta が parse するため変更しないこと。
書き出し前に `.claude/rules/redaction.md` の正規表現で Secrets / PII を [REDACTED-*] 置換すること。
-->

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/42 / commit 336f9a4 / merge 2026-06-07T16:56:31Z / 差分 20 files +388/-129。
> 02-data-model-redesign Phase 2（計画群の核）。解禁判定の正本を per-regulation に一本化（A案・ADR 0021）。
> regulation.yaml→regulations/<id>.yaml（period 付き）、per-reg 別型 + index 集約、SpeciesBase.regulations[] 廃止、
> ConstrainParty/validateParty を per-reg 解禁集合参照へ。code-review blocking0 / harness-review blocking1（architecture 同期漏れ）を fix。

## ✅ Keep（継続したいこと）

- **不可逆な型機構変更を 1 PR で完結**: per-reg 生成 → 型機構付け替え（ConstrainParty）→ ランタイム（validateParty）→ テスト → ADR を 1 PR にまとめ、中間状態で緑を割らなかった。phase doc の「1 PR で完結」方針が機能。
- **A案の正本一本化を貫いた**: SpeciesBase.regulations[] を廃止し解禁情報を per-reg に集約。SoT 二重化を避ける設計判断を計画段階（AskUserQuestion）→ ADR 0021 まで一貫させた。
- **per-reg 別型 + index 集約のハイブリッド**: 1 レギュ = 1 ファイル（別型）を保ちつつ index.ts で regulationDex に集約し、型レベル lookup（RegulationDex[R].species）を維持。「別ファイル別型」と「汎用 lookup」を両立。
- **生成段の参照整合チェックを横展開**: Phase 1 の abilities 整合 throw を、レギュ allow（species/items/mega/moves）のカタログ整合にも適用。append-only マスターの担保が一貫。
- **CLI end-to-end で legality 反転を実証**: validateParty の判定基盤を species 配列→per-reg 集合へ変えた後、standard=exit0 / broken=exit1（mewtwo 未解禁検出）を独立実行で確認。
- **独立レビューの Generator/Evaluator 分離が機能**: harness-review が architecture.md:210 の旧モデル残滓（自己点検で見落とした正本同期漏れ）を blocking 検出。fix 後に Evaluator が解消を再確認。
- **ADR を adr-new で採番・0012/0014 を supersede せず参照**: vendor 方式は不変＝拡張 ADR と整理し、安易な supersede を避けた。

## ⚠️ Problem（詰まった点・制約）

- **正本 doc（architecture.md）の同期漏れ**: L80/L290 等は更新したが、離れた L210 のパーティ型制約説明（旧 regulations 配列前提）を見落とし、harness-review が blocking 検出。同一ファイル内の関連記述が分散していると追従漏れが起きる。
- **wikilink を実在 rule に確認せず付けた**: party-analysis.ts に `[[regulation]]` と書いたが該当 rule が無く dangling（code-review nit）。新概念の wikilink は実在を確認すべき。
- **biome の export sort で手戻り**: regulation.ts の `export type` / `export` 並びが biome 指摘になり format 修正。生成・手書きとも biome 整形を最初から意識すべき。
- **worktree の raw 再 fetch（再掲）**: Phase 1 と同様、worktree に data/raw が無く fetch:data が再度必要だった（learning #40 の改善提案が未反映のため再発）。
- **per-reg 型の coverage 分岐**: validateParty に「未知レギュ→全員 not-legal」分岐を足したが、テストが全て既知レギュだった。明示テストを追加して契約を固定（v8 は 100% だったが防御的に補強）。

## 🚀 Try（次回の改善案）

- **正本 doc は変更後に対象キーワードを全文 grep して同期漏れを潰す**: 例 `grep -n "regulations" architecture.md` で全 hit を確認。Phase 4 セルフ検証 / code-review.md に「正本 doc の用語全 hit 確認」を補足。
- **新規 wikilink は実在 rule を確認**: `[[name]]` を書く前に `.claude/rules/name.md` の存在を確認する手順を skill-authoring / harness-review nit 観点に追記。
- **learning #40 の worktree raw 手順を実際に implementation-workflow.md へ反映**（未反映で再発）。harness-meta で優先採用候補。

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit |
| カバレッジ | 100% | 100% | ±0 | Vitest 70 tests（+1 未知レギュ） |
| Biome 違反 | 0 | 0 | ±0 | biome check（63 files） |
| CI | pass | pass | — | server-side pnpm verify（SUCCESS で auto-merge） |
| 差分 files/行 | — | 20 files +388/-129 | +259 | gh pr diff |
| レビュー blocking | — | 1→0 | -1 | harness-review が architecture 同期漏れ検出・fix |

## 🤖 ハーネス改善提案

<!-- harness-meta が parse する正規構造。未処理は [ ]、採用後に [x] + PR リンク。 -->

- [ ] `[rule]` `code-review.md` / phase テンプレ（Phase 4 セルフ検証）に「正本 doc を変更したら対象用語を全文 grep して同期漏れ（離れた箇所の旧モデル残滓）を潰す」を補足。
- [ ] `[rule]` `skill-authoring.md` / harness-review 観点に「新規 `[[wikilink]]` は対応 rule の実在を確認してから書く（dangling 防止）」を追記。
- [ ] `[rule]` `implementation-workflow.md` Phase 0 に「worktree は data/raw 非共有。メイン側 raw を cp するか fetch:data を回す」を反映（learning #40 から再掲・再発につき優先）。

## 📝 harness-meta フィードバック

<!-- 空でも見出しは維持する。harness-meta が後から各表に追記する。 -->

### 採用

<!-- 採用した提案と採用先 PR リンク -->

### 見送り

<!-- 見送り理由（harness-meta-criteria.md の見送り基準に対応） -->

### 保留

<!-- 保留理由 -->
