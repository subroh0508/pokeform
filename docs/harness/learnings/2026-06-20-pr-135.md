---
id: learning-pr-135
title: PR #135 レトロスペクティブ (plan 04 Phase 3 ハーネス追従)
type: learning
status: draft
related_pr: 135
related_plan: 04-generated-layout-redesign / phase-03-harness-followup
generated_at: 2026-06-20T18:07:31Z
generator: pr-retrospective skill
---

<!--
この雛形は `.claude/rules/retrospective-format.md`（SoT）に準拠する。
見出し（絵文字・文言）は harness-meta が parse するため変更しないこと。
書き出し前に `.claude/rules/redaction.md` の正規表現で Secrets / PII を [REDACTED-*] 置換すること。
-->

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/135 / commit 71e8612e20dab0a7a3c702a76d6fe5a5a4795788 / merge 2026-06-20T18:07:31Z / 差分 13 files +285/-240。ADR 0035/0036 で確定した新レイアウト（specs / languages / per-reg 3 軸直交・メガ独立 spec）へ rule 3 + skill 4 + architecture.md / data/README.md / AGENTS.md / 計画 README を追従させ旧パスを一掃した doc/skill 専用 PR。harness-review は blocking 0。

## ✅ Keep（継続したいこと）

- **編集前に新レイアウトの実体（生成物・型・CLI）を Read で確定してから doc 追従した**: `data/generated/champions/*-specs.ts` / `languages/*.ts` / per-reg `<reg>/index.ts` のシンボル（`speciesSpecsDex` / `megaSpecsDex` / `moveSpecsDex` / `NameEntry` / `PerRegSpecies` / `speciesNamesAll`）と `check-regulation.ts` の引数解決を先に把握したため、skill のコマンド記述（`check:regulation data/champions`）と型名が実体と一致した（harness-review が全シンボル実在を確認）。
- **section ごとに小さく Edit を分割した**: data-pipeline.md / architecture.md の長い節も 1 Edit = 1 関心で適用し、約47分ストールの単一巨大編集（learning #72/73/77）を回避。
- **旧パスを「廃止/移行注記」として残し、新旧の対応を明示した**: `species-base.ts は廃止`・`regulations/champions/moves → move-specs へ精緻化` のように旧→新を本文に残すことで、矛盾記述ゼロかつ追跡可能性を確保。grep の機械照合では `廃止|旧 |移行` を除外して残存ゼロを確認できた。
- **architecture.md（MVP 設計 doc）は supersede redirect 注記で現行 SoT へ誘導した**: MVP 当時の型例（`name` 埋め込み / `names.ts`）を全書換せず、ADR 0025 既存注記を ADR 0035/0036 へ拡張して「本節以下は MVP 当時・現行は type-conventions/data-pipeline + ADR 0035/0036 が正本」と一括 redirect。可変でない設計 doc の追従コストを抑えた。
- **cross-agent パリティを symlink で自動担保した**: canonical（`.claude/skills/<name>`）のみ編集し `.agents/skills/<name>` は相対 symlink のため copy 同期不要。harness-review が 4 skill 全 symlink の解決を確認。
- **harness-review の non-blocking 指摘（`reverseEn` 架空シンボル）をマージ前に取り込んだ**: doc 1 行の正確性是正を fix commit で先取りし、READY 報告前に最終 HEAD へ反映（マージ取りこぼし防止の同期点を遵守）。

## ⚠️ Problem（詰まった点・制約）

- **rule に実装の架空シンボルを書いてしまった**: cli-and-io.md で逆引きを「`reverseJa` / `reverseEn`」と記したが、コード上は `reverseJa` のみで en は ID 直接照合（`ids.has`）だった。実装 SoT を指す rule での未確認シンボルは reader/agent が探して見つけられない dangling。harness-review で検出し是正。
- **API/classifier の一時不可（`temporarily unavailable`）で Edit が 1 度ブロックされた**: 20 秒待機 → 再試行で回復したが、長尺セッション中の一過性エラーへの待機が必要だった。
- **fix commit（doc 1 行）で server-side CI が head SHA に起動しなかった**: 最初の commit（270edb3）の CI は success だったが、2 度目の push（3738fc3）の `pull_request synchronize` 由来 run が約 2 分待っても作成されず、`statusCheckRollup` が空のままだった。branch protection に required status check が無いため auto-merge は CLEAN 即マージで完了したが、「CI 緑 on 最終 SHA」を厳密に満たせなかった。
- **architecture.md の MVP 双方向名称マップ記述（`IdByJaName` / `IdByEnName`）は supersede 注記の射程内だが現行 cli-and-io.md と表現が乖離したまま**: nit として残り、将来の architecture.md 現行化で追従が要る。

## 🚀 Try（次回の改善案）

- doc/skill が**実装シンボル名**（関数 / dex / 型）を引くときは、書く前に `grep` で実在を確認する手順を skill-authoring のセルフチェックに明示する（現状は「ADR/rule/ファイルの実在確認」はあるが**コードシンボルの実在確認**が弱い）。
- 設計 doc（architecture.md 等）の大規模レイアウト変更時は、**MVP 型例を supersede redirect 注記でまとめて現行 SoT へ誘導する**パターンを data-pipeline / type-conventions 追従の定石として skill / rule に残す（今回うまくいった手筋の明文化）。
- implementation-workflow の Phase 7 に、**required status check 不在 + 軽微 fix commit で head SHA に CI が起動しないケースの扱い**（doc-only fix は local verify 緑 + 直前 commit の CI 緑で許容、または空コミットで再トリガ）を明記して判断のぶれを抑える。

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit（doc/skill のみ・型不変） |
| カバレッジ | 100% | 100% | ±0 | Vitest（テスト不変） |
| Biome 違反 | 0 | 0 | ±0 | biome check + check:yaml-style |
| CI | pass | pass(※) | — | server-side pnpm verify（※fix commit の head SHA に run 未起動） |
| 差分 files/行 | — | 13 files | +285/-240 | gh pr diff |

## 🤖 ハーネス改善提案

<!-- harness-meta が parse する正規構造。未処理は [ ]、採用後に [x] + PR リンク。 -->

- [x] `[rule]` `skill-authoring.md` のセルフチェックに「**コードシンボル（関数 / dex / 型名）を doc/skill に書く前に grep で実在確認**」を追加する（架空 `reverseEn` 混入の再発防止・rule は実装 SoT を正確に指す）。→ 採用（PR #152・grep 実在確認テーマ #133/#135/#139 の反復が根拠）
- [ ] `[rule]` `implementation-workflow.md` Phase 7 に「**required status check 不在 + doc-only fix commit で head SHA に CI run が起動しない場合の扱い**」（local verify 緑 + 直前 commit CI 緑で許容 / 空コミット再トリガ）を明記する。
- [ ] `[skill]` 設計 doc の大規模レイアウト変更追従で「**MVP 型例は全書換せず supersede redirect 注記で現行 SoT（rule + ADR）へ一括誘導**」する手筋を `data-pipeline` / `type-conventions` 追従の定石として skill 系の参照に残す。
- [ ] `[rule]` 旧パス一掃の検証で「`廃止|旧 |移行|精緻化` を除外した `git grep` で残存ゼロを機械確認」する手順を harness-review / cross-agent 系のチェックに定石化する（今回の dangling ゼロ確認が有効だった）。

## 📝 harness-meta フィードバック

<!-- 空でも見出しは維持する。harness-meta が後から各表に追記する。 -->

### 採用

- `[rule]` skill-authoring セルフチェックにコードシンボル grep 実在確認 → **PR #152**（grep 実在確認テーマ #133/#135/#139 反復）。

### 見送り

- `[rule]` implementation-workflow Phase 7 の CI run 不起動時の扱い → 単発・skill 改修要のため見送り（`[ ]` 据え置き）。
- `[skill]` MVP 型例の supersede redirect 注記手筋 → 単発・skill 参照改修要のため見送り。
- `[rule]` 旧パス一掃の grep（除外ワード付き）残存ゼロ確認 → 既存 [[code-review]]「全文 grep で残滓追従」と重複寄りのため見送り。

### 保留
