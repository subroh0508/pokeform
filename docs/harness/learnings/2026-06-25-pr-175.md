---
id: learning-pr-175
title: PR #175 レトロスペクティブ (AGENTS.md 刷新 — 列挙から SoT 参照へ転換)
type: learning
status: actioned
related_pr: 175
related_plan: 08-docs-restructure/phase-05
generated_at: 2026-06-26T00:00:00Z
generator: pr-retrospective skill
---

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/175 / commit b78ad7987cc19ae0d90d663b85328f6c0e0ef6ed / merge 2026-06-25T17:26:13Z / 差分 2 files +20/-20。plan 08 Phase 5。陳腐化した AGENTS.md（指示 SoT）を実態へ追従し、コマンド表・skill 節・ディレクトリ説明を**列挙（実体重複）から参照（SoT へポインタ）へ転換**。

## ✅ Keep（継続したいこと）

- **列挙から SoT 参照への転換で陳腐化を構造的に解消**: コマンド表は「正本は package.json」、skill 節は「正本一覧は `.claude/skills/`」へ寄せ、実体重複を排除。今後 script/skill が増減しても AGENTS.md の追従が不要に。
- **コマンド実体を package.json と機械照合**: `pnpm verify` の連鎖を実体（`typecheck → test:cov → lint → check:yaml-style`）へ修正し、漏れていた `check:yaml-style` を回収。harness-review が架空コマンドゼロを確認。
- **skill 列挙を実在 16 skill と全件照合**: `ls .claude/skills/` と突き合わせ未列挙ゼロ・架空ゼロを確認。陳腐化注記「後続フェーズで追加される」も除去。
- **ディレクトリ説明の粒度を実態へ調整**: `*-specs.yaml` 総称を 6 種別へ、`languages/` の名前 SoT 役割を明示、data-pipeline.md へポインタ追加。実ディレクトリ構成と一致。
- **CLAUDE.md 薄アダプタを不変に保った**: `@AGENTS.md` import の薄アダプタ構造を崩さず、cross-agent 整合を維持。

## ⚠️ Problem（詰まった点・制約）

- **追加した導入行が節末の既存文と同一文重複**: 「前進の単位は `pnpm verify` が緑になること」が同一節内に二重出現（nit）。後続コミット b47b17e で「日常的に使う要点のみ:」へ絞り解消したが、レビュー指摘を要した。
- **「列挙→参照」転換の網羅性が手照合依存**: skill 16 / rule 11 / コマンド群を `ls` / `grep` で 1 件ずつ照合する必要があり、件数が増えると人手コストが上がる。
- **小 diff（+20/-20）でも意味的レビューが必須**: 行数は小さいが指示 SoT の正確性に直結し、機械ゲートでは捕れない（架空コマンド・陳腐化注記）。レビュー観点の重みが行数と非比例。

## 🚀 Try（次回の改善案）

- AGENTS.md / 指示 SoT の「列挙された skill / コマンドがすべて実在し、実在するものがすべて参照経由で辿れるか」を検査する軽量 script を harness-review の手順に組み込み、手照合を機械化する。
- 同一節内の重複文を検出する lint（または harness-review-checklist の「同一文重複」観点）を追加し、PR #175 の nit のような冗長を早期に拾う。
- 指示 SoT の刷新 PR は diff 行数が小さくても high effort レビュー（架空参照・陳腐化注記の網羅確認）を既定にする旨を [[code-review]] effort 段階に補足する。

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit |
| カバレッジ | 100% | 100% | ±0 | Vitest（185 tests） |
| Biome 違反 | 0 | 0 | ±0 | biome check |
| CI | pass | pass | — | server-side pnpm verify 緑 |
| 差分 files/行 | — | 2 files +20/-20 | ±0 | AGENTS.md + plan README |

## 🤖 ハーネス改善提案

- [ ] `[skill]` 指示 SoT（AGENTS.md）の「列挙された skill / コマンドが全実在し、実在物が全て参照経由で辿れるか」を検査する軽量 script を harness-review 手順に組み込み手照合を機械化。
- [ ] `[rule]` harness-review-checklist に「同一節内の同一文重複」観点を追加（PR #175 nit 由来）。
- [ ] `[rule]` [[code-review]] の effort 段階に「指示 SoT 刷新 PR は小 diff でも high effort（架空参照・陳腐化注記の網羅確認）を既定」を補足。

## 📝 harness-meta フィードバック

### 採用
（なし。本 learning の提案は下記理由で見送り）

### 見送り
- `[skill]` AGENTS.md の skill/コマンド実在検査 script → tooling。コスト過大で別 tooling phase 候補（指示 SoT 刷新時の手照合 + harness-review で当面カバー）。
- `[rule]` harness-review-checklist「同一節内の同一文重複」観点 → 単発 nit。反復シグナルなく見送り。
- `[rule]` code-review effort「指示 SoT 刷新 PR は high effort 既定」→ 単発。effort は呼び出し時判断で足り、規約固定は過剰。見送り。

### 保留
