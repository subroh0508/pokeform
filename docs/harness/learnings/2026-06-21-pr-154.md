---
id: learning-pr-154
title: PR #154 レトロスペクティブ (技マスター専用取得経路を新設し move-specs を Champions 準拠値へ是正)
type: learning
status: draft
related_pr: 154
related_plan: 05-move-master-scraper-refactor/phase-02
generated_at: 2026-06-21T07:48:50Z
generator: pr-retrospective skill
---

<!--
この雛形は `.claude/rules/retrospective-format.md`（SoT）に準拠する。
見出し（絵文字・文言）は harness-meta が parse するため変更しないこと。
書き出し前に `.claude/rules/redaction.md` の正規表現で Secrets / PII を [REDACTED-*] 置換すること。
-->

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/154 / commit 71ab30fa8b6cba6c127108266c8db3b525245656 / merge 2026-06-21T07:48:50Z / 差分 18 files +1369/-370。plan 05 Phase 2（ADR 0037 実装）。技専用ページ（attackdex-champions/<slug>.shtml）から威力 / 命中 / タイプ / 分類 / PP / **priority** を独立取得する決定論スクレイパー経路を `parse.ts`（`parseMoveMaster`）/ `schema.ts`（`validateMoveMaster`）/ `to-catalog.ts` / scripts 3 本に新設し、`move-specs.yaml` の全 219 技を Champions 準拠値（PP を `{8,12,16,20}` スケール）へ後勝ち上書き是正。種族ページ副産物抽出の除去は Phase 3 に分離し、両者が同 move-specs を書く間は技マスター後勝ちで Champions 値を保護した。

## ✅ Keep（継続したいこと）

- **ADR 0037 設計メモを実装へ忠実に落とした**: DOM 契約（type/damageClass/power/accuracy/pp/priority）・exit code 契約（0 健全 / 2 取得失敗 / 3 schema 欠落 / 4 件数・健全性）を設計メモ 1-2 のとおり `parseMoveMaster` / `validateMoveMaster` に実装。設計 phase（#153）で前倒しした判断がそのまま実装の指針になり、Phase 2 で再決定が発生しなかった。
- **決定論判定をスクレイパーに閉じ込めトークンを節約した**: 必中 accuracy `101`→null・変化技 power `0`→null・可変威力技（Serebii 表記 `1`）→null を parse で機械判定し、HTML を LLM コンテキストに載せず exit code で扱う方針を維持。
- **実 HTML 最小切片を fixture として vendored 固定しカバレッジ 100% を満たした**: 物理 / 特殊 / 変化 / 優先度(+1/-6) / 可変威力の `__fixtures__/serebii-move-*.html` を投入し parse / schema / to-catalog を網羅。実ページの罠を回帰テストで固定でき、取得経路の退行に強い。
- **slug 曖昧性をフォールバックで吸収した**: Serebii 技 slug がスペース圧縮形と実ハイフン保持形で混在する罠を、圧縮 → ハイフンの候補フォールバック取得（`data/raw/serebii/attackdex/` キャッシュ）で決定論的に解決。取りこぼし種を生まない。
- **同一データ（move-specs）の二重書きを後勝ちで安全側に倒した**: 種族ページ副産物抽出の除去を Phase 3 に分離しつつ、両者が同 move-specs を書く移行期間は技マスター専用取得を後勝ち上書きにして Champions 値が消えないよう保護した。Phase 分割と移行期の整合を両立。
- **是正結果を機械確認で検証した**: 全技の `pp ∈ {8,12,16,20}`（逸脱 0 件）・`check:regulation` 0 終了・代表技（earthquake PP=12 / draco-meteor PP=8 / quick-attack priority +1 / roar priority -6）の Serebii スポット照合まで確認。前作 PP 残存（5/10/15）の根絶を実証。

## ⚠️ Problem（詰まった点・制約）

- **同一 SoT（move-specs.yaml）を 2 経路が書く移行期間が Phase 2-3 をまたいで残った**: 技マスター専用取得（本 PR）と種族ページ副産物抽出（Phase 3 で除去）が同じ move-specs を書く状態が一時的に発生し、後勝ち順序という暗黙の不変条件に依存した。Phase 境界で「どちらが SoT か」が serebii-to-catalog の書き込み順に宿るため、順序を崩すと是正値が消える脆さがある。
- **是正の正しさ検証がスポット照合 + 機械チェックの組み合わせ依存**: 全 219 技を Champions 準拠値へ再 populate したが、値の網羅的な正しさは Serebii 一次ソースとの全件照合でなく代表技スポット + `pp` レンジゲートでの確認。レンジ外を弾けても、レンジ内で誤った PP は機械的に検出できない。
- **PR コメント（レビュー記録）が残っていない**: code-review / harness-review の PR コメントが付いておらず、+1369/-370 の大きめ実装 PR にもかかわらずマージ前レビューの記録が PR 上に無い。learning が SoT とはいえ、後追いでレビュー観点を辿る材料が PR コメントに無い。
- **取得経路新設で scripts 3 本（fetch / scrape / serebii-to-catalog）に横断的な配線が必要だった**: move-master サブコマンドを 3 スクリプトへ一貫配線する必要があり、stage 写像（exit 0/2/3/4）・構造化診断（`{slug, stage, missingFields, rawHtmlPath}`）を既存 species 経路と同型に保つ手作業が発生した。

## 🚀 Try（次回の改善案）

- **`[rule]` data-pipeline / serebii-sourcing に「同一 SoT を複数取得経路が書く移行期間は後勝ち順序を明記し、移行完了 phase で単一経路へ収束させる」運用を明文化**する。後勝ち順序という暗黙の不変条件を doc 化し、Phase 境界での是正値消失を防ぐ。
- **`[skill]` survey-regulation に「is master 是正後の検証は pp レンジゲート + 代表技スポット照合だけでなく、件数（全 219 技）と前作残存値（5/10/15）の不在 grep を必須にする」検証チェックリストを追加**する。レンジ内の誤値は機械検出できない前提を補う手順を明記。
- **`[skill]` implementation-workflow / code-review に「大きめ実装 PR（数百行超）はマージ前レビューコメントを PR に残す（ブロッキングなしでも記録）」を再確認**する観点を入れる。本 PR がレビュー記録なしでマージされた死角の再発防止。
- **`[rule]` data-pipeline に「取得経路を新設するときは fetch / scrape / serebii-to-catalog の 3 スクリプトへ stage 写像・構造化診断を species 経路と同型に配線する」テンプレを明記**し、横断配線の取りこぼしを防ぐ。

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit |
| カバレッジ | 100% | 100% | ±0 | Vitest（新 parse/schema/to-catalog を fixture で網羅） |
| Biome 違反 | 0 | 0 | ±0 | biome check + check:yaml-style |
| CI | pass | pass | — | server-side pnpm verify |
| 差分 files/行 | — | 18 files | +1369/-370 | gh pr diff（move-specs 全 219 技再 populate 含む） |

## 🤖 ハーネス改善提案

<!-- harness-meta が parse する正規構造。未処理は [ ]、採用後に [x] + PR リンク。 -->

- [ ] `[rule]` data-pipeline / serebii-sourcing に「同一 SoT を複数取得経路が書く移行期間は後勝ち順序を明記し移行完了 phase で単一経路へ収束させる」運用を明文化する（Phase 2-3 移行期の暗黙不変条件の制度化）。
- [ ] `[skill]` survey-regulation の技マスター是正検証に「pp レンジゲート + 代表技スポット照合 + 件数 + 前作残存値（5/10/15）不在 grep」を必須チェックとして追加する（レンジ内誤値の死角補完）。
- [ ] `[skill]` implementation-workflow / code-review に「大きめ実装 PR（数百行超）はブロッキングなしでもマージ前レビューコメントを PR に残す」観点を再確認する（PR #154 のレビュー記録なしマージの再発防止）。
- [ ] `[rule]` data-pipeline に「取得経路新設時は fetch / scrape / serebii-to-catalog 3 スクリプトへ stage 写像・構造化診断を既存経路と同型に配線する」テンプレを明記する。

## 📝 harness-meta フィードバック

<!-- 空でも見出しは維持する。harness-meta が後から各表に追記する。 -->

### 採用

### 見送り

### 保留
