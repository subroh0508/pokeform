---
id: learning-pr-114
title: PR #114 レトロスペクティブ (per-reg 持ち物 legality + メガストーン保持ルールの型強制)
type: learning
status: draft
related_pr: 114
related_plan: 03-survey-regulation-rework/phase-07
generated_at: 2026-06-14T15:41:45Z
generator: pr-retrospective skill
---

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/114 / commit 3cc258e / merge 2026-06-14T15:41:45Z / 差分 13 files +98/-33。`HoldableItems<R,S>` の `items: "any"` をグローバル `ItemId` から per-reg 解禁プール `RegulationItemId<R>` へ接続し、メガストーン保持ルール（base=全件 / メガ形態=対応ストーン 1 個）を `MegaStoneOf<S>` + `megaSpecies` リンクで型強制した。dual-reg 3 個体は M-A 単独へ絞り解禁持ち物へ修正。

## ✅ Keep（継続したいこと）
- **オーケストレーターが「ユーザー1回確認」事項を着手前に解決して worker へ確定値で渡した**。phase doc が代替持ち物選定をユーザー確認事項にしていたが、確定済み（garchomp=focus-sash / dragapult=lum-berry / hydreigon=choice-scarf+u-turn）を worker タスクに明記したため、実装中の往復ゼロで完了できた。
- **編集を小さく分割して適用した**（型 → item.ts → generate.ts → catalog → individual.ts → fixture → 個体 YAML → demo 新規）。単一巨大編集のストールを避けられ、各段で意図が追える差分になった。
- **負例を一時ファイルで実機検証してから本体に触らなかった**。負例①（非解禁 rocky-helmet）/ 負例②（メガ形態の他形態ストーン・通常持ち物）を `/tmp` の使い捨て YAML で `check:individual` し、tracked ファイルを汚さず受け入れ基準を確認できた。
- **doc 同期（phase doc タスク [x]・README 進捗・決定記録）をフィーチャー PR に同梱した**（learning #82/#102 と整合）。マージ後の別 PR 分離を避け、main に進捗が即反映された。
- **Accepted な ADR 0021 を編集せず phase doc 側に決定を記録した**。`status: Accepted` の本文改変（不変ログ規約違反）を回避し、「補注で足りる・新規 ADR 不要」方針と一貫させた。
- **生成物 `data/generated/items.ts` を手編集せず `generate:data` 経由で再生成した**。catalog → generate の決定性を保ち、megaSpecies 4 件が過不足なく反映された。

## ⚠️ Problem（詰まった点・制約）
- **`megaSpecies?: SpeciesId` の型表現に揺れがあった**。phase doc は `SpeciesId` と書くが、pokeform には global SpeciesId 型が無く（per-reg `keyof SpeciesDex` のみ）、既存 `megaStoneFor` は自己参照回避のため素の `string`。慣習に倣い `string` を採用したが、phase doc の字面と実装が一見食い違う（コメントで補った）。
- **`Read` 前に `Bash cat` で読んだファイルは Edit/Write が弾かれた**。items.yaml / 個体 YAML を最初 cat で確認したため、編集時に「File has not been read yet」エラーで Read し直す往復が複数回生じた。
- **コメント著述で簡体字「对」が混入した**（「対面操作」を「对面操作」と誤記）。日本語コメントの直後確認で気づいて修正したが、機械ゲート（Biome/型）では検出されない種類の事故。
- **Biome の format がコード変更で 1 度赤化**（`ItemsCatalog` の 1 行が長くなり複数行へ整形要求）。`pnpm format` で自動修正できる軽微なものだが、fix loop を 1 回消費した。
- **phase doc の「リスク・備考」が着手前の交差プール想定（M-A ∩ M-B）を残したまま**で、確定タスク（M-A 単独へ絞る）との追跡性がやや弱い（harness-review nit）。

## 🚀 Try（次回の改善案）
- phase doc / 型タスクで「global に存在しない型名（ここでは `SpeciesId`）」を仕様参照する際は、**実在する型 or 慣習（素の `string` + 自己参照回避）を併記**するよう type-conventions に一文足すと、worker の型表現の揺れを減らせる。
- `implementation-workflow` の worker 運用ノートに **「YAML/データファイルは確認も `Read` ツールで行う（`cat` で読むと Edit/Write 前に Read し直しになる）」** を追記すると往復を減らせる。
- 日本語コメント著述後の **簡体字/誤字の self-check** を、redaction と同様に「書き出し前チェック」の一項目として明文化する（機械ゲート非対象の事故枠）。
- phase doc テンプレの「リスク・備考」に、**着手後に確定した決定で前提が変わった場合の追記欄**（または確定値への置換）を促す注記を入れると、検討前提と最終解の追跡性が上がる。

## 📊 指標
| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit |
| カバレッジ | 100% | 100% | ±0 | Vitest（427/427 stmts・242/242 branches・90/90 funcs・355/355 lines） |
| Biome 違反 | 0 | 0 | ±0 | biome check（実装中 1 度 format 赤→`pnpm format` で解消） |
| CI | pass | pass | — | server-side pnpm verify（24s・run 27503703008） |
| 差分 files/行 | — | 13 files +98/-33 | — | gh pr diff |

## 🤖 ハーネス改善提案
- [ ] `[rule]` `type-conventions.md` に「global に存在しない型名を仕様参照する際は実在型 / 慣習（素の `string` + 自己参照回避）を併記する」一文を追加（megaSpecies の `SpeciesId` 揺れ由来）。
- [x] `[rule]` `implementation-workflow.md` の worker 運用ノートへ「YAML/データファイルの確認も `Read` ツールで行う（`cat` 読みは Edit/Write 前に Read し直しになる）」を追記。
- [ ] `[rule]` `redaction.md` または authoring 系 rule に「日本語コメント著述後の簡体字 / 誤字 self-check（書き出し前チェック）」を一項目として明文化。
- [ ] `[template]` phase doc テンプレの「リスク・備考」に、着手後に確定した決定で前提が変わった場合の追記 / 置換を促す注記を追加。

## 📝 harness-meta フィードバック
### 採用

- `[rule]` worker は YAML/データ確認も `Read` で行う運用ノートを `implementation-workflow.md` へ追記（#116 と反復＝Read ツール先行）。→ harness/meta-batch-114-123（本バッチ PR）
### 見送り

- `[rule]` type-conventions global 型名併記 / `[rule]` redaction 簡体字 self-check / `[template]` リスク・備考 前提変化注記 — 単発・低反復で現状運用可のため見送り（`[ ]` 据え置きで再評価余地）。
### 保留
