---
id: learning-pr-86
title: PR #86 レトロスペクティブ (情報源の役割 SoT 集約 + フロー図 Mermaid 化・phase-19)
type: learning
status: draft
related_pr: 86
related_plan: 02-data-model-redesign/phase-19
generated_at: 2026-06-13T06:51:32Z
generator: pr-retrospective skill
---

<!--
この雛形は `.claude/rules/retrospective-format.md`（SoT）に準拠する。
見出し（絵文字・文言）は harness-meta が parse するため変更しないこと。
書き出し前に `.claude/rules/redaction.md` の正規表現で Secrets / PII を [REDACTED-*] 置換すること。
-->

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/86 / commit 142a3ed3c7927b9627c5fd4ce2415d6c84b3161b / merge 2026-06-13T05:58:06Z / 差分 6 files +79/-21。情報源 3 系統（① Serebii 第一優先 / ② 補助裏取り / ③ PokeAPI 構造データ）の役割・関係性を `serebii-sourcing.md` に集約し、`architecture.md` / `data/README.md` の両フロー図を Mermaid 化（skill 著述 + 機械転記の 2 系統合流）。計画群 02 phase-16〜19 の締め。

## ✅ Keep（継続したいこと）

- **情報源の役割を `serebii-sourcing.md` に 1 か所集約**し、`SKILL.md` / `data-pipeline.md` / `data/README.md` をポインタ化して二重管理を解消（harness-review が重複記述ゼロを確認）。
- **両フロー図を Mermaid 化**し、3 流入 + 2 系統合流を明示。GitHub レンダリング前提で ASCII 罫線を排除（`data/README.md` の box-drawing 文字 0 件を検証）。
- **手順（取得順序 `fetch:data → materialize`・Serebii 第一優先）を変えず「図と SoT の可視化・整理」に範囲を限定**。ADR 0026/0027 の決定を再設計せず追従できた。
- **phase-18（用語 rename）後に着手して churn を最小化**。図中ラベルの用語が全資産で揃った状態で図を描き直せた（依存グラフのソフト依存 18→19 が機能）。

## ⚠️ Problem（詰まった点・制約）

- **non-blocking**: ユーザー auto-memory `serebii-first-priority-champions-data` の "How to apply" 末尾に「PokeAPI learnset を `check:regulation` の learnable 検証に使う」という記述が残り、ADR 0026（learnset 照合の撤去・`loadLearnsets` 廃止）および新設 SoT 節「PokeAPI learnset への照合はしない」と**直接矛盾**。memory はリポジトリ管理外で本 PR diff 外のため blocking にはできず、フォローアップ宿題として残った。
- **nit**: `architecture.md` の Mermaid `subgraph CH` 内の `rules.yaml`（`R`）ノードがどの辺にも接続されない宙ぶらりんノード。skill/AI 直接著述で raw/materialize 経路に乗らないため意図的だが、無接続ノードは「描き忘れ」と誤読されうる。
- **memory と doc SoT の同期ギャップ**: リポジトリ内の SoT を更新しても、リポジトリ外の auto-memory は自動追従しない。memory が古い決定を保持し続けるリスクが構造的に残る。

## 🚀 Try（次回の改善案）

- **memory `serebii-first-priority-champions-data` の stale ブレットを ADR 0026 に追従して修正**する（PokeAPI learnset 検証の記述を削除 or「撤去済み」へ更新）。本 learning のフォローアップとして即対応する。
- `architecture.md` の Mermaid 宙ぶらりん `rules.yaml` ノードに**凡例 or 注記**（「skill/AI 直接著述・パイプライン経路外」）を添えるか、図の意図を明示する（任意 nit）。
- ADR で決定が変わったとき、**関連 memory の追従点検**を `adr-new` / finish-phase の確認観点に加えることを検討（リポジトリ外 SoT のドリフト防止）。

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit |
| カバレッジ | 100% | 100% | ±0 | Vitest |
| Biome 違反 | 0 | 0 | ±0 | biome check |
| CI | pass | pass | — | server-side pnpm verify |
| 差分 files/行 | — | 6 files +79/-21 | — | gh pr diff |

## 🤖 ハーネス改善提案

<!-- harness-meta が parse する正規構造。未処理は [ ]、採用後に [x] + PR リンク。 -->

- [ ] `[rule]` `adr.md` / `finish-phase`: ADR で決定が変わった際に**関連 memory（リポジトリ外 auto-memory）の追従点検**を確認観点へ加える（doc SoT と memory のドリフト防止）。
- [ ] `[skill]` `harness-review`: Mermaid 図の**無接続ノードは凡例/注記で意図を明示**する観点を checklist に追加（「描き忘れ」誤読の防止）。

## 📝 harness-meta フィードバック

<!-- 空でも見出しは維持する。harness-meta が後から各表に追記する。 -->

### 採用

### 見送り

### 保留
