---
id: learning-pr-96
title: PR #96 レトロスペクティブ (serebii-to-catalog 転記 + ja 補完 + パイプライン結合 + ADR)
type: learning
status: draft
related_pr: 96
related_plan: 03-survey-regulation-rework / phase-03-catalog-writer-and-adr
generated_at: 2026-06-14T04:15:00Z
generator: pr-retrospective skill
---

<!--
この雛形は `.claude/rules/retrospective-format.md`（SoT）に準拠する。
見出し（絵文字・文言）は harness-meta が parse するため変更しないこと。
書き出し前に `.claude/rules/redaction.md` の正規表現で Secrets / PII を [REDACTED-*] 置換すること。
-->

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/96 / merge commit 03d2918 / merge 2026-06-14T04:09:16Z / 差分 14 files +814/-20。Serebii 中間 JSON を catalog / regulation YAML へ転記する決定論パイプライン（層1）を結合し、日本語名の取得元を PokeAPI `names` に定めた（ADR 0031 / 0032）。`serebii-to-catalog`（純関数 + 薄い配線）・`fetch-pokeapi` の names 取得・`materialize` の ja 補完を追加し、既存 M-A で git diff 空の idempotency を実証。

## ✅ Keep（継続したいこと）

- **純関数 + 薄い配線の層分離を Phase 1/2 から一貫踏襲**。判断分岐を持つ転記ロジックを `src/codegen/serebii/to-catalog.ts`（テスト100%）に置き、`scripts/serebii-to-catalog.ts` は fs/YAML I/O のみ（coverage 除外）。`materialize.ts` の `planFields` を再利用して append/既存尊重を二重実装しなかった。
- **設計の不確実点を実データで事前に潰した**。新規 ability の en を id から導出する `abilityEnFromId`（kebab→Title Case）が既存 catalog 46 件すべてと一致（mismatch 0）と確認してから採用。ja-Hrkt 優先 / `ja` フォールバックも実 raw で検証。推測でなく実証で設計を確定。
- **空 map が flow `{}` になり check:yaml-style に抵触する罠を実機 probe で事前検出**し、新規エントリを非空 block 化（`abilityEnFromId` の en 付与）で回避。ゲート抵触をコミット前に潰せた。
- **idempotency を materialize の append/既存尊重に委譲して低コストで担保**。既存 M-A garchomp で `serebii-to-catalog → materialize → check:regulation → generate:data` を通し **git diff 空**（0 fields filled）。mega 種族 ja（`メガリザードンＸ` vs PokeAPI base `リザードン` 5 件）は conflict 提示で表記揺れを可視化しつつ既存値を保護。ADR 0027 の設計がそのまま効いた。
- **名前と技メタを分離して ADR 0026 と矛盾させなかった**。PokeAPI を「ja 名の取得元」にしつつ（ADR 0032）、技メタ（type/power 等）の信頼源にはしない（ADR 0026 不変）を ADR 本文と fetch-pokeapi コメントで明示。
- **Generator/Evaluator 分離**。`code-review`（source）と `harness-review`（ADR/rule/skill）を `Agent` で並行独立起動し、両者 blocking 0 を PR コメントに記録してからマージ。
- **メガ linking の決定論変換不可を正直にスコープ縮小**。`Mega Garchomp` → catalog `garchomp-mega` の規約ずれを自動化せず diagnostic でエスカレーションし、ADR 0031 の Alternatives に却下理由を残した。

## ⚠️ Problem（詰まった点・制約）

- **中間 JSON（`ParsedSpecies`）に slug/id が無く**、`serebii-to-catalog` へ slug を別引数で渡す設計を要した。中間 JSON 契約（Phase 1 で確定）が転記側の必要情報（catalog key）を持たない隙間で、`en` → `toCatalogId` 推測に頼ると地域フォルム/メガで破綻するため明示引数にした。
- **items フィクスチャが合成（架空の `floettite`）でパイプライン idempotency 検証に使えず** revert を要した。fixture の用途（パーサ単体テスト用 synthetic）とパイプライン検証用（実データ subset）を一瞬混同し、`floettite`（ja 無し→generate 破綻）を一時投入してしまった。
- **空 block map が flow `{}` にレンダされる** yaml ライブラリ挙動の罠。`check:yaml-style`（flow 禁止ゲート）に抵触するため、新規エントリを必ず非空にする必要があり、設計（ability の en 補完手段）に制約を課した。
- **`data/raw` が worktree 間非共有 + メイン側 raw も不完全**（`salamence.json` 欠落）で `materialize` が fail-fast。`pnpm fetch:data` 再実行で補完を要した（cp は部分的になりうる・#94 でも近い観測）。
- **メガ種族 ja の materialize conflict が 5 件発生**（base 名 vs form 名）。既存値保護で実害は無いが、毎回 conflict ノイズが出る（form 種族を names 補完対象から除外する余地）。

## 🚀 Try（次回の改善案）

- Phase 4（Haiku 取得 SubAgent）の中間 JSON 契約設計時に、**`scrape-serebii` の出力 / `ParsedSpecies` に catalog slug を含める**か検討する（`serebii-to-catalog` が slug 推測や別引数に頼らず転記できる）。
- **fixture の用途分類**（パーサ単体用 synthetic / パイプライン検証用 real-subset）を `serebii-sourcing.md` か `testing.md` に明記し、合成 fixture を end-to-end 検証に誤用しない指針を残す。
- **新規 YAML map エントリは非空で作る（空 `{}` は flow になり check:yaml-style 抵触）** 運用ノートを `data-pipeline.md` か `testing.md` に追加（YAML 著述スクリプトの罠）。
- **worktree のデータ作業は `data/raw` 完全性を `pnpm fetch:data` で担保**（メイン cp は部分的になりうる）を `implementation-workflow` 運用ノートへ（#94 でも近い摩擦・反復観測）。

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit |
| カバレッジ | 100% | 100% | ±0 | Vitest（427 stmts / 242 branches / 90 funcs / 355 lines） |
| Biome 違反 | 0 | 0 | ±0 | biome check（format 自動修正後） |
| check:yaml-style | pass | pass | — | flow 禁止ゲート（全 catalog / regulation block） |
| CI | pass | pass | — | server-side pnpm verify（21s） |
| idempotent diff | — | 0 | — | 既存 garchomp 再転記で git diff 空 |
| 差分 files/行 | — | 14 files | +814/-20 | gh pr diff |

## 🤖 ハーネス改善提案

<!-- harness-meta が parse する正規構造。未処理は [ ]、採用後に [x] + PR リンク。 -->

- [ ] `[skill]` Phase 4 の SubAgent 中間 JSON 契約に **catalog slug を含める**（`scrape-serebii` 出力 / `ParsedSpecies` 拡張）。`serebii-to-catalog` の slug 別引数依存を解消する余地。
- [ ] `[rule]` `serebii-sourcing.md` か `testing.md` に **fixture の用途分類**（synthetic パーサ単体用 / real-subset パイプライン検証用）を明記し、合成 fixture の end-to-end 誤用を防ぐ。
- [ ] `[rule]` `data-pipeline.md` か `testing.md` に **新規 YAML map エントリは非空で作る（空 `{}` は flow＝check:yaml-style 抵触）** 運用ノートを追加。
- [ ] `[skill]` `implementation-workflow` 運用ノートに **worktree のデータ作業は `fetch:data` で `data/raw` を完全化する（メイン cp は部分的になりうる）** を追記（#94 と反復）。
- [ ] `[remove]` （なし）

## 📝 harness-meta フィードバック

<!-- 空でも見出しは維持する。harness-meta が後から各表に追記する。 -->

### 採用

### 見送り

### 保留
