---
id: learning-pr-214
title: PR #214 レトロスペクティブ (items.yaml を対戦持ち物のみに絞る・PokeAPI item-category whitelist 列挙)
type: learning
status: draft
related_pr: 214
related_plan: —
generated_at: 2026-07-05T05:41:03Z
generator: pr-retrospective skill
---

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/214 / commit 4527e0fe69442ed915cc2a97a1fb627e5c929fa8 / merge 2026-07-05T05:41:03Z / 差分 11 files +1012/-19613。`languages/items.yaml` を PokeAPI `/item` 全件（2176）→ **item-category whitelist の union（268件）**へ剪定。属性(holdable)は PokeAPI データが不完全ゆえ不採用、category ベースで列挙。純ロジック(sortedUnion/pruneToKeep)を src/codegen へ + テスト100%、ADR 0042 起票。独立レビュー 3 本（code/harness/data）とも blocking 0。

## ✅ Keep（継続したいこと）

- **属性ベースを実測で棄却してから category ベースを選定した**。`holdable`/`holdable-active` 属性は PokeAPI が古いアイテムにしか付与せず assault-vest / booster-energy / mega-stones を取りこぼすことを curl で確認し、全アイテムに必ず付く `category` を採用。設計選択の根拠を実データで固めた。
- **coverage 除外の狭め方で「除外死角」を能動的に潰した**。純関数 `sortedUnion` / `pruneToKeep` を `src/codegen/materialize.ts` に置いて 100% ゲートに載せ、coverage 除外を `normalize.ts`/`run-tsc.ts`/`emit-*-ts.ts` の 4 本へ狭めた。checklist が警告する #191→#206 の除外死角を能動的に縮小した良い変更（code-review 評価）。
- **union manifest で fetch→prune を安全に結合した**。`writeItemUnionManifest(ids)` が union 全件（新規取得分だけでない）を書くため prune の keep 集合が完全で既取得エントリを誤除去しない。manifest 不在時は `pruneItemsToUnion` が `return 0` で剪定せず他 4 種の全件辞書に無影響、という安全既定を持つ。
- **剪定が非破壊で ja/en を保持した**。`removed` のみ `map.delete`、`kept` は未変更ゆえ retained id の ja/en は byte-for-byte 保持（diff は純削除 5682 行・追加 0）。
- **二重の安全網で silent breakage を防いだ**。剪定後に `generate:data`（spec ⊆ languages の superset 判定・ADR 0041）が走り、仮に spec 参照 item を剪定しても非0終了で loud に検知する。
- **代表アイテム残存 + 除去対象消滅を実データ突合で裏取りした**。pokemon-data-reviewer が全カテゴリ件数合計（=268）と剪定後件数の一致・item-specs 86 件と per-reg items の参照整合（`comm` 差分空）まで機械照合。
- **doc-data 乖離ゼロで追従した**。category 選定根拠と `medicine` カテゴリの額面 vs 実体（木の実でありポーションでない）を code コメント・ADR 0042・rule・skill の全箇所へ漏れなく反映。

## ⚠️ Problem（詰まった点・制約）

- **whitelist 13 カテゴリが rule と code の両方に実体列挙され SoT の向きが循環的で曖昧**。`data-pipeline.md` の列挙と `fetch-pokeapi.ts` の `ITEM_CATEGORIES` が完全一致するが機械同期できず drift 温床。ADR 0042 は「正本は [[data-pipeline]]」、rule は「（SoT・`ITEM_CATEGORIES` と一致）」と code を正とし、SoT が循環している（harness-review non-blocking）。完全な enumerable value（13 slug）を rule が抱えるのは「要点再記述 + 正本ポインタ」を超えた重複。
- **ADR の「除外カテゴリの正本は rule」文言が rule 実体と不整合**。`data-pipeline.md` は whitelist（対象）を列挙する一方、除外は散文の例示のみで明示的カテゴリ列挙を持たない。「除外は whitelist の補集合」と表現する方が正確（harness-review nit）。
- **items の DATASETS `list` フィールドが dead**。`listCategories` 設定時に三項短絡され items 経路では未使用。将来「items も listAllIds を通る」と誤読されうる（code-review non-blocking）。
- **`sync:ja-names` 単体を古い manifest に手実行すると stale union へ剪定しうる**。workflow 経路（raw は CI で ephemeral・逐次実行）では起きないが、手動誤用時のみのエッジ（code-review nit）。
- **数値表記ゆれ**（ADR/rule「~270 件」vs 実測 268）。近似として許容範囲だが実数へ寄せる余地。

## 🚀 Try（次回の改善案）

- **多カテゴリ enum のような「完全 enumerable value」は rule と code に二重列挙せず SoT を一意化する**。`ITEM_CATEGORIES` を列挙 SoT に確定し rule は「選定基準（持って対戦効果があるか）+ `ITEM_CATEGORIES` へのポインタ」に絞る、または人間可読ミラーとして残すなら「列挙 SoT は code。以下は可読ミラー」と一意化する。→ `[rule]` data-pipeline / skill-authoring（「値の実体は src/ に置き rule は要点+ポインタ」の enum への適用）。
- **whitelist モデルの「除外」は補集合として表現する**。ADR/rule の「除外カテゴリの正本」を「除外 = whitelist の補集合」へ言い換え、除外の実体列挙を持たない設計と文言を一致させる。→ `[rule]` data-pipeline。
- **dead フィールド（items の `list`）は型で表現する**。`list?: string` にするか items から省き、経路の分岐を型レベルで明確化。→ `[skill]` code-review checklist（「dataset 分岐で使わないフィールドは optional 化 or 除去」を AI 生成コードの観点に追加）。

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit |
| カバレッジ | 100% | 100% | ±0 | Vitest（materialize.ts を exclude から外し 100% gate に） |
| Biome 違反 | 0 | 0 | ±0 | biome check |
| CI | pass | pass | — | server-side pnpm verify |
| 差分 files/行 | — | 11 files +1012/-19613 | — | items.yaml 純削除 5682 行含む（[[planning]] 6基準⑤例外） |

## 🤖 ハーネス改善提案

- [ ] `[rule]` `data-pipeline` / `skill-authoring`: 完全 enumerable value（whitelist カテゴリ全列挙等）は rule と code に二重列挙せず SoT を一意化する（列挙 SoT は code = `ITEM_CATEGORIES`、rule は選定基準 + ポインタ、または「可読ミラー」明示）。「値の実体は src/ に置き rule は要点 + 正本ポインタ」の enum への適用を明文化。
- [ ] `[rule]` `data-pipeline`: whitelist モデルの「除外」は実体列挙でなく「whitelist の補集合」として表現する（ADR/rule の「除外カテゴリの正本」文言を実体と一致させる）。
- [ ] `[skill]` `code-review`: AI 生成コード観点に「dataset/分岐ごとに使わない共通フィールド（例 items 経路の `list`）は optional 化 or 除去して経路を型で明確化」を追加。

## 📝 harness-meta フィードバック

### 採用

### 見送り

### 保留
