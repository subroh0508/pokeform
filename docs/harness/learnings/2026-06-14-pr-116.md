---
id: learning-pr-116
title: PR #116 レトロスペクティブ (per-reg species から不要な種族名 ja/en を削除)
type: learning
status: draft
related_pr: 116
related_plan: 03-survey-regulation-rework/phase-08
generated_at: 2026-06-14T15:59:12Z
generator: pr-retrospective skill
---

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/116 / commit 4747bff / merge 2026-06-14T15:59:12Z / 差分 8 files +32/-168。読み手のいない per-reg species dex の `name`（ja/en）を生成形から削除し、`PerRegSpecies = Omit<SpeciesBase, "name">` を新設。種族名 SoT は `speciesBaseDex` に一本化（重複 dedup）。型・生成形をセットで変更し pnpm verify 緑・差分は純減 136 行。

## ✅ Keep（継続したいこと）
- **型と生成形をセットで変更した**。`name` を削除すると `satisfies Record<string, SpeciesBase>` が通らなくなるため、`PerRegSpecies` 新設 → generate.ts の entry/satisfies → 追従先（regulation/individual）まで一括で揃え、中間状態で型が割れなかった。
- **phase doc に明記されていなかった追従先（`RegulationBase.speciesDex`）を整合チェックで先に洗い出した**。`satisfies RegulationBase` が `speciesDex: Record<string, SpeciesBase>` を要求するため必須の連鎖変更で、Phase 2 の整合性チェックで漏れなく拾えた。
- **「読み手がいない」を grep で実機確認してから削除した**。`speciesDex[...].name` 系アクセスが src/scripts に 0 件・種族名表示は全て `speciesBaseDex`（`src/cli/format.ts`）経由であることを確認し、安全な削除と判断できた。
- **生成物 `species.ts` を手編集せず `generate:data` で再生成し、差分が name 削除のみに限定されることを git diff で確認した**。決定性（再実行で clean）も確認。
- **編集を小さく分割して適用した**（species.ts 型新設 → generate.ts 2 箇所 → regulation.ts → individual.ts）。各段の意図が差分で追える。
- **doc 同期（phase doc タスク [x]・README 進捗）をフィーチャー PR に同梱した**（learning #82/#102 と整合）。

## ⚠️ Problem（詰まった点・制約）
- **phase doc のタスクに `RegulationBase.speciesDex` の追従が抜けていた**。実装上は必須（satisfies RegulationBase が name を要求）だが、タスク列挙は `individual.ts` の `SpeciesEntryOf` までで止まっていた。整合チェックで補えたが、phase doc 単体では追従先が不完全だった。
- **`Read` 前に `Bash grep/cat` で参照したファイルは Edit/Write が弾かれた**（individual.ts）。型の確認は grep で済ませたが、編集時に Read し直しの往復が生じた（Phase 7 と同種の摩擦・反復）。
- **`analyze:coverage team/individuals` が無出力で受け入れ確認の材料にしづらかった**。種族名表示の確認は `stat` で代替できたが、`check:party` は party MD が team/ に無く実行材料が用意されていなかった。

## 🚀 Try（次回の改善案）
- 「型 X を変えると `satisfies Y` 連鎖で追従が必要な箇所」を **phase doc / split-criteria に "satisfies 連鎖の追従先を列挙する" 一項目**として足すと、タスク列挙の漏れ（今回 `RegulationBase.speciesDex`）を設計段で拾える。
- `implementation-workflow` の worker 運用ノートへ **「ソースファイルの確認も `Read` ツールで行う（`grep`/`cat` 読みは Edit/Write 前に Read し直しになる）」** を追記（Phase 7 #114 でも同提案・反復）。
- **受け入れ基準が CLI 表示を要求する phase では、検証用のサンプル party MD（または stat 対象個体）を phase doc が明示**すると、`check:party` 等の確認材料が用意でき受け入れ確認が滑らかになる。

## 📊 指標
| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit |
| カバレッジ | 100% | 100% | ±0 | Vitest（427/427 stmts・242/242 branches・90/90 funcs・355/355 lines） |
| Biome 違反 | 0 | 0 | ±0 | biome check（fix loop 0 回） |
| CI | pass | pass | — | server-side pnpm verify（22s・run 27504191903） |
| 差分 files/行 | — | 8 files +32/-168 | — | gh pr diff（純減 136・生成形 dedup） |

## 🤖 ハーネス改善提案
- [ ] `[skill]` `plans-new` の split-criteria（references/split-criteria.md）に「型変更時の `satisfies` 連鎖の追従先を列挙する」観点を追加し、phase doc タスクの追従漏れ（今回 `RegulationBase.speciesDex`）を設計段で拾えるようにする。
- [ ] `[rule]` `implementation-workflow.md` の worker 運用ノートへ「ソース/データファイルの確認も `Read` ツールで行う（`grep`/`cat` 読みは Edit/Write 前に Read し直しになる）」を追記（#114 と反復）。
- [ ] `[template]` phase doc テンプレの「検証手順」に、CLI 表示確認を要求する場合の検証用サンプル（party MD / stat 対象個体）の明示を促す注記を追加。

## 📝 harness-meta フィードバック
### 採用
### 見送り
### 保留
