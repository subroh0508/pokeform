---
id: learning-pr-176
title: PR #176 レトロスペクティブ (rule/skill の plan 参照違反是正 + 全 rule へ front matter)
type: learning
status: actioned
related_pr: 176
related_plan: 08-docs-restructure/phase-06
generated_at: 2026-06-26T00:00:00Z
generator: pr-retrospective skill
---

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/176 / commit 651c2b1dbb6b6e584c09d96b149bbe2ad6808a13 / merge 2026-06-25T17:31:28Z / 差分 18 files +73/-5。plan 08 最終 Phase 6。規約 / skill の plan 参照違反・陳腐化を是正し、ADR 0038 で定めた `.claude/rules/` 全 15 Markdown へ front matter（`last_modified` + `adr`）を付与。文体圧縮でなく正しさの是正に徹した。

## ✅ Keep（継続したいこと）

- **plan 参照違反を ADR 直接参照へ張り替え**: `code-review.md` の「Phase 9」phase doc 参照を ADR 0013 へ向け直し、[[adr]] の「可変 plan ファイルを引かない」違反を解消。`grep -nE 'Phase [0-9]|phase-[0-9]'` で 0 hit を確認。
- **全 15 rule の front matter `adr` を active のみで付与**: 各 rule の決定根拠 ADR を精査し active ADR のみ列挙、archive 退避番号（0025/0026/0031/0032/0034）の混入ゼロを harness-review が全件照合で確認。
- **front matter 付与の `paths` 非干渉を再生成 diff ゼロで証明**: `gen-rules-index.ts` 再生成で `rules-index.md` の diff が空であることを確認し、`last_modified` / `adr` が独自キーとして `paths` 解釈に非干渉と実証。
- **skill 陳腐化の素の phase 番号依存を文脈表記へ**: `serebii-sourcing.md` の「Phase 7」2 箇所を「M-A ロスター正確化 / M-A 解禁データ調査」へ置換し、plan renumber 耐性を持たせた。
- **start-phase の trigger 精度を skill-authoring 基準で強化**: implementation-workflow との使い分け（単発準備 vs 端から端まで駆動）を description（≤1024 字）・本文・関連節で明示し over-trigger を抑制。
- **既に是正済みの項目を再変更しない判断**: `harness-meta` の旧 phase 表記は先行計画 07 で是正済みと確認し、追加変更を行わなかった（過剰変更の回避）。

## ⚠️ Problem（詰まった点・制約）

- **front matter `adr` が「本文が背景として引く archive 退避 ADR」を網羅しない**: `skill-authoring.md`（→0031）/ `implementation-workflow.md`（→0026/0034）等が背景に archive ADR を引くが、その active 後継が `adr:` に必ずしも載らない。dangling は無いが「背景まで網羅するか」の基準が未定（non-blocking）。
- **design ↔ rule で front matter `adr` 先頭の体裁が割れる**: design は規約制定元 ADR 0038 を先頭に載せるが rule は載せない。rule の存在自体は 0038 由来でないため整合的だが、体裁を厳密に揃えるか未決（nit）。
- **`last_modified` を全 15 rule で同一値（手入力）に統一**: `2026-06-26T00:00:00+09:00` を一括手入力。実際の各 rule 最終更新日とは無関係の付与時刻で、今後の更新時に実態とずれる構造。

## 🚀 Try（次回の改善案）

- front matter `adr` の網羅範囲（決定根拠のみ / 本文が背景に引く ADR まで）を ADR 0038 または front matter 規約 SoT で 1 箇所定義し、archive 後継の追補要否を機械判断できるようにする（PR #176 non-blocking 由来）。
- design / rule の front matter 体裁（`adr` 先頭の制定元 ADR を載せるか）の統一方針を front matter 規約に明文化する（nit 由来）。
- `last_modified` を一括手入力でなく、変更ファイルに対し commit 時刻で機械更新する仕組み（pre-commit / prepare）を検討し、付与値と実態の乖離を防ぐ（PR #171 / #176 で反復）。

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit |
| カバレッジ | 100% | 100% | ±0 | Vitest |
| Biome 違反 | 0 | 0 | ±0 | biome check |
| CI | pass | pass | — | server-side pnpm verify 緑 |
| 差分 files/行 | — | 18 files +73/-5 | +68 | rules 15 + skill 2 + README |

## 🤖 ハーネス改善提案

- [ ] `[rule]` front matter `adr` の網羅範囲（決定根拠のみ / 本文が背景に引く archive 後継まで）を ADR 0038 / front matter 規約 SoT で 1 箇所定義（PR #176 non-blocking 由来）。
- [ ] `[rule]` design / rule の front matter 体裁（`adr` 先頭の制定元 ADR を載せるか）の統一方針を front matter 規約に明文化（nit 由来）。
- [ ] `[skill]` `last_modified` を一括手入力でなく commit 時刻で機械更新する仕組み（pre-commit / prepare）を検討し付与値と実態の乖離を防ぐ（PR #171 / #176 反復）。

## 📝 harness-meta フィードバック

### 採用
（なし。本 learning の提案は下記理由で見送り）

### 見送り
- `[rule]` front matter `adr` 網羅範囲の SoT 定義 → **ADR 0038 が現行 SoT**。網羅範囲（決定根拠のみ/archive 後継まで）の細則は規約確立直後で、新規 live rule 化は時期尚早。見送り（#170 と同主題）。
- `[rule]` front matter 体裁（adr 先頭の制定元 ADR）統一方針 → 同上。ADR 0038 の例示で足り、細則明文化は時期尚早。見送り。
- `[skill]` `last_modified` の commit 時刻機械更新（pre-commit/prepare）→ tooling。ADR 0038 が「機械強制しない・手動更新」を意図的に選択済（#171 と反復・方針一致のため見送り）。

### 保留
