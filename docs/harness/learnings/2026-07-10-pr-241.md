---
id: learning-pr-241
title: PR #241 レトロスペクティブ (M-A/M-B 新規メガストーン 34件を languages へ ja/en 先行投入)
type: learning
status: draft
related_pr: 241
related_plan: —
generated_at: 2026-07-18T18:18:08Z
generator: pr-retrospective skill
---

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/241 / commit 00f11f6e3086ace563175b8f8693f1e30a53f005 / merge 2026-07-10T18:05:46Z / 差分 1 files +102/-0

## ✅ Keep（継続したいこと）

- **ブロッカー解除の前段を最小 PR に切り出した**: showdown-sync（フル rollout）が `generate.ts` の `requireNames` で新規メガストーンの ja/en 欠落により停止し `data:authoritative` PR が立たない、という根因を特定し、「名前 34 件の先行投入」だけを 1 file・+102 行の独立 PR に分離した。解禁データ本体（roster/技/メガ構造）投入と切り離すことで PR 粒度がぶれず、後続のフル rollout のゲートを通す道を先に開けた。
- **en は authoritative 抽出・ja は慣例著述と出典を明確に分離**: en は pokemon-showdown 抽出（`showdown:items` / `showdown:mega`）由来と明記し、ja は PokeAPI（names 空）も Serebii（itemdex の Japanese Name 欄が非公開）も現時点で未公開である事実を PR 本文に書いた上で命名慣例に基づくと宣言。名前の由来（正 vs 慣例）を混同せず追跡可能にした。
- **命名慣例をルールとして明文化**: 「ベース種族カタカナ末尾が長音（ー）/撥音（ン）なら 1 字落として + ナイト、それ以外はそのまま + ナイト」「X/Y フォルムは全角Ｘ／Ｙ（既存 `リザードナイトＸ` 等に整合）」という規則を言語化し、例外（`chimechite` は撥音を省略せず チリーンナイト）も注記。個別の当てずっぽうでなく再現可能な規則として投入した。
- **id→ja→ベース種族の全対応表を PR 本文に残した**: 34 件すべてを M-A（23）/ M-B（11）の表で列挙し、ユーザー確認済み（✓）マークも併記。後日の出典照合や慣例の検証が PR 本文だけで辿れる。
- **append-only master のアルファベット順整列を維持**: `data/languages/items.yaml` の既存整列を崩さず挿入し、`check:yaml-style` を含む `pnpm verify` 緑。生成データ（`src/generated/`）は spec 未参照の orphan として不変（ADR 0041 で許容）を確認済み。

## ⚠️ Problem（詰まった点・制約）

- **ja を出典で裏取りできない技術的負債が残る**: PokeAPI の names が空、Serebii itemdex の Japanese Name 欄も非公開のため、34 件の ja は命名慣例に頼らざるを得なかった。公開され次第の照合が follow-up として残るが、その追跡が PR 本文の一文以外に仕組み化されていない（放置されると陳腐化に気づけない）。
- **命名慣例に機械化できない例外がある**: `chimechite`（チリーン）は撥音を省略せず チリーンナイト とする、という規則外の個別判断が混じる。慣例をそのまま自動適用できず、種族ごとに例外可否を人が判断する必要が残る。
- **名前先行投入と解禁データ本体の整合がこの PR 単独では取れない**: M-B の authoritative roster は 76 メガだが、コミット済みの 10 体はクラシックメガのみの placeholder。本 PR は M-B が実参照するストーン名を先行投入するが、roster 本体の投入は後続 PR に依存する。名前と構造が別 PR に分かれる前提で、単独では reg 全体の整合を確認できない。
- **ユーザー確認済み（✓）と未確認の慣例著述が混在**: 対応表で ✓ が付くものと付かないものがあり、未確認の慣例名がそのままマージされている。未確認分の妥当性は公開後照合まで保証されない。

## 🚀 Try（次回の改善案）

- **[skill] author-static-data に「メガストーン ja が PokeAPI/Serebii 双方で未公開のケースは命名慣例で著述し、公開後照合を follow-up 化する」分岐を明記**する。あわせて命名慣例ルール（末尾長音/撥音を 1 字落として + ナイト・X/Y は全角Ｘ／Ｙ・例外は個別注記）を references に SoT 化し、慣例例外（`chimechite` の撥音非省略等）を列挙して個別判断のブレを減らす。
- **[rule/skill] 出典なし（慣例著述）で投入した名前に「公開後照合 follow-up」を追跡する仕組みを定める**。PR 本文の一文に埋めるのでなく、follow-up issue 起票または languages への TODO/未確認マーカー慣行を data-pipeline 系 rule か author-static-data に明文化し、公開後の照合漏れを防ぐ。
- **[skill] author-regulation-data 側に「解禁データ本体投入の前段として、新規メガストーン名を先行投入して requireNames を解く」定型ステップを明記**する。今回は名前先行投入 → フル rollout の順で解けたが、この順序が skill から辿れず暗黙知になっている。

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit |
| カバレッジ | 100% | 100% | ±0 | Vitest（データ投入ゆえコード不変） |
| Biome 違反 | 0 | 0 | ±0 | biome check |
| CI | pass | pass | — | server-side pnpm verify（25s・自作 PR ゆえ on:pull_request 発火） |
| 差分 files/行 | — | 1 files +102/-0 | — | items.yaml へ 34 件（M-A 23 / M-B 11） |

## 🤖 ハーネス改善提案

- [ ] `[skill]` author-static-data に「メガストーン ja が PokeAPI/Serebii 双方未公開のケースは命名慣例で著述・公開後照合を follow-up 化」分岐と命名慣例ルール（長音/撥音落とし + ナイト・X/Y 全角・例外注記）を references へ SoT 化。
- [ ] `[rule]` 出典なし（慣例著述）で投入した名前の「公開後照合 follow-up」を追跡する慣行（follow-up issue / languages の未確認マーカー）を data-pipeline 系 rule か author-static-data に明文化。
- [ ] `[skill]` author-regulation-data に「解禁データ本体投入の前段として新規メガストーン名を先行投入し requireNames を解く」定型ステップを明記。

## 📝 harness-meta フィードバック

### 採用

### 見送り

### 保留
