---
id: learning-pr-58
title: PR #58 レトロスペクティブ (survey-regulation を Serebii 第一優先・全量 materialize へ拡張)
type: learning
status: draft
related_pr: 58
related_plan: 02-data-model-redesign/phase-08
generated_at: 2026-06-10T06:05:00Z
generator: 手動
---

<!--
この雛形は `.claude/rules/retrospective-format.md`（SoT）に準拠する。
見出し（絵文字・文言）は harness-meta が parse するため変更しないこと。
書き出し前に `.claude/rules/redaction.md` の正規表現で Secrets / PII を [REDACTED-*] 置換すること。
-->

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/58 / commit 97827ae / merge 2026-06-10T05:32:05Z / 差分 2 files +181/-53

## ✅ Keep（継続したいこと）

- 人手で実行した一連の調査・投入手順（Phase 7）を、次フェーズが routine 再利用できる skill 手順へ**恒久化**した。実投入（Phase 9）の直前に手順を固める順序が de-risk に効いた。
- 詳細手順（Serebii URL パターン・件数検証・learnset 照合機構）を `references/serebii-sourcing.md` へ分離し、本体を 161 行（≤500）に保った（progressive disclosure）。
- メモリ `serebii-first-priority-champions-data` の方針を skill 手順へ固定化し、方針とその参照先を一致させた。
- 旧スキーマ記述（`allow.{...}` / `moves 記録のみ`）を現行スキーマ（種族キー = 解禁・per-species `moves` 全量・block 記法）へ更新し、ADR 0021/0022/0023 を正しく参照（doc-data 乖離の解消）。
- `harness-review` を独立 Agent で起動し、Generator/Evaluator を分離して blocking 0 を確認。

## ⚠️ Problem（詰まった点・制約）

- プロジェクト固有の `skill-creator` skill が存在せず（グローバル example 版は汎用 scaffolding）、AGENTS.md / rules が「skill 改修は `skill-creator` を使う」と指示するが実体が無い。`skill-authoring` rule の基準を直接適用して代替したが、指示と実体の乖離がある。
- ADR のファイル名を最初に推測で書き（`0022-move-recording-schema.md` 等）、実在名（`0022-per-regulation-species-keyed-moves.md`）と不一致だった。`ls docs/adr/` で確認して修正した。
- description の文字数制約（≤1024）を `wc -c`（バイト）で誤判定しかけた。日本語は 3 バイト/字のため、文字数で数え直す必要があった（809字で適合）。
- skill 本体に file 参照を `[[references/...]]`（wikilink）で書きかけた。wikilink は rule/memory 名用で、ファイル参照は markdown リンクが正。

## 🚀 Try（次回の改善案）

- `skill-creator` の実体不在を解消する: グローバル skill-creator をプロジェクトに導入するか、AGENTS.md / `skill-authoring` rule の「`skill-creator` を使う」記述を「不在時は `skill-authoring` rule 基準を直接適用」と明記する。
- skill 改修時のセルフチェックに「ADR/ファイル参照は `ls` で実在確認」「description は文字数（バイトでなく）で ≤1024」「ファイル参照は markdown リンク・`[[...]]` は rule/memory 名のみ」を追加する。

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit |
| カバレッジ | 100% | 100% | ±0 | Vitest |
| Biome 違反 | 0 | 0 | ±0 | biome check |
| CI | pass | pass | — | server-side pnpm verify |
| 差分 files/行 | — | 2 files | +181/-53 | gh pr diff |

## 🤖 ハーネス改善提案

<!-- harness-meta が parse する正規構造。未処理は [ ]、採用後に [x] + PR リンク。 -->

- [ ] `[skill]` `skill-creator` の実体不在を解消する（グローバル版導入 or AGENTS.md/`skill-authoring` に「不在時は rule 基準を直接適用」を明記）。指示と実体の乖離を埋める。
- [ ] `[rule]` `skill-authoring` のセルフチェックに「ADR/ファイル参照は `ls` で実在確認・description は文字数で ≤1024・ファイル参照は markdown リンク（`[[...]]` は rule/memory 名のみ）」を追記。
- [ ] `[template]`
- [ ] `[adr]`
- [ ] `[remove]`

## 📝 harness-meta フィードバック

<!-- 空でも見出しは維持する。harness-meta が後から各表に追記する。 -->

### 採用

### 見送り

### 保留
