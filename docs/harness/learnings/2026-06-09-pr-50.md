---
id: learning-pr-50
title: PR #50 レトロスペクティブ (plan README 群の薄索引化・テンプレ正本一本化)
type: learning
status: draft
related_pr: 50
related_plan: 00-harness-setup（ハーネス資産横断・doc 構造）
generated_at: 2026-06-09T17:26:28Z
generator: pr-retrospective skill
---

<!--
この雛形は `.claude/rules/retrospective-format.md`（SoT）に準拠する。
見出し（絵文字・文言）は harness-meta が parse するため変更しないこと。
-->

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/50 / commit 36663fa6f6bd27cc92ada028c2ac5e1185f03188 / merge 2026-06-09T17:23:44Z / 差分 11 files +214/-181。plan README 群が OVERVIEW/rules に置くべき内容を抱えて drift する問題を、確定スコープ A〜H（テンプレ正本一本化・トップ進捗の status ロールアップ化・00 OVERVIEW 新設・薄索引化・陳腐化修正・進捗表更新の申し送り）で解消。doc のみ・ロジック影響なし・`pnpm verify` 緑。

## ✅ Keep（継続したいこと）

- **ブリーフ（確定スコープ）を SoT に implementation-workflow を駆動**: ユーザー承認済みの A〜H スコープを仕様として OVERVIEW/phase doc を新規に起こさず端から端まで実装。確定済みスコープには plans-new を経ずブリーフ駆動で進める判断が妥当だった。
- **テンプレ正本の一本化で drift 源を断つ**: phase doc テンプレを `docs/plan/README.md` と `plan-templates.md` の双方が持つ二重管理を、`plan-templates.md` を正本に格上げして解消。SoT を 1 箇所に寄せる原則が doc にも効く。
- **進捗の SoT 分離（per-phase=サブ README / 計画単位 status=トップ）**: トップを分数なしの status ロールアップ表に絞り、per-phase チェックをサブ README のみに残すことで Markdown 間の状態ズレ頻度を構造的に下げた。
- **Generator/Evaluator 独立を厳守**: harness-review を Agent で独立起動し、自己採点で代替しなかった。non-blocking で相対リンク深さの実バグ（自分が作り込んだ）を捕捉でき、独立レビューの価値が出た。
- **non-blocking/nit を blocking 0 でも cheap fix として取り込み**: drift 低減という PR の目的に直結する指摘（テンプレ片の相対リンク深さ・repoint 漏れ）を follow-up コミットで即修正し、将来のコピー時 dangling を予防した。

## ⚠️ Problem（詰まった点・制約）

- **テンプレ片（コード塊内の literal リンク）の相対深さを誤った**: README テンプレを `docs/plan/NN-{slug}/` へコピーする前提なのに `../../.claude/...`（2 階層）と書き、正は 3 階層だった。リポジトリ内では code block 内なので dangling せず、機械ゲートでも検出されない（コピー時に初めて壊れる）盲点。
- **repoint 漏れが複数箇所に散った**: テンプレ正本を移す変更で、`plans-new/SKILL.md` の関連リンク descriptor（「共通テンプレ」表記）や出力 bullet が旧表記のまま残り、grep の初回点検（"Phase doc 共通テンプレート" 文字列）では拾えなかった（descriptor の言い回し違いで漏れる）。
- **見出し順がテンプレ正本と不整合**: 02 OVERVIEW に受け入れ基準を追加する際、テンプレの順（スコープ外 → 受け入れ基準）と逆に挿入した。テンプレ正本を決めても既存ファイル編集時に順序整合まで機械チェックできない。
- **worktree の Node 版警告**: `pnpm verify` 実行時に `Unsupported engine: wanted node>=24 (current v23.11.0)` の WARN が出続ける（失敗はしない）。doc PR では無害だが、環境差のノイズが verify ログに常駐する。
- **phase doc 非紐付け PR の finish-phase 適用が曖昧**: 単発ハーネス/doc 横断 PR は finish-phase の README 進捗更新対象 phase が無く、rule の「スキップ可」条項に頼って判断した。横断 PR の完了処理フローが skill 手順として明示されていない。

## 🚀 Try（次回の改善案）

- **テンプレ内 literal リンクの相対深さを点検する観点を harness-review に追加**: code block 内のテンプレ片は「コピー先からの相対パス」で検証する必要がある旨をチェックリスト化（リポジトリ内 lint では捕れない盲点として明記）。
- **repoint 時の点検 grep を「文字列」だけでなく「リンク先パス」でも行う方針を rule 化**: テンプレ正本移動時は旧パス（`docs/plan/README.md` への テンプレ目的リンク）を grep して descriptor の言い回し差を漏らさない手順を planning.md / cross-agent に補足。
- **OVERVIEW/README の見出し順を plan-templates.md の並びに合わせる注記を強化**（テンプレ正本に「見出しはこの順」を明示済みだが、既存ファイル改修時の整合チェックを finish-phase の追従点検に含める）。
- **phase doc 非紐付け（横断ハーネス/doc）PR の完了処理を finish-phase に明文化**: 進捗更新をスキップしてよい条件と、代わりに何を点検するか（dangling・cross-agent パリティ）を skill 手順へ追記。
- **worktree での `pnpm verify` engine 警告の扱いをメモ化**（`.node-version` 準拠運用の前提で WARN は無害である旨を docs/harness か verify skill の Gotchas に残し、毎回の誤解を防ぐ）。

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit |
| カバレッジ | 100% | 100% | ±0 | Vitest（199 stmts / 87 branches / 37 funcs / 167 lines） |
| Biome 違反 | 0 | 0 | ±0 | biome check（65 files） |
| CI | pass | pass | — | server-side pnpm verify（run 27223156492） |
| 差分 files/行 | — | 11 files +214/-181 | — | gh pr diff（2 commit: 実装 + review fix） |

## 🤖 ハーネス改善提案

<!-- harness-meta が parse する正規構造。未処理は [ ]、採用後に [x] + PR リンク。 -->

- [ ] `[rule]` harness-review のチェックリストに「テンプレ片（code block 内 literal）のリンクはコピー先基準の相対深さで検証する」観点を追加（リポジトリ内 lint では検出できない盲点）。
- [x] `[rule]` テンプレ正本/パス移動の PR では「旧パスを grep してリンク descriptor の言い回し差まで点検する」手順を planning.md / cross-agent に補足（文字列 grep だけでは repoint 漏れる）。
- [ ] `[skill]` finish-phase に「phase doc 非紐付けの横断ハーネス/doc PR」の完了処理（進捗更新スキップ条件 + dangling/パリティ点検）を明文化。
- [ ] `[skill]` finish-phase の追従点検に「OVERVIEW/README の見出し順が plan-templates.md 正本と一致するか」を追加。
- [ ] `[skill]` verify skill（または docs/harness）の Gotchas に「worktree の `pnpm verify` engine WARN（node>=24）は `.node-version` 準拠運用では無害」を記載。

## 📝 harness-meta フィードバック

<!-- 空でも見出しは維持する。harness-meta が後から各表に追記する。 -->

### 採用

- harness-meta batch 2026-W24（PR #79）で採用。`[rule]` パス移動 PR で旧パスを grep してリンク descriptor まで点検する手順（行58）を `planning.md`「phase の insert/renumber 追従チェックリスト」（番号整合 git grep・リンク実在照合）へ合流（#70/#76/#42/#58 と合流）。

### 見送り

### 保留
