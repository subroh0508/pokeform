---
id: learning-pr-38
title: PR #38 レトロスペクティブ (02-data-model-redesign 計画群を plans-new で起票)
type: learning
status: draft
related_pr: 38
related_plan: 02-data-model-redesign
generated_at: 2026-06-07T15:07:30Z
generator: pr-retrospective skill
---

<!--
この雛形は `.claude/rules/retrospective-format.md`（SoT）に準拠する。
見出し（絵文字・文言）は harness-meta が parse するため変更しないこと。
書き出し前に `.claude/rules/redaction.md` の正規表現で Secrets / PII を [REDACTED-*] 置換すること。
-->

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/38 / commit f8f9811 / merge 2026-06-07T15:07:30Z / 差分 7 files +448/-2。
> ポケモンデータ保持モデル再設計（レギュレーション per-reg 化 / カタログ分離 / 解禁判定正本一本化 / M-A 全量投入）の計画群を `plans-new` で起票。
> 設計判断は AskUserQuestion で 3 点合意（A案 / per-reg 別型 / 特性カタログ id 参照）。harness-review 実施・非ブロッキング 2 + nit 1。実装は未着手。

## ✅ Keep（継続したいこと）

- **`plans-new` を入口に通した**: 生の再設計指示をいきなり実装せず OVERVIEW → 6 基準 → 4 phase（直列）へ分解。粒度ブレを防ぎ、PR は計画ドキュメントのみで意味的に完結した。
- **設計が分岐する判断を AskUserQuestion で先に確定**: 解禁判定の正本（A案）/ per-reg 型の形 / 特性カタログの扱いを着手前に合意。実装フェーズでの手戻りリスクを計画段階で潰せた。
- **ユーザーの誤解に「具体コード例」で応答した**: 型レベル解禁判定（`ConstrainParty` が `SpeciesDex[H]["regulations"][number]` を見る現状）をコード片で示し直し、A案/B案の選択を実のあるものにできた。
- **大規模再設計を「構造変更」と「データ投入」で分離**: Phase 1-2（構造）と Phase 3-4（情報源確定・全量投入）を分け、各 phase が単独で green を保てる設計にした。
- **データ投入 PR の行数例外を計画に明記**: Phase 4（~186種）を `planning.md` の「意味ある粒度分割が困難なケース」例外として OVERVIEW に根拠記載し、>1000 行を許容した。
- **plans-new → PR → harness-review → merge → pr-retrospective の一連を回した**: 計画ドキュメントも実装と同じハーネスループ（レビュー・レトロ）に乗せ、SoT として残した。
- **追加要望（skill 化タスク）を同一ブランチに追記で反映**: 「解禁情報取得の skill 化」を Phase 3 に組み込み、Phase 4 と M-B 以降の再利用まで波及させて整合を取った。

## ⚠️ Problem（詰まった点・制約）

- **AskUserQuestion を 2 往復要した**: 1 回目で Q1（解禁判定の型）が「イメージがつかない」と返り、コード例で再説明して 2 回目に持ち越した。初回の選択肢説明が抽象的すぎた。
- **Phase 2 が重い**: per-reg YAML 化 + period + per-reg 型生成 + A案型機構（party.ts）+ `regulations[]` 廃止 + CLI 追従 + ADR を 1 PR に束ねた（想定 ~500-700 行・不可逆性高）。harness-review でも 2 分割余地を非ブロッキング指摘。
- **ADR の supersede 関係が計画段階で曖昧**: 新 ADR が 0012(vendor)/0014(lang) を「必要なら supersede」と幅を持たせたまま。harness-review で「vendor 方式は不変＝拡張 ADR の公算」と指摘が入った。
- **新規 skill の description trigger 規約が phase doc に未明記**: Phase 3 で起こす解禁情報取得 skill のタスクに「何を+いつ」三人称等の trigger 規約への言及が抜け、nit 指摘。`skill-creator` 依存で実害は小だが取りこぼしリスク。
- **WebSearch を伴う skill の非決定性**: Phase 3 で作る skill は出力が非決定的になりやすく、検証可能性の担保（複数ソース突き合わせ・判断過程の追跡）を doc 側で設計せざるを得なかった。
- **計画ドキュメント PR に harness テンプレが噛み合わない**: `.github/PULL_REQUEST_TEMPLATE/harness.md` は learning 採用駆動の構造で、新規計画群の PR には項目が合わず本文を手書き再構成した。

## 🚀 Try（次回の改善案）

- **`plans-new` に「型/設計判断を問う際は現状コード片を添える」ガイドを追記**: 抽象的な選択肢提示で AskUserQuestion が往復する事象を減らす（今回 Q1 で発生）。
- **`planning.md` / phase テンプレに「重い型機構変更フェーズは段階分割を検討」ヒントを追記**: party.ts 等の不可逆な型機構変更を含む phase は 2a/2b 分割余地を `start-phase` で検討する旨を残す。
- **phase doc テンプレに「新規 skill を起こすタスクは description trigger 規約に従う」注記**: skill 作成タスクを含む phase で trigger 規約の言及漏れを防ぐ。
- **計画群 PR 用の軽量 PR テンプレ追加を検討**: harness.md（learning 採用駆動）と別に、`plans-new` 起票の計画ドキュメント PR 向けの節構成を用意する。

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit（pre-commit 緑） |
| カバレッジ | 100% | 100% | ±0 | Vitest（ソース変更なし） |
| Biome 違反 | 0 | 0 | ±0 | biome check（60 files） |
| CI | pass | pass | — | server-side pnpm verify |
| 差分 files/行 | — | 7 files +448/-2 | +446 | gh pr diff（全て docs/plan/） |

## 🤖 ハーネス改善提案

<!-- harness-meta が parse する正規構造。未処理は [ ]、採用後に [x] + PR リンク。 -->

- [ ] `[skill]` `plans-new`: 設計/型の選択肢をユーザーに問う際は「現状の関連コード片」を添えて提示するガイドを追記し、AskUserQuestion の往復を減らす。
- [ ] `[rule]` `planning.md`: 不可逆な型機構変更（例: party.ts の解禁判定）を含む phase は、着手時に段階分割（2a/2b）の余地を検討する旨を phase 分割基準に補足する。
- [ ] `[template]` phase doc テンプレ（`docs/plan/README.md` / plan-templates.md）: 新規 skill を起こすタスクには「`skill-creator` で description trigger 規約に従う」を含める注記を追加。
- [x] `[template]` 計画群 PR 向けの軽量 PR テンプレートを追加検討（harness.md は learning 採用駆動で計画起票に不適）。 → **採用** PR #108（`.github/PULL_REQUEST_TEMPLATE/plan.md` 新設）
- [ ] `[adr]` データ保持モデル再設計（解禁判定正本の per-reg 一本化 / カタログ分離 / period）の ADR を Phase 2 で起票（0012/0014 は supersede せず参照する想定を明記）。

## 📝 harness-meta フィードバック

<!-- 空でも見出しは維持する。harness-meta が後から各表に追記する。 -->

### 採用

<!-- 採用した提案と採用先 PR リンク -->

### 見送り

<!-- 見送り理由（harness-meta-criteria.md の見送り基準に対応） -->

### 保留

<!-- 保留理由 -->
