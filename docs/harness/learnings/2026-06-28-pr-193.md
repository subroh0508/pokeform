---
id: learning-pr-193
title: PR #193 レトロスペクティブ (showdown-sync ワークフロー + ADR 0039 / 0012・0027 supersede)
type: learning
status: draft
related_pr: 193
related_plan: 10-showdown-first-data/phase-03
generated_at: 2026-06-28T23:10:00Z
generator: pr-retrospective skill
---

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/193 / commit a930c87 / merge 2026-06-28T22:59:43Z / 差分 18 files +216/-26。pokemon-showdown を第一の正とする `showdown-sync.yml`（workflow_dispatch 手動・抽出→転記→ja backfill→verify→data:authoritative PR）を新設し、取得元の根本転換を ADR 0039 に記録、被 supersede の ADR 0012/0027 を archive へ退避して inbound 参照を追従した。

## ✅ Keep（継続したいこと）

- **ADR supersede 衛生を手順どおり遂行**: frontmatter `status` のみ更新・本文不変・`git mv` で archive 退避・README の active/archive 表整合・後継 0039 を archive 相対リンクで参照（[[adr]] の step 1-4 に忠実）。
- **既存配線への忠実な workflow 化**: `sync-showdown.ts` の `<dataset> <regId>` 契約・`SHOWDOWN_DIR` 解決・`REG_TO_MOD` 写像を読み切ってから yml を書き、input を `m-a`/`m-b` choice に固定して契約ずれを回避した。
- **権限最小化と redaction を最初から織り込み**: `permissions: contents/pull-requests: write` のみ・cron なし・showdown shallow checkout（`ref: master`/`fetch-depth: 1`）・PR 本文に secrets/PII を出さない構成。
- **ADR 本文の自己完結**: 可変 plan ファイル（phase doc / 「Phase N」）を参照せず、決定根拠と被 supersede のみを記し、具体値は rule（[[data-pipeline]]）・他 ADR へ参照委譲した。
- **独立レビューで blocking を捕捉し即修正**: harness-review agent が reference 式リンク定義の追従漏れ 4 件を検出 → trivial 修正 → 再 grep で dangling ゼロ確認、というレビュー→修正ループが機能した。

## ⚠️ Problem（詰まった点・制約）

- **inbound 参照追従の grep パターンが inline リンクに偏っていた**: 初回 sweep は `](...)` 形（inline）だけを対象にし、reference 式リンク定義 `[ADR NNNN]: path`（行頭定義）4 箇所（completed/02 の phase-17/18/19）を取りこぼした。harness-review の blocking でようやく顕在化。
- **被 supersede ADR を再記述するライブ rule の一時的不整合**: `.claude/rules/data-pipeline.md` が 0012/0027 を本文・`adr:` frontmatter に抱えたまま（「取得元 PokeAPI 維持」）で、Accepted な 0039 と矛盾する窓が Phase 5 まで残る。意図的スコープ分割だが `paths` 自動ロードで読まれる点に注意。
- **生成 PR の CI トリガー制約**: `GITHUB_TOKEN` で作る PR は `ci.yml`（`on: pull_request`）を自動起動しないため、生成 PR の required status check が populate されず auto-merge/branch protection を阻害しうる（peter-evans 既知制約・phase doc がトークン整合をタスク化済）。
- **workflow_dispatch の実 dispatch がマージ前に不可能**: トリガーはデフォルトブランチに yml が存在して初めて有効化されるため、ロジック・構文精査までを実装 PR のスコープとし、実ドライランはマージ後へ繰り延べた。

## 🚀 Try（次回の改善案）

- ADR archive 退避時の参照追従チェックリストに **reference 式リンク定義（行頭 `[label]: path`）を inline リンクと並ぶ一級の走査対象**として明示し、grep 例に両形を併記する（[[adr]] / [[harness-review]] checklist）。
- ライブ rule が被 supersede ADR を再記述している場合、本文書き換えが後続 phase のときでも **「ADR NNNN は ADR-MMMM で supersede 済み・現行 SoT は後続 phase で改訂」**の前方ポインタ注記を archive 退避と同時に残せるか検討（mutable doc の鮮度・[[planning]] supersede 追補と整合）。
- `peter-evans/create-pull-request` の CI 非トリガー制約を **workflow doc / phase doc のリスク欄に定型注記**として残し、生成 PR の verify をワークフロー内 inline で必ず通す設計を標準化する。

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit |
| カバレッジ | 100% | 100% | ±0 | Vitest 218 passed |
| Biome 違反 | 0 | 0 | ±0 | biome check（122 files） |
| CI | pass | pass | — | server-side pnpm verify |
| 差分 files/行 | — | 18 files | +216/-26 | gh pr diff |

## 🤖 ハーネス改善提案

- [ ] `[skill]` `harness-review` / `adr-new` の archive 退避追従チェックに「reference 式リンク定義 `[label]: path`（行頭定義）」を inline リンクと並記し、`git grep -nE "\]\(...\)|^\[[^]]*\]:.*NNNN"` の両形走査例を載せる。
- [ ] `[rule]` [[adr]] の supersede step 4 に、ライブ rule が被 supersede ADR を再記述しているが本文改訂が後続 phase のときの「前方ポインタ注記」運用を追記する。
- [ ] `[rule]` [[implementation-workflow]] か workflow 著述 note に、`peter-evans/create-pull-request` 生成 PR が `ci.yml` を自動トリガーしない制約とワークフロー内 inline verify の標準化を残す。
- [ ] `[adr]` （新規不要）取得元転換の決定は本 PR の ADR 0039 で記録済み。追加の ADR は起こさない。

## 📝 harness-meta フィードバック

### 採用

### 見送り

### 保留
