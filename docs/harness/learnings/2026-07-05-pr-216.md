---
id: learning-pr-216
title: PR #216 レトロスペクティブ (mega 名を PokeAPI form_names 取得へ一本化 + showdown mega 名ルート削除)
type: learning
status: draft
related_pr: 216
related_plan: —
generated_at: 2026-07-05T06:23:13Z
generator: pr-retrospective skill
---

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/216 / commit d324c0915441f9086e1c8bf64e80eb867bd872a8 / merge 2026-07-05T06:23:13Z / 差分 17 files +1178/-94。`languages/mega.yaml` の ja/en を PokeAPI `pokemon-form` の form_names（`is_mega` 判別）から取得する 6 種目へ移し（11→97件）、showdown の mega 名取得ルート（`megaEnName`）を削除。mega 構造(mega-specs)+linking は showdown 維持。ADR 0043 起票（0040/0041 の派生不変条件 refine）。独立レビュー 3 本とも blocking 0。

## ✅ Keep（継続したいこと）

- **候補フィルタ + 取得後 `is_mega` 最終判別の二段構えにした**。`megaFormCandidates`（`/-mega($|-x$|-y$|-z$)/`）で fetch コストを事前削減し、最終判別は取得後の `is_mega`（`extractMegaNames` が `is_mega !== true` を弾く）へ委譲。regex 末尾アンカーで `meganium`/`yanmega`/`-primal` を正しく除外し test 網羅。
- **言語コード揺れを抽出関数で一本化(DRY)した**。`findLangName`/`pickJa` で `ja-Hrkt`/`ja-hrkt` 揺れを吸収し `extractJaName`/`extractEnName`/`extractMegaNames` から共有。境界 test（is_mega false/undefined・form_names 欠落・片言語のみ）が意味ある境界を突く。
- **showdown 名ルート削除がクリーンだった**。`LANG` は species/moves/items/abilities で継続使用ゆえ未使用化せず、`megaEnName` は全 import・test・実体を除去（`grep megaEnName`=0）、dangling は ADR/README の歴史記述のみ。mega-specs 構造 + linking は不変（diff で確認）。
- **ADR 0043 の refine 手順が正当だった**。0040/0041 の**派生不変条件（mega 名取得元分担）のみ**を新 ADR で refine し、被 refine ADR の本文は不変・status 変更や archive 退避なし。前例（0041→0035 / 0042→0041）と整合し [[adr]] の supersede とは別系統の「派生不変条件 refine」として一貫。
- **完了 phase doc に supersede 追補を正しく残した**。phase-06 は原文（`(当時)` 明示）を凍結し `> supersede 追補（issue #215 / ADR 0043）` の前方ポインタを追加。[[planning]]「完了済み plan の決定を後続 PR が覆すとき」に厳密一致。
- **照合ドリフトを予防する境界注記を足した**。`verify-showdown-pr` に「`languages/mega.yaml` の名前は showdown PR に現れない・照合は構造 + membership のみ」と明記し、存在しない mega 名を Serebii と突き合わせる事故を予防。
- **既存 11 件を非破壊で保持し生成物手編集もゼロ**。diff は追加のみ、`src/generated/languages/mega.ts` はキー列突合で `mega.yaml` と完全一致。

## ⚠️ Problem（詰まった点・制約）

- **coverage 除外ファイルのヘッダ陳腐化がまた再発した**。`scripts/materialize.ts` のヘッダが「mega は対象外・en=showdown / ja=手作業」のまま新挙動と正面から矛盾（追加コミットで解消）。coverage 除外ゆえ型/テストが縛らず機械ゲートで捕れない。これは **#191→#206→#214 に続く 4 度目**の同型（除外ファイルのヘッダ/コメントが責務変更に未追従）で、#206 で code/harness-review checklist に点検項目を入れた**にもかかわらず初版で漏れた** = チェックの実効性（セルフ検証での発火）に課題。
- **6 種化に伴う doc コメント未追従が複数**。`fetch-pokeapi.ts:145` の `needsJaEn`「全 5 種とも」・`materialize.ts:151`「全 5 種とも PokeAPI names から」が mega を含む 6 dataset 化に未追従（nit・DATASETS コメントは追従済みで取りこぼし）。
- **PokeAPI の datamine 由来 orphan form が規約外 id で混入する**。`absol-mega-z`/`garchomp-mega-z`/`lucario-mega-z`（メガ〇〇Z）・`tatsugiri-{curly,droopy,stretchy}-mega`・`meowstic-{male,female}-mega`・`magearna-original-mega` は `is_mega:true` だが実ゲーム未実装。spec 未参照ゆえ generate は 0 終了（orphan 許容）だが、`<base>-mega[-x|-y]` 規約を外れる派生サフィックス（`-mega-z`/`-female-mega`/`-curly-mega`/`-original-mega`）を持ち、将来 base 種族実装時に命名衝突リスク。
- **PokeAPI 由来 ja の全角/半角表記ゆれ**。`charizard-mega-x/y`・`mewtwo-mega-x/y` は全角Ｘ/Ｙ（公式）だが `raichu-mega-x/y` は半角（`メガライチュウX`）。PokeAPI upstream の未公式フォームの表記ゆれで転記ミスではないが、ADR 0043 は「PokeAPI 由来を as-is 採用」で normalize 方針を持たない。
- **ADR 0043 の Decision がコードシンボルを列挙**（追加/削除/存置/配線の関数名）。決定の concreteness を優先した許容範囲だが rename でドリフトしうる（Accepted 後は本文不変ゆえ意味保存 rename に留める必要）。

## 🚀 Try（次回の改善案）

- **coverage 除外ファイルのヘッダ点検を「セルフ検証(Phase 4)の必須ステップ」に格上げする**。checklist に項目を入れても initial commit で漏れ 4 度反復したため、レビュー観点（受動）だけでなく実装ワークフローの**セルフ検証で能動的に発火**させる。「責務を追加/変更する PR は、変更した経路が触れる coverage 除外ファイル（`src/codegen/*` の exclude 分・`scripts/*`）のファイルヘッダ/先頭コメントを grep して実態と一致するか確認する」を implementation-workflow Phase 4 / セルフ検証チェックに追加。→ `[rule]` implementation-workflow（Phase 4 セルフ検証）/ `[skill]` code-review 既存項目の強化。
- **「N 種」等の集合サイズを指す散文は dataset 追加時に横断 grep で追従する**。`fetch-pokeapi.ts`/`materialize.ts` の「全 5 種」の類は dataset 数に依存するため、6 種化のような数変更時に `grep "5 種"` で全 hit を追従する手順を明示。→ `[skill]` author-static-data / code-review（doc-data 乖離観点の具体化）。
- **PokeAPI 由来名の datamine orphan と表記ゆれの扱いを data-pipeline に明文化する**。「`is_mega` には未実装 datamine form（規約外サフィックス）が混じり orphan として許容する」「PokeAPI ja の全角/半角ゆれは as-is 採用・normalize しない（表示利用時に注意）」を SoT に記す（一部 ADR 0043/skill に記載済み・rule へ集約）。→ `[rule]` data-pipeline。

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit |
| カバレッジ | 100% | 100% | ±0 | Vitest（mega-fields/materialize 純ロジック網羅） |
| Biome 違反 | 0 | 0 | ±0 | biome check |
| CI | pass | pass | — | server-side pnpm verify |
| 差分 files/行 | — | 17 files +1178/-94 | — | mega.yaml 11→97件・生成物含む |

## 🤖 ハーネス改善提案

- [ ] `[rule]` `implementation-workflow`（Phase 4 セルフ検証）: 責務を追加/変更する PR は、変更経路が触れる **coverage 除外ファイル（`src/codegen/*` の exclude 分・`scripts/*`）のファイルヘッダ/先頭コメントを grep して実態一致を確認**する能動ステップを追加。レビュー観点（#206 で追加済）だけでは #214/#216 で初版漏れが反復したため、セルフ検証で発火させる。
- [ ] `[skill]` `code-review` / `author-static-data`: 「N 種」等の集合サイズを指す散文コメントは dataset 追加時に `grep "<N> 種"` で全 hit 追従する（doc-data 乖離観点の具体化・6 種化で「全 5 種」が複数残った反復の解消）。
- [ ] `[rule]` `data-pipeline`: PokeAPI 由来名の扱いを明文化 —「`is_mega` の未実装 datamine form（`-mega-z`/`-*-mega` 等 規約外サフィックス）は orphan として許容」「PokeAPI ja の全角/半角表記ゆれは as-is 採用・normalize 方針なし（表示利用時に注意）」を SoT へ集約。

## 📝 harness-meta フィードバック

### 採用

### 見送り

### 保留
