---
id: learning-pr-172
title: PR #172 レトロスペクティブ (type-validation.md + individuals-and-parties.md)
type: learning
status: actioned
related_pr: 172
related_plan: 08-docs-restructure/phase-02
generated_at: 2026-06-26T00:00:00Z
generator: pr-retrospective skill
---

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/172 / commit 02251f0a40c71c29c4a523bf148b07c7f849f67d / merge 2026-06-25T16:17:34Z / 差分 4 files +198/-3。plan 08 Phase 2。design 残り 2 本（type-validation / individuals-and-parties）をコードなしで執筆し design 4 ファイルを揃えた。レビューで **ツールコール残骸タグ（`</content>` / `</invoke>`）のファイル末尾混入**（blocking 1）を検出・修正してマージ。

## ✅ Keep（継続したいこと）

- **保証する不変条件を自然言語で列挙**: 覚えない技 / 使えない特性 / ポイント配分 / 同種族重複 / レギュ解禁 / 体数 / 参照解決を、閾値・数式の実体を持たず `[[game-spec]]` 等へ委譲して列挙。design が設計意図に専念できた。
- **harness-review が blocking を確実に捕捉**: ツールコール由来の残骸タグ末尾混入を independent review が `path:line` 付きで指摘し、マージ前に修正できた。機械ゲート（型 / Biome）では捕れない層をレビューが拾った好例。
- **コードなし方針を機械スキャンで再確認**: `git grep -nE 'interface |Dex|=> '` 無マッチ・`@source` が `src/codegen/*` に実在することを検証。
- **README のテーマ別ナビを実リンクへ更新**: 「後続フェーズで追加」プレースホルダ 2 本を解消し、design 4 ファイルの相互到達性を確保した。
- **具体値の参照委譲が徹底**: 合計値・上限・実数値式を数値で持たず rule / `src/domain/` へ委譲し、二重記述を避けた。

## ⚠️ Problem（詰まった点・制約）

- **ツールコール残骸タグ（`</content>` / `</invoke>`）が実ファイル末尾へ漏れた**: Write/tool-call シリアライズの残骸が 2 ファイル（`individuals-and-parties.md:103` / `type-validation.md:94-95`）にコミットされ、blocking 指摘を受けた。機械ゲートは通過するため独立レビューがなければ見逃していた。
- **design doc 間の相互参照に rule 用 wikilink 記法（`[[type-validation]]`）を誤用**: `.claude/rules/` に同名 rule が無く、[[skill-authoring]] の「wikilink は rule/memory slug 専用」慣習では dangling 扱い。README は markdown リンクを使っており記法が割れた（non-blocking）。
- **ADR 0010 の決定根拠を design に再記述しすぎ**: 「なぜ実行時バリデーションを採らないか」3 点が ADR の却下理由の再記述に寄り、ADR 0038 §6「design は決定根拠を再記述せず索引」から逸脱気味（nit）。

## 🚀 Try（次回の改善案）

- **doc 生成物のツールコール残骸タグ混入を機械検出する**: `</content>` / `</invoke>` / `<parameter` 等のタグが `docs/**` の md に混入していないかを lint / pre-commit で検査し、Write 残骸の再発を防ぐ（最も再発インパクトが大きい）。
- **design doc 間参照は markdown 相対リンクに統一する規約を明文化**: [[skill-authoring]] / harness-review-checklist に「wikilink=rule/memory 専用・doc 間は `[name](./name.md)`」を design にも適用する旨を追記。
- **design の ADR 決定根拠は要約に留める**規約を [[code-review]] harness-review-checklist に観点として追加（PR #171 Try と重複 → harness-meta で集約候補）。

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit（docs-only） |
| カバレッジ | 100% | 100% | ±0 | Vitest（185 tests） |
| Biome 違反 | 0 | 0 | ±0 | biome check |
| CI | pass | pass | — | server-side pnpm verify 緑 |
| 差分 files/行 | — | 4 files +198/-3 | +195 | design 2 新規 + README 2 |
| レビュー blocking | — | 1→0 | 解消 | 残骸タグ混入を修正後マージ |

## 🤖 ハーネス改善提案

- [ ] `[skill]` doc 生成物へのツールコール残骸タグ（`</content>` / `</invoke>` / `<parameter` 等）混入を lint / pre-commit で機械検出する（PR #172 blocking 由来・機械ゲートで捕れない死角）。
- [x] `[rule]` design doc 間の相互参照を markdown 相対リンクに統一する旨を [[skill-authoring]] / harness-review-checklist に明文化（wikilink は rule/memory slug 専用）。
- [x] `[rule]` [[code-review]] harness-review-checklist に「design は ADR 決定根拠を再記述せず要約・索引に留める」観点を追加（PR #171 と反復）。

## 📝 harness-meta フィードバック

### 採用
- `[rule]` design 相互参照を markdown 相対リンクに統一 → harness-review-checklist「design doc の altitude」観点に明記（PR #179）。
- `[rule]` harness-review-checklist に design 要約・索引観点（#171 と反復）→ 同観点に集約（PR #179）。

### 見送り
- `[skill]` ツールコール残骸タグの lint/pre-commit 検出 → tooling・単発。blocking は実害だったが頻度低く、.githooks 改変はコスト過大。別 tooling phase 候補として見送り。

### 保留
