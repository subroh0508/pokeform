---
id: learning-pr-156
title: PR #156 レトロスペクティブ (survey-regulation skill をオーケストレーター化し技マスターを独立単位に統合)
type: learning
status: draft
related_pr: 156
related_plan: 05-move-master-scraper-refactor/phase-04
generated_at: 2026-06-21T08:41:59Z
generator: pr-retrospective skill
---

<!--
この雛形は `.claude/rules/retrospective-format.md`（SoT）に準拠する。
見出し（絵文字・文言）は harness-meta が parse するため変更しないこと。
書き出し前に `.claude/rules/redaction.md` の正規表現で Secrets / PII を [REDACTED-*] 置換すること。
-->

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/156 / commit ae079466afa3ec491323aa1ae2a7e1b222067e78 / merge 2026-06-21T08:41:59Z / 差分 6 files +170/-81。plan 05 Phase 4。肥大した `survey-regulation` skill を情報種別ごとの 4 独立単位（roster / 覚える技の一覧 / 技マスター / 持ち物）に分けて本体をオーケストレーターへ再編し、Phase 2 由来の技マスター専用取得経路（attackdex・ADR 0037）を skill 手順へ統合。サブスキルは増やさず references 分離 + 単一 fan-out workflow に `kind`（species | move）追加で技マスターを呼び分け（既定 species で後方互換）。mega（ADR 0033）・技メタ per-game 所在（ADR 0035）は機械化済みのため非分割（過分割回避）。

## ✅ Keep（継続したいこと）

- **過分割を避けて references 分離 + Workflow `kind` 呼び分けで再編した**: サブスキルを増やさず、4 単位が共有する同一の層1/層2-3 機構を単一 fan-out workflow に `kind`（species | move）を足して呼び分けた。ADR 0037 設計メモ 4 の risk note「過分割を避ける」を実装で守り、skill 本数を増やさず凝集度を上げた。
- **Workflow 追加を後方互換に設計した**: `fetch-fanout.workflow` の `kind` 追加は既定 `species`。`self-heal.workflow` からの kind 無し呼び出しは species にフォールバックし、`move` 指定時のみ `scrape-serebii move-master` / `fetch-serebii move` を呼ぶ。既存呼び出し元を壊さない拡張。
- **責務分離を skill・rule・reference の 3 か所で一貫させた**: 「種族ページ = 覚える技の名前一覧のみ / 技専用ページ = 技メタ全項目（後勝ち上書き是正）」を SKILL.md・`data-pipeline.md`・`references/serebii-sourcing.md` の 3 か所で揃え、SoT の一貫性を担保（ADR 0037）。
- **skill 品質基準（行数 / description 字数）を機械確認で満たした**: SKILL.md 本文 287 行（≤500）・description 1009 字（≤1024）に収め、DOM / exit code 詳細は `references/serebii-sourcing.md` へ progressive disclosure。skill-authoring の基準を遵守。
- **`.workflow` を軽量スモークで構文検証した**: body を async function ラップした一時 `.mjs` で `node --check` 緑。機械ゲート対象外の `.workflow` に skill-authoring 規定の最小ネットを張った。
- **cross-agent パリティを機械確認した**: canonical `.claude/skills/survey-regulation` + `.agents/skills/survey-regulation` 相対 symlink 一致、`references/` / `workflows/` も symlink 経由で解決することを確認。機械ゲートを skill 内で再実装せず verify / check:regulation / generate:data へ委譲。

## ⚠️ Problem（詰まった点・制約）

- **`.workflow` 同士（層2↔層3）の返り値契約がテスト固定できない**: `fetch-fanout` ↔ `self-heal` の shape 整合（`kind` 追加の後方互換）は手元の `node --check` 軽量スモーク + dry-run メモでしか担保できず、機械ゲートに乗らない。skill-authoring も「変更時は harness-review で shape 整合を点検する」と人手依存を明記している死角。
- **PR コメント（レビュー記録）が残っていない**: harness 資産（skill / rule / workflow）を変える PR にもかかわらず harness-review の PR コメントが付いていない。cross-agent パリティ・description trigger・SoT 一貫性などレビュー観点を後追いで PR から辿る材料が無い（learning が SoT とはいえ）。
- **同一責務分離の記述が 3 か所に分散しドリフトの芽になる**: 「種族ページ = 名前一覧のみ / 技専用ページ = 技メタ全項目」を SKILL.md・rule・reference の 3 か所に書いており、一貫はしているが将来どれか 1 つだけ更新されるとドリフトしうる。SoT がどれかが明示されていない。
- **オーケストレーター化の正しさ検証が dry-run メモ依存**: skill 再編はコード変更なしで `pnpm verify` 緑だが、4 単位の呼び分けが実際に意図通り動くかは dry-run メモ（`move` 指定時の呼び出し先確認）でしか示されておらず、routine 実行による end-to-end の動作確認は本 PR スコープ外。

## 🚀 Try（次回の改善案）

- **`[rule]` skill-authoring に「`.workflow` 同士の返り値契約（shape）を変えるときは harness-review で shape 整合を点検し、変更点を PR の dry-run メモに必ず残す」運用をさらに具体化**する。テスト固定できない層2↔層3 契約の人手点検手順を明文化（本 PR で機が熟した死角）。
- **`[skill]` implementation-workflow / harness-review に「harness 資産（skill / rule / workflow）を変える PR はブロッキングなしでも harness-review コメントを PR に残す」を再確認**する。PR #156 のレビュー記録なしマージの再発防止（#154 と反復）。
- **`[rule]` cross-agent / skill-authoring に「同一の責務分離・契約を複数資産（SKILL.md / rule / reference）に書くときは SoT を 1 つ定め他はそこへ参照する」原則を明記**する。3 か所重複記述のドリフト防止。
- **`[skill]` survey-regulation に「オーケストレーター化後は `kind` 呼び分けの end-to-end（routine 1 周）を次回 routine 実行時に確認する」フォローアップを明記**する。dry-run メモ止まりの動作確認を補完。

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit（コード変更なし） |
| カバレッジ | 100% | 100% | ±0 | Vitest（コード変更なし） |
| Biome 違反 | 0 | 0 | ±0 | biome check + check:yaml-style（`.workflow` は Biome 対象外・node --check で代替） |
| CI | pass | pass | — | server-side pnpm verify |
| 差分 files/行 | — | 6 files | +170/-81 | gh pr diff（skill / rule / workflow / plan doc） |

## 🤖 ハーネス改善提案

<!-- harness-meta が parse する正規構造。未処理は [ ]、採用後に [x] + PR リンク。 -->

- [ ] `[rule]` skill-authoring に「`.workflow` 同士の返り値契約（shape）変更時は harness-review で shape 整合を点検し変更点を PR の dry-run メモに必ず残す」運用を具体化する（テスト固定できない層2↔層3 契約の人手点検手順・PR #156 で機が熟した）。
- [ ] `[skill]` implementation-workflow / harness-review に「harness 資産（skill / rule / workflow）を変える PR はブロッキングなしでも harness-review コメントを PR に残す」を再確認する（#154/#156 のレビュー記録なしマージの反復防止）。
- [ ] `[rule]` cross-agent / skill-authoring に「同一の責務分離・契約を複数資産（SKILL.md / rule / reference）に書くときは SoT を 1 つ定め他はそこへ参照する」原則を明記する（3 か所重複記述のドリフト防止）。
- [ ] `[skill]` survey-regulation に「オーケストレーター化後は `kind` 呼び分けの end-to-end（routine 1 周）を次回 routine 実行時に確認する」フォローアップを明記する（dry-run メモ止まりの動作確認補完）。

## 📝 harness-meta フィードバック

<!-- 空でも見出しは維持する。harness-meta が後から各表に追記する。 -->

### 採用

### 見送り

### 保留
