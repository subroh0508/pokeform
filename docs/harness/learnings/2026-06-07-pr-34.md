---
id: learning-pr-34
title: PR #34 レトロスペクティブ (実装の入口スキル plans-new を新設)
type: learning
status: draft
related_pr: 34
related_plan: —
generated_at: 2026-06-07T10:28:47Z
generator: pr-retrospective skill
---

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/34 / commit c41f8ca / merge 2026-06-07T10:26:20Z / 差分 11 files +454/-0。実装指示を OVERVIEW 化し 6 基準で 1 phase = 1 PR に分割する入口スキル `plans-new` を新設し、rule（planning.md）・ADR 0020・AGENTS.md / implementation-workflow.md / docs/plan/README.md の統合まで一括で実施。

## ✅ Keep（継続したいこと）

- **手順 skill を正しく経由**: skill 作成は `skill-creator`、ADR は `adr-new`、駆動は `implementation-workflow` を使い、手書きや独自フローに逃げなかった。新スキル新設の定型が再現できた。
- **description = 入口トリガを強めに**: under-trigger を避けるため「実装指示を受けたら着手前に必ず最初に通す入口」と三人称で明示しつつ、確定済み phase doc 着手は start-phase へ、と SKIP 条件も添えた（誤発火耐性）。
- **cross-agent パリティを最初から担保**: canonical `.claude/skills/plans-new/` + `.agents/skills/plans-new` 相対 symlink（commit で mode 120000 を確認）。
- **セルフ検証を機械化**: 相互参照リンク・`[[wikilink]]` の実在を絶対パスで全件チェックし dangling ゼロを確認。`gen-rules-index.ts` 再生成で diff ゼロ＝手編集なしを担保。
- **SoT 一貫性の徹底**: 6 基準と数値（~500 行目安 / >1000 行積極分割 / データセット例外）を SKILL.md・planning.md・split-criteria.md・ADR 0020 の 4 箇所で一致させた。
- **Generator / Evaluator 分離**: `harness-review` を独立 `Agent` で起動し結果を PR コメントへ記録。自己採点に代替しなかった。

## ⚠️ Problem（詰まった点・制約）

- **レビュー fix とマージのタイミング競合**: harness-review の non-blocking / nit を反映したコミット（句点統一 + start-phase 差し戻し SKIP）を push したが、PR がそれより前にマージされ、修正が main に入らず follow-up PR が必要になった。Draft 解除〜マージとレビュー fix loop の順序が並行ペイン運用で衝突した。
- **phase doc 非紐付け PR の扱い**: 本 PR は `docs/plan/**` の phase に紐付かない単発ハーネス PR で、`finish-phase` の README 進捗更新フローに乗らない。implementation-workflow の Phase 8 が想定する経路から外れる。
- **node engine WARN の常時表示**: `pnpm verify` が毎回 `Unsupported engine: wanted node >=24 (current v23)` を出す。失敗ではないが緑判定の確認時にノイズになる。
- **採番規約の重複記述**: 計画ディレクトリ / phase doc の採番規約が docs/plan/README.md・planning.md・plan-templates.md の 3 箇所に要約再掲。各所で「正本は README」と明記して緩和したが、将来の更新漏れリスクは残る。

## 🚀 Try（次回の改善案）

- レビュー fix loop 中は **Draft 解除（`gh pr ready`）前に harness-review の指摘取り込みを完了**してからマージへ進む運用を明文化し、並行ペインのオーケストレーターとワーカーで「fix 取り込み待ち」を同期する。
- **phase doc 非紐付けの単発ハーネス PR** は finish-phase の README 進捗更新をスキップしてよい旨を implementation-workflow rule に一言追加し、ワークフローの想定外パスを正規化する。
- `plans-new` description の start-phase 棲み分け（本 PR の follow-up で対応中）を確実にマージする。
- node engine WARN は `.npmrc` の `engine-strict` 方針か `.node-version` 追従で恒久対処するか、learning として注記に留めるかを判断する。

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit |
| カバレッジ | 100% | 100% | ±0 | Vitest（197/197 stmts） |
| Biome 違反 | 0 | 0 | ±0 | biome check（60 files） |
| CI | pass | pass | — | server-side pnpm verify |
| 差分 files/行 | — | 11 files +454/-0 | +454 | gh pr diff（skill/rule/ADR/統合） |

## 🤖 ハーネス改善提案

- [ ] `[rule]` implementation-workflow.md に「phase doc 非紐付けの単発ハーネス PR は finish-phase の README 進捗更新をスキップ可」を追記し、想定外パスを正規化する。
- [ ] `[skill]` implementation-workflow / pr-retrospective: レビュー fix loop 中は Draft 解除前に harness-review 指摘を取り込んでからマージへ進む順序を明記（fix がマージに乗り遅れる競合の防止）。
- [ ] `[template]` 計画採番規約の重複（README / planning.md / plan-templates.md）を、参照一本化できないか点検（現状は「正本は README」明記で緩和）。
- [ ] `[adr]` なし（本 PR で ADR 0020 を起票済み）。
- [ ] `[remove]` なし。

## 📝 harness-meta フィードバック

### 採用

### 見送り

### 保留
