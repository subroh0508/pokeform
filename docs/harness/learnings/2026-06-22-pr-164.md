---
id: learning-pr-164
title: PR #164 レトロスペクティブ (plan07 phase-03 survey-regulation 圧縮)
type: learning
status: draft
related_pr: 164
related_plan: 07-rules-skills-simplify/phase-03
generated_at: 2026-06-22T17:06:49Z
generator: pr-retrospective skill
---

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/164 / commit 2058e6f4798ab4d1f6f75a67c821343036bae105 / merge 2026-06-22T17:06:49Z / 差分 2 files +131/-185
> survey-regulation skill（手順 skill 最大級）を SKILL 287→232 行・description 1054→503 字へ圧縮。逐語再掲を references 参照へ集約し、安全性記述は全保持。

## ✅ Keep（継続したいこと）

- **「実体の重複」を圧縮の照準にした**: 削減対象を `references/serebii-sourcing.md` の逐語再掲・3 層ハイブリッド節/Gotchas/手順 table に跨る三重〜五重再掲に絞り、操作手順そのものは温存した。冗長度の高い箇所を優先する優先順位付けが効いている。
- **安全性記述を keyword grep で機械確認してから提出した**: HTML を LLM に載せない / exit code 判定 / 取りこぼし自己修復 / append-only / curate せず全量 / Serebii 第一優先 / 生成物手編集禁止 / 機械ゲート再実装しない / redaction を「踏み外すと壊れる」契約として列挙し残存確認した。圧縮 PR の情報欠落リスクに対する再現可能なセーフガード。
- **description を trigger 文へ純化しつつ発火パターンを全維持した**: 「M-A の解禁データを集めて」「survey-regulation <id>」「M-B が公開されたので反映して」等を保持し、機構説明（層1/2-3・exit code・ADR 詳細）を本文へ逃がした。under-trigger を避けつつ rendered 文字数を ≤1024 に収めた。
- **dangling ゼロを機械確認した**: references / workflows / ADR（active 7 + archive 3）/ skill リンク全 `./` の実在を確認し、archive へ退避した ADR（0031/0032/0034）の相対パス解決も含めて検証した。
- **削減目安未達を理由付きで明示した**: phase doc の目安 80-120 行に対し 55 行（19%）に留めた根拠を「9 ステップ操作手順 + 安全性 Gotchas は受け入れ基準 1 / OVERVIEW の自己完結性・安全性保護が明示する領域」と PR 本文で説明し、過剰圧縮を選ばなかった判断を可視化した。
- **cross-agent パリティを確認した**: canonical `.claude/skills/survey-regulation` + `.agents/skills` 相対 symlink が同一実体、層2-3（Claude 固有 Workflow）/ Codex 逐次フォールバックの縮退記述、`.workflow` 無変更を点検した。

## ⚠️ Problem（詰まった点・制約）

- **ADR 0033 のクリック可能リンクが圧縮で消失（harness-review nit）**: メガ自動著述節を要点化した際、`[ADR 0033](../../../docs/adr/...)` の markdown リンクが本文・関連節から全て落ち、bare-text「ADR 0033」4 箇所だけ残った。他 ADR（0021/0022/0031/0034 等）は linked で残るため、0033 だけ「なぜ」へ辿れない非対称が生じた。圧縮時にリンク化された ADR 参照を bare-text へ畳むと追跡可能性が局所的に劣化する典型例。
- **削減目安と実績の乖離（55 vs 80-120 行）**: phase doc の目安が「自己完結性 / 安全性保護で削れない領域」を織り込んでいなかったため、実績 19% が目安を下回り「未達」に見える。目安の立て方が手順 skill の不可削減コア（操作手順 + 安全契約）を考慮していない。
- **逐語再掲の検出が手作業依存**: 三重〜五重再掲（「正しさは層1 に宿る」「メガ自動著述」「技マスター別単位」）の発見と集約は目視に頼った。同種の重複検出を支える機械的補助（重複フレーズ検出）が無く、見落としリスクが圧縮者の注意力に依存する。
- **安全性記述の「保持」確認が手動 grep**: keyword grep は実施したが、grep 対象 keyword 一覧そのものが skill 横断で標準化されておらず、PR ごとに著者が列挙し直している。圧縮系 PR で反復するチェックの定型化余地がある。
- **ADR 参照群の整理で群分けの意味付けが暗黙**: 過剰列挙を「なぜ（解禁/記録/検証/取得経路）」「SoT 設計」の 2 群へ整理したが、群の境界基準が PR 本文にのみあり skill 内には残らないため、次回更新者が同じ群分けを再現できる保証が弱い。

## 🚀 Try（次回の改善案）

- **圧縮系 skill PR の「リンク非劣化」チェックを harness-review checklist へ追加**: 圧縮前後で ADR / skill / rule への markdown リンク本数が減っていないか（bare-text 化していないか）を点検する観点を `harness-review` の checklist に足す。今回の 0033 nit はこれで未然に拾える。→ `[skill]`
- **安全性 keyword リストを skill 内に常設アンカー化**: survey-regulation の「踏み外すと壊れる契約」keyword を skill 本文の固定セクション（または references）に列挙し、圧縮時の grep 基準を SoT 化する。PR ごとの再列挙を不要にする。→ `[skill]`
- **圧縮系 phase の削減目安に「不可削減コア」注記を加える**: 手順 skill の操作手順 + 安全 Gotchas は削減対象外である旨を planning / phase doc テンプレに明記し、目安行数は「重複・逐語再掲」分に限定する。目安と実績の乖離を構造的に解消する。→ `[template]`
- **圧縮 PR 用の軽量チェックリストを pr 説明テンプレ化**: 「trigger 全維持 / 安全性記述 grep / dangling ゼロ / cross-agent パリティ / リンク非劣化」を圧縮系 PR の定型確認項目として残す。→ `[rule]`

## 📊 指標
| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit |
| カバレッジ | 100% | 100% | ±0 | Vitest |
| Biome 違反 | 0 | 0 | ±0 | biome check |
| CI | pass | pass | — | server-side pnpm verify |
| 差分 files/行 | — | 2 files | +131/-185 | gh pr diff |

## 🤖 ハーネス改善提案
- [ ] `[skill]` `harness-review` の checklist に「圧縮系 PR のリンク非劣化」観点を追加する（圧縮前後で ADR/skill/rule への markdown リンク本数が減って bare-text 化していないかを点検・今回の ADR 0033 nit を未然に拾う）。
- [ ] `[skill]` `survey-regulation` SKILL 内に「踏み外すと壊れる契約」の安全性 keyword を固定アンカー（本文セクション or references）として常設し、圧縮時 grep 基準を SoT 化する（PR ごとの再列挙を不要にする）。
- [ ] `[template]` phase doc / planning テンプレに「手順 skill の操作手順 + 安全 Gotchas は削減対象外」注記を加え、削減目安行数を逐語再掲・重複分に限定する（目安 80-120 vs 実績 55 の乖離を構造的に解消）。
- [ ] `[rule]` 圧縮系（文体シンプル化）PR の定型確認項目（trigger 全維持 / 安全性記述 grep / dangling ゼロ / cross-agent パリティ / リンク非劣化）を retrospective-format ないし PR テンプレに軽量チェックリストとして残す。

## 📝 harness-meta フィードバック
### 採用
### 見送り
### 保留
