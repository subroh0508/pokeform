---
id: learning-pr-239
title: PR #239 レトロスペクティブ (gender-forme canonical を languages の -female/-male へ揃える)
type: learning
status: draft
related_pr: 239
related_plan: —
generated_at: 2026-07-09T19:41:43Z
generator: pr-retrospective skill
---

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/239 / commit 101e22fb4e7387a6115529b227d29255e87d67e8 / merge 2026-07-09T19:39:17Z / 差分 4 files +89/-9。showdown の性別二形（女=`-F`/男=bare）canonical を名前 SoT languages（PokeAPI 由来 `-female`/`-male`）へ揃え、M-A/M-B フル roster 投入時の `generate:data` gender メガ停止（`meowstic-f-mega` has no name entry）を解消。#229（canonical species id）/#231（distinct-forms 名合成）/#238（cosmetic fold）に続く gender 前提修正。

## ✅ Keep（継続したいこと）

- **自己完結の計画書を別セッションから引き継ぎ迷いなく着手**: 背景・網羅監査結果・修正範囲（`canonical-species-id.ts` + `mega-fields.ts` + test）・検証手順・ブランチ/PR 規約まで 1 doc で完結し、AGENTS.md / rules 確認 → 実装 → verify → PR を直線で通せた。特に §3 の**網羅監査**（showdown を clone し M-A/M-B usable roster 全種の `canonicalSpeciesId` を languages と突合 → 差分 8 件/2 カテゴリに同定）が修正範囲を確定させた。
- **両経路を単一 canonical SoT へ収束**: 構造側 `canonicalSpeciesId` の passthrough と mega 側 `megaId` の双方を共通の `canonicalFormId`/`FORM_SLUG_RENAME` に通し、data-pipeline rule の「構造側・名前側が同じ canonicalFormId を通す」方針に整合。独自命名を発明せず languages の既存 id へ揃えた（#229 で確立した canonical 正本方針の枠内）。
- **テスト期待値を SoT で裏取りしてから確定**（plausible≠correct 回避）: `languages/mega.yaml`（`meowstic-female-mega`/`meowstic-male-mega`）と `languages/species.yaml`（4 種の `-female`/`-male`）を grep で実在確認し、期待値が実装なぞりでなく SoT 由来であることを担保。
- **2 経路の破損モードを別々に説明**: gender-forme は #238 cosmetic fold が誤発火し**メス→オスへ silent 誤畳み**（species）、mega は `megaId` 非 canonical で**loud fail**（generate 停止）という別症状を、id を languages に在らせる（fold 抑止）+ megaId を canonical 経由にする（名前解決）で個別に解消。
- **過剰実装を避け semantic gap を明示的に非ブロッキング化**: gender-aware base linking（女メガを女 base に紐づける）は `MegaInput` が gender/forme フィールドを持たずスコープ外と判断し、PR 本文 + code-review コメントに非ブロッキングとして明記。unblock を最優先しつつ負債を可視化。
- **マージ前の定型ゲート遵守**: `pnpm verify` 緑（type/cov100%/Biome/yaml-style）+ pre-commit（check:individual/regulation で m-a/m-b 整合）+ `code-review` skill でレビューコメント記録 + CI 緑を確認してからマージ。

## ⚠️ Problem（詰まった点・制約）

- **抽出層はローカル検証不可でエンドツーエンドが本 PR 外に残る**: `scripts/showdown/*`（showdown ツリー実行）は typecheck/coverage 非対象でローカル検証できず、canonical/megaId の**実データ効果は `showdown-sync.yml` 再 dispatch でしか確認できない**。本 PR は機構 + unit test 止まりで、generated 実データへの反映（gender メガで generate が止まらず `data:authoritative` PR が立つ）は未検証のまま。
- **部分 roster での検証がフル roster の gap を捕れない**（#208 と同根）: gender-forme は過去の限定 roster（placeholder / m-b 32種）に一度も入らず**潜伏し、フル roster で初顕在化**した。限定 roster での緑は canonical gap の不在を保証しない。
- **semantic gap をデータ点検頼みで残した**: 女メガ・男メガの両方が bare base `meowstic-male` に linking され、生成 `megaEvolvesTo` は `meowstic-male` に 2 メガがぶら下がり `meowstic-female` はメガ無しになる。非ブロッキングだが、実害の有無は後続 `pokemon-data-reviewer` の点検に依存し、点検観点として明文化されていない。
- **`FORM_SLUG_RENAME` が多目的化**: 「PokeAPI slug → 短い canonical」の個別リネームマップに「showdown gender suffix（`-f`/`-f-mega`/`-m-mega`）→ languages」を相乗り。コメントで補ったが、キーの出自（PokeAPI slug か showdown kebab か）が混在し、将来キー追加時にどちらの経路を写すか読み解きにくい。

## 🚀 Try（次回の改善案）

- **`[skill]`**: `verify-showdown-pr`（または `author-regulation-data` の前提ゲート）に「**gender-forme（`-F`/`-M` forme 種・gender メガ）の canonical が languages に在るか**」を per-reg フル投入**前**チェックとして明文化する。本 PR の網羅監査手法（showdown roster × languages 突合）を skill 手順化し、フル roster 投入時に canonical gap で generate が止まる事故を事前検出する（部分 roster では潜伏する gap を先取り）。
- **`[skill]`**: `pokemon-data-reviewer` に「**gender メガの base linking**（女メガが女 base に紐づくか / `megaEvolvesTo` の性別対応）」を点検観点として追加する。本 PR が非ブロッキングで残した semantic gap をデータ点検で確実に拾う受け皿を用意する。
- **`[rule]`**: `data-pipeline.md` の `SUPPRESS_BASE_SPECIES` 節へ「**gender-forme の canonical 正規化**（showdown 女=`-F`→languages `-female` / gender メガ `-f-mega`→`-female-mega`・男 bare→`-male` は `DEFAULT_TO_EXPLICIT` 済み）」を 1 行追記する。現状 bare default→`-male` は書かれているが、女 `-F` と gender メガの扱いが未記載で、次に gender 種を触るとき rule から辿れない。

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit |
| カバレッジ | 100% | 100% | ±0 | Vitest（231 tests・追加分岐なし） |
| Biome 違反 | 0 | 0 | ±0 | biome check |
| CI | pass | pass | — | server-side pnpm verify（26s） |
| 差分 files/行 | — | 4 files | +89/-9 | gh pr diff（src/codegen/showdown の 2 実装 + 2 test） |

## 🤖 ハーネス改善提案

<!-- harness-meta が parse する正規構造。未処理は [ ]、採用後に [x] + PR リンク。 -->

- [ ] `[skill]` `verify-showdown-pr` / `author-regulation-data` の前提ゲートに「gender-forme（`-F`/`-M` 種・gender メガ）の canonical が languages に在るか」の per-reg フル投入前チェックを明文化（網羅監査 = showdown roster × languages 突合の手順化）。
- [ ] `[skill]` `pokemon-data-reviewer` に「gender メガの base linking（女メガが女 base に紐づくか）」点検観点を追加。
- [ ] `[rule]` `data-pipeline.md` の `SUPPRESS_BASE_SPECIES` 節へ gender-forme canonical 正規化（女 `-F`→`-female` / gender メガ / 男 bare→`-male`）を 1 行追記。

## 📝 harness-meta フィードバック

<!-- 空でも見出しは維持する。harness-meta が後から各表に追記する。 -->

### 採用

### 見送り

### 保留
