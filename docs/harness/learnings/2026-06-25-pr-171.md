---
id: learning-pr-171
title: PR #171 レトロスペクティブ (design/ 骨組み + README + data-pipeline.md)
type: learning
status: draft
related_pr: 171
related_plan: 08-docs-restructure/phase-01
generated_at: 2026-06-26T00:00:00Z
generator: pr-retrospective skill
---

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/171 / commit bfe4b0c316b27ea56fb704bdd344349a23d32fc5 / merge 2026-06-25T16:03:17Z / 差分 3 files +209/-1。plan 08 Phase 1。`docs/design/` を新設し README（全体像 index）と data-pipeline.md を **TypeScript コードなし**で執筆。ADR 0038 の配置・front matter 規約・コードなし方針に準拠し、具体値は末尾「実装 SoT ポインタ」節で rule / src へ誘導。

## ✅ Keep（継続したいこと）

- **コードなし方針を機械スキャンで担保**: `git grep -nE 'interface |: [A-Z][a-zA-Z]+Dex|export const|function |=>'` が design 配下で空であることを受け入れ基準に組み込み、型例・YAML キー・CLI フラグの混入をゼロにした。design が陳腐化しにくい設計意図だけを持つ。
- **mermaid ノードを抽象語に留めた**: 「構造 SoT / 名前 SoT / per-regulation SoT / 生成」等の抽象ラベルにし、具体ファイル名・キーを埋めないことで図の陳腐化を防いだ。
- **末尾「実装 SoT ポインタ」節で具体値を委譲**: 型・スキーマ・コマンドの実体を design に再記述せず rule / `scripts/` / `src/` へ誘導。Explanation 象限と Reference 象限の責務を分離できた。
- **ADR 参照を active のみに限定**: archived の 0026 / 0034 を避け active な 0021 / 0037 等を引いた。harness-review が相対 ADR リンク 7 件すべて active 実在を確認、dangling ゼロ。
- **旧 architecture.md の撤去を Phase 3 へ意図的に分離**: 本 phase は新設に集中し、inbound 参照張り替え・撤去を後続に回すことで 1 PR の scope を絞った（1 phase = 1 PR）。

## ⚠️ Problem（詰まった点・制約）

- **「コードなし」の判定が grep パターン依存**: `interface` / `Dex` / `=>` 等のパターンを列挙して空を確認するが、パターン網羅性は人手の judgment に依存。新しいコード混入形（例: 装飾的な疑似コード）を取りこぼし得る。
- **design ↔ rule の二重記述リスクが残置**: 設計意図サマリと rule の規約本文の境界が曖昧で、「どこまで要約しどこから参照に寄せるか」が著者判断。後続 PR #172 で実際に「ADR 決定根拠の再記述に寄りすぎ」nit が出た。
- **front matter `last_modified` が手動入力**: `"2026-06-26T00:00:00+09:00"` を手で埋めるため、更新時に実態とずれるリスク。機械生成の仕組みがない。

## 🚀 Try（次回の改善案）

- design doc の「コードなし」チェックを手 grep でなく、`check:yaml-style` 同様の軽量 lint script（`docs/design/**` に対する禁止パターン検出）として固定し、phase をまたいで再現可能にする。
- design と rule の責務境界（「要約はどこまで / 参照へどこから」）を [[code-review]] の harness-review-checklist に「design は ADR 決定根拠を再記述せず索引に留める」観点として明文化する（PR #172 の nit を予防）。
- front matter `last_modified` の手入力ずれを避けるため、`prepare` または lint で「変更ファイルの last_modified が当日か」を warn する仕組みを検討する。

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit（docs-only・非干渉） |
| カバレッジ | 100% | 100% | ±0 | Vitest |
| Biome 違反 | 0 | 0 | ±0 | biome check |
| CI | pass | pass | — | server-side pnpm verify 緑 |
| 差分 files/行 | — | 3 files +209/-1 | +208 | design 2 新規 + plan README |

## 🤖 ハーネス改善提案

- [ ] `[skill]` design doc の「コードなし」検証を手 grep から軽量 lint script（`docs/design/**` 禁止パターン検出）へ固定し、phase 横断で再現可能にする。
- [ ] `[rule]` [[code-review]] の harness-review-checklist に「design は ADR 決定根拠を再記述せず要約・索引に留める（再記述は nit）」観点を追加（PR #172 で顕在化した nit を予防）。
- [ ] `[skill]` front matter `last_modified` の手入力ずれを防ぐ warn（変更ファイルの last_modified が当日か）を `prepare` / lint で検討。

## 📝 harness-meta フィードバック

### 採用

### 見送り

### 保留
