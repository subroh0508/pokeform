---
id: learning-pr-153
title: PR #153 レトロスペクティブ (ADR 0037 技マスター専用取得経路 + plan05 Phase1 設計確定)
type: learning
status: actioned
related_pr: 153
related_plan: 05-move-master-scraper-refactor/phase-01
generated_at: 2026-06-21T07:05:01Z
generator: pr-retrospective skill
---

<!--
この雛形は `.claude/rules/retrospective-format.md`（SoT）に準拠する。
見出し（絵文字・文言）は harness-meta が parse するため変更しないこと。
書き出し前に `.claude/rules/redaction.md` の正規表現で Secrets / PII を [REDACTED-*] 置換すること。
-->

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/153 / commit ca43c4ec593327d749e9bb650b1f2c3cb92b4ec7 / merge 2026-06-21T07:05:01Z / 差分 8 files +167/-16。plan 05 Phase 1 = コードなしの設計確定。技メタの取得を「種族ページ Standard Moves 表の副産物抽出」→「技専用ページ（attackdex-champions）専用取得」へ切り替える決定を **ADR 0037** で起票し、SoT 所在・出自・「PokeAPI を技メタ信頼源にしない」核は不変のため現行 carrier **ADR 0035 を supersede せず補完**。DOM 契約・exit code 契約・役割分割境界・skill 粒度を設計メモ 1-4 に明文化し Phase 2-4 タスクと 1:1 対応させた。

## ✅ Keep（継続したいこと）

- **コードなし設計 phase を ADR + 設計メモに集約した**: 取得方式変更という不可逆寄りの決定を実装より先に ADR 0037 で確定し、DOM 契約（URL slug / type / damageClass / power / accuracy / pp / **priority** 新フィールド）・exit code 契約（0/2/3/4）・役割分割境界・skill 粒度を設計メモ 1-4 で後続 phase 着手可能な粒度まで言い切った。Phase 2-4 のタスクが設計メモと 1:1 になり、実装 PR の判断負荷を前倒しで解消できた。
- **supersede でなく「補完」を正しく選択した**: SoT 所在（move-specs・per-game）・出自（Serebii 第一優先）・「PokeAPI を技メタ信頼源にしない」核がいずれも不変で、変わるのは取得方式のみ。`adr.md` の supersede 基準（決定文・理由・Consequences・Alternatives の意味を変える書き換え）に該当しないため 0035 を補完で据え置いた。ADR が Consequences・Alternatives 両所で「誤って supersede と読まれてはならない」と予防記述したのも良い。
- **実ページで DOM 契約を実証してから ADR に書いた**: Earthquake（PP=12）/ Quick Attack（priority +1）/ Swords Dance / Roar（priority -6）で抽出フィールドと罠（変化技 power `--`→null・必中 accuracy `101`→null・latin-1/CRLF）を確認済みのうえで設計メモ化した。推測でなく一次ソース検証に基づく契約。
- **archive 化済み ADR をライブ SoT 参照していた陳腐化を同 PR で是正した**: plan 05 docs が archive 0026/0034 をライブ SoT として参照し、OVERVIEW が廃止パス `regulations/champions/moves.yaml` を参照して Phase 2-4 実装者を誤誘導していた箇所を、現行 carrier の ADR 0035/0037 へ向け直し（残存 grep 0 hit 確認）。
- **不変ログ規約（可変 plan 非参照）を満たした**: ADR 0037 本文に phase-NN リンク・「Phase N」表記・OVERVIEW.md リンクを持たず、03 Phase 13 廃止経緯も概念記述に留めて具体 phase 番号を避けた。harness-review で全重点観点（採番 / dangling ゼロ / supersede 判断 / 陳腐化是正 / SoT 一貫性 / redaction）合格。

## ⚠️ Problem（詰まった点・制約）

- **具体数値（PP スケール `{8,12,16,20}` / priority レンジ）を不変ログ ADR 本文に持った**: 設計メモ 2 が具体値を ADR に書いており、Phase 2 で同契約が `serebii-sourcing.md`（ライブ手順 SoT）にも書かれると同一数値が 2 箇所に存在しドリフトしうる。不変ログである ADR は後で直せない側のため、数値の正本が ADR かライブ SoT かの役割境界が曖昧だと将来のドリフトの芽になる（harness-review non-blocking 1）。
- **phase-01 doc の task 節と確定事項節で補完先 ADR 名がずれて見えた**: task / 受け入れ基準節が起票時表現のまま「ADR 0034 補完/supersede の判断」と古い候補 0034 を名指しし、追補節が「補完先は 0035」と解決していた。0034→0035 の系譜は説明済みで誤誘導には至らないが、task 節が古い候補を指したまま残ると後続読者が混乱しうる（harness-review non-blocking 2）。
- **2 回目以降の ADR 参照が bare shorthand `[ADR 0035]` でリンク化されていない**: 初出は inline link 付きだが再出は角括弧のみで GitHub 上はリテラル文字列描画。ただしこれは既存 Accepted ADR 共通の house style で本 PR 固有の退行ではなく、リポジトリ全体の表記方針として揃える話（harness-review nit 3）。
- **DOM 契約の正本所在が phase をまたいで forward-looking 依存だった**: ADR は「DOM 契約の正本は serebii-sourcing.md」と前方参照で書くが、現 `serebii-sourcing.md` に attackdex セクションは未存在（Phase 4 で追記予定）。現時点で矛盾はないが、正本が「これから書かれるファイル」を指す状態は Phase 2-4 の着手順序に依存する。

## 🚀 Try（次回の改善案）

- **`[rule]` adr.md に「具体数値・スキーマ値（PP/priority レンジ等）の SoT はライブ手順 SoT（serebii-sourcing.md / rule）に置き、ADR には決定根拠のみ書く」役割境界を明文化**する。不変ログに具体値を持つとドリフトしても直せない死角を制度化で防ぐ（本 PR non-blocking の制度化）。
- **`[skill]` adr-new / start-phase に「設計 phase の数値契約は phase doc 側に『具体値の SoT は serebii-sourcing.md』と補記する」チェックを追加**し、後続実装 phase が役割境界を守れるようにする。
- **`[rule]` planning（または adr.md）に「phase doc の task 節と確定事項節で同一 ADR を指すときは番号を確定後の carrier へ揃える」運用**を明記し、起票時の古い候補番号が task 節に残らないようにする。
- **`[template]` PR / plan template の ADR 参照記法を「初出も再出も inline link / reference 定義で書く」と注記**し、bare `[ADR NNNN]` のリテラル描画を house style 単位で解消する（リポジトリ全体方針なので別途集約）。

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit（docs のみ・非該当） |
| カバレッジ | 100% | 100% | ±0 | Vitest（docs のみ・変化なし） |
| Biome 違反 | 0 | 0 | ±0 | biome check |
| CI | pass | pass | — | server-side pnpm verify（docs のみ緑） |
| 差分 files/行 | — | 8 files | +167/-16 | gh pr diff |

## 🤖 ハーネス改善提案

<!-- harness-meta が parse する正規構造。未処理は [ ]、採用後に [x] + PR リンク。 -->

- [x] `[rule]` adr.md に「具体数値・スキーマ値の SoT はライブ手順 SoT（serebii-sourcing.md / rule）・ADR は決定根拠のみ」の役割境界を明文化し、不変ログに具体値を持つドリフト死角を制度化で防ぐ（PR #153 non-blocking）。
- [ ] `[skill]` adr-new / start-phase に「設計 phase の数値契約は phase doc 側へ『具体値の SoT は <ライブ手順 SoT>』と補記する」チェックを追加する。
- [ ] `[rule]` planning / adr.md に「phase doc の task 節と確定事項節で同一 ADR を指すときは確定後 carrier 番号へ揃える」運用を明記する（起票時の古い候補番号残置防止）。
- [ ] `[template]` PR / plan template に「ADR 参照は初出も再出も inline link / reference 定義で書く（bare `[ADR NNNN]` を避ける）」を注記する（house style 単位の集約案件）。

## 📝 harness-meta フィードバック

<!-- 空でも見出しは維持する。harness-meta が後から各表に追記する。 -->

### 採用

- `[rule]` adr.md の役割境界明文化（#153-56）→ 本 meta-writeback PR（`harness/plan05-meta-writeback`）で `.claude/rules/adr.md` に「ADR が持つもの / ライブ SoT が持つもの（具体値を不変ログに抱えない）」節を追加。既存「可変な plan ファイルを引かない」と同根で、明確なドリフト死角の制度化として採用。

### 見送り

- `[skill]` adr-new / start-phase に数値契約の補記チェック（#153-57）→ 上記採用（adr.md 役割境界）が原則を rule レベルで担保するため、skill 手順への補記は現状運用で足り見送り。反復すれば skill 化を再検討。
- `[rule]` planning / adr.md に carrier 番号揃え運用（#153-58）→ 単発・niche。既存 planning の renumber 追従チェックで実害が低く見送り。
- `[template]` ADR 参照の inline link house-style（#153-59）→ house-style 単位の nit で、linter / レビュー個別指摘の圏内。template 改変コスト>効果で見送り。

### 保留
