---
id: learning-pr-237
title: PR #237 レトロスペクティブ (PokeAPI 全件名同期・languages 名前辞書の初回全量投入)
type: learning
status: draft
related_pr: 237
related_plan: —
generated_at: 2026-07-18T18:17:55Z
generator: pr-retrospective skill
---

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/237 / commit 9cd618abbd0bce97e249515071df3886ae9a8415 / merge 2026-07-05T20:50:58Z / 差分 12 files +28180/-0

## ✅ Keep（継続したいこと）

- **workflow 駆動の全件列挙で名前辞書を機械生成**: `pokeapi-names.yml`（GitHub Actions）が PokeAPI `names`（ja=ja-Hrkt 優先 / en）を全件列挙して `data/languages/*.yaml` 6 種（species / items / moves / abilities / types / mega）+ 生成 `src/generated/languages/*.ts` を一括投入し、`github-actions[bot]` が PR を立てた。reg 非依存の名前 SoT を人手写経せず bot に委譲する分担（ADR 0041 / 0043）が機能。
- **append / 既存尊重で権威序列を壊さない**: 既存の en（showdown 正 / Serebii 速報）を上書きせず追記のみとし、PokeAPI を ja 専任・構造/解禁データの権威を侵さない設計を維持（data-pipeline の取得元分担どおり）。
- **mega を pokemon-form の form_names（is_mega 判別）から取る 6 種目化**: species の form 名でなく `pokemon-form.form_names` を `is_mega` で判別してメガ名を取得（ADR 0043）。メガ名も PokeAPI 経路で自動化でき、6 種の全件名辞書として揃った。
- **distinct-forms 173 件を PR 本文にレビュー表として同梱**: 含有判定合成で機械生成した distinct-form（タイプ / 種族値が base と異なる form）を id / en / ja / decision（compose / manual / passthrough / canonical-override）/ distinct 根拠まで表で開示。機械合成した名前を人間・pokemon-data-reviewer が監査できる状態を PR 内に残した。
- **PokeAPI 非存在分を手作業カバー箇所として明示**: Champions 固有名（メガストーン item 名等）は本 PR で埋まらないことを PR 本文に明記し、照合すべき Serebii ページ（items / moves / pokemon / megaabilities / newabilities）を列挙。取得元の限界と follow-up 導線を PR に添えた。
- **CI が server-side で緑を担保**: `pnpm verify`（型 / テスト100% / Biome）が ~22s で SUCCESS。生成物（YAML + generated TS）の整合が機械ゲートで確認された。

## ⚠️ Problem（詰まった点・制約）

- **レビュー実施の記録が PR コメントに残っていない**: PR コメントは 0 件。173 件の distinct-forms 表と手作業カバー チェックリストは PR 本文に開示されたが、pokemon-data-reviewer が実際に distinct-forms 名を点検した記録や verify 結果が PR 上に無く、レトロが収集できる review 痕跡が欠落している（機械合成した ja/en 名の妥当性が out-of-band 判断に依存）。
- **生成データ PR が巨大で行単位レビューが非現実的**: 12 files +28180 行（全て新規・削除 0）。YAML SoT と生成 TS が同一データを二重に持つため diff が倍化し、人手の行単位確認は不可能。生成器の正しさ + スポットチェックに信頼を置くしかない。
- **Champions 固有名の手作業カバーが追跡されない**: メガストーン item 名等の PokeAPI 非存在分は「PR 本文の Serebii リンクを見て手で足す」導線のみで、チェックボックス / follow-up issue 化されておらず、埋め漏れが検知されず放置されるリスクがある。
- **bot 生成 PR にレビュー / 検証 skill が自動連結していない**: `data:authoritative` 系の showdown PR には verify-showdown-pr があるが、`pokeapi-names.yml` の名前辞書 PR には照合 skill の適用が定型化されておらず、生成物の妥当性チェックが人手の任意発火に委ねられている。

## 🚀 Try（次回の改善案）

- **[skill] author-static-data に「生成 PR の distinct-forms 表を pokemon-data-reviewer に点検させ、結果を PR コメントとして残す」ステップを明記**する。レビュー痕跡を PR 上に固定し、pr-retrospective がコメントから learning 素材を収集できるようにする（code-review の「PR コメントに残す」原則と揃える）。
- **[skill] author-static-data 手順3（手作業カバー）を、PR 本文の Serebii リンク列挙から「チェックボックス化 or follow-up issue 起票」へ格上げ**し、Champions 固有名（メガストーン item 名等）の埋め漏れが追跡・close 判定できるようにする。
- **[rule] code-review に「bot 生成の巨大データ PR（生成器出力 + generated TS 二重化）は行単位でなくスポットチェック + 生成器の冪等性で評価する」レビュー方針を一言追記**する。行数ゲートで詰まらせず健全性の純改善（Google 基準）で判断する導線を明示。
- **[skill] author-static-data に「PokeAPI 非存在の Champions オリジナル名は Bulbapedia 等の出典で手作業著述」分岐を再確認**する（PR #254 learning と同根・PokeAPI 専任 skill では埋まらない箇所の一貫した扱い）。

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit |
| カバレッジ | 100% | 100% | ±0 | Vitest（データ投入ゆえコード不変・生成 TS はデータ） |
| Biome 違反 | 0 | 0 | ±0 | biome check |
| CI | pass | pass | — | server-side pnpm verify（~22s・SUCCESS） |
| 差分 files/行 | — | 12 files +28180/-0 | — | languages 6 YAML + generated 6 TS・全件初回投入 |

## 🤖 ハーネス改善提案

- [ ] `[skill]` author-static-data に「生成 PR の distinct-forms 表を pokemon-data-reviewer に点検させ結果を PR コメントに残す」ステップを追加し、レビュー痕跡を PR 上に固定する。
- [ ] `[skill]` author-static-data 手順3 の手作業カバー（Champions 固有名）を Serebii リンク列挙からチェックボックス / follow-up issue 化へ格上げし埋め漏れを追跡可能にする。
- [ ] `[rule]` code-review に「bot 生成の巨大データ PR（生成器出力 + generated TS 二重化）はスポットチェック + 冪等性で評価する」方針を一言追記する。
- [ ] `[skill]` author-static-data に「PokeAPI 非存在の Champions オリジナル名は Bulbapedia 等出典で手作業著述」分岐を明記（PR #254 learning と重複計上）。

## 📝 harness-meta フィードバック

### 採用

### 見送り

### 保留
