---
id: learning-pr-254
title: PR #254 レトロスペクティブ (M-B 解禁データを pokemon-showdown から全量同期)
type: learning
status: draft
related_pr: 254
related_plan: —
generated_at: 2026-07-15T17:01:00Z
generator: pr-retrospective skill
---

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/254 / commit f8fbf5b2d32ef7cde95afe67a12d8a0dc2358c89 / merge 2026-07-15T16:59:06Z / 差分 19 files +32058/-3177

## ✅ Keep（継続したいこと）

- **reset 要否を転記層の実挙動から判断**: `sync-showdown.ts` が roster/moves/mega/items をすべて `unionSeq`（sorted union）で転記する事実を確認し、限定セット placeholder の古い技が union で残存するリスクを根拠に「reset + 全量同期」を選択（推測でなくコード裏取り）。
- **whack-a-mole を避ける一括欠落把握**: `generate.ts` が最初の欠落名で throw するため、`ability-specs.yaml`(seq) vs `languages/abilities.yaml`(map) を直接 diff して全欠落（eelevate / fire-mane の2件のみ）を一度に確定。iterative CI 再 dispatch を回避。
- **Champions オリジナル特性 ja の authoritative 著述**: PokeAPI 非存在・Serebii abilitydex も Jp Name 欄が空という二重欠落を、Bulbapedia の abilitydex ページ（`Eelevate (Ability)` / `Fire Mane (Ability)`）で確定（うなぎのぼり / ほのおのたてがみ）。ja を発明せず出典で裏取り。
- **cross-agent フォールバックの活用**: `showdown-sync.yml` dispatch が名前欠落で generate 失敗した後、skill 記載のローカル経路（showdown build → `showdown:* m-b` → generate → 手動 PR）へ縮退して完遂。
- **pokemon-data-reviewer が showdown ソースと直接突合**: roster 235 / mega 76 / 2特性の紐付け / movepool 継承(ADR 0024) / append-only union を pokedex.ts と照合し blocking 0。生成物の再現性も git status 空で確認。

## ⚠️ Problem（詰まった点・制約）

- **author-regulation-data 手順3 が Champions オリジナル特性を取りこぼす**: 手順3「新規 id の名前欠落は author-static-data（差分追加）で補完」は PokeAPI 前提だが、Champions オリジナル特性（eelevate/fire-mane）は PokeAPI 非存在ゆえ author-static-data では埋まらない。手作業 Bulbapedia 著述が必要で、skill にその分岐が明示されていない。
- **showdown-sync.yml が名前欠落で全体失敗し PR が立たない**: generate 段で1件の missing name でも workflow 全体が exit 1 になり `data:authoritative` PR が作られない。欠落名を事前に埋めない限り workflow 単独では M-B を投入できなかった。
- **reset ブランチを --ref に dispatch する試みが機能せず**: 空 M-B は roster が `unknown[]` で typecheck 赤（`m-b/index.ts`）。--no-verify で reset ブランチを push し `--ref` dispatch したが、workflow は checkout→sync→generate の generate で別要因（名前欠落）失敗。結局ローカル build 経路が必要だった。
- **showdown ローカル build が重い**: full clone + `npm ci` + `node build` ×2（抽出層 copy 後の再 build）で数分。cross-agent フォールバックの前提コストが高い。

## 🚀 Try（次回の改善案）

- **[skill] author-regulation-data 手順3 に「Champions オリジナル特性/技（PokeAPI 非存在）は Bulbapedia 等の出典で手作業 ja 著述」分岐を明記**。author-static-data（PokeAPI 専任）では埋まらないことと、欠落一括把握（specs vs languages 直接 diff）を追記する。
- **[skill] author-regulation-data に「reset 要否は転記層が union か overwrite かで判断（union なら placeholder 除去に reset 必須）」の判断基準を明記**。
- **[skill] cross-agent フォールバック手順に「showdown-sync が Champions オリジナル名欠落で失敗したらローカル build 経路へ縮退」を dispatch 失敗時の既定リカバリとして追記**（今回の経路を再現可能に）。

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit |
| カバレッジ | 100% | 100% | ±0 | Vitest（248 tests・データ投入ゆえコード不変） |
| Biome 違反 | 0 | 0 | ±0 | biome check |
| CI | pass | pass | — | server-side pnpm verify（27s・自作 PR ゆえ on:pull_request 発火） |
| 差分 files/行 | — | 19 files +32058/-3177 | — | M-B roster 235 / mega 76 |

## 🤖 ハーネス改善提案

- [ ] `[skill]` author-regulation-data 手順3 に「Champions オリジナル特性/技（PokeAPI 非存在）は Bulbapedia 等出典で手作業 ja 著述」分岐と欠落一括把握（specs vs languages 直接 diff）を明記。
- [ ] `[skill]` author-regulation-data に reset 要否の判断基準（転記層 union/overwrite）を明記。
- [ ] `[skill]` cross-agent フォールバックに「showdown-sync が名前欠落で失敗 → ローカル build 経路へ縮退」を既定リカバリとして追記。

## 📝 harness-meta フィードバック

### 採用

### 見送り

### 保留
