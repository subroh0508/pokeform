---
id: learning-pr-137
title: PR #137 レトロスペクティブ (plan 04 へ Phase 4-6 追加・plan 03 完了化)
type: learning
status: draft
related_pr: 137
related_plan: 04-generated-layout-redesign
generated_at: 2026-06-20T18:49:52Z
generator: pr-retrospective skill
---

<!--
この雛形は `.claude/rules/retrospective-format.md`（SoT）に準拠する。
見出し（絵文字・文言）は harness-meta が parse するため変更しないこと。
書き出し前に `.claude/rules/redaction.md` の正規表現で Secrets / PII を [REDACTED-*] 置換すること。
-->

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/137 / commit bf2ea80770cbd623dc32eac77df05ea8f55713ab / merge 2026-06-20T18:49:52Z / 差分 6 files +220/-8。generated/YAML レイアウト再編計画（plan 04）の末尾に配置最終化 3 件を新 Phase（4=src/generated 移動 / 5=レギュ名 SoT を languages へ / 6=メガ items legality）として 1 phase = 1 PR で追加し、OVERVIEW/README/トップ rollup を追従、先行 plan 03（Phase 1-12 完了済）を ✅ 完了へ更新した plan doc 専用 PR。harness-review は blocking 0（non-blocking 1 / nit 1）。

## ✅ Keep（継続したいこと）

- **3 件の追加 phase を 6 基準（独立判断 / 別レイヤ / 独立マージ）に照らして 1 phase = 1 PR に分割した**: Phase 4（ビルド配置 = `src/generated/` 移動）/ Phase 5（名前 SoT = languages 一本化）/ Phase 6（型 legality = メガ items 制約）はレイヤが直交し独立にマージ可能。planning の分割粒度が機能し、harness-review も「分割理由は妥当」と総評した。
- **plan doc の整合を多面（番号 / リンク / mermaid / rollup）で同時更新した**: 新 phase doc 見出し `# Phase 4/5/6` と番号一致、README の `./phase-NN-*.md` 全 6 本が実在解決、mermaid 依存グラフ（`P3→P4→P5→P6→P05`）と OVERVIEW 分割表・直列チェーン・受け入れ基準・総数表記（「計 6 phase」）を一括で揃え、harness-review の insert/renumber 追従チェックを全 pass させた。
- **plan 03 完了化を機械的根拠（README 12 phase 全 `[x]`・未チェック 0）で裏取りした上でトップ rollup を ✅ へ更新した**: 印象でなく下位 README のチェック状態を確認してから rollup を変えたため、rollup ↔ 実体のドリフトを生まなかった。
- **「Phase 1-3 / 1-12 / 1-6」等の範囲表記がすべて文脈上正しい歴史的/範囲参照であることを明示確認した**: 番号再編後に残る範囲表記を dangling と誤認せず、Phase 1-3 = 当初の 3 軸直交再編サブセットと読めることを PR body・review 双方で言語化した。
- **コード変更ゼロの plan doc PR として機械ゲートを再実行せず CI 緑に委ねた**: 型 / カバレッジ / Biome を doc PR で再チェックせず、`pnpm verify` / CI の責務に委譲（機械ゲート非再実装の原則を遵守）。
- **harness-review を独立に起動し、blocking 0 を要約コメントとして PR に残した**: レビュー履歴が PR 上に残り、本 retrospective がそこから non-blocking/nit を learning 素材として収集できた。

## ⚠️ Problem（詰まった点・制約）

- **同一計画内で「Phase 4」番号が新旧 2 義になった**: 本 PR で plan 04 に別テーマの新「Phase 4」（generated-into-src）が入り、旧「Phase 4」（全種族投入・06 へ移動済）と番号が衝突。現状は「旧 04 Phase 4」と明示修飾され dangling ではないが、番号で参照する箇所（06 OVERVIEW:17 / 04 README:31 / learning #125）に将来の mis-point リスクが残る（learning #118 の同一番号 mis-point パターンの再来予兆）。
- **後続 plan 05/06 の依存前提（「04 の Phase 1-3 完了」）と本 PR の plan 04 全体完了前提との粒度がずれた（harness-review non-blocking）**: Phase 4-6（`src/generated/` 移動 / レギュ名 SoT / メガ legality）は 05/06 が consume するレイアウト・パスを最終化する変更を含むのに、05/06 側は「Phase 1-3 完了」を最小前提として固定したまま。リンクは壊れていないが cross-plan の意図が一貫しない（特に Phase 4 のパス移動が 05 consumer パスに波及）。
- **末尾 phase 追加（既存計画への別テーマ phase の後付け）が plan のスコープ境界を曖昧にしうる**: plan 04 は当初「3 軸直交再編」がテーマで、Phase 4-6 は「配置最終化」という別テーマ。同一 plan に異テーマ phase を足すと番号衝突（上記）と前提粒度ズレ（上記）の両方を誘発する構造的な原因になった。

## 🚀 Try（次回の改善案）

- planning の「phase insert / renumber 追従チェックリスト」に、**同一 plan 内で過去に使われ移動/廃止された phase 番号を新テーマで再利用しない（番号再利用を避け末尾連番にするか、テーマ名で参照する）**を明記し、新旧同番号の 2 義化を構造的に防ぐ。
- planning に、**末尾 phase 追加で後続 plan の依存前提が波及する場合、後続 plan 側の前提節・mermaid ノードを「どの phase まで完了が前提か」へ同 PR で揃える（逆方向の cross-plan dangling も点検）**チェックを追加する（今回の 05/06 粒度ズレを発生時に閉じる）。
- planning に、**既存 plan へ別テーマ phase を末尾追加するか新 plan を起こすかの判断基準**（テーマ一貫性 vs 分割コスト）を明文化し、異テーマ後付けによる番号衝突・前提粒度ズレの再発を抑える。

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit（doc のみ・型不変） |
| カバレッジ | 100% | 100% | ±0 | Vitest（テスト不変） |
| Biome 違反 | 0 | 0 | ±0 | biome check |
| CI | pass | pass | — | server-side pnpm verify |
| 差分 files/行 | — | 6 files | +220/-8 | gh pr diff |

## 🤖 ハーネス改善提案

<!-- harness-meta が parse する正規構造。未処理は [ ]、採用後に [x] + PR リンク。 -->

- [ ] `[rule]` `planning.md` の phase insert/renumber 追従チェックリストに「**同一 plan 内で移動/廃止された phase 番号を別テーマで再利用しない（末尾連番 or テーマ名参照）**」を追加し、新旧同番号の 2 義化（#118 同型）を構造的に防ぐ。
- [ ] `[rule]` `planning.md` に「**末尾 phase 追加が後続 plan の依存前提に波及する場合、後続 plan の前提節・mermaid ノードを完了前提 phase まで同 PR で揃える（逆方向 cross-plan dangling 点検）**」を明記する（本 PR 05/06 粒度ズレの発生時クローズ）。
- [ ] `[skill]` `plans-new` に「**既存 plan へ別テーマ phase を末尾追加するか新 plan を起こすか**の判断基準（テーマ一貫性 vs 分割コスト）」を追記し、異テーマ後付けによる番号衝突・前提粒度ズレの再発を抑える。

## 📝 harness-meta フィードバック

<!-- 空でも見出しは維持する。harness-meta が後から各表に追記する。 -->

### 採用

<!-- 採用した提案と採用先 PR リンク -->

### 見送り

<!-- 見送り理由（harness-meta-criteria.md の見送り基準に対応） -->

### 保留

<!-- 保留理由 -->
