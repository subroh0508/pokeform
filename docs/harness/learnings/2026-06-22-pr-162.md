---
id: learning-pr-162
title: PR #162 レトロスペクティブ (plan07 phase-01 軽量級 rules を P1-P5 で圧縮)
type: learning
status: draft
related_pr: 162
related_plan: 07-rules-skills-simplify/phase-01
generated_at: 2026-06-22T16:48:28Z
generator: pr-retrospective skill
---

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/162 / commit 34de2f8c7a293dc5ed40bf96370f2a31a0ff3265 / merge 2026-06-22T16:48:28Z / 差分 13 files +138/-296

## ✅ Keep（継続したいこと）
- **P1-P5 圧縮パターンの一括適用**: 軽量級 12 rules を「前置き圧縮 / 重複削減 / 詳細の ADR・src 委譲 / ハードラップ解消 / 二重削減」の 5 系統で機械的に処理し、-296/+138（実質 -158 行）の大幅削減を一貫した判断線で達成した。rule ごとに変換種別を表で開示しているため、何をどう削ったかが追跡可能。
- **「やりすぎ防止 5 点」を PR 説明に明文化**: 自己完結性維持 / learning 番号保持 / trigger 不変 / 安全性記述保持 / references≠削除 を OVERVIEW 設計方針として掲げ、各方針への遵守を本文で逐一確認した。圧縮 PR で「削りすぎ」を防ぐ自己ガードとして有効。
- **trigger（frontmatter）完全不変の担保**: 全 12 ファイルで `description` / `paths` / `last_modified` / `adr` の frontmatter に diff hunk を出さず本文のみを変更。自動ロード範囲・起動意味を変えず、`rules-index.md` のドリフトをゼロに抑えた。
- **dangling ゼロの事前検証**: 全 `[[wikilink]]` / 相対リンク / ADR 参照（archive パス含む 0013/0031/0034 等）の実在を変更後に全件 resolve 確認。圧縮で参照リンクを巻き込み破壊していないことを担保した。
- **安全性記述の選別保持**: redaction 正規表現テーブル（バイト一致）、archive `git mv` 後の inbound 参照追従、harness-meta の 2 段階撤去、二重 floor 式（境界 0/32）など「再発防止・運用上クリティカル」な記述を圧縮対象から除外した判断が一貫。
- **harness-review が non-blocking 2 件を機械検出**: 圧縮で落ちた learning #90 / PR 番号変遷の追跡性を、`git diff origin/main` の行差分から具体的に拾えており、レビュー skill が圧縮 PR の「根拠取りこぼし」を捕捉できることを実証した。

## ⚠️ Problem（詰まった点・制約）
- **planning.md で再発防止根拠 learning #90 が単独脱落**: cross-plan move の「完了済み phase doc forward 参照は凍結可」ルールから `#90` 引用が消え、末尾の集約引用行（#42/#58/#76/#104/#118/#122/#137）にも含まれなかった。挙動は不変だが「凍結可」判断の唯一の根拠が辿れなくなり、OVERVIEW 設計方針②（根拠は保持・冗長羅列のみ集約）に照らすと過剰削除に当たる。
- **type-conventions.md で決定変遷ログ（PR #145→#147→#149）が脱落**: item legality（メガストーン専有）の provenance が「SoT は本 bullet」に圧縮され、3 度の変遷経緯が消えた。同じ legality を testing.md / planning.md が #145/#147/#149 として引いており、type-conventions 側だけ変遷が消えると「なぜ確定か・何を捨てたか」の追跡が片肺になる。
- **「learning 番号保持」と「冗長羅列の集約」の境界が judgment 依存**: PR 説明では「根拠は残し羅列のみ集約」を掲げたが、実際には #90 / PR 変遷の 2 箇所で線引きを誤った。どの番号が「唯一の根拠」でどれが「重複羅列」かを判定する明示的基準が rule 側に無く、圧縮者の主観に委ねられている。
- **12 ファイル一括圧縮ゆえレビュー負荷が集中**: 1 PR で 12 rules を変更したため、各 rule の「削った 1 行が他 rule の整合先か」を横断確認する必要があり、harness-review が全 hunk を精査する負荷が高い。粒度を rule 群で分割するか否かのトレードオフが残る。
- **redaction.md の検証例インライン化で grep 件数が変動**: `[REDACTED-*]` の出現が 19→17 に減少。example の意味は保持されているが、件数ベースの自動チェックがあれば誤検知しうる差分で、「example の畳み込み」と「記述欠落」を機械的に区別しにくい。

## 🚀 Try（次回の改善案）
- **圧縮 PR で「根拠 1 件 = 単独削除禁止」を rule 化する**: learning 番号・決定変遷 PR 番号が「その判断の唯一の根拠」なら削除せず、冗長な羅列（複数行に同一根拠が反復）のみ末尾集約行へ寄せる、という線引きを skill-authoring か planning の圧縮節へ明記する（→ `[rule]`）。
- **脱落した #90 / PR #145→#147→#149 を後続フォローで復元する**: 本 PR の non-blocking 2 件を、phase-02 以降の rules 圧縮 PR か追補 PR で planning.md 末尾集約行へ `#90` を、type-conventions.md へ「PR #145→#147→#149 で変遷の末確定した SoT」1 句を戻す（→ `[rule]`）。
- **圧縮 skill に「根拠保持セルフチェック」を追加する**: rules 圧縮を行う skill / 手順に「削った各行が learning/ADR/PR 番号の唯一根拠でないか」「集約先に番号が漏れなく移ったか」を確認するチェック項目を入れ、judgment 依存を減らす（→ `[skill]`）。

## 📊 指標
| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit |
| カバレッジ | 100% | 100% | ±0 | Vitest |
| Biome 違反 | 0 | 0 | ±0 | biome check |
| CI | pass | pass | — | server-side pnpm verify |
| 差分 files/行 | — | 13 files | +138/-296 | gh pr diff |

## 🤖 ハーネス改善提案
- [ ] `[rule]` rules 圧縮の線引きを明文化: learning 番号・決定変遷 PR 番号が「その判断の唯一の根拠」なら削除禁止、複数行に反復する冗長羅列のみ末尾集約行へ寄せる、を `skill-authoring.md` か `planning.md` の圧縮節に追記する。
- [ ] `[rule]` 本 PR で脱落した根拠を後続で復元: `planning.md` の集約引用行へ `#90` を、`type-conventions.md` の item legality bullet へ「PR #145→#147→#149 で変遷の末確定した SoT」1 句を戻す。
- [ ] `[skill]` 圧縮系 skill に「根拠保持セルフチェック」項目を追加: 削った各行が learning/ADR/PR 番号の唯一根拠でないか・集約先へ番号が漏れなく移ったかを圧縮完了前に確認させる。
- [ ] `[skill]` 多数ファイル一括圧縮 PR で「横断整合先チェック」を skill 手順に追加: ある rule で削った根拠が他 rule の引用先になっていないかを `git grep` で確認する手順を入れ、片肺脱落（type-conventions 例）を防ぐ。

## 📝 harness-meta フィードバック
### 採用
### 見送り
### 保留
