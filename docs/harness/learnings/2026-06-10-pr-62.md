---
id: learning-pr-62
title: PR #62 レトロスペクティブ (メガ先 moves を base の per-reg moves 継承へ・ADR 0024)
type: learning
status: draft
related_pr: 62
related_plan: 02-data-model-redesign（follow-up・issue #61）
generated_at: 2026-06-10T07:08:00Z
generator: 手動
---

<!--
この雛形は `.claude/rules/retrospective-format.md`（SoT）に準拠する。
見出し（絵文字・文言）は harness-meta が parse するため変更しないこと。
書き出し前に `.claude/rules/redaction.md` の正規表現で Secrets / PII を [REDACTED-*] 置換すること。
-->

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/62 / commit d2414e0 / merge 2026-06-10T07:03:20Z / 差分 7 files +239/-170

## ✅ Keep（継続したいこと）

- 前 PR（#59）のレビュー learning（メガ先技プールの過大表現）を放置せず、`plans-new` → issue → `implementation-workflow` → `adr-new` の定型ループで follow-up 解決した。learning → 改善 → ADR の循環が機能。
- 設計判断（メガ先 moves = base 継承）を **ADR 0024** で記録し、Alternatives（catalog∩learnset 維持 / メガ先 moves 明示 / 空）と却下理由を残した。「なぜ」の追跡可能性。
- 実装が `generate.ts` の逆引きマップ追加 + base moves 継承の最小変更で、不要化した `learnSets` 構築も同時に除去（dead code を残さない）。
- レビューを `code-review`（ソース）/ `harness-review`（ADR・doc）で**分担**し、各 Evaluator が独立に blocking 0 を確認。code-review は再生成の byte-identical / 逆引きの undefined 安全性を実測で裏取り。
- learning #59 の `[adr]` 提案を本 PR で `[x]` + リンク化し、harness-meta の集約対象を汚さないようにした。

## ⚠️ Problem（詰まった点・制約）

- `generate.ts` のコメント・宣言が全角括弧（`（）`）を含み、Edit の old_string が半角括弧で書いて初回マッチに失敗した。日本語コメントを編集する際は Read 結果から正確にコピーする必要がある。
- メガ先 moves の過大表現は `charizard-mega-x` で Phase 7 から潜在していたが、3 種小データセット（Phase 9）でメガを増やすまで顕在化しなかった。**既存挙動の設計レビューが追いついていなかった**。
- `check:regulation` が overrides を適用しない件（learning #59 の別提案）は本 PR スコープ外で未解決のまま残っている。

## 🚀 Try（次回の改善案）

- Phase 10（全量投入）では本 ADR 0024 によりメガ先が base 継承で自動整合するため、メガ先固有の per-reg moves を書かない運用を徹底する（phase-10 doc に反映済み）。
- 既存挙動でも「データ規模が小さいうちは顕在化しない過大表現」を早期に拾うため、`pokemon-data-reviewer` のメガ整合観点を定常チェックに含める。
- learning #59 の残提案（`check:regulation` の overrides 適用方針・survey-regulation の差異記述修正）を harness-meta で集約処理する。

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit |
| カバレッジ | 100% | 100% | ±0 | Vitest |
| Biome 違反 | 0 | 0 | ±0 | biome check |
| CI | pass | pass | — | server-side pnpm verify |
| 差分 files/行 | — | 7 files | +239/-170 | gh pr diff（生成物含む） |

## 🤖 ハーネス改善提案

<!-- harness-meta が parse する正規構造。未処理は [ ]、採用後に [x] + PR リンク。 -->

- [ ] `[skill]` `pokemon-data-reviewer` のレビュー観点に「メガ先 movepool が base と一致するか（過大表現検出）」を明示で含める（小規模データで顕在化しない設計問題の早期検出）。
- [ ] `[rule]` 既存挙動の設計レビュー漏れを減らすため、データ規模拡大（種族/メガ追加）時に既存生成ロジックの妥当性を再点検する観点を `code-review` / `data-pipeline` に補記する。
- [ ] `[template]`
- [ ] `[adr]`
- [ ] `[remove]`

## 📝 harness-meta フィードバック

<!-- 空でも見出しは維持する。harness-meta が後から各表に追記する。 -->

### 採用

### 見送り

### 保留
