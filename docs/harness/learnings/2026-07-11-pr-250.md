---
id: learning-pr-250
title: PR #250 レトロスペクティブ (gender メガの畳み込みを条件付き化し per-gender を既定に・ADR 0046)
type: learning
status: draft
related_pr: 250
related_plan: —
generated_at: 2026-07-19T00:00:00Z
generator: pr-retrospective skill
---

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/250 / commit b6d6a0b95f2e90876e909691518f4187797891f2 / merge 2026-07-11T04:54:02Z / 差分 17 files +285/-104

## ✅ Keep（継続したいこと）

- **前 PR のレビュー指摘 + ユーザー方針を 1 決定へ束ねて ADR 化**: PR #248 の `pokemon-data-reviewer` 指摘（`megaStoneFor` 非 canonical）と、ユーザー方針（♂♀で覚える技が違うなら畳まない）を単一の設計変更へ集約し、ADR 0046 で「無条件畳み込み → 条件付き畳み込み」へ明示的に supersede した。決定の Why が連番ログに残る。
- **畳み込み条件を純関数で形式化しテストで固定**: `gender-mega-collapse.ts` の `genderMegaFormsIdentical`（種族値・タイプ・特性・learnset すべて一致で畳む）を coverage 100% の純関数として実装。判定ロジックを IO から分離し、将来一致例が出たときの実装余地を判定 SoT として先置きした。
- **実データ突き合わせで畳み込み不可を根拠づけ**: meowstic は ♀♂で movepool が異なる（♀ extrasensory/future-sight・♂ imprison 等）ことを showdown 抽出で確認し、per-gender 分離が正しいと判断。推測でなくデータで方針を裏取り。
- **schema 変更を最小の機械移行で吸収**: `megaSpecies: string → readonly string[]` を、1 ストーンが ♀♂両形態に対応する要件（meowsticite）から導出。既存 item-specs の string 11 件を配列へ機械移行し、生成 `item-specs.ts` の `_ItemConforms`（Assignable）で typecheck 通過を確認。
- **pre-existing bug をついでに封じた**: per-reg reset（空スタブ null）で `ids is not iterable` になっていた既存バグを `?? []` / `?? {}` で耐性化し、reset reg からの generate 再生成を可能にした（本 PR の主目的に付随する健全化）。
- **end-to-end 検証で生成物の再現性を確認**: 最新 pokemon-showdown master で M-A 全抽出 → meowstic 分離・meowsticite 両形態リンク・`megaStoneFor: meowstic-male`・`check:regulation` 整合・`generate:data` 成功まで通し、純関数テストだけに頼らず裏取りした。

## ⚠️ Problem（詰まった点・制約）

- **ADR 0045 を採用直後に 0046 で supersede した方針の揺り戻し**: 0045（無条件畳み込み）と 0046（条件付き畳み込み）はともに 2026-07-11 付で、同日中に採用 → 覆しが起きた。0045 の決定時に「gender メガは種族値/タイプ/特性が一致」だけを畳み込み根拠にし、learnset の一致を条件に含めなかったことが早期の見直しを招いた。不可逆でない実装 PR だったため巻き戻しコストは小さかったが、ADR を「決定の不変ログ」に残す前に条件網羅の検討が浅かった。
- **判定 SoT を実装したがランタイム畳み込みマージは未配線**: `genderMegaFormsIdentical` は純関数 + テスト済みだが generate のランタイム畳み込みには繋いでいない（learnset が per-reg・mega-specs が global で整合が非自明、かつ現状 Champions の gender メガに一致例が無いため）。判定 SoT と実挙動の間にギャップが残り、将来一致例が出たときに実装忘れのリスクがある（ADR 0046 に明記でカバー）。
- **gender メガの canonical id 規則が複数ファイルに分散**: `mega-fields.ts`（`megaEvolveBaseId` / `genderMegaSiblingId`）・`items-fields.ts`（`megaStoneFor` canonical 化）・`canonical-species-id.ts`（FORM_SLUG_RENAME 復活）・`generate.ts`（配列展開）に gender メガの id 導出規則が分散し、1 決定の変更が 4 ファイルに波及した。規則の所在が追いにくい。
- **main の generated 不整合を本 PR 範囲外として据え置き**: `src/generated/champions/m-a/*` は main で「yaml reset・generated populated」の不整合を抱えるが、サンプルパーティ保護のため本 PR では触れず、後続 showdown-sync（#248 差し替え）に委ねた。PR 単独では生成物の整合が完結しない状態が続く。

## 🚀 Try（次回の改善案）

- **[rule] game-spec / data-pipeline に「フォルム畳み込みの判定軸は種族値・タイプ・特性・learnset の 4 点をすべて評価する」を明記**する。0045 が learnset を落として 0046 で補ったのを繰り返さないよう、畳み込み判定の必須軸を rule に固定する。
- **[skill] adr-new または code-review に「フォルム/形態を統合する決定は、統合軸が全同値条件を網羅しているか（特に learnset）を ADR 化前にチェック」を観点として追加**する。同日 supersede の揺り戻しは ADR 化前の条件網羅レビューで防げた。
- **[skill] verify-showdown-pr / author-regulation-data の references に「gender メガの canonical id 導出規則の所在（mega-fields / items-fields / canonical-species-id / generate の 4 箇所）」を索引として明記**し、規則変更時の追従漏れを防ぐ。
- **[adr] ADR 0046 に「判定 SoT（genderMegaFormsIdentical）とランタイム畳み込みマージのギャップを埋める条件（Champions に learnset 一致の gender メガが出現したら配線する）」をトリガとして残す**（既に本文に記載あり・将来の実装忘れ防止として TODO 追跡を検討）。

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit |
| カバレッジ | 100% | 100% | ±0 | Vitest（240 tests・新規純関数 gender-mega-collapse を網羅） |
| Biome 違反 | 0 | 0 | ±0 | biome check |
| CI | pass | pass | — | server-side pnpm verify（33s） |
| 差分 files/行 | — | 17 files +285/-104 | — | gh pr diff |

## 🤖 ハーネス改善提案

- [ ] `[rule]` game-spec / data-pipeline にフォルム畳み込みの判定軸（種族値・タイプ・特性・learnset の 4 点をすべて評価）を明記し、learnset 見落としによる再度の supersede を防ぐ。
- [ ] `[skill]` adr-new または code-review に「形態統合を伴う決定は統合軸が全同値条件（特に learnset）を網羅しているか ADR 化前に検証」観点を追加する。
- [ ] `[skill]` verify-showdown-pr / author-regulation-data の references に gender メガ canonical id 導出規則の所在（mega-fields / items-fields / canonical-species-id / generate の 4 箇所）を索引として明記する。
- [ ] `[adr]` ADR 0046 の「判定 SoT ↔ ランタイム畳み込みマージのギャップ」を埋めるトリガ（learnset 一致の gender メガ出現時に配線）を TODO 追跡できる形にする。

## 📝 harness-meta フィードバック

### 採用

### 見送り

### 保留
