---
id: learning-pr-231
title: PR #231 レトロスペクティブ (distinct-forms 名を含有判定合成で機械生成)
type: learning
status: draft
related_pr: 231
related_plan: 11-static-restore-hardening/phase-04
generated_at: 2026-07-05T15:32:00Z
generator: pr-retrospective skill
---

<!--
この雛形は `.claude/rules/retrospective-format.md`（SoT）に準拠する。
見出し（絵文字・文言）は harness-meta が parse するため変更しないこと。
-->

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/231 / commit (merge) / merge 2026-07-05T15:32:12Z / 差分 7 files +418/-58。手動 `SPECIES_FORMS` whitelist を廃し、タイプ/種族値が異なる全 form を含有判定ルール（`composeFormName`）で機械生成する reg 非依存の distinct-forms 名辞書を実装。`deriveBaseId`（最長前置一致）+ distinct 述語 + `FORM_INCLUDE`/`MANUAL_NAME_OVERRIDE` + PR レビュー表。214 tests / カバレッジ100%。

## ✅ Keep（継続したいこと）

- **含有判定合成の純関数化**。`composeFormName(base, form, brackets)` = 空→base / 含む→passthrough / 含まない→`base（form）`、brackets 引数で ja「（）」・en" ()" を対称適用。読みやすく破綻しない（合成側は必ず base を先頭に置く）。
- **distinct フィルタで reg 非依存の全 form 名辞書を機械生成**（type/stat 差 + `FORM_INCLUDE`、cosmetic/`-mega`/`-gmax`/`-primal`/`-totem`/`-starter` 除外）。手動 whitelist を廃止。
- **worker が実データで over-inclusion 2 件を自己検出・修正**（`-totem-alola` セグメント漏れ / minior 色コアの兄弟間 cosmetic 膨張）。実データ試行が机上設計の穴を埋めた。
- **Phase 3 の `CANONICAL_ID_OVERRIDE` を export して DRY 再利用**（override マップを複製せず SoT 1 箇所）。
- **PR レビュー表を `pokeapi-names.yml` に組み込み**、form ごと id/en/ja/decision/distinct 根拠を可視化しレビュー容易性を確保。
- 代表フォルム（gimmighoul 箱徒歩 / 各リージョン / greninja-battle-bond / tauros 3 breed）の正しい landing を明示確認。

## ⚠️ Problem（詰まった点・制約）

- **distinct fetch が全 variety の stats 込みで重い**（from-scratch で ~20 分・~3000 raw + pokemon 詳細列挙）。CI / ローカル時間コストが大きく、差分 / manifest キャッシュの最適化余地。
- **cosmetic 除外が正規表現 + 実データ依存**。totem-alola のセグメント漏れ・minior 色兄弟膨張を実装後に実データで初めて発見 → 除外網羅性が実データ試行に依存する。
- **赤 main 継承で verify に毎回フル復元が必要**（`fetch:ja-names → sync → generate`・#229 と同根）。
- **CI 赤のまま admin-merge**。server-side verify の裏取り無しで landing（#229 と同根・green main であれば正規 auto-merge）。

## 🚀 Try（次回の改善案）

- `author-static-data` skill に distinct フィルタの除外規則（totem セグメント化 / cosmetic-sibling 先着1代表畳み込み）と、新 form 追加時に実データで over/under-inclusion を確認する手順を明記。
- `data-pipeline` rule に distinct-forms 辞書の distinct 判定（type/stat 差）+ `FORM_INCLUDE` / `MANUAL_NAME_OVERRIDE` の SoT 位置（`src/codegen/materialize.ts` / `scripts/fetch-pokeapi.ts`）を明記。
- distinct fetch の全 variety stats 取得を差分 / manifest キャッシュ化し、from-scratch 以外は再取得を避けて重さを軽減する。

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass(復元後) | pass(復元後) | — | languages ローカル復元前提 |
| カバレッジ | 100% | 100% | ±0 | Vitest・214 tests |
| Biome 違反 | 0 | 0 | ±0 | biome check |
| CI | red(環境) | red(環境) | — | languages 撤去による既存赤・admin-merge |
| 差分 files/行 | — | 7 files +418/-58 | — | gh pr diff |

## 🤖 ハーネス改善提案

- [ ] `[skill]` `author-static-data`: distinct フィルタの除外規則（totem セグメント化 / cosmetic-sibling 先着畳み込み）+ 新 form 追加時の実データ over/under-inclusion 確認手順を明記。
- [ ] `[rule]` `data-pipeline`: distinct-forms 辞書の distinct 判定（type/stat 差）+ `FORM_INCLUDE` / `MANUAL_NAME_OVERRIDE` の SoT 位置を明記。
- [ ] `[skill]` distinct fetch の全 variety stats 取得を差分 / manifest キャッシュ化し from-scratch 以外の再取得を避ける最適化。
- [ ] `[remove]` （該当なし）

## 📝 harness-meta フィードバック

### 採用

### 見送り

### 保留
