---
id: learning-pr-251
title: PR #251 レトロスペクティブ (M-A 解禁データを pokemon-showdown から全量同期・authoritative)
type: learning
status: draft
related_pr: 251
related_plan: —
generated_at: 2026-07-18T18:19:10Z
generator: pr-retrospective skill
---

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/251 / commit 5a80048ec7f172dec0859bafbf4af3ceb18d09bd / merge 2026-07-11T05:15:50Z / 差分 19 files +39842/-508

## ✅ Keep（継続したいこと）

- **verify-showdown-pr + pokemon-data-reviewer の二段照合で per-gender メガ（ADR 0046）を裏取り**: showdown 抽出の `meowstic-female-mega` / `meowstic-male-mega`（各 gender base の movepool）を Serebii scraper 中間 JSON と生成 TS の双方で突合し blocking 0。自動照合（速報一致）と妥当性レビュー（構造整合）を別レイヤで重ねた。
- **Serebii 表現差を権威序列で「データ誤りでない」と明示判断**: Serebii mega scrape は `meowstic-mega`（単一）を返すが、これは表示簡略化。権威序列（showdown=正 > Serebii=速報・ADR 0039/0040）に従い reg 固有メガ構造は showdown を正とし、per-gender が game-accurate と結論。乖離を差異でなく表現差として非 blocking で吸収した。
- **showdown-sync.yml が M-A で end-to-end 成功し bot が `data:authoritative` PR を自動生成**: M-A roster は PokeAPI 存在特性のみ（Champions オリジナル特性を含まない）ゆえ ja backfill が完走し、workflow が checkout→sync→generate→PR まで無停止で通った。M-B（#254）が eelevate/fire-mane の名前欠落で workflow 全体失敗しローカル build へ縮退したのと対照的で、「オリジナル特性の有無」が workflow 完走可否を分ける事実を確認できた。
- **初回フル reg 同期が reg 非依存 specs も同時に全量 backfill**: `ability-specs` / `item-specs` / `move-specs` / `species-specs` / `mega-specs`（共有構造データ）が M-A 同期で一気に埋まり、後続 reg（M-B）の土台になった。1 reg の同期が共有 SoT を前進させる設計を実証。
- **#248 → #251 の再生成後継で ADR 0046（per-gender mega）改訂を reg 全体へ一貫反映**: gender メガ表現の決定を既存 PR の手直しでなく再生成（作り直し）で反映し、meowsticite の `megaSpecies: [female-mega, male-mega]`（配列）・`megaStoneFor: meowstic-male`（canonical）まで生成物全体を整合させた。

## ⚠️ Problem（詰まった点・制約）

- **巨大 diff で全量目視レビューが不可能**: 19 files +39842/-508、うち `species-moves`（YAML 13209 行 / 生成 TS 13512 行）が単体で全体の 2/3。レビューは Serebii scraper 中間 JSON の照合と pokemon-data-reviewer のサンプリング（重点フォルム）に依存し、行単位の網羅は原理的にできない。
- **Serebii scraper が reg 非依存 + フォルム表現差ゆえ roster 件数照合ができない**: scraper は全 Champions を返し canonical フォルム表現も異なるため件数が一致せず、「件数レベルの網羅裏取り」は既知の非差異として毎回スキップされる。数の抜け漏れを機械照合で担保できない。
- **gender メガの per-form vs 単一表示の乖離が人手の権威序列判断を要する gray zone**: showdown per-gender と Serebii 単一表示の食い違いを「データ誤りか表現差か」人が ADR 0039/0040 に照らして裁定する必要があり、自動照合だけでは判定不能（verify-showdown-pr が非 blocking 注記で吸収した）。
- **ADR 0046 決定が既存 PR #248 に遡及し再生成コストが発生**: per-gender メガの決定確定後、#248 を破棄して #251 で reg を丸ごと再同期する必要があった。メガ表現のような横断決定は同期済み reg 全体の作り直しを誘発する。

## 🚀 Try（次回の改善案）

- **[skill] verify-showdown-pr に「Serebii が reg 非依存 / フォルム表現差で件数一致しないのは既知の非差異」と件数照合の限界を明記**し、件数で網羅裏取りできない前提と代替（重点フォルムのサンプリング観点）を定型化する。今回・#248 で反復した注記を skill 側へ固定する。
- **[skill] verify-showdown-pr に「showdown per-form（gender メガ等）vs Serebii 単一表示の乖離は権威序列（ADR 0039/0040）で表現差と判定」の判断分岐を明記**し、meowstic の裁定を再現可能にする（自動照合の gray zone を skill の明示ルールで埋める）。
- **[skill] author-regulation-data / verify-showdown-pr に「初回フル reg 同期は reg 非依存 specs も全量変わり diff が巨大化する。行単位目視は求めず中間 JSON 照合 + data-reviewer サンプリングに委ねる」旨を明記**し、巨大 data PR のレビュー期待値を揃える。
- **[skill] author-regulation-data に「メガ表現等の横断 ADR 決定は同期済み reg 全体の再生成を誘発しうる」注意を添える**（#248→#251 の作り直しコストを事前に想定させる）。

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit |
| カバレッジ | 100% | 100% | ±0 | Vitest（データ投入ゆえプロダクションコード不変） |
| Biome 違反 | 0 | 0 | ±0 | biome check |
| CI | pass | pass | — | server-side pnpm verify（github-actions[bot] 生成 PR ゆえ on:pull_request 発火） |
| 差分 files/行 | — | 19 files +39842/-508 | — | M-A roster + reg 非依存 specs 全量 backfill・species-moves が約 2/3 |

## 🤖 ハーネス改善提案

- [ ] `[skill]` verify-showdown-pr に「Serebii は reg 非依存 / フォルム表現差で件数一致しない（既知の非差異）」と件数照合の限界・代替（重点フォルムのサンプリング）を明記。
- [ ] `[skill]` verify-showdown-pr に「showdown per-form vs Serebii 単一表示の乖離は権威序列（ADR 0039/0040）で表現差と判定」の判断分岐を明記し meowstic 裁定を再現可能化。
- [ ] `[skill]` author-regulation-data / verify-showdown-pr に「初回フル reg 同期は reg 非依存 specs も全量変わり巨大 diff になる。行単位目視でなく中間 JSON 照合 + data-reviewer サンプリングで担保」を明記。
- [ ] `[skill]` author-regulation-data に「メガ等の横断 ADR 決定は同期済み reg 全体の再生成を誘発しうる」注意を追記（#248→#251 の再生成コスト想定）。

## 📝 harness-meta フィードバック

### 採用

### 見送り

### 保留
