---
id: learning-pr-247
title: PR #247 レトロスペクティブ (battleOnly フォルムを roster 除外しフォルム learnset を base 継承)
type: learning
status: draft
related_pr: 247
related_plan: —
generated_at: 2026-07-18T18:18:39Z
generator: pr-retrospective skill
---

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/247 / commit 7344c53cad052b1d3cc7b7f6f00ba4582d794431 / merge 2026-07-11T03:50:25Z / 差分 6 files +33/-812

## ✅ Keep（継続したいこと）

- **メガ巻き込み回帰を落とし穴として明示コメント化**: メガフォルムも `battleOnly` である事実を掴み、素朴な `!battleOnly` が全メガを roster から除外する回帰を `(!species.battleOnly || isMegaForme(species))` で回避。なぜこの分岐が必要かを 5 行コメントで残し、将来の「単純化」による再回帰を防いだ。レビューもこの落とし穴回避を明示評価。
- **最新 pokemon-showdown master で end-to-end 検証**: `scripts/showdown/**` は showdown ツリーで動く抽出層（tsconfig exclude・coverage 対象外）ゆえ unit test でなく、実 master で M-A 全抽出 → species 244→237（除外7フォルム）・メガ 60 維持・gourgeist base 技継承 → check:yaml-style / check:regulation / generate:data / verify 緑を通して機械裏取り。coverage 対象外層の担保方法が一貫。
- **除外・継承の対象種を具体列挙して自己文書化**: 除外される battleOnly（castform 天候 / aegislash-blade / mimikyu-busted / morpeko-hangry / palafin-hero）と残る base（castform / aegislash-shield）、継承する空 learnset フォルム（gourgeist サイズ違い）をコメントに列挙。diff だけで意図が追える。
- **base 遡上を while ループで多段耐性化**: `learnsetNames` の base 継承を 1 段固定でなく `baseSpecies` を辿る while ループにし、`base !== current.name` で終端。gourgeist は 1 段だが将来の入れ子フォルムにも耐える防御的実装（レビュー nit でも肯定）。
- **reset との協調で per-reg スタブ耐性を同時修正**: battleOnly 除去で空 movepool が生じる問題と、per-reg reset（空スタブ `moves:`/`mega:` = null）で block map が無い問題を `getOrCreateBlockMap` へ寄せ、materialize P2 と同根の耐性化として sync-showdown.ts に一貫適用。

## ⚠️ Problem（詰まった点・制約）

- **flow スタイル違反が最終段まで潜伏していた**: 空 movepool による flow `[]` は generate/lint 段で先に落ちて `check:yaml-style` まで未到達だったため、M-A フル rollout で初めて表面化。パイプライン段階の落ちる順序に依存して不具合が隠れ、根本原因（battleOnly 混入 + base 非継承）の露見が遅れた。
- **抽出層が coverage 対象外ゆえ回帰検知が手動 end-to-end 頼み**: `scripts/showdown/**` は tsconfig exclude で unit test が無く、battleOnly/learnset の分岐修正が自動テストで守られない。今回の正しさは手動 e2e でしか確認できず、将来の再回帰を CI が捕まえられない構造的死角が残る。
- **修正が data reset と抽出ロジックの 2 層に跨る**: 本 PR は roster/moves/mega/species-moves の YAML reset（-812 行）と dex.ts/sync-showdown.ts のロジック（+33 行の実体）を 1 PR に同梱。data 全量削除の巨大 diff がロジック変更の可視性を薄める（機能的には reset→再取得の前提として妥当だが差分の読みやすさは犠牲）。
- **battleOnly の網羅列挙が手動確認依存**: 除外対象（darmanitan-zen 等も含む）をコメントで例示したが、showdown 側で battleOnly フラグが付く種の全量は skill/rule に固定されておらず、次に showdown が新 battleOnly フォルムを足したとき roster 変化を人手で気づく必要がある。

## 🚀 Try（次回の改善案）

- **[skill] verify-showdown-pr / author-regulation-data に「battleOnly フォルムは roster 除外・size 系フォルムは base learnset 継承」の抽出契約を references に明記**し、showdown master 更新で roster 数が変わったときの期待挙動（除外/継承の対象種）を skill から辿れるようにする（今回の暗黙知を成文化）。
- **[rule] `scripts/showdown/**`（coverage 対象外の抽出層）の変更は「最新 showdown master で end-to-end（抽出→check:yaml-style→check:regulation→generate→verify）」を必須手順として code-review checklist か data-pipeline rule に明示**。unit test 死角を e2e 手順で機械的に埋める慣行を固定する。
- **[skill] data reset（全量削除）と抽出ロジック修正を同一 PR に載せるときは、PR 本文で「ロジック diff = 実体・data diff = reset 前提」と役割を明記する運用を implementation-workflow / code-review に一言添える**（巨大 reset 差分がロジック変更を埋没させないための可視化）。

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit |
| カバレッジ | 100% | 100% | ±0 | Vitest（233 tests・抽出層は coverage 対象外・e2e 担保） |
| Biome 違反 | 0 | 0 | ±0 | biome check（127 files） |
| CI | pass | pass | — | server-side pnpm verify |
| 差分 files/行 | — | 6 files +33/-812 | — | gh pr diff（data reset -812 / ロジック +33） |

## 🤖 ハーネス改善提案

- [ ] `[skill]` verify-showdown-pr / author-regulation-data の references に「battleOnly フォルムは roster 除外（メガは isMegaForme で保全）・size 系フォルムは base learnset 継承」の抽出契約と対象種例を明記。
- [ ] `[rule]` `scripts/showdown/**`（coverage 対象外の抽出層）変更は最新 showdown master で end-to-end 検証を必須とする慣行を code-review checklist か data-pipeline rule に明示。
- [ ] `[skill]` data 全量 reset とロジック修正を同一 PR に載せるとき、PR 本文で diff の役割（reset 前提 vs 実体ロジック）を明記する運用を implementation-workflow / code-review に添える。

## 📝 harness-meta フィードバック

### 採用

### 見送り

### 保留
