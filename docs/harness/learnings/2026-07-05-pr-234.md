---
id: learning-pr-234
title: PR #234 レトロスペクティブ (canonical species id を短い独自形へ変更し form マッピングを調整)
type: learning
status: draft
related_pr: 234
related_plan: 11-static-restore-hardening/phase-04
generated_at: 2026-07-18T18:17:24Z
generator: pr-retrospective skill
---

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/234 / commit 4582bcee5808479d28bc8876488ba85e6769e2cb / merge 2026-07-05T19:41:54Z / 差分 6 files +383/-73

## ✅ Keep（継続したいこと）

- **canonical を単一 SoT に収束させる両側一貫設計**: 構造側（`canonicalSpeciesId`: showdown 名 → canonical）と名前側（`fetch-pokeapi.ts`: PokeAPI slug → canonical）の両入力を新設純関数 `canonicalFormId` に通し、どちらからも同じ短い canonical へ収束させた。接尾辞除去（`-strike`/`-breed`/`-mask`/`-plumage`/`-segment`/`-striped`/`-power-construct`/`family-of-`）のルールを 1 箇所に集約したことで、二経路のドリフト源を構造的に断った。
- **curated マップの語彙を用途別に分離**: base 畳み込み（`FORM_EXCLUDE`）/ cosmetic 別種族化（`FORM_INCLUDE`）/ bare default 明示分割（`CANONICAL_ID_OVERRIDE`）/ 手動名上書き（`MANUAL_NAME_OVERRIDE`）を役割ごとに分け、27 項目のユーザー指示マッピングを既存の curated マップパターンへ相似的に落とした。レビューも各項目を語彙単位で spot-check でき一貫性を評価。
- **実データ復元による spot-check**: 純関数テストだけに頼らず `pnpm fetch:ja-names && sync:ja-names && generate:data` でローカル復元し、`data/languages/species.yaml` で A（リネーム後 id 存在・旧 suffixed 不在）/ B（pyroar・tatsugiri は base のみ・minior コア不在）/ C（hoopa-confined・squawkabilly 4色 等の合成名）/ D（tauros breed ja/en・pumpkaboo サイズ・darmanitan-galar）を意図どおりと機械的に確認。
- **生成物を混入させない明示ステージング**: languages/generated の復元物は commit せず、`git add` で対象 5 ファイルのみ明示ステージして差分をコード変更に限定した。復元は検証専用と割り切り、生成物汚染を防いだ。
- **純関数 / IO 層分離とコロケーション test の維持**: 判断ロジック（`canonicalFormId` / `canonicalSpeciesId`）を coverage 100% 対象の純関数に置き、コロケーション test（45 件緑）を追加・更新。薄い YAML/IO 層と判断層を分けた既存構造に沿った。
- **ADR consequence 抵触を PR 本文で明示申告**: canonical を「PokeAPI slug 恒等」から短い独自形へ変えたことで ADR 0044 の consequence（名前側の突合は PokeAPI slug と恒等）を revise していると自認し、レビュー留意点として明記。rule（`data-pipeline.md`）は同 PR で追記し、ADR supersede 要否は人判断へエスカレーションした。

## ⚠️ Problem（詰まった点・制約）

- **CI 赤のまま admin-merge**: main は plan 11 で languages/generated を撤去済みのため CI が既存要因で赤。本 PR のコード変更は無関係だが、server-side CI 緑を merge ゲートにできず admin-merge で landing した。plan 進行中に「本 PR 無関係の CI 赤」を都度判断して手動マージする運用が反復しており、判断基準が明文化されていない。
- **Accepted ADR の consequence を revise したが supersede 手続きが未確定**: ADR 0044 本体（Accepted）の consequence を本 PR が実質的に覆したが、supersede + archive + inbound reref は「意図的な decision-level 操作」ゆえ本 PR スコープ外とし人判断に委ねた。rule だけ先行追記され、ADR 本体と rule の間に一時的な不整合（決定の根拠が古いまま）が残る。
- **名前側マッピングの検証がローカル復元に依存**: languages/generated が main で撤去済みのため、マッピング変更の実効を確認するには fetch → sync → generate のフル復元を毎回回す必要があり、軽量な確認手段がない（純関数テストは canonical 化ロジックは押さえるが実データ合成名までは担保しない）。
- **default variety の bare→explicit 分割が複数語彙に散る**: `hoopa`→`hoopa-confined` / `castform`→`castform-normal` は `CANONICAL_ID_OVERRIDE`、pyroar-male / tatsugiri-curly の default slug 除外は `FORM_EXCLUDE`、squawkabilly green は default slug リネームで拾う、と「bare default の扱い」が複数マップに分散し、どの語彙で拾うかの判断が curated 知識に依存する。

## 🚀 Try（次回の改善案）

- **[adr] Accepted ADR の consequence を実装が覆したときの後続手続きを定型化する**: 本 PR は ADR 0044 consequence を revise しつつ supersede を人判断へ逃がした。`adr.md` に「consequence を覆す変更は同 PR か直後に supersede（新 ADR）or 明示的な不要判断を残す」フローを一言足し、rule 先行追記と ADR 本体の一時不整合を解消する導線を作る。
- **[skill] レビュー観点に「ADR consequence への抵触検知」を追加する**: `code-review` の checklist に「curated マップ / canonical 規約変更が既存 ADR の consequence（例 ADR 0044 の slug 恒等）を覆していないか」を重点観点として明記し、rule 追記と ADR supersede の追従漏れを PR マージ前に拾えるようにする。
- **[rule] plan 進行中の「本 PR 無関係な既存 CI 赤」で admin-merge を許容する判断基準を明文化する**: plan 11 で languages/generated 撤去に起因する CI 赤が反復している。`code-review.md` の auto-merge ゲート節に「CI 赤が当該 PR 無関係の既知の plan 中間状態に起因すると PR 本文で立証済みかつ blocking 0 のときに限り admin-merge を許容」の但し書きを添え、判断を再現可能にする。
- **[skill] `author-static-data` / `verify` に「languages/generated 復元物は commit せず対象ファイルのみ明示 git add」を手順として明記する**: 今回の 5 ファイル明示ステージを定型化し、復元検証時の生成物混入事故を skill 手順で防ぐ。

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit（ローカル復元下で verify 緑） |
| カバレッジ | 100% | 100% | ±0 | Vitest（canonicalFormId / canonicalSpeciesId コロケーション test 45 件緑） |
| Biome 違反 | 0 | 0 | ±0 | biome check |
| CI | fail | fail | — | server-side pnpm verify（languages/generated 撤去の既存要因で赤・本 PR 無関係／admin-merge で landing） |
| 差分 files/行 | — | 6 files +383/-73 | — | gh pr diff（コード 4 + rule 1 + test は canonical-species-id / species-fields の 2 ファイル） |

## 🤖 ハーネス改善提案

- [ ] `[adr]` `adr.md` に「Accepted ADR の consequence を実装が覆す変更は同 PR か直後に supersede（新 ADR）または明示的な不要判断を残す」後続手続きを追記し、rule 先行追記と ADR 本体の一時不整合を解消する導線を作る。
- [ ] `[skill]` `code-review` の checklist に「canonical 規約 / curated マップ変更が既存 ADR の consequence を覆していないか」を重点観点として明記する（ADR 0044 slug 恒等の revise 検知）。
- [ ] `[rule]` `code-review.md` の auto-merge ゲート節に「CI 赤が当該 PR 無関係の既知 plan 中間状態に起因すると PR 本文で立証済みかつ blocking 0 のときに限り admin-merge を許容」の但し書きを追記する。
- [ ] `[skill]` `author-static-data` / `verify` に「languages/generated 復元物は commit せず対象ファイルのみ明示 git add でステージ」を手順として明記する。

## 📝 harness-meta フィードバック

### 採用

### 見送り

### 保留
