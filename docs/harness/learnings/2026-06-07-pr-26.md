---
id: learning-pr-26
title: PR #26 レトロスペクティブ (01-mvp Phase 2 個体 tsc 検証層)
type: learning
status: draft
related_pr: 26
related_plan: 01-mvp/phase-02
generated_at: 2026-06-07T00:35:00Z
generator: pr-retrospective skill
---

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/26 / commit 0ccad08 / merge 2026-06-07T00:29:54Z / 差分 20 files +848/-16。「検証は tsc のみ」をブランドエラー型 + codegen + `@source` 逆引きで実装し、覚えない技・未解禁・合計66 違反等を `tsc --noEmit` で弾く層を追加。`implementation-workflow` Phase 0〜6 を fix loop 0 回・レビュー blocking 0 件で通過。

## ✅ Keep（継続したいこと）

- **受け入れ基準を実 fixture で機械確認**: 覚えない技・性格 up=down・ポイント≠66・重複・未解禁・参照切れ・6体超過を /tmp の不正 YAML/MD で実際に非0終了 + ブランド名 + 行番号まで確認してから PR を出した。型レベル検証は「動くはず」で済ませず実行確認が効く。
- **ブランド基盤の単一ソース化**: `Invalid<Message>`（`unique symbol`）を 1 箇所に置き、個体/パーティ双方のブランド型を被せた。診断にブランド名が表面化し、Zod 不採用でも可読性を担保できた。
- **codegen は常に成功し tsc が弾く**原則の徹底: codegen 側で値の妥当性を例外で弾かず、不正値も生成 TS に埋めて tsc に検出させた（[[tsc-verification]] / ADR 0010 と整合）。
- **巨大 union での分配回避**: `SpeciesDex[S]["moves"][number]` のプロパティアクセス主体で制約を組み、union 展開による tsc 速度劣化を避けた（phase doc のリスク項目を設計で吸収）。
- **Generator/Evaluator 独立の遵守**: 実装（本ワーカー）とレビュー（`code-review` / `harness-review` を Agent で並行起動）を分離し、両レビューを PR コメントに残してから停止した。自己採点で代替しなかった。
- **既存ゲートを壊さない hook 追加**: pre-commit/pre-push に `pokeform` の検証を足す際、不正デモ（broken.md）を意図的に対象外にし、コミット/プッシュ時のゲートが常時赤にならないよう配慮した。

## ⚠️ Problem（詰まった点・制約）

- **codegen 層のテスト空白**: `vitest.config.ts` で `src/codegen/**` を丸ごとカバレッジ除外したため、本 PR で最もロジック密度の高い `@source` 行逆引き（`run-tsc.ts`）と lang 別名称解決（`normalize.ts`）に自動回帰テストがゼロ。architecture 上は許容だが、リファクタ耐性は低い（code-review non-blocking 指摘）。
- **満点診断の非対称**: 個体の技/特性は行単位で `@source` が効くが、パーティのメンバー違反は `members:` 行 1 点に丸まる。どのメンバーが違反かは型名で分かるが行は粗い（code-review 指摘）。
- **regulation が lang 正規化を素通り**: 個体側の名称は lang に従い ID 正規化するのに、`emit-party` の `regulation` は生値直埋め。現データは ID 形式で「たまたま通る」状態で、ADR 0014 の per-file lang 厳格化と乖離（code-review 指摘）。
- **pre-push のゲート対象がファイル直指定**: `team/parties/standard.md` を直指定したため、将来妥当パーティを追加してもゲートを素通りする（check:individual 側はディレクトリ glob で非対称・harness-review 指摘）。
- **points 部分指定の診断がブランド化しない**: `points` のキー不足は `PointAllocation` の「プロパティ不足」エラーになり `PointTotalMustBe66` 等のブランド名が出ない（実装単純性を優先したトレードオフ・ADR 0019 に明記）。

## 🚀 Try（次回の改善案）

- **codegen の純ロジックを domain へ抽出してテスト**: `@source` 逆引きと lang 別 StatKey 解決のような純関数を `src/domain/` 相当へ切り出し、コロケーションテストで網羅する（薄い I/O だけを codegen に残す）。`testing` rule の「ドメインは完全網羅 / 薄い層は除外」をより忠実にできる。
- **不正 fixture をリポジトリ資産化**: 各ブランド型に対応する最小の不正個体/パーティを `team/__invalid__/` 等に置き、`check:individual` が期待ブランド名で非0終了することをスナップショット的に検証する仕組みを `author-individual` skill か新規 skill に足す。
- **broken.md をデモ専用ディレクトリへ移し pre-push をディレクトリ glob 化**: 妥当パーティを自動でゲート対象に含められるようにする（hook の自己文書化・harness-review 提案）。
- **emit-party の regulation も lang 正規化する**: 個体と同じ `resolveName` 経路に通し、per-file lang 厳格化（ADR 0014）の一貫性を回復する。

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit |
| カバレッジ | 100% | 100% | ±0 | Vitest（126 stmt / 60 branch / 20 func / 111 line） |
| Biome 違反 | 0 | 0 | ±0 | biome check |
| CI | pass | pass | — | server-side pnpm verify |
| 差分 files/行 | — | 20 files +848/-16 | +20f | gh pr diff #26 |

## 🤖 ハーネス改善提案

<!-- harness-meta が parse する正規構造。未処理は [ ]、採用後に [x] + PR リンク。 -->

- [ ] `[rule]` `testing`: codegen の純ロジック（`@source` 逆引き・lang 別名称解決）を domain へ抽出してテストする指針を追記し、`src/codegen/**` 一律除外の例外（純関数は domain でテスト）を明文化する。
- [ ] `[skill]` `author-individual`: 各ブランド型に対応する最小の不正 fixture で「期待ブランド名 + 非0終了」を検証する手順を足し、tsc 検証層の回帰を利用者側からも担保する。
- [ ] `[skill]` `verify` or `.githooks`: 妥当サンプルパーティをディレクトリ glob でゲート対象にできるよう、不正デモ（broken.md）を専用ディレクトリへ分離する運用を定める（pre-push のファイル直指定を解消）。
- [x] `[rule]` `cli-and-io`: codegen の名称正規化は個体・パーティ・regulation を含め全フィールドで lang 宣言に従う、と一貫性を明記する（emit-party の regulation 素通りの再発防止）。
- [x] `[adr]` （対応済み）本 PR のブランドエラー型 + `@source` 逆引きパターンは ADR 0019 として起票済み。

## 📝 harness-meta フィードバック

<!-- 空でも見出しは維持する。harness-meta が後から各表に追記する。 -->

### 採用

- harness-meta batch 2026-23（PR `harness/meta-batch-2026-23`）で rule 反映。codegen の名称正規化は個体・パーティ・`regulation` を含む全フィールドで `lang` 宣言に従う旨を `cli-and-io.md` に明記（emit-party の regulation 素通り再発防止）。

### 見送り

- `[rule]` codegen 純ロジックの domain 抽出テスト: 後続実装タスクで対応（`src/codegen` のリファクタを伴う）。
- `[skill]` 不正 fixture 検証手順 / `[skill]` broken.md 専用ディレクトリ分離: 次ラウンドで skill-creator 経由（本 PR は rule-only に限定）。再発で再評価。

### 保留
