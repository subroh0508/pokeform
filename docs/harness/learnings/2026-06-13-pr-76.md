---
id: learning-pr-76
title: PR #76 レトロスペクティブ (02 に Phase 15 YAML ブロックスタイルゲート挿入・M-A 全量を 16 へ繰り下げ)
type: learning
status: draft
related_pr: 76
related_plan: 02-data-model-redesign
generated_at: 2026-06-13T02:36:26Z
generator: pr-retrospective skill
---

<!--
この雛形は `.claude/rules/retrospective-format.md`（SoT）に準拠する。
見出し（絵文字・文言）は harness-meta が parse するため変更しないこと。
書き出し前に `.claude/rules/redaction.md` の正規表現で Secrets / PII を [REDACTED-*] 置換すること。
-->

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/76 / commit 278473395034ec34af0b3ce405a50b7c5eaeac36 / merge 2026-06-13T02:36:26Z / 差分 13 files +167/-42
> `plans-new` による計画化（doc のみ・実装は後続 PR #77）。02-data-model-redesign に Phase 15「data/ 全 YAML ブロックスタイル強制ゲート」を挿入し、M-A 全量投入を Phase 15→16 へ renumber（PR #70 と同パターン）。harness-review が blocking 1 件（dangling ADR リンク 2 本）を検出し修正コミットで解消。

## ✅ Keep（継続したいこと）

<!-- 最低 3 / 推奨 5-10 -->

- phase 挿入 + renumber を **独立した doc-only PR** として分離し、実装（PR #77）と切り離した。PR #70 で確立したパターンを踏襲しており、diff を doc に限定してレビュー負荷を下げる運用が再現できている。
- 「全量データ投入の手前で仕組み（ブロックスタイルゲート）を確定させる」という de-risk の論理（Phase 10/13 と同型）を明示し、なぜ Phase 15 を M-A 全量の手前に置くかを背景セクションで自己説明している。
- harness-review が **dangling ADR リンク 2 本**（slug 不一致）と **renumber 取り残し**（OVERVIEW 散文の素の `15`）を的確に捕捉し、blocking / non-blocking / nit の重大度を付けて指摘できている。レビューが機械ゲートで漏れる層を埋めている。
- renumber 後に **全 phase doc のリンク解決・mermaid（P14→P15→P16）整合・phase 一覧と実ファイル名の一致** を機械的に確認し、PR 本文のチェックリストに残している。
- 修正コミット（eb789bbe5）で harness-review 指摘（ADR slug 訂正 + OVERVIEW renumber 取り残し）をまとめて解消し、blocking を残さずマージできている。
- `plans-new` 経由で OVERVIEW（phase 表に行追加・直列チェーン・「16 phase に分割」）まで一貫して renumber を反映し、計画ドキュメント群の整合を保っている。

## ⚠️ Problem（詰まった点・制約）

<!-- 最低 3 / 推奨 5-10 -->

- **手書きの ADR 参照リンクが slug 不一致で dangling になった**（blocking）。`0010-tsc-only-validation.md`（正: `0010-tsc-only-verification.md`）/ `0023-generate-transform-only-check-regulation.md`（正: `0023-generate-transformer-and-check-regulation.md`）。ADR は採番が紛らわしく slug を取り違えやすい。これは learning #42 / #58 でも指摘された **反復課題**。
- **多数の merged phase doc に跨る forward 参照の renumber が手作業で漏れた**（non-blocking）。OVERVIEW の「6 基準で分割した理由」散文 7 箇所で M-A 全量投入を素の `15` のまま参照しており、renumber 後の実体（yaml ゲート = Phase 15）とずれた。表・チェーンは正しく更新済みだが、散文の素の数字が特に漏れやすい。
- 散文中の **アンカーなしの素の phase 番号**（「全量投入（15）」等）は renumber 時に grep で機械検出しづらく、文脈依存で「これは旧番号か新番号か」の判断が要る。renumber のたびにヒューマンエラー余地が残る。
- 新 phase doc 作成時に ADR への相対リンクを**実ファイル存在で検証する仕組みがない**ため、dangling は harness-review という後段レビューまで検出されなかった（生成時点で弾けるとより早い）。
- de-risk 挿入リストの導入散文が Phase 14 止まりで、新設 Phase 15（yaml ゲート）が連鎖説明に未言及（nit）。表・チェーンには載っているが導入散文と非対称。

## 🚀 Try（次回の改善案）

<!-- rule / skill / template / ADR 単位まで具体化し、下の 🤖 改善提案へ落とす -->

- `plans-new` / `harness-review` に「ADR / rule への相対リンクは**実ファイル存在を機械確認**する」手順を明文化する。`ls docs/adr/ | grep <NNNN>` 等で slug を実体照合してからリンクを書く / レビューで弾く。learning #42/#58/#76 の反復を断つ。
- phase doc テンプレ（または skill-authoring）で **ADR 参照を slug 依存でなく `ADR-NNNN` 番号参照に寄せる**方針を検討し、slug 取り違えの構造的余地を減らす。
- renumber 補助の方針を定める: 散文中も含め phase 参照を **素の数字でなくアンカー / 安定ラベル**で書くか、`plans-new` の renumber 工程に「散文中の素の旧番号 grep チェックリスト」を加える。
- `plans-new` の renumber チェックに「表・mermaid だけでなく**散文 bullet の素の phase 番号**も走査する」明示ステップを足し、forward 参照の取り残しを構造的に防ぐ。

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | doc のみ・tsc 非該当 |
| カバレッジ | 100% | 100% | ±0 | doc のみ・コード変更なし |
| Biome 違反 | 0 | 0 | ±0 | doc のみ |
| CI | pass | pass | — | server-side pnpm verify（doc only・緑） |
| 差分 files/行 | — | 13 files +167/-42 | — | gh pr diff（初回 + harness-review 修正コミット） |

## 🤖 ハーネス改善提案

<!-- harness-meta が parse する正規構造。未処理は [ ]、採用後に [x] + PR リンク。 -->

- [ ] `[skill]` `plans-new` / `harness-review` に「ADR / rule への相対リンクは実ファイル存在を機械確認する（`ls docs/adr/ \| grep <NNNN>` 等で slug を実体照合）」手順を明文化し、手書きリンクの dangling（learning #42/#58/#76 反復）を生成・レビュー時に弾く。
- [ ] `[skill]` `plans-new` の renumber 工程に「表・mermaid だけでなく**散文 bullet 中の素の phase 番号**も走査する」明示チェックを追加し、forward 参照 renumber 取り残し（OVERVIEW 散文の素の `15`）を防ぐ。
- [ ] `[template]` phase doc テンプレで ADR 参照を slug 依存でなく `ADR-NNNN` 番号参照（または安定アンカー）へ寄せ、slug 取り違えと renumber 時の素の数字漏れの構造的余地を減らす。
- [ ] `[rule]` `skill-authoring` / planning の renumber 方針に「散文中の phase 参照は素の数字でなく安定ラベル / アンカーで書く」原則を追記し、grep で機械検出可能な形に統一する。

## 📝 harness-meta フィードバック

<!-- 空でも見出しは維持する。harness-meta が後から各表に追記する。 -->

### 採用

<!-- 採用した提案と採用先 PR リンク -->

### 見送り

<!-- 見送り理由（harness-meta-criteria.md の見送り基準に対応） -->

### 保留

<!-- 保留理由 -->
