---
id: learning-pr-122
title: PR #122 レトロスペクティブ (技メタを per-game へ移転・ADR 0026 を 0034 へ supersede)
type: learning
status: draft
related_pr: 122
related_plan: 03-survey-regulation-rework/phase-11
generated_at: 2026-06-15T00:00:00Z
generator: pr-retrospective skill
---

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/122 / commit 3839e603088a268c564f0ff696895d7fb3f17e26 / merge 2026-06-14T19:25:10Z / 差分 29 files +3546/-2699。技メタ SoT を `catalog/moves.yaml` → per-game `regulations/champions/moves.yaml` へ移転（`MoveBase` を id+name に縮小・`MoveStats` 分離）。ADR 0026 を 0034 へ supersede し archive 退避。

## ✅ Keep（継続したいこと）
- **等価移設の機械検証**: 219 技の技メタ値が移設前後で一致することを `generate.ts` の id 集合双方向検証（name without stats / stats without name）で生成段に throw させ、authoring 漏れを tsc 以前に弾いた。データレイアウト変更で「移設前後の等価性」をスクリプトで担保する型は再現したい。
- **型分離の責務明快化**: `MoveBase`（id+name・ゲーム非依存）と `MoveStats`（type/damageClass/power…・per-game）の分割が既存 Dex パターンと一貫し、消費側 repoint が必要十分（coverage のみ `moveStatsDex` を引き、名前引き 4 箇所は据え置き）で済んだ。
- **ADR supersede 作法の準拠**: 採番 0034（active 0033 / archive 0031 の次・最大連番+1）・0026 frontmatter status のみ変更で本文不変・archive へ `git mv`・README active/archive 表整合・Alternatives あり・決定文が能動断定形、と adr.md の規約を満たした（harness-review で「規約準拠で問題なし」）。
- **2 レビュアー分業の機能**: code-review（src/scripts）と harness-review（ADR/rules/skills/docs）が scope を分けて並走し、harness-review が src レビュー対象外の手順アセット（serebii-sourcing.md）の旧 SoT 残骸を blocking で捕捉した。
- **inbound リンクの能動追従**: archive 化で dangling になる 0026 inbound（active ADR 0027/0030/0032・skill・archive/0031）を同 PR で archive/ パスへ機械修正した。

## ⚠️ Problem（詰まった点・制約）
- **ADR archive 移設の inbound 追従が網羅できず blocking**: harness-review が、本 PR で一切編集していない**ライブ手順 SoT** `serebii-sourcing.md` に旧所在「技メタ=catalog/moves.yaml」「`satisfies MoveBase`」が残存と指摘（blocking 1）。skill 再実行時に誤った場所・前提で技メタを著述する実害があり、ワーカーが同 PR で修正。supersede 時に「その ADR を参照する全 SoT doc + inbound リンク」を網羅走査する仕組みが無かった。
- **非編集ファイルにも追従漏れ**: `data/README.md`（L33/52）と `phase-17/18/19` の `[ADR 0026]:` reference link が active パスのまま dangling（non-blocking 2 件）。手で潰すと scope 表に挙がらないファイルを取りこぼす（同型は #104 の renumber 追従でも反復）。
- **game 導出ロジックの DRY 違反**: `serebii-to-catalog.ts:153` の技メタ書き出しが game を独自導出（`regId.includes("-") ? slice(0, indexOf("-")) : "champions"`）し、short-form `m-a` を game=`m` に誤分割。同ファイルの `regPath()` が支える short-form 契約と乖離（code-review non-blocking）。
- **per-game 共有ファイル除外の二重定義**: `REG_SHARED_FILES = new Set(["moves.yaml"])` が `generate.ts:137`（scripts 層）と `check-regulation.ts:33`（src/cli 層）に分散。共有ファイルが増えた際の lockstep 更新ドリフト点（code-review nit）。
- **大規模差分のレビュー負荷**: +3546/-2699・29 files（型分離 / データ移行 / 生成スクリプト / ADR / docs 追従）が 1 PR に集中し、追従漏れの検出をレビューに依存した。

## 🚀 Try（次回の改善案）
- **ADR supersede 時の inbound 走査をチェックリスト化**: `adr-new`（supersede モード）に「対象 ADR を参照する全ファイル（リンク式 `NNNN-...md` + reference 式 `[ADR NNNN]:` + 番号参照 + ライブ手順 SoT 内の SoT 所在記述）を機械走査し archive/ パス・後継 ADR へ追従する」手順を加える。`grep -rn '0026'` 級の全走査を archive `git mv` 直後に必須ステップ化。
- **ライブ手順 SoT の SoT 所在を ADR と紐付け**: skill references（serebii-sourcing.md 等）が ADR の決定（SoT 所在・型名）を再記述している箇所を、supersede 時に追従対象として明示する規約を data-pipeline / cross-agent rule に足す。
- **game 導出を `regPath` と一貫化**: `serebii-to-catalog.ts` の game 導出を `regPath` と同一ロジック（regGame ヘルパー抽出 / regPath から game を返す）に寄せ、short-form 契約を honor する。
- **`REG_SHARED_FILES` の集約 or 相互参照**: 二重定義に相互参照コメントを置くか、共有最小モジュールへ集約してドリフトを防ぐ。

## 📊 指標
| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit |
| カバレッジ | 100% | 100% | ±0 | Vitest |
| Biome 違反 | 0 | 0 | ±0 | biome check + yaml-style |
| CI | pass | pass | — | server-side pnpm verify |
| 差分 files/行 | — | 29 files +3546/-2699 | — | gh pr diff |

## 🤖 ハーネス改善提案   <!-- harness-meta が parse する正規構造 -->
- [x] `[skill]` `adr-new`（supersede モード）に「archive `git mv` 直後に対象 ADR 番号の全 inbound（リンク式 + reference 式 `[ADR NNNN]:` + 番号参照 + ライブ手順 SoT 内の所在/型記述）を `grep -rn` 走査し archive/ パス・後継 ADR へ追従する」必須ステップを追加。
- [x] `[rule]` `data-pipeline.md`（または `cross-agent.md`）に「skill references が ADR の決定（SoT 所在・型名）を再記述している箇所は、その ADR の supersede 時に追従対象とする」旨を明記し、ライブ手順 SoT の追従漏れ（本 PR の serebii-sourcing.md・blocking）を防ぐ。
- [x] `[rule]` ADR/plan の reference link（`[ADR NNNN]:`）追従漏れ（#104 / #122 で反復）を、`adr.md` または `planning.md` の「archive 移設・renumber 時の dangling-zero 走査」チェックリストとして恒久化。
- [ ] `[skill]` `survey-regulation`（serebii-to-catalog 系）の game 導出を `regPath` と一貫化する（regGame ヘルパー抽出）注記を skill / references に足し、short-form 契約乖離の再発を防ぐ。

## 📝 harness-meta フィードバック  <!-- 空でも見出し維持。meta が後追記 -->
### 採用

- `[skill]`/`[rule]` ADR supersede 時の inbound 全走査（reference 式 `[ADR NNNN]:` リンク・skill references の ADR 決定再記述含む・#104/#117/#122 反復）を `adr.md` supersede 手順 4 + planning.md dangling 走査へ恒久化。→ harness/meta-batch-114-123（本バッチ PR）
<!-- meta が採用提案を採用先 PR リンク付きで追記 -->
### 見送り

- `[skill]` survey-regulation game 導出 regGame ヘルパー注記 — 単発のため見送り。
<!-- meta が見送り理由を追記 -->
### 保留
<!-- meta が保留理由を追記 -->
