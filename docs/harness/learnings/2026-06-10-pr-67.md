---
id: learning-pr-67
title: PR #67 レトロスペクティブ (ADR の可変 plan 参照除去 + adr.md codify)
type: learning
status: draft
related_pr: 67
related_plan: 02-data-model-redesign/phase-11
generated_at: 2026-06-10T17:17:02Z
generator: pr-retrospective skill
---

<!--
この雛形は `.claude/rules/retrospective-format.md`（SoT）に準拠する。
見出し（絵文字・文言）は harness-meta が parse するため変更しないこと。
-->

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/67 / commit 8661272219eaa5e5d0e3eb7fbe2663280237fd70 / merge 2026-06-10T17:17:02Z / 差分 12 files +34/-28（レビュー fix 1 commit 含む）

## ✅ Keep（継続したいこと）

- **方針の codify と既存資産への遡及適用を同一 PR で完結**: `adr.md` にルールを書くだけでなく、過去 ADR 10 件の可変参照も同時に揃え、方針とリポジトリ実態のドリフトを残さなかった。
- **順序厳守（不変則の例外を先に明文化 → 過去 ADR 編集）**: 「Accepted ADR は本文を書き換えない」則に遡及除去の例外を先に書いてから ADR を編集し、規約違反のまま編集する事故を避けた。
- **blanket sed せず判断ベースで編集**: plan 運用が主題の ADR（0020 plans-new / 0018 implementation-workflow）の概念参照と、skill 内部の「Phase 0〜9」を保持し、陳腐化する具体 phase 番号のみ除去した。
- **Generator / Evaluator 分離が機能**: harness-review（独立 Agent）が `grep` で取りこぼし・過剰除去を独立再検証し、残存ヒットが全て許可カテゴリ（architecture.md / 0020 概念 / 0018 skill 内部）であることを確認した。
- **renumber 由来の陳腐化を連鎖させず即解消**: phase 追加（PR #66）で full-data を renumber した直後に、本 hygiene PR で ADR 0024 の旧「Phase 10」等を解消し、plan↔ADR の陳腐化連鎖を断った。

## ⚠️ Problem（詰まった点・制約）

- **plan↔ADR の結合が脆い**: phase 追加で full-data を renumber した結果、ADR 0024 等の phase 番号参照が即座に陳腐化した。可変な plan を不変ログから引く構造そのものが負債だった（本 PR の動機）。
- **既存 ADR 多数の手編集コスト**: 10 ファイルの遡及編集が必要で、Edit ツールは編集前に各ファイル Read を要するため round-trip が増えた（`sed` の一括置換では Edit 制約と判断を要する箇所に対応できない）。
- **可/不可の線引きに人間判断が要る**: `architecture.md` も `docs/plan/` 配下だが「安定 spec 正本」として許可する一方、phase doc は不可。`grep` だけでは機械的に分けられず、許可リストの維持が要る。
- **自己が書いたルール文の見落とし**: `adr.md` の「への リンク」分かち書きを自分のセルフ検証で見落とし、harness-review の nit で初めて検出した（Generator の自己レビュー限界）。

## 🚀 Try（次回の改善案）

- ADR への可変 plan 参照の再混入を防ぐ軽量ゲート（`grep` ベース・許可リスト除外）を CI/check に置けるか検討する。
- `adr-new` / `implementation-workflow` の自己検証に「ADR 新規/編集時に可変 plan 参照が無いか」観点を組み込む。
- `docs/adr/template.md` に「plan ファイル/phase 番号を参照しない（architecture.md・rules・他 ADR へ寄せる）」注記を足し、悪習の再生産を断つ。

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit |
| カバレッジ | 100% | 100% | ±0 | Vitest |
| Biome 違反 | 0 | 0 | ±0 | biome check |
| CI | pass | pass | — | server-side pnpm verify |
| 差分 files/行 | — | 12 / +34 -28 | — | gh pr diff（レビュー fix 含む） |

## 🤖 ハーネス改善提案

- [ ] `[rule]` `adr.md` の「参照のルール」に対する自動点検観点（`docs/adr/*.md` への `phase-NN` / 「Phase N」 / `OVERVIEW` 参照の混入を grep で検出し、許可リスト = architecture.md・0020/0018 概念・skill 内部 Phase を除外）を検討・明記する。
- [x] `[skill]` `adr-new` に ADR 生成後の可変 plan 参照チェック（grep + 警告）を組み込む。 → **採用** PR #112（生成後 grep 点検ステップ 7 を追加）
- [ ] `[skill]` `implementation-workflow` の Phase 4 セルフ検証に「ADR を新規/編集した場合、可変 plan 参照が無いか」点検を追加する。
- [ ] `[template]` `docs/adr/template.md` に「plan ファイル/phase 番号を参照せず architecture.md・rules・他 ADR へ寄せる」注記を追加する。
- [ ] `[remove]` なし。

## 📝 harness-meta フィードバック

<!-- 空でも見出しは維持する。harness-meta が後から各表に追記する。 -->

### 採用

### 見送り

- `[rule]` adr.md 自動点検観点（52）/ `[skill]` implementation-workflow Phase 4 ADR 可変 plan 参照点検（54）/ `[template]` template.md 注記（55）: 自動点検の実体は **adr-new ステップ 7（PR #112）** に集約した。adr.md は既に「参照のルール」を持ち、ADR 起票導線は adr-new 一本のため、同じ grep 点検を rule / implementation-workflow / template へ多重化するとドリフトの温床になる（コスト過大・現状運用で十分）。`[ ]` 据え置きで再評価余地は残す。

### 保留
