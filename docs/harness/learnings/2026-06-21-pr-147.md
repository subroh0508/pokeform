---
id: learning-pr-147
title: PR #147 レトロスペクティブ (メガストーン専有を base 種族からメガ形態種族へ反転・Phase 6 の取り違え修正)
type: learning
status: draft
related_pr: 147
related_plan: 04-generated-layout-redesign/phase-06
generated_at: 2026-06-21T05:06:24Z
generator: pr-retrospective skill
---

<!--
この雛形は `.claude/rules/retrospective-format.md`（SoT）に準拠する。
見出し（絵文字・文言）は harness-meta が parse するため変更しないこと。
書き出し前に `.claude/rules/redaction.md` の正規表現で Secrets / PII を [REDACTED-*] 置換すること。
-->

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/147 / commit 19757abdff9a2de9ffe0979b3f98e7d14ef39923 / merge 2026-06-21T05:06:24Z / 差分 9 files +28/-59。PR #145（plan 04 Phase 6）がメガストーン専有を**メガシンカ前（base）種族**へ誤適用していた回帰を、**メガシンカ後（メガ形態）種族のみ**へ反転。generate の emit を全種 `items: "any"` に戻し、専有は既存の型側 `MegaStoneOf<S>` 分岐に委ねた。復元先は plan 03 Phase 7 で確定済みの仕様（base = 全件 / メガ形態 = 対応ストーンのみ）。

## ✅ Keep（継続したいこと）

- **型式不変・emit/コメントのみで反転できた**: `HoldableItems` / `ItemNotHoldableBy` / `PerRegSpecies.items` を一切変えず、generate の emit を `"any"` に戻すだけでモデルが反転した。メガ形態の専有は Phase 6 以前から存在する `MegaStoneOf<S>` 分岐がそのまま担い、破壊的変更ゼロ。
- **正負両方 + 既存仕様との突き合わせで検証した**: メガ形態 `charizard-mega-x` + life-orb → `ItemNotHoldableBy` / + charizardite-x → 通過、base garchomp/charizard → 通過を実証。さらに `git show 51e8695^` で Phase 6 前後の型式が同一と確認し、機構ではなく emit 値の取り違えと特定。
- **全文 grep で旧モデル残滓を一掃 → doc-data 乖離を最小化**: 「対応ストーンのみ」「タプル emit」を rule / src / 生成物から grep し、rule（type-conventions / data-pipeline）と doc コメントを反転後モデルへ同期。タプル残存なし（全 `items: "any"`）も機械確認。
- **着手前に Phase 6 のフットプリントを git show で全特定した**: PR #145 の commit から変更 9 ファイル（generate / 型 / 生成物 / fixture / 個体 / rule）を洗い、漏れなく反転対象を列挙してから着手した。
- **混在 PR を code-review + harness-review で分担レビューした**: src/scripts/生成物は code-review、rule 2 ファイルは harness-review と、対象別に観点を当ててコメントを残した。

## ⚠️ Problem（詰まった点・制約）

- **同一セマンティクスが 2 連続で取り違えられた**: plan 03 Phase 7（PR #104/#117）で「base = 全件 / メガ形態 = 対応ストーンのみ」と確定・実装済みだったのに、plan 04 Phase 6（PR #145）が逆向き（base = ストーン専有）に実装し、レビュー（code-review/data/harness とも blocking 0）でも検出されなかった。確定仕様との突き合わせがレビュー観点に乗っていなかった。
- **メガ形態の専有（`MegaStoneOf<S>`）を固定する committed テスト/fixture が無い**: 制約は型レベルで line カバレッジ 100% だが、`check:individual` ゲートに乗る負例 fixture（base の garchomp-bad-move 相当）が無く、回帰を機械的に止められない。今回も一時ファイルの手動 probe で確認した。
- **完了済み plan doc が現状と乖離した**: plan 04 OVERVIEW/README/phase-06 が Phase 6 成果を現状事実として記述し、本 PR で reverted。mutable WIP だが、完了 plan の記述が誤モデルのまま残ると将来の参照源として誤誘導になりうる。
- **legality モデルの帰結が `pnpm verify` の非ゲート対象に出る**: メガ可能種の items 反転は team 個体（focus-sash 等）の合否に影響するが、これは `check:individual`/`check:party` 専用で verify では顕在化しない。影響洗い出しは grep 頼り（learning #145 でも既出の反復）。

## 🚀 Try（次回の改善案）

- **`[skill]` code-review/harness-review に「確定済み仕様（plan 03 Phase 7 等の AskUserQuestion 確定事項）との突き合わせ」観点を追加**: legality 等のユーザ確定モデルを変える PR は、過去に確定した phase doc / learning の仕様と矛盾しないか grep で照合する手順を checklist へ。2 連続取り違えの再発防止。
- **`[skill]` author-individual / survey-regulation に「メガ形態種の負例 fixture」を保守対象として明記**、または型レベル negative-test 手段（コンパイルエラーを期待する仕組み）の検討を起票。メガ形態 → ストーン専有を回帰固定する。
- **`[rule]` planning/cross-agent に「完了 plan の決定を後続 PR が supersede したら、該当 plan OVERVIEW/README に supersede 追補を入れる」運用を明記**（ADR の supersede に相当する plan 版・mutable doc の乖離防止）。
- **`[rule]` data-pipeline or testing に「legality モデル変更時は team 個体への影響を grep で洗う（verify 非ゲート）」を明記**（learning #145 と本 PR で反復した制約の明文化）。

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit |
| カバレッジ | 100% | 100% | ±0 | Vitest（161 tests） |
| Biome 違反 | 0 | 0 | ±0 | biome check |
| CI | pass | pass | — | server-side pnpm verify（22s） |
| 差分 files/行 | — | 9 files | +28/-59 | gh pr diff（net -31） |

## 🤖 ハーネス改善提案

<!-- harness-meta が parse する正規構造。未処理は [ ]、採用後に [x] + PR リンク。 -->

- [ ] `[skill]` code-review / harness-review checklist に「ユーザ確定モデル（過去 phase doc / learning の確定仕様）との突き合わせ」観点を追加し、legality 等の確定事項を逆向きに実装する PR をレビューで検出する（PR #145→#147 の 2 連続取り違え再発防止）。
- [ ] `[skill]` メガ形態種の専有（`MegaStoneOf<S>`）を回帰固定する負例 fixture / 型レベル negative-test 手段を author-individual / survey-regulation または検証ゲートへ用意する（型レベル legality がゲートに乗らない死角の解消）。
- [ ] `[rule]` planning（または cross-agent）に「完了済み plan の決定を後続 PR が覆したら plan OVERVIEW/README に supersede 追補を入れる」運用を明記し、mutable plan doc の doc-data 乖離を防ぐ。
- [ ] `[rule]` data-pipeline / testing に「legality モデル変更時は team 個体（`check:individual`/`check:party` 専用・verify 非ゲート）への影響を grep で洗う」を明記する（learning #145 と本 PR で反復）。

## 📝 harness-meta フィードバック

<!-- 空でも見出しは維持する。harness-meta が後から各表に追記する。 -->

### 採用

### 見送り

### 保留
