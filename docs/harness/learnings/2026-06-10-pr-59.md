---
id: learning-pr-59
title: PR #59 レトロスペクティブ (3 種小データセットの全 Serebii movepool + M-A 持ち物全件 投入)
type: learning
status: draft
related_pr: 59
related_plan: 02-data-model-redesign/phase-09
generated_at: 2026-06-10T06:08:00Z
generator: 手動
---

<!--
この雛形は `.claude/rules/retrospective-format.md`（SoT）に準拠する。
見出し（絵文字・文言）は harness-meta が parse するため変更しないこと。
書き出し前に `.claude/rules/redaction.md` の正規表現で Secrets / PII を [REDACTED-*] 置換すること。
-->

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/59 / commit 4659909 / merge 2026-06-10T06:00:59Z / 差分 14 files +2561/-67

## ✅ Keep（継続したいこと）

- 全量投入（Phase 10・全186種）の前に **3 種小データセットでパイプラインを本格スケール（各種族57〜75技・持ち物62件）で先に通す** de-risk が機能した。実際に既存設計の論点（後述）を低コストで炙り出せた。
- **投入前 learnset 照合（`check:regulation`）が Serebii 抽出の誤りを 2 件捕捉**（garchomp:thrash / gengar:clear-smog）。Serebii 第一優先 × PokeAPI learnset 照合の二段構えが効いた。
- Serebii Champions ページを WebFetch で再確認し、handoff データ（前スレッド調査）を上書き確認してから materialize した（単一ソース依存回避）。
- 278 行規模の YAML 変更を一時スクリプト（slug 変換・catalog 追加算出・block 生成）で決定論的に生成し、手編集ミスを避けた。投入後にスクリプトを削除してリポジトリを汚さなかった。
- 出典・差異解消（除去 2 件の根拠）を `ma-roster-source.md`「Phase 9」節に記録し、再現可能性を担保した。
- 生成物（`data/generated/**`）を手編集せず `generate:data` 出力に委ね、手動編集は catalog/per-reg/doc に限定した。

## ⚠️ Problem（詰まった点・制約）

- 一時スクリプトの `indexOf("itemMeta:")` がコメント行内の "itemMeta:" に誤マッチし、`items.yaml` を破損した。`fetch:data` の YAML パースエラーで発覚 → git restore して改行アンカー（`\nitemMeta:\n`）で再適用した。YAML をテキスト置換する際のアンカー脆さ。
- `check:regulation` は `overrides.yaml` を**適用しない**（raw PokeAPI learnset を正とする）。`survey-regulation` skill / `overrides.yaml` は「Serebii にあり PokeAPI に無い技は override で足す」と読めるが、check ゲートでは通せず**除去でしか解消できない**。skill の記述と check の実挙動に乖離がある。
- `pokemon-data-reviewer` が **メガ先の技プール過大表現**を検出（non-blocking）。`generate.ts` は mega 先のみ種族の `moves` を `catalog ∩ learnset` で導出するため、base 種族の per-reg `moves`（Serebii subset）より広くなる。`charizard-mega-x` で Phase 7 から既存の挙動だが、全量投入（59メガ）で波及する。
- 大半の diff（+2561 行）が生成物で、手動編集（catalog/per-reg）が埋もれてレビューしにくい。

## 🚀 Try（次回の改善案）

- **メガ先の技プールを base の per-reg `moves` へ揃える設計**を Phase 10 着手前に確定する（`generate.ts` の mega-only ロジック変更 or per-reg 追記）。不可逆なら `adr-new` で記録。Phase 10 doc のリスク節に申し送り済み。
- `check:regulation` と `overrides.yaml` の関係を明確化する: check が overrides を適用すべきか（Serebii ∩ PokeAPI 差異を override で吸収）、しないなら `survey-regulation` skill の「override で足す」記述を「check は raw learnset 正・差異は除去で解消」に修正する。
- データ投入 PR は **手動カタログ/per-reg 差分と生成物差分を分けて説明**する運用を `code-review` / PR テンプレで促す（レビュー容易性）。

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit |
| カバレッジ | 100% | 100% | ±0 | Vitest |
| Biome 違反 | 0 | 0 | ±0 | biome check |
| CI | pass | pass | — | server-side pnpm verify |
| 差分 files/行 | — | 14 files | +2561/-67 | gh pr diff（大半は生成物） |

## 🤖 ハーネス改善提案

<!-- harness-meta が parse する正規構造。未処理は [ ]、採用後に [x] + PR リンク。 -->

- [ ] `[adr]` メガ先の技プールを base の per-reg `moves` へ揃える設計（`generate.ts` の mega-only ロジック変更 or per-reg 追記）を Phase 10 前に確定・ADR 化する。
- [ ] `[skill]` `survey-regulation` の「Serebii ∩ PokeAPI 差異は override で足す」記述を、`check:regulation` の実挙動（overrides 非適用・raw learnset 正・差異は除去で解消）に合わせて修正する。
- [ ] `[rule]` `check:regulation` が `overrides.yaml` を適用するか否かを `data-pipeline` / `cli-and-io` に明記する（現状は raw learnset のみ・skill 記述と乖離）。
- [ ] `[template]` データ投入 PR は手動カタログ/per-reg 差分と生成物差分を分けて説明する旨を PR テンプレ（feat）に促し書きする。
- [ ] `[remove]`

## 📝 harness-meta フィードバック

<!-- 空でも見出しは維持する。harness-meta が後から各表に追記する。 -->

### 採用

### 見送り

### 保留
