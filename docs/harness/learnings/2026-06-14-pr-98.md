---
id: learning-pr-98
title: PR #98 レトロスペクティブ (Haiku 取得 SubAgent + Workflow fan-out・層2)
type: learning
status: draft
related_pr: 98
related_plan: 03-survey-regulation-rework/phase-04
generated_at: 2026-06-14T04:35:00Z
generator: pr-retrospective skill
---

<!--
この雛形は `.claude/rules/retrospective-format.md`（SoT）に準拠する。
見出し（絵文字・文言）は harness-meta が parse するため変更しないこと。
書き出し前に `.claude/rules/redaction.md` の正規表現で Secrets / PII を [REDACTED-*] 置換すること。
-->

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/98 / commit ab9c140 / merge 2026-06-14T04:31:47Z / 差分 2 files +150/-3。
> plan 03 Phase 4。層1 決定論スクレイパーの上に Claude 固有の層2（Haiku 取得 SubAgent + Workflow `parallel()` 礼儀バッチ fan-out）を新設。
> 取得 SubAgent は read-only Explore + haiku で「判断するな・スクリプトを呼べ」に縮退し HTML を読まず exit code だけで判定。

## ✅ Keep（継続したいこと）

- **Workflow スクリプトを `.workflow` 拡張子に置き biome `ignoreUnknown` で自動 skip**。biome.json / tsconfig（ゲート設定）を一切無改変のまま、標準 ES モジュールでない（注入グローバル + top-level `return`）スクリプトを機械ゲート対象外にできた。実地に `npx biome check` で "ignored" を確認してから採用した。
- **層1 契約（Haiku agent が実際に呼ぶ実体）を worktree で安価に実地実証**: exit 2（キャッシュ未取得）→ fetch → exit 0（中間 JSON 健全）→ 冪等 cache skip → 壊した種で exit 3 構造化レポート、を直接スクリプト実行で再現。multi-agent をライブで回さずとも受け入れ基準の核を証跡化できた。
- **取得 SubAgent を read-only な `agentType:"Explore"` + `model:"haiku"` に縮退**。Edit/Write 不可・Bash で層1スクリプトのみ実行、で「read-only / 判断しない」を構造的に担保。
- **取得 SubAgent の返り値を `StructuredOutput` schema で強制**し、自然言語を介さず成功/失敗を機械集約（`{ ok, failed, counts }`）。失敗種は `{slug, stage, missingFields, rawHtmlPath}` の修正 SubAgent 入力契約へそのまま流せる。
- **cross-agent パリティを symlink で自然維持**: `.agents/skills/survey-regulation` が canonical への相対 symlink のため、新設 `workflows/` も copy 同期不要で共有。Codex は層1 逐次フォールバック（正しさは層1 に宿る）を SKILL に明記。
- **独立 harness-review（Evaluator 分離）で blocking 0 を確認**してから auto-merge。依頼の 5 観点（ゲート無改変の妥当性 / パリティ / リンク / プロンプト↔exit code 契約 / ゲート再実装なし）を実体照合で pass。

## ⚠️ Problem（詰まった点・制約）

- **Workflow スクリプトは in-repo で機械検証できない**。top-level `return` + 実行時注入グローバルゆえ biome は parse エラー（実証済み）、tsc も対象化不可。リポジトリ初の `.workflow` は型/lint のセーフティネットが無く、構文ミスは実走するまで検出されない。
- **ライブ multi-agent fan-out を main worktree から実走できなかった**。Workflow サブエージェントの Bash cwd が main リポジトリで、その作業ツリーが Phase 3 マージ前（serebii スクリプト不在）だったため。全 roster ライブ fan-out は Phase 7（M-A 全投入）へ繰り延べた。
- **phase doc の `pipeline()`/`parallel()` 併記に対し実装は `parallel()` のみ採用**（礼儀バッチ = バッチ境界バリア）。doc が代替 API を列挙すると「どちらを採ったか/なぜ」が doc と実装で乖離しやすい（harness-review non-blocking 指摘）。
- **Workflow スクリプト内で JS の礼儀 sleep を書けない**（`Date`/`Math.random`/`setTimeout` 不可）。同時実行キャップ + バッチ境界 + 層1 `fetch-serebii` 内部 sleep で代替したが、明示的なバッチ間 sleep は表現できない。
- **finish-phase の README 進捗更新が feature PR に同梱されず follow-up に回った**（オーケストレーター主導マージで feature PR マージ後に進捗更新が確定）。learning ブランチに README 進捗 + learning を相乗りさせて吸収した。

## 🚀 Try（次回の改善案）

- **`.workflow` 拡張子 + `ignoreUnknown` 機械ゲート除外の規約を rule/skill に SoT 化する**。Phase 5 で 2 本目の `.workflow`（修正 SubAgent）が来るため、配置（`.claude/skills/<name>/workflows/`）・拡張子・除外理由・cross-agent 位置づけを明文化し、PR body だけでなく恒久 doc に残す。
- **Workflow スクリプトの軽量 in-repo スモーク検証手段を検討する**。例: `.workflow` を async 関数本体としてラップして `node --check` 相当の構文検証を回す小スクリプト（top-level return を許容）。機械ゲートで拾えない構文ミスの最低限のネットになる。
- **phase doc が代替 API を列挙する場合、実装側はどれを採ったか + 理由を注記する**運用を code-review/harness-review の観点に加える（本 PR はコード comment で対応済みだが、doc 追従の取りこぼし防止）。

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit（`.workflow` は対象外・src/** のみ） |
| カバレッジ | 100% | 100% | ±0 | Vitest（427/427 stmts） |
| Biome 違反 | 0 | 0 | ±0 | biome check（83 files・`.workflow` は ignoreUnknown skip） |
| CI | pass | pass | — | server-side pnpm verify（23s） |
| 差分 files/行 | — | 2 files +150/-3 | — | gh pr diff |

## 🤖 ハーネス改善提案

<!-- harness-meta が parse する正規構造。未処理は [ ]、採用後に [x] + PR リンク。 -->

- [ ] `[skill]` Workflow スクリプトの配置規約（`.claude/skills/<name>/workflows/*.workflow`・biome `ignoreUnknown` でゲート除外・cross-agent は Claude 固有/層1 フォールバック）を skill-authoring または survey-regulation references に SoT 化する。
- [ ] `[skill]` `.workflow` の軽量スモーク構文検証（async ラップで `node --check` 相当）手段を整備し、Workflow スクリプトの最低限の機械ネットを用意する。
- [ ] `[rule]` phase doc が代替 API（例 `pipeline()`/`parallel()`）を列挙するとき、実装が採用した方と理由を注記する観点を code-review に加える（doc/impl 乖離防止）。
- [ ] `[template]` —
- [ ] `[remove]` —

## 📝 harness-meta フィードバック

<!-- 空でも見出しは維持する。harness-meta が後から各表に追記する。 -->

### 採用

### 見送り

### 保留
