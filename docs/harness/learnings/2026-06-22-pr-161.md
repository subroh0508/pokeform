---
id: learning-pr-161
title: PR #161 レトロスペクティブ (重量級 2 rule の圧縮 — 不変条件集約 + reference 分離 + de-dup)
type: learning
status: actioned
related_pr: 161
related_plan: 07-rules-skills-simplify/phase-00
generated_at: 2026-06-22T16:36:14Z
generator: pr-retrospective skill
---

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/161 / commit 0cafa0d6eed758b7b404a3e3955effd3627e9033 / merge 2026-06-22T16:36:14Z / 差分 5 files +117/-122
> plan07 Phase 0。最削減余地の 2 rule（implementation-workflow / data-pipeline）を「意味・trigger 精度・安全性記述・自己完結性」を保ったまま圧縮。不変条件を末尾へ一本化・worker 分業ノートを reference へ分離・三重記述を de-dup した。

## ✅ Keep（継続したいこと）

- **不変条件の「唯一の正本」化**: implementation-workflow の各 Phase に散在していた不変条件（fix loop 上限3・Generator/Evaluator 独立・gh CLI/body-file・worktree ペア規律・auto-merge 委譲）を末尾「不変条件（まとめ）」へ集約し「ここが唯一の正本・各フェーズは再説明しない」と明記。各 Phase は参照に留め、二重記述の温床を構造的に断った。-38 行の主因。
- **progressive disclosure による reference 分離**: 「ワーカー分業時の運用ノート」+「取り込まないもの」を `references/worker-orchestration-notes.md`（新規・実体 3106B）へ逃がし、rule 2 箇所と SKILL.md「関連」から markdown リンク。rule 本文には「機構でなく委譲の作法」「端末多重化ツール非依存」の核だけ残し、詳細のみ外出し。dangling ゼロを機械確認した姿勢が良い。
- **安全性記述を漏らさず保持**: 圧縮対象でも `materialize` fail-fast・append/既存尊重・`generate` raw 非依存・`check:yaml-style` flow 禁止ゲート・人間直編集 NG・`check:regulation` 参照整合・worker/orchestrator マージ同期点（最終 HEAD SHA 照合）等の安全性・決定論性記述を全て温存。harness-review が grep で各 term 存在を確認した。
- **frontmatter 不変で trigger を守った**: 両 rule の `paths` / `description` を一切触らず、`gen-rules-index.ts` 生成物 `rules-index.md` も差分なし。圧縮が自動ロード trigger・cross-agent 索引に副作用を出さないよう関心分離した（last_modified / adr キーは後続 Phase 08 の責務と明示）。
- **de-dup を「内容ロスなし」と機械的に裏取り**: data-pipeline の line16 から削った `move-specs` 技メタ / `type-specs` `damageTo` schema が line49（「ディレクトリの扱い」節）に現存することを確認し、新設クロスリンクでそこを指した。純粋な重複削減であることをレビューで再検証できた。
- **cross-agent パリティの実地確認**: 新 reference が `.agents/skills/implementation-workflow/references/` から symlink 経由で同一 inode に到達することを確認。Codex 側の解決性を画一的に点検する運用が定着している。

## ⚠️ Problem（詰まった点・制約）

- **「行数削減」と「読みやすく分割」が構造的に相反**: data-pipeline.md は 1 行=巨大段落の構造で、段落を箇条書きへ割ると行数は増える。結果この rule は 93→96 行（+3 行）/ -1.2KB と、行数では逆行しバイトで削減という妥協になった。phase doc の受け入れ基準（行数 vs 冗長量）が一義でなく、PR body に長い注記で釈明する必要が生じた。
- **「圧縮」の測定指標が曖昧**: 圧縮量を「行」「バイト」のどちらで測るか rule/phase doc に明文の基準がなく、implementation-workflow は -38 行で語り、data-pipeline は -1.2KB で語る、と PR 内で指標が混在した。Phase 1 以降の残り rule 圧縮でも同じ釈明が反復する懸念。
- **「脚注」という語の不正確さ（nit）**: data-pipeline.md:52 の「取得経路は下表・脚注」の「脚注」は末尾の箇条書き「補足」ブロックを指すが、リテラルな markdown footnote（`[^1]`）ではない。意味は通るが用語が実体と不一致。harness-review が nit 指摘。
- **ADR/learning 番号の素参照が散在**: 圧縮後も本文に `learning #94 / #96 / #82 / #102 / #114 / #116`・`ADR 0034 = ADR 0026 改訂` 等の素番号参照が多数残り、archive 化や renumber で dangling 化するリスクが構造的に残る（今回は live dangling なしを確認できたが、圧縮では参照健全性を毎回手で grep する負担が発生）。
- **doc-data 同期点検が Phase 4 セルフ検証に依存**: 「rule が architecture.md を正本宣言しつつ仕様追記する場合の同一 PR 同期」点検は Phase 4 のチェックリスト項目に留まり、機械ゲートがない。今回は該当しなかったが、圧縮で SoT 表現を動かす Phase では人手点検漏れの死角が残る。

## 🚀 Try（次回の改善案）

- **`compression.md`（仮）rule か phase doc テンプレに「圧縮の測定単位」を明文化**: 「行数でなくバイト（冗長量）を一次指標とし、可読性のための箇条書き化で行が増えてもよい」と判断線を SoT 化。plan07 の残り Phase が同じ釈明を PR body に毎回書かずに済む。
- **reference dangling 検出を機械ゲート化**: PR で「Python で 25 リンク実在確認」を手動実行している。`.md` 内の相対リンク・reference リンクの実在チェックを `scripts/` へ起こし `.githooks` か `check:*` に組み込めば、圧縮 PR ごとの手検証を畳める（learning #104/#117/#122 の dangling 反復の恒久対策にも接続）。
- **data-pipeline.md:52 の「脚注」を「末尾補足」へ表現修正**: nit 指摘の曖昧語を実体（箇条書き補足ブロック）に揃える。Phase 1 の data 系 rule 仕上げに同梱できる軽微修正。
- **「不変条件まとめへ集約」パターンを残り rule の標準形に昇格**: implementation-workflow で効いた「散在不変条件 → 末尾 1 箇所 +『唯一の正本』明記 + 各節は参照」を skill-authoring / 圧縮手順の推奨パターンとして明記し、Phase 1 以降へ横展開する。

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit |
| カバレッジ | 100% | 100% | ±0 | Vitest 185 passed |
| Biome 違反 | 0 | 0 | ±0 | biome check + yaml-style |
| CI | pass | pass | — | server-side pnpm verify |
| 差分 files/行 | — | 5 files | +117/-122 | gh pr diff |
| implementation-workflow.md | 225 行 | 187 行 | -38 行 / -3.8KB | 不変条件集約 + reference 分離 |
| data-pipeline.md | 93 行 | 96 行 | +3 行 / -1.2KB | de-dup・行は増/バイト減 |

## 🤖 ハーネス改善提案

- [x] `[rule]` 圧縮の測定単位（行 vs バイト=冗長量を一次指標）と「可読性のため行が増えてよい」判断線を圧縮手順 SoT（skill-authoring もしくは phase doc テンプレ）に明文化し、PR body での都度釈明を不要にする
- [x] `[skill]` 「散在不変条件 → 末尾『唯一の正本』へ集約 + 各節は参照」パターンを skill-authoring の推奨形として記載し、plan07 残り rule 圧縮へ横展開する
- [ ] `[rule]` data-pipeline.md:52 の「脚注」を実体（末尾の箇条書き補足）に合わせ「下表・末尾補足」へ表現修正する（harness-review nit）
- [ ] `[skill]` `.md` 相対リンク / reference リンクの実在（dangling ゼロ）チェックを `scripts/` 化して `.githooks` か `check:*` ゲートへ組み込み、圧縮 PR ごとの手動 Python 確認を機械化する

## 📝 harness-meta フィードバック

### 採用
- `[rule]` 圧縮の測定単位 / 可読性判断線 → `skill-authoring.md`「圧縮（文体シンプル化）PR の規約」節に集約（PR #168）。
- `[skill]` 散在不変条件 → 末尾集約パターン → 同節に推奨形として記載（PR #168）。

### 見送り
- `[rule]` data-pipeline.md:52「脚注」表現修正 → 単発 nit。本書き戻しの範囲外（必要なら軽微修正 PR で）。
- `[skill]` dangling リンク実在チェックの scripts化/ゲート組込 → link checker + ゲート新設はコスト過大。圧縮 PR の点検は harness-review の「リンク非劣化」観点（PR #168）で当面カバーし、ツール化は別 tooling phase に保留。

### 保留
