---
id: learning-pr-166
title: PR #166 レトロスペクティブ (plan07 phase-05 データ系 skills 圧縮)
type: learning
status: draft
related_pr: 166
related_plan: 07-rules-skills-simplify/phase-05
generated_at: 2026-06-22T17:21:02Z
generator: pr-retrospective skill
---

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/166 / commit 62a058a5c92ac27b3a4356e4b1a6a345d0b07531 / merge 2026-06-22T17:21:02Z / 差分 7 files +67/-84

## ✅ Keep（継続したいこと）
- **検証委譲の記述を一切削らず圧縮**。`check:individual` / `check:party` / `analyze:coverage` / domain 純関数 / `generate:data` / `verify` への委譲はすべて本文に残し、機械ゲート再実装を招かないまま冗長だけを削った（[[skill-authoring]] の「機械ゲートを再実装しない」を守りつつ縮める良いパターン）。
- **安全性記述（不可逆・制約）を保護対象として明示**。合計66・各≤32、dep-update のマージ不可逆性、update-catalog の append / 既存尊重・特性 id 集約漏れ=生成エラー、stat-tuning の実現不能=`null` 報告を「やりすぎ防止」節で列挙し、harness-review が 1 つずつ行番号で残存確認した。圧縮 PR で消えやすい安全弁を保てた。
- **trigger 句を 1 つも削らず under-trigger を回避**。description の「…」トリガ語句・SKIP 条件・sibling skill 境界（stat-tuning→author-individual/review-party、update-catalog→survey-regulation 等）を全保持し、削ったのは再記述（メソッド列挙・「現状 vs 目標」等）のみに限定した。
- **dep-update の判定 3 例を `references/examples.md` へ逐語移設**。SKILL 本体を 167→134 行へ薄くしつつ、移設先冒頭に判断基準への要約注記を付けて自己完結化。harness-review が `diff` で content loss ゼロ・canonical/symlink 双方からリンク解決を確認した。
- **PR 本文に before→after 行数・desc 文字数・主因（P1/P2/P4）を表で開示**。圧縮の根拠が定量で追え、レビューが「どこを削ったか」を即座に照合できた。
- **cross-agent パリティをチェックリスト化して確認**。5 skill すべて `.agents/skills/<name>` 相対 symlink が健在で、`references/` も symlink 経由で辿れることを明示確認した。

## ⚠️ Problem（詰まった点・制約）
- **description から「技 / 特性の名前補完」capability が脱落**（update-catalog:4）。本文（日本語名 ja / ja・特性 en 補完源 / `languages/abilities.yaml`）には残り trigger 語句も保持されたため実害なしと判定されたが、description は capability の検索面でもあり、本文にしか無い能力は発火されにくいリスクが残る。圧縮で「再記述」と「唯一記述」の線引きが description 圏では曖昧になる。
- **review-party は 50→50 行で実質圧縮できず**（「余地小」と PR 自記）。一律の行数目安を全 skill に当てると、既に締まった skill では変更が intro の言い換え（コマンド列挙→ポインタ）に留まり、diff の費用対効果が薄い。圧縮対象の事前選別基準が無い。
- **計画 07 全 phase 完了に伴うロールアップ追従が PR スコープ外で宙吊り**（README:30 nit）。phase README は `[x]` 化したが、上位 `docs/plan/README.md` のロールアップ表（🚧→完了）の更新が別 PR 任せになり、追従漏れの余地が残った。
- **「どこまで削ってよいか」の判断が author / reviewer の暗黙知に依存**。やりすぎ防止リスト（検証委譲・安全性・trigger 保持）は PR 本文に都度書かれたが、圧縮 skill 全般の保護対象を定義した rule/checklist が無く、毎回手で再列挙している。
- **content loss ゼロの確認が reviewer 側の手作業 `diff`**。references 移設の逐語一致・symlink 解決を harness-review が手動で照合した。圧縮 PR が増えると移設の逐語性チェックが定型作業として反復する。

## 🚀 Try（次回の改善案）
- skill 圧縮 PR 向けに「**圧縮時の保護対象チェックリスト**」を harness-review checklist へ追加する（検証委譲記述 / 安全性・不可逆制約 / description trigger 句・SKIP・sibling 境界 / references 逐語一致 / cross-agent パリティ）。PR 本文での都度再列挙を rule 化して暗黙知を成文化する。→ `[skill]`
- description を圧縮する際の「**capability を本文へ落とす前に trigger 語句が残るか**」を確認する一文を skill-authoring rule に追記する。update-catalog の名前補完脱落のような検索面の欠落を防ぐ。→ `[rule]`
- 圧縮対象 skill の**事前選別基準**（行数 / 再記述比率の閾値、既に締まった skill は除外）を planning/OVERVIEW 段で持たせ、review-party のような「余地小」を変更対象から外せるようにする。→ `[skill]`
- plan の全 phase 完了時に**上位 `docs/plan/README.md` ロールアップ表の追従**を finish-phase のチェックに含める。→ `[skill]`

## 📊 指標
| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit |
| カバレッジ | 100% | 100% | ±0 | Vitest |
| Biome 違反 | 0 | 0 | ±0 | biome check |
| CI | pass | pass | — | server-side pnpm verify |
| 差分 files/行 | — | 7 files | +67/-84 | gh pr diff |

## 🤖 ハーネス改善提案
- [ ] `[skill]` harness-review checklist に「skill 圧縮 PR の保護対象」観点（検証委譲記述 / 安全性・不可逆制約 / description trigger 句・SKIP・sibling 境界 / references 逐語一致 / cross-agent パリティ）を追加し、PR 本文での都度再列挙を不要にする。
- [ ] `[rule]` skill-authoring に「description の capability を本文へ移す際、対応 trigger 語句が description に残るか確認する」一文を追記し、検索面での capability 脱落（update-catalog の名前補完）を防ぐ。
- [ ] `[skill]` 圧縮対象 skill の事前選別基準（行数 / 再記述比率の閾値・既に締まった skill は除外）を planning/OVERVIEW に持たせ、「余地小」skill を変更対象から外す。
- [ ] `[skill]` finish-phase のチェックに「plan 全 phase 完了時の上位 `docs/plan/README.md` ロールアップ表追従」を加え、phase README の `[x]` 化とロールアップ更新の乖離を防ぐ。

## 📝 harness-meta フィードバック
### 採用
### 見送り
### 保留
