---
id: learning-pr-197
title: PR #197 レトロスペクティブ (verify-showdown-pr 新設 + survey-regulation 廃止 + 取得元記述を showdown 序列へ整合)
type: learning
status: draft
related_pr: 197
related_plan: 10-showdown-first-data / Phase 5
generated_at: 2026-06-29T00:35:00Z
generator: pr-retrospective skill
---

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/197 / merge 2026-06-29T00:29:15Z / 差分 17 files +267/-1032。showdown 第一の正への取得元転換に伴い `verify-showdown-pr` skill を新設し `survey-regulation` skill を廃止、`.claude/rules/*` 5 ファイル + design / AGENTS / data README を権威序列（showdown=正 > Serebii=速報 > PokeAPI=ja）へ最終整合。harness-review は blocking 0（non-blocking 1 を即反映）。

## ✅ Keep（継続したいこと）

- **削除対象の inbound 参照を全形態で先行 grep**: skill 廃止前に `git grep -l "survey-regulation" / "serebii-sourcing" / 旧 script 名` を打ち、active rule/skill/AGENTS/design/data と completed/履歴の切り分けを最初に作ったことで追従漏れを防げた。
- **dangling を「リンク」と「prose 歴史記述」で峻別**: 削除 skill 名の残存を、(a) クリック可能リンク（要追従）と (b) Accepted ADR / 意図的 rule note の inline code（凍結可）に分けて判定。`grep -oE "\]\([^)]*survey-regulation[^)]*\)"` でリンクのみ抽出して dangling ゼロを機械確認した。
- **新 skill のリンク実在を機械検証**: wikilink（rule slug）/ ADR パス / 相対リンクを `test -f` で実解決してから commit。`../../../docs/adr/...` の深度ミスを未然に検出。
- **cross-agent パリティを集合 diff で確認**: `diff <(ls .claude/skills) <(ls .agents/skills)` で canonical ≡ symlink を一発確認。skill 削除 + 新設を同一 PR で一対追従。
- **照合 skill を読み取り専用に固定**: 検証 skill が作業ツリーを汚さないよう、書き込み系 `pnpm serebii:*`（sync）ではなく JSON を stdout する `node scripts/scrape-serebii.ts` を流用と明記。WebFetch を排し決定論スクレイパー + exit code で照合。
- **レビュー指摘を即時反映**: harness-review の non-blocking（allowed-tools が `gh pr merge` まで許可し「merge しない」不変条件と乖離）を読み取り専用へ絞って即修正。

## ⚠️ Problem（詰まった点・制約）

- **Accepted ADR 本文の歴史記述 vs 受け入れ基準の緊張**: phase doc の受け入れ基準は「`git grep survey-regulation` が learnings / archive ADR 以外に残らない」だが、active な ADR 0030（Accepted）が決定当時の著述主体として `survey-regulation` を inline code で参照する。adr.md「Accepted ADR は本文を書き換えない」と衝突するため、「active Accepted ADR の inline code 歴史記述も凍結対象」と解釈して残した（判断を PR 本文 + 本 learning に明記）。基準の文言が archive ADR のみを例示し active Accepted ADR を含意していない。
- **phase doc の指示と実装の軽微な乖離**: 「skill-authoring.md の survey-regulation workflow 例を verify-showdown-pr へ」とあるが、verify-showdown-pr は `.workflow` を持たない（照合は逐次スクリプト）。実態は「Workflow を持つ skill が現時点で 0」になったため、例の差し替えでなく「撤去済み survey-regulation 由来の一般規約として残す」注記へ変更した。
- **plan §4 の `pnpm scrape:serebii` 表記が実装と不一致**: 計画書は照合に `pnpm scrape:serebii` を流用と書くが、§2.4 でその alias は廃止され実体は `node scripts/scrape-serebii.ts` 直叩き。skill は実体側に合わせた。

## 🚀 Try（次回の改善案）

- 受け入れ基準の「歴史記述」定義に **active Accepted ADR の inline code 参照**を明示的に含める（archive ADR / learnings だけでなく）。retrospective-format / planning の dangling 判定ガイドにも反映。
- skill 廃止 PR の標準チェックに「リンク（要追従）と prose 歴史記述（凍結可）を `grep -oE` で峻別」を明文化する。
- 計画書（plan メモ）と package.json scripts の命名乖離を phase 着手時に `git grep` で突合する手順を start-phase / implementation-workflow に一言加える。

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit |
| カバレッジ | 100% | 100% | ±0 | Vitest（182 tests・src 変更なし） |
| Biome 違反 | 0 | 0 | ±0 | biome check（125 files） |
| CI | pass | pass | — | server-side pnpm verify |
| 差分 files/行 | — | 17 files +267/-1032 | — | gh pr diff（survey-regulation 全削除で純減） |

## 🤖 ハーネス改善提案

- [ ] `[rule]` `.claude/rules/adr.md` または受け入れ基準の運用に「active Accepted ADR の inline code 参照は歴史記述として dangling 判定の対象外（本文不変ゆえ）」を明文化する（#197 で初出・基準文言が archive のみ例示）。
- [ ] `[skill]` skill 廃止系 skill / harness-review checklist に「削除 skill 名の残存を `grep -oE "\]\([^)]*<name>[^)]*\)"` でクリックリンクのみ抽出し dangling ゼロ確認、prose 歴史記述は凍結」の手順を追記。
- [ ] `[rule]` planning / implementation-workflow に「計画メモの script 名 / alias と package.json scripts の乖離を着手時に突合」を一言追加（計画書の `pnpm scrape:serebii` 不一致が実例）。
- [ ] `[template]` —
- [ ] `[adr]` —
- [ ] `[remove]` —

## 📝 harness-meta フィードバック

### 採用

### 見送り

### 保留
