---
id: learning-pr-221
title: PR #221 レトロスペクティブ (復元機構堅牢化 P1-3 + languages from-scratch 復元)
type: learning
status: draft
related_pr: 221
related_plan: 11-static-restore-hardening/phase-01
generated_at: 2026-07-05T08:29:04Z
generator: pr-retrospective skill
---

<!--
この雛形は `.claude/rules/retrospective-format.md`（SoT）に準拠する。
見出し（絵文字・文言）は harness-meta が parse するため変更しないこと。
書き出し前に `.claude/rules/redaction.md` の正規表現で Secrets / PII を [REDACTED-*] 置換すること。
-->

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/221 / commit 250024ddf5cd / merge 2026-07-05T08:29:04Z / 差分 19 files +26880/-21。author-static-data 動作確認で判明した from-scratch 復元の 3 前提を機構修正: P1(workflow 順序で generate 先行)/P2(materialize を欠損ファイル・null map 耐性化・純関数 getOrCreateBlockMap 追加)/P3(SPECIES_FORMS whitelist で固有フォーム rotom-wash を pokemon-form から取得)。fixed flow で languages 全件復元し main を緑復帰。

## ✅ Keep（継続したいこと）

- **動作確認を「機構の恒久修正」へ昇華した**: 撤去→復元の動作確認で seed/scaffold の workaround（PR #219）を一旦作ったが、それを close し 3 前提を機構側で根本修正した。symptom を回避せず原因を潰す判断が resilient な結果を生んだ。
- **純関数 / IO の分離を testing rule 通りに保った**: 判断ロジック（block map の get-or-create）を `src/codegen/materialize.ts`（カバレッジ100%・4 ケース網羅）へ置き、ファイル存在チェック等の IO は `scripts/materialize.ts`（除外）に留めた。「除外パスに判断ロジックを置かない」（#26/#73/#191）を遵守。
- **P3 を疎結合な合成で解いた**: `pokemon-form` の `form_names` を `{ names: form_names }` へ写像して species raw に書くことで、`materialize` の species 経路（`extractNames`）を無改修で通した。取得層の追加だけで転記層を触らない設計。
- **過去 learning が次 PR に効いた好例**: PR #208 の learning が課題に挙げた「pokeform 固有フォーム rotom-wash は PokeAPI 全件辞書の対象外・注記不足」を、本 PR の SPECIES_FORMS で恒久解消した。KPT ループが機能した実例。
- **acceptance を実測で固定した**: 「`data/languages/*` 全削除 → fetch→sync→generate→verify が scaffold/seed 無しで緑」をローカル + CI（Phase 2 の E2E）で実行し、3 前提の解消を口頭でなくゲート通過で確認した。
- **独立レビュー（code-review）blocking 0**、nit（form_names 空時の空キャッシュ固定化）を即修正して retry 可能性を担保した。

## ⚠️ Problem（詰まった点・制約）

- **from-scratch 復元は 3 前提を同時に満たさないと 1 つも動かず、動作確認で初めて全貌が露見した**: 「空 block map が YAML 構文上不可 / check:yaml-style の生成 ts 依存 / 固有フォーム欠落」が別レイヤに散在し、設計時（plan 10 Phase 6-7）には予見されなかった。scaffold 手順（skill step 4）は 3 前提のいずれも支えられていなかった。
- **P1 は workflow 順序での回避に留めた**: `check:yaml-style` が `src/cli/index.ts` の eager import 経由で `src/generated/languages/*.ts` に依存する根本結合は残置（fresh checkout で generate 前に CLI コマンドを叩けば同様に落ちる）。latent footgun を将来送りにした。
- **`src/codegen/materialize.ts` のヘッダが追加 helper を説明していない**: 冒頭 doc は「名前抽出・転記計画の純変換」専任のままで、追加した YAML block-map helper（`getOrCreateBlockMap`）に触れない。coverage 除外ファイルのヘッダ陳腐化テーマ（#191→#206→#214→#216）に接する（本ファイルは #214 で非除外化されテスト有だが、ヘッダの網羅性は別問題）。
- **broken main が後続を全赤にした**: 撤去 PR #218 マージで main が verify 赤になり、本 PR が緑化するまで plan doc・後続 phase の PR がすべて赤継承。PR 分割・マージ順の制約（roadmap 先行 vs Phase 1 先行）をユーザー判断に委ねる必要が生じた。

## 🚀 Try（次回の改善案）

- data-pipeline rule に from-scratch 復元の 3 前提（scaffold 不要 / generate 先行 / 固有フォーム whitelist）を明文化する（本 PR で実施済み・追認）。
- author-static-data skill の scaffold step を「materialize 耐性化で不要」へ改訂する（本 PR で実施済み）。
- 基盤 phase の受け入れ基準に「from-scratch（完全削除）からの end-to-end 実行」観点を含める（#208 の「end-to-end 未検証で bug 潜伏」と同根の予防）。
- `check:yaml-style` の generated 依存 decouple（CLI lazy import）を将来 ADR 候補として残す。

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | fail(broken main) | pass | ↑ | tsc --noEmit（撤去状態は赤） |
| カバレッジ | 100% | 100% | ±0 | Vitest（getOrCreateBlockMap 4 ケース網羅） |
| Biome 違反 | 0 | 0 | ±0 | biome check（import 順 auto-fix 済） |
| CI | fail(broken main) | pass | ↑ | server-side pnpm verify |
| 差分 files/行 | — | 19 files +26880/-21 | — | 生成 ts + languages 復元含む |

## 🤖 ハーネス改善提案

<!-- harness-meta が parse する正規構造。未処理は [ ]、採用後に [x] + PR リンク。 -->

- [x] `[rule]` data-pipeline に from-scratch 復元の 3 前提（scaffold 不要 / generate 先行 / SPECIES_FORMS 固有フォーム）を明文化（本 PR #221 で実施）。
- [x] `[skill]` author-static-data の scaffold step 4 を「materialize 耐性化で不要」へ改訂 + Gotchas に P1/P3 追記（本 PR #221 で実施）。
- [ ] `[rule]` planning / testing: 基盤 phase の受け入れ基準に「from-scratch（完全削除）end-to-end 実行」観点を追加（#208 と反復する「設置≠稼働」の予防）。
- [ ] `[adr]` `check:yaml-style`（および CLI コマンド）の `src/generated` eager import 依存を lazy import で decouple する決定を ADR 化候補として検討（P1 の根本対応）。
- [ ] `[skill]` code-review / harness-review checklist に「coverage 非除外でも、追加 export がファイル冒頭 doc の責務記述に反映されているか」を追加（ヘッダ陳腐化テーマの拡張）。

## 📝 harness-meta フィードバック

<!-- 空でも見出しは維持する。harness-meta が後から各表に追記する。 -->

### 採用

<!-- 採用した提案と採用先 PR リンク -->

### 見送り

<!-- 見送り理由（harness-meta-criteria.md の見送り基準に対応） -->

### 保留

<!-- 保留理由 -->
