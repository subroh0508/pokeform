---
id: learning-pr-126
title: PR #126 レトロスペクティブ (biome.json を 2.5.0 設定スキーマへ追従)
type: learning
status: draft
related_pr: 126
related_plan: —
generated_at: 2026-06-20T14:20:28Z
generator: pr-retrospective skill
---

<!--
この learning は `.claude/rules/retrospective-format.md`（SoT）に準拠する。
見出し（絵文字・文言）は harness-meta が parse するため変更しないこと。
-->

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/126 / commit 5fa1f394d405a50c3e595175a6bcfe63306b6090 / merge 2026-06-20T14:20:28Z / 差分 1 file +2/-2。dep-update PR #89（biome 2.4.16→2.5.0）の **biome.json 追従漏れ**是正。`biome migrate --write` で `$schema` を 2.5.0 へ、非推奨化した `linter.rules.recommended: true` を後継 `preset: "recommended"` へ移行。biome check の info を 2→0。

## ✅ Keep（継続したいこと）

- biome 2.5.0 が出していた info 2 件（`$schema` URL ドリフト + `recommended` の deprecation）を見逃さず追従した。verify 緑のまま放置されがちな info を拾えた。
- 手動編集でなく公式の `biome migrate --write` を使い、設定の**意味保存**（recommended → preset:"recommended"）を担保した。
- dep-update（#89）マージ時に気づいた追従漏れを、無関係な進行中 PR（#14 Node bump）へ混ぜず**独立 PR として focused に切り出した**。
- 「$schema のみ」か「deprecation も含めるか」をユーザーにスコープ確認してから実行し、勝手にスコープを広げなかった。
- verify 緑・info 2→0・mergeable CLEAN を確認してからマージした。

## ⚠️ Problem（詰まった点・制約）

- **根本原因は #89（dep-update）側**: biome 本体を 2.5.0 へ上げた際に、`biome.json` 内の `$schema` バージョン参照と非推奨設定の追従が漏れた。dep-update skill が「ツール本体更新時の設定ファイル追従」を点検手順に持っていない。
- info レベルの警告は `pnpm verify` を**緑のまま通す**ため CI では検出されず、人手の気づき（dep-update 後の verify ログ確認）に依存した。検出が属人的。
- 1 行 chore のために独立 PR + CI 1 サイクルのオーバーヘッドが発生した（#89 と同 PR で追従できていれば不要だった）。

## 🚀 Try（次回の改善案）

- **dep-update に設定ファイル追従の点検を追加**: linter / formatter / 型チェッカ等ツール本体の major/minor 更新時は、設定ファイル（`biome.json` の `$schema`、deprecated になった設定キー・API）を grep / `<tool> migrate` で点検し、**同 PR 内で追従**する手順を dep-update skill に明記する。今回の追従漏れの再発防止に直結。
- **info レベル警告の扱いを decide**: biome の deprecation 等 info は verify を落とさないため放置されやすい。CI / verify で deprecation を検出・可視化するか（例: `biome check` の info を集計してコメント）を運用方針として決める。
- **dep-update 後の verify ログを必ず確認**: dep-update skill の検証ステップに「verify 緑でも info / warning 行を確認し、ツール更新に伴う追従漏れ（schema / deprecation）が無いか点検する」を明記する。

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit |
| カバレッジ | 100% | 100% | ±0 | Vitest 161 passed |
| Biome 違反 | 0 | 0 | ±0 | biome check（info は 2→0 に解消） |
| CI | pass | pass | — | server-side pnpm verify |
| 差分 files/行 | — | 1 file | +2/-2 | gh pr diff（biome.json のみ） |

## 🤖 ハーネス改善提案

<!-- harness-meta が parse する正規構造。未処理は [ ]、採用後に [x] + PR リンク。 -->

- [ ] `[skill]` dep-update に「linter/formatter/型チェッカ等ツール本体の更新時は、設定ファイル（`biome.json` の `$schema`・deprecated 設定キー/API）を grep または `<tool> migrate` で点検し同 PR で追従する」チェックを追加。
- [ ] `[skill]` dep-update の検証ステップに「verify 緑でも info/warning 行を確認し、ツール更新に伴う追従漏れ（schema/deprecation）が無いか点検する」を明記。
- [ ] `[rule]` biome / verify 運用方針として「deprecation 等の info をどこで検出・可視化するか（CI で落とさず放置しない仕組み）」を decide する。

## 📝 harness-meta フィードバック
### 採用
### 見送り
- harness-meta（plan04 batch・PR #152）で評価。3 提案（`[skill]` dep-update のツール本体更新時に `biome.json` $schema/deprecated 設定を grep/migrate 追従 / verify 緑でも info/warning 確認 / `[rule]` biome deprecation の検出・可視化）はいずれも #126 単発で、dep-update skill の改修を要する。2 件目のツール更新追従 learning が出た時点で再評価（`[ ]` 据え置き）。
### 保留
