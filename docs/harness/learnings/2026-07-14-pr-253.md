---
id: learning-pr-253
title: PR #253 レトロスペクティブ (装飾フォルム × メガの姿別 slug を単一 canonical id へ畳む)
type: learning
status: draft
related_pr: 253
related_plan: —
generated_at: 2026-07-14T23:40:14Z
generator: pr-retrospective skill
---

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/253 / commit afc2bade45ca441fdaab2e9d8d13a5e1cf72c608 / merge 2026-07-14T23:38:31Z / 差分 8 files +265/-50

## ✅ Keep（継続したいこと）

- **着手前の実データ突き合わせでユーザー認識のズレを検知**: 「メガニャオ/マギアナ/シャリタツは技・特性・タイプが一致するのに畳まれない」という指示に対し、着手前に mega-specs.yaml / per-reg species-moves を直接 grep したことで、(1) meowstic は技が♀♂で実際に異なる（ADR 0046 で per-gender 保持が正）、(2) magearna/tatsugiri は構造データが存在せず名前辞書スカフォールドのみ、という**指示と実態の食い違い**を計画前に発見できた。誤った前提のまま実装せず AskUserQuestion で方針をすり合わせられた。
- **plans-new → implementation-workflow の入口経由を厳守**: 指示を OVERVIEW 化 → 6 基準で 1 PR 妥当と判定 → issue #252 起票 → workflow 駆動、の定型に乗せたことで着手粒度がぶれなかった。
- **既存パターンへの相似実装**: mega の畳み込みを species 側の `FORM_EXCLUDE` / `MANUAL_NAME_OVERRIDE` と同型の curated マップ + 純関数（materialize.ts）へ寄せ、`canonicalFormId`（species）と設計思想を揃えた。レビューも「species 側の mega 版」として一貫性を評価。
- **実パイプラインでの冪等性検証**: 手編集で済ませず、main の `data/raw/pokemon-form` をコピーして実際に `sync:ja-names` を回し、collapse + prune の動作と「再実行で差分ゼロ」を機械的に確認した（純関数テストだけに頼らない裏取り）。
- **純関数 / IO の層分離維持**: 判断ロジック（canonicalMegaId / resolveMegaEntry / megaIdsToPrune）を coverage 100% の materialize.ts に置き、YAML ノード操作だけを coverage 対象外の scripts/materialize.ts に残した。既存の pruneItemsToUnion と対称。
- **剪定の名前消失ガード**: `megaIdsToPrune` を「canonical target が存在する source のみ」に限定し、raw 欠落時に姿別 id を消して名前を失う事故を型でなくロジックで防いだ（テストで absent-target ケースを固定）。

## ⚠️ Problem（詰まった点・制約）

- **名前辞書がスカフォールド生成物ゆえ手編集が非冪等**: `data/languages/mega.yaml` は PokeAPI 全件スカフォールド由来で、手で畳んでも次の `pokeapi-names.yml` 実行で姿別 id が復活する。この非冪等性は「生成物を直接直したくなる」直感と衝突し、生成側にルールを足す必要を最初に見抜く判断が要った。
- **docstring の blanket 断言が陳腐化していた**: `extractMegaNames` の「mega slug は id 正規化不要 / slug をそのまま id に使う」という断言が、今回の refine で部分的に誤りになった（レビューで non-blocking 指摘）。ADR 0043 前提を写した固定コメントが実装変更に追従していなかった。
- **collapse 後の新規 id が末尾 append される**: sync が新規 id を map 末尾に足すため、`tatsugiri-mega` が既存のアルファベット順を崩して zygarde-mega の後に並ぶ。パイプライン挙動としては正しいが、既存ファイルの整列と不揃いになる（機能影響なし・据え置き）。
- **worktree に data/raw が共有されない**: 実パイプライン検証のため main から `pokemon-form` raw を手動コピーする一手間が必要だった（gitignore ゆえ worktree に無い・rule 既知の制約）。

## 🚀 Try（次回の改善案）

- **[skill] author-static-data の references に mega 畳み込み対象（MEGA_ID_COLLAPSE）を明記**し、新装飾フォルム × メガが PokeAPI に増えたとき追記すべき場所を skill から辿れるようにする（curated マップの保守漏れ防止）。
- **[rule] docstring の固定断言（「常に不要」「必ず一致」等）を書くとき、将来の例外に備えた refine ポインタを添える慣行を testing / data-pipeline のどこかに一言添える**か、レビュー観点として明示する（今回の doc-code 乖離は機械ゲート外で拾えたが体系化されていない）。
- **[skill] implementation-workflow 系で「データ系変更は着手前に実データ grep で指示前提を検証する」ステップを Phase 2 整合性チェックに明示的に足す**か検討（今回の meowstic 誤認回避が再現性を持つように）。

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit |
| カバレッジ | 100% | 100% | ±0 | Vitest（248 tests・新規純関数 3 種を網羅） |
| Biome 違反 | 0 | 0 | ±0 | biome check（format 自動修正後） |
| CI | pass | pass | — | server-side pnpm verify（23s） |
| 差分 files/行 | — | 8 files +265/-50 | — | gh pr diff |

## 🤖 ハーネス改善提案

- [ ] `[skill]` author-static-data の references に mega 畳み込み対象（`MEGA_ID_COLLAPSE` = magearna-original / tatsugiri 3 姿）と追記先を明記し、新装飾フォルム × メガ追加時の保守導線を作る。
- [ ] `[rule]` 固定 docstring の blanket 断言（「id 正規化は不要」等）に refine ポインタを添える慣行、または doc-code 乖離をレビュー観点として code-review checklist に一言追加する。
- [ ] `[skill]` implementation-workflow の Phase 2（整合性チェック）に「データ系変更は着手前に実データを grep して指示の前提を裏取りする」を明示する（指示と実態の食い違い早期検知）。

## 📝 harness-meta フィードバック

### 採用

### 見送り

### 保留
