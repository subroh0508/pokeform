---
id: learning-pr-186
title: PR #186 レトロスペクティブ (メガストーン情報源 Serebii 化・誤った PokeAPI 404 仮定の是正)
type: learning
status: draft
related_pr: 186
related_plan: 09-champions-data-rollout/phase-03
generated_at: 2026-06-28T11:35:00Z
generator: pr-retrospective skill
---

<!--
この雛形は `.claude/rules/retrospective-format.md`（SoT）に準拠する。
見出し（絵文字・文言）は harness-meta が parse するため変更しないこと。
書き出し前に `.claude/rules/redaction.md` の正規表現で Secrets / PII を [REDACTED-*] 置換すること。
-->

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/186 / commit 00b33c9 / merge 2026-06-28T11:32:51Z / 差分 8 files +148/-39。plan 09 Phase 3 skill 改修。Champions 固有メガを「PokeAPI 非存在ゆえ投入不可」とした Phase 1/2 の判断が**未検証の誤り**（メガ本体は 200・404 はストーンのみ）と判明し、メガストーンの category 取得元を Serebii 化 + item 404 graceful skip でメガを常時記録可能にした。

## ✅ Keep（継続したいこと）

- **外部 API の「非存在」主張は実 HTTP で検証してから記録する**: 今回ユーザー指摘で `curl` 検証したところ、除外根拠だった「`pokemon/starmie-mega` は PokeAPI 404」が**実際は 200**だった。404 はメガ"ストーン"（`item/starminite`）のみ。思い込みでなく実測が正した。
- **機能実証スモーク（注入 → 実行 → revert）**: Champions ストーン（starminite）を一時注入し `fetch:data` の 404 graceful skip 到達 + `materialize` 非クラッシュ + category 維持を確認してから revert。コード変更が end-to-end で効くことを committed data を汚さず実証できた。
- **Generator/Evaluator 分離 + 2 系統レビュー**: code-review（src/scripts）と harness-review（skill/docs）を独立起動し、harness-review が「同一 doc の別節に残った誤記」を捕捉。観点分担が機能した。
- **Serebii 第一優先（ADR 0026）の一貫適用**: メガストーン category も Serebii 由来に寄せ、PokeAPI 非対応エンティティの取得元を Serebii に統一。既存方針の自然な延長で ADR 追加不要。
- **plan 09 の de-risk 設計が効いた**: 限定セット検証（Phase 1/2）→ 人間ゲート（Phase 3）の流れで、全 186 種本投入の前に skill/pipeline の誤判定を是正できた。

## ⚠️ Problem（詰まった点・制約）

- **未検証の仮定が 2 phase + doc + memory に伝播した**: Phase 1 で「`item/chandelurite` が 404」→「ならメガ本体も 404 のはず」と**実測せず**推論し、starmie-mega/dragonite-mega を除外。Phase 2 が同じ前提を踏襲し計 13 形を除外。誤りが roster-source doc ×2 + memory のギャップ記述に固定化され、ユーザー指摘まで気づかれなかった。
- **「ギャップ/限界」の記録が誤りだと下流を誤誘導する**: 誤った gap#1 が memory・2 つの roster-source doc・Phase 3 申し送りに複製され、訂正に複数ファイルの追従を要した。limitation の記録は事実確認のコストを惜しむと負債になる。
- **同一 doc 内の重複主張の取りこぼし**: m-b roster-source の「メガの扱い」節は訂正したが、別節「既知ギャップ」に同じ誤記が残り harness-review で検出された（1 パスで全箇所を grep していれば防げた）。
- **スモークが別の既知 gap（pyroar gendered slug）を踏んで一旦ブロック**: worktree の raw キャッシュに Phase 2 の pyroar workaround が無く `pokemon/pyroar` 404 で fetch:data が停止。本 PR スコープ外だが、worktree 間で raw（gitignore）が共有されない運用コストを再認識。

## 🚀 Try（次回の改善案）

- **gap/limitation を doc・memory に書く前に、失敗する正確なコマンド（`curl <exact endpoint>`）を 1 回実行して事実確認する**運用を skill（survey-regulation / update-catalog）と retrospective-format に明記する。「materialize できない」のような主張は対象エンティティ単位で検証する。
- **同種の主張が複数ファイル/節に複製されていないか `git grep` で横断確認**してから「訂正済み」とする（圧縮 PR 規約の「横断整合を git grep で確認」と同根・本件は gap 記述に適用）。
- pyroar 系 gendered slug・null placeholder crash・mega ability 自動転記は **Phase 3 の残タスク**として継続（本 PR はメガストーン情報源に集中）。

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit |
| カバレッジ | 100% | 100% | ±0 | Vitest 186 tests / 515 stmts |
| Biome 違反 | 0 | 0 | ±0 | biome check |
| CI | pass | pass | — | server-side pnpm verify |
| 差分 files/行 | — | 8 files | +148/-39 | gh pr diff |

## 🤖 ハーネス改善提案

<!-- harness-meta が parse する正規構造。未処理は [ ]、採用後に [x] + PR リンク。 -->

- [ ] `[skill]` `survey-regulation` / `update-catalog`: 「外部 API 非存在/取得不可」を doc・memory に記録する前に、失敗する正確なエンドポイントを 1 回実測（`curl`）して事実確認する手順を明記（未検証の伝播防止）。
- [ ] `[rule]` `retrospective-format` または `data-pipeline`: gap/limitation 記述は対象エンティティ単位で実測根拠を添える + 訂正時は `git grep` で同主張の全複製を横断追従する旨を追記。
- [ ] `[skill]` `survey-regulation`: Phase 3 残タスク（pyroar 系 gendered PokeAPI form-slug の override 経路 / 新規種族 null placeholder の materialize 非クラッシュ + megaEvolvesTo 自動著述 / mega ability の catalog 自動転記）を継続改修。

## 📝 harness-meta フィードバック

<!-- 空でも見出しは維持する。harness-meta が後から各表に追記する。 -->

### 採用

### 見送り

### 保留
