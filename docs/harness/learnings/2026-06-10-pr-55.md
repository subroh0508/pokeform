---
id: learning-pr-55
title: PR #55 レトロスペクティブ (M-A 現ロスター26種 持ち物・技 正確化)
type: learning
status: draft
related_pr: 55
related_plan: 02-data-model-redesign/phase-07
generated_at: 2026-06-10T01:28:18Z
generator: pr-retrospective skill
---

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/55 / commit 0c1a1a6 /
> merge 2026-06-10T01:28:18Z / 差分 12 files +2796/-186。M-A 現ロスター26種の暫定 items / moves を
> 実情へ正確化（life-orb 等3持ち物を非解禁除去・各種族 moves を learnset ∩ legal の競技 pool 13〜16技へ）。
> 全186種/全持ち物/全メガの全量投入は Phase 8 へ分離。

## ✅ Keep（継続したいこと）

- **curate→実 learnset 機械照合→投入の順**。各種族の競技 movepool を一時スクリプト（`_tmp-movepools.mjs`）で
  PokeAPI learnset へプログラム照合してから YAML 生成。curate 段で覚えない技 2 件（`corviknight:snarl` /
  `meowscarada:sucker-punch`）を `check:regulation` 到達前に検出・差替え。AI 生成データの hallucination を
  機械照合で潰す型が有効だった。
- **持ち物の多ソース突き合わせで誤抽出を是正**（survey-regulation 手順3）。初回 WebSearch 要約は life-orb を
  「解禁 item」と誤抽出したが、一次に近い複数ソース（pokemondb pokebase / games.gg / pokemon-zone）の
  非在籍一致で上書き。WebSearch 非決定性に対し矛盾と採用根拠を doc に残し追跡可能化。
- **一時生成スクリプトで append-only 差分を決定論生成**。126技の catalog 追記と26種の YAML ブロックを手書き
  せず生成し、コミット前にスクリプトを削除。大量 YAML 手編集のミスを回避。
- **生成物手編集ゼロをレビューが再生成で裏取り**。code-review が `generate:data` 再実行 → `git status` 空を確認し、
  `data/generated/**` がソース+raw からの決定論再生成と一致することを数値で担保。
- **interim phase の scope 分割が機能**。現ロスター26種（正確化）と全186種（Phase 8 全量投入）を分け、
  種族数不変・全量化分離を両レビューが確認。1 PR の意味的完結性を保てた。
- **独立 2 レビューの分担**（code-review = data / harness-review = docs/plan）で blocking 0、表記ゆれを早期捕捉。

## ⚠️ Problem（詰まった点・制約）

- **「約27種」見積りと実体26種の表記ゆれが doc 横断で残存**。phase doc の目的/スコープ/受け入れ基準と catalog
  コメントに 27 が残り、両レビューが non-blocking / nit 指摘 → wrapup で 26 へ統一。着手前に種族数を YAML から
  確定して doc を揃えれば防げた。
- **"M-A legal(467)" universe が repo 内に未 materialize**。`check:regulation` は learnset + catalog 所属のみ
  強制し、legal フィルタは curate 者責務に degrade。実害は小（標準競技技はほぼ legal）だが、Phase 8 の全量化では
  467 リストとの突き合わせが要る。
- **legal 総数 467/117 が MetaVGC 単一ソース依存**（186 のみ Game8 と一致）。Phase 8 で RotomPicks /
  Victory Road 等の再突き合わせが必要。
- **fetch:data を 2 回踏んだ**。初回 fetch 後に catalog を126技拡張し再 fetch（network）。catalog 確定後に
  一括 fetch すれば 1 回で済んだ。

## 🚀 Try（次回の改善案）

- `survey-regulation` skill に「curate した per-species moves を投入前に PokeAPI learnset へプログラム照合する」
  機械照合ステップを明記し、覚えない技の混入検出を定型化する。
- データ正確化 phase の着手時に、対象集合の件数（種族数等）を一次データ（YAML / raw）から確定して doc の
  見積り数値を揃える点検を入れる（表記ゆれの再発防止）。
- `data-pipeline` rule に、M-A legal moves universe(467) の扱い（catalog と別に materialize するか
  `check:regulation` で legal を強制するか）を Phase 8 で決める旨を残す。
- `fetch:data` は catalog 確定後に一括実行する運用注記を残す（拡張→再 fetch の二度手間回避）。

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit |
| カバレッジ | 100% | 100% | ±0 | Vitest（純関数層不変・データ変更のみ） |
| Biome 違反 | 0 | 0 | ±0 | biome check |
| CI | pass | pass | — | server-side pnpm verify |
| 差分 files/行 | — | 12 files | +2796/-186 | gh pr view（うち生成物再生成が大半） |

## 🤖 ハーネス改善提案

- [ ] `[skill]` survey-regulation: curate した per-species moves を投入前に PokeAPI learnset へプログラム照合する手順を明記（覚えない技を check:regulation 前に検出）。
- [ ] `[skill]` survey-regulation: データ正確化着手時に対象件数（種族数等）を一次データから確定し doc の見積り数値の表記ゆれを防ぐ点検を追加。
- [ ] `[rule]` data-pipeline: M-A legal moves universe(467) の扱い（catalog 別 materialize or check:regulation での legal 強制）の方針を Phase 8 で決める旨を残す。
- [ ] `[rule]` data-pipeline: fetch:data は catalog 確定後に一括実行する旨の運用注記（拡張→再 fetch の二度手間回避）。
- [ ] `[remove]` （該当なし）

## 📝 harness-meta フィードバック

### 採用

### 見送り

### 保留
