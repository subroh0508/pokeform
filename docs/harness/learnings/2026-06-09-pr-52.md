---
id: learning-pr-52
title: PR #52 レトロスペクティブ (per-regulation 技記録の species-keyed 化 + mega 配列化)
type: learning
status: draft
related_pr: 52
related_plan: 02-data-model-redesign/phase-05
generated_at: 2026-06-10T00:00:00Z
generator: pr-retrospective skill
---

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/52 / commit 8de93d3 /
> merge 2026-06-09T18:33:11Z / 差分 21 files +763/-207。regulations YAML を species-keyed 化し
> generate を新スキーマ読取りへ追従、megaEvolvesTo を配列化。出力は配列化以外等価。

## ✅ Keep（継続したいこと）

- **出力等価を生成物 diff で機械的に裏取り**。移行 YAML を現行生成値（committed `species.ts`）から
  起こすワンショット script を用い、再生成 diff が `megaEvolvesTo` 配列化の 3 行のみに収束。移行の安全性を
  数値で担保できた。
- **検証の段階的移設で安全性の空白を作らない**。Phase 5 では generate に learnset 検証を残し、除去は
  `check:regulation` を用意する Phase 6 に回す順序を計画段階で明文化した。
- **計画 doc を worktree へ stash 経由で持ち込み Phase 5 PR に同梱**。未コミットの OVERVIEW/phase doc を
  `git stash push -- docs/plan` → worktree で pop し、`team/_demo`（無関係）を巻き込まず搬入できた。
- **独立レビューを 2 種並行起動**（code-review = src/scripts/data・harness-review = rules/adr/plan）。
  Generator/Evaluator 分離で blocking 0・non-blocking/nit のみを早期に得てマージ判断できた。
- **ADR を full supersede せず部分改訂**。0021 の核（per-reg species 正本・reg-aware 型機構）は不変とし、
  記録方法の該当箇所のみ 0022 で改訂・0021 末尾に前方ポインタを追記して discoverability を確保。

## ⚠️ Problem（詰まった点・制約）

- **`data/raw` 空でローカル再生成に `pnpm fetch:data`（ネットワーク）が必須**だった。generate の出力等価検証に
  raw キャッシュが要るが gitignore のため worktree 初期は空で、初回に取得コストが乗る。
- **`noUncheckedIndexedAccess` 下で配列先頭参照 `megaEvolvesTo[0]` が `string|undefined` になり branch 増**。
  カバレッジ100%維持に「空メガ配列」フィクスチャのテスト追加が必要だった（実データに無いエッジ）。
- **計画 doc の worktree 受け渡し手順が未定義**。plans-new 産物（未コミット doc）を
  implementation-workflow Phase 0 が origin/main 起点 worktree へどう載せるかが曖昧で、stash 運用を都度判断した。
- **phase 跨ぎの前方参照表現でレビュー指摘**。移行 YAML コメントが未実装の `check:regulation` を「弾く」と
  断定し、Phase 5 時点の実態（generate が検証）と齟齬。polish で修正した。
- **ADR rule に部分改訂の明示機構が無い**。「full supersede でない・核は不変」という運用を ADR 本文と
  前方ポインタで補ったが、rule 側に類型が無く判断を都度説明する必要があった。

## 🚀 Try（次回の改善案）

- implementation-workflow に「**plans-new 産物（未コミット計画 doc）の worktree 搬入手順**」（stash 経由 or
  計画コミット先行）を明記し、都度判断を排す。
- data-pipeline / survey-regulation に「**`data/raw` 空時は `fetch:data` を先行**」の前提を実装手順へ明記
  （Phase 7 の全量投入で特に効く）。
- adr.md に「**部分改訂（核不変・該当箇所のみ改訂、full supersede しない）**」の類型と前方ポインタ追記の
  作法を明文化する。
- testing/type-conventions に「`noUncheckedIndexedAccess` 下で配列先頭参照の branch をエッジテストで
  被覆する」定石を一行残す。

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit |
| カバレッジ | 100% | 100% | ±0 | Vitest（199 stmt / 89 branch・空メガ配列テスト追加） |
| Biome 違反 | 0 | 0 | ±0 | biome check（throw 整形を format で吸収） |
| CI | pass | pass | — | server-side pnpm verify（19s） |
| 差分 files/行 | — | 21 files | +763/-207 | gh pr view |

## 🤖 ハーネス改善提案

- [ ] `[skill]` implementation-workflow: plans-new 産物（未コミット計画 doc）の worktree 搬入手順（stash 経由 / 計画コミット先行）を Phase 0 に明記する。
- [ ] `[rule]` data-pipeline: `data/raw` 空時は `pnpm fetch:data` を先行する前提を明記（出力等価検証・全量投入の前提）。
- [ ] `[rule]` adr: 部分改訂（核不変・該当箇所のみ改訂・full supersede しない）の類型と、旧 ADR への前方ポインタ末尾追記の作法を明文化する。
- [ ] `[rule]` testing: `noUncheckedIndexedAccess` 下の配列先頭参照は空配列エッジテストで branch を被覆する定石を一行残す。
- [ ] `[skill]` pr-retrospective: learning の置き場（batch 専用ブランチ）と phase wrapup PR への同梱という実運用の乖離を整理（バンドル可否の方針統一）。

## 📝 harness-meta フィードバック

### 採用

### 見送り

### 保留
