---
id: learning-pr-70
title: PR #70 レトロスペクティブ (02 PokeAPI 除外/構造データ catalog 化/data README フェーズ挿入 + renumber)
type: learning
status: draft
related_pr: 70
related_plan: 02-data-model-redesign/phase-12〜15
generated_at: 2026-06-12T19:10:57Z
generator: pr-retrospective skill
---

<!--
この雛形は `.claude/rules/retrospective-format.md`（SoT）に準拠する。
見出し（絵文字・文言）は harness-meta が parse するため変更しないこと。
書き出し前に `.claude/rules/redaction.md` の正規表現で Secrets / PII を [REDACTED-*] 置換すること。
-->

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/70 / commit c4d4e320185a8489ffaf00acb7404dbe4a1288aa / merge 2026-06-12T19:10:57Z / 差分 12 files +491/-67。既存計画の最後（M-A 全データ投入）の手前に 2 度の壁打ちで 2 フェーズ（Phase 12 PokeAPI 除外・Phase 13 構造データ catalog 化）を挿入し、data README/M-A 投入を Phase 14/15 へ繰り下げた計画追加 PR（実装なし）。

## ✅ Keep（継続したいこと）

<!-- 最低 3 / 推奨 5-10 -->

- **plans-new を入口に壁打ちで方針を固めてから phase doc 化した**。`AskUserQuestion` で「PokeAPI 範囲 / learnset ゲート / data Markdown 体裁 / フェーズ構成」の 4 論点を一度に確認し、解釈のぶれを着手前に潰せた。
- **renumber を含む計画編集を機械確認した**。`git grep -n "Phase 1[2-5]"`・README リンク 15/15 の実在チェック・旧ファイル名への dangling 参照ゼロ確認を都度回し、番号整合を目視に頼らず担保した。
- **harness-review を独立 subagent で実施**し、セルフレビューの盲点（`phase-15:91` の overrides 残存）を 1 件検出→即修正できた。実装ワークフロー後半（独立レビュー→マージ→レトロ）を「実装なし」で運用できた。
- **フェーズ追加と実装を分離**した。ADR 起票（PokeAPI 除外・構造データ SoT 移設）は実装フェーズ送りと明記し、計画 PR の責務を「計画のみ」に絞った。
- **OVERVIEW の「設計方針の改訂（Phase N で導入）」節を積み増す既存パターンに追従**した。Phase 4/5-6/10 の前例に合わせ Phase 12/13 の改訂節を足し、計画の意思決定ログを連続させた。
- **廃止判断を調査で裏取りした**。`overrides.yaml` は「コード参照ゼロ + 中身空 + learnset 撤去で役割消滅」を確認してから廃止と結論し、phase doc に根拠を残した。

## ⚠️ Problem（詰まった点・制約）

<!-- 最低 3 / 推奨 5-10 -->

- **2 回連続の renumber で stale 参照が複数回混入した**。1 回目（12→14）と 2 回目（13→14, 14→15）で、`phase-12:35`（README=旧13 のまま）と `phase-15:91`（overrides 補正記述）の追従漏れが発生し、後者は harness-review でようやく検出。手動 grep + 個別修正の負荷が高い。
- **「Phase N」が文脈で別エンティティを指す**ため blanket 置換が危険だった。OVERVIEW 内で「14」が M-A（→15）と README（=14）の両方を意味し、一括 perl 置換ができず個別 Edit が必要になった。
- **rule とコードのドリフトが顕在化**した。`data-pipeline.md` は overrides を「適用範囲は generate.ts」と記述していたが、実装は overrides を一切参照しておらず（デッドファイル）、要否判断に実コード調査が必要だった。
- **多段の壁打ちで成果物の再生成コストが積み上がった**。PokeAPI 除外 → 構造データ catalog 化 → items → overrides → 責務分離 と方針が育つたびに「リスト再提示 → 計画追記」を繰り返した。
- **renumber 追従先が分散**（OVERVIEW 分割表 / 依存グラフ / 受け入れ基準 / README / 完了済み phase 07〜12 の forward-reference / ma-roster）しており、追従漏れの面が広い。

## 🚀 Try（次回の改善案）

<!-- rule / skill / template / ADR 単位まで具体化し、下の 🤖 改善提案へ落とす -->

- phase の insert/renumber を補助するスクリプト/手順（番号繰り下げ + cross-reference 追従 + dangling/番号整合の機械検査）を用意し、手動 grep の取り残しを防ぐ。
- `planning.md` に「phase insert/renumber 時の追従チェックリスト」を明文化（番号整合 git grep・README リンク・OVERVIEW 分割表/依存グラフ/受け入れ基準・完了済み doc の forward-reference）。
- `harness-review` の docs/plan 観点に「renumber を含む plan PR は番号整合を git grep で機械確認」を追記（今回有効だった手筋の恒久化）。
- rule とコードのドリフト（記述された機構が実装で参照されない）を検出/抑止する仕組みを検討。

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit（docs のみ変更） |
| カバレッジ | 100% | 100% | ±0 | Vitest |
| Biome 違反 | 0 | 0 | ±0 | biome check |
| CI | pass | pass | — | server-side pnpm verify（18s） |
| 差分 files/行 | — | 12 files | +491/-67 | gh pr diff（新 doc 2・renumber 2・追従 8） |

## 🤖 ハーネス改善提案

<!-- harness-meta が parse する正規構造。未処理は [ ]、採用後に [x] + PR リンク。 -->

- [x] `[rule]` `planning.md` に「phase insert/renumber 追従チェックリスト」を追加（番号整合 git grep・README リンク実在・OVERVIEW 分割表/依存グラフ/受け入れ基準・完了済み phase の forward-reference・ma-roster 等の関連 doc）。renumber 追従漏れの再発（本 PR で 2 回混入）を抑える。
- [x] `[skill]` `harness-review` の docs/plan 重点観点に「renumber を含む plan PR は `git grep "Phase N"` と README リンク実在で番号整合を機械確認する」を明記（今回 stale 参照検出に有効）。
- [ ] `[skill]` phase の insert/renumber を機械化する補助手順（番号繰り下げ + cross-reference 一括追従 + dangling/番号整合検査）を `plans-new` references か独立スクリプトとして用意。「Phase N」が文脈依存で blanket 置換できない問題を、対象限定の安全な置換 + 検査で吸収。
- [ ] `[template]` plan-templates の phase doc 節に「renumber 時に追従が要る cross-reference の一覧」を注記（前提節・OVERVIEW 参照・README リンク）。
- [ ] `[remove]` `data-pipeline.md` の overrides 記述（「適用範囲は generate.ts」）は実装と乖離（参照ゼロ）。Phase 13 実装で overrides.yaml 廃止に合わせ rule からも除去する（本 PR の Phase 13 doc にタスク化済み・追従確認用）。

## 📝 harness-meta フィードバック

<!-- 空でも見出しは維持する。harness-meta が後から各表に追記する。 -->

### 採用

<!-- 採用した提案と採用先 PR リンク -->

- harness-meta batch 2026-W24（PR #79）で採用。`[rule]` phase insert/renumber 追従チェックリスト（行64）・`[skill]` renumber plan PR の `git grep "Phase N"` + README リンク実在の機械確認（行65）を `planning.md`「phase の insert/renumber 追従チェックリスト」（末尾で `code-review`/`harness-review` を点検対象に指定）へ反映（#76/#50/#42/#58 と合流）。機械化補助スクリプト（行66）・template cross-ref 一覧（行67）は据え置き。

### 見送り

<!-- 見送り理由（harness-meta-criteria.md の見送り基準に対応） -->

### 保留

<!-- 保留理由 -->
