---
id: learning-pr-118
title: PR #118 レトロスペクティブ (plan 03 へ 3 phase 挿入 + 全投入を Phase 13 へ renumber・cross-plan/intra-doc forward 参照追従)
type: learning
status: draft
related_pr: 118
related_plan: 03-survey-regulation-rework
generated_at: 2026-06-14T18:22:55Z
generator: pr-retrospective skill
---

<!--
この雛形は `.claude/rules/retrospective-format.md`（SoT）に準拠する。
見出し（絵文字・文言）は harness-meta が parse するため変更しないこと。
-->

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/118 / commit 156e2aedca3abf6b494759ba386419035279b42c / merge 2026-06-14T18:22:55Z / 差分 13 files +324/-39。全種族投入の手前にデータレイアウト整備 3 phase（ゲームグルーピング / per-game 技メタ / 取得スキル 2 分割）を挿入し、旧 Phase 10（M-A 全投入）を Phase 13 へ renumber した plan-doc PR。#104 と同型の renumber + cross-plan 追従。

## ✅ Keep（継続したいこと）

- **入口を `plans-new` → `implementation-workflow` で通した**: 生の指示を preview（OVERVIEW/分割/renumber 影響）でユーザー承認 → worktree → 書き出し → Draft PR → 独立レビュー → マージまで定型どおり駆動できた。
- **renumber 追従を `git mv` + repo 全体 `git grep` で機械実行**: 旧 `phase-10-ma-full-data` への dangling リンク（02 README 2 件）と 02 OVERVIEW の cross-plan 参照 6 箇所を追従し、書き出し直後の自己 grep で dangling 0 を確認した。
- **harness-review を Agent で独立起動**（Generator/Evaluator 分離）: 自分が見落とした intra-doc の旧番号残り（03 README 導入段落）を blocking として捕捉できた。自己採点では漏れていた。
- **書き出し前に preview をユーザー承認**: phase 粒度・順序・命名・renumber 影響を提示してから着手し、手戻りを防いだ。
- **完了済み phase doc の誤指し forward 参照を横展開で一括是正**: nit 指摘（phase-09）を起点に phase-03/04/05/06/09 の「全186種投入（Phase 10）」を Phase 13 へ統一し、誤指しを残さずマージした。

## ⚠️ Problem（詰まった点・制約）

- **同一 README 内の導入散文の旧番号追従漏れ（blocking）**: OVERVIEW の対応箇所は Phase 13 へ直したが、03 README 冒頭の概要段落「最終 phase（Phase 10）へ移動」を見落とし、同 README の mermaid/一覧/補足（すべて Phase 13）と矛盾した。表・mermaid・リンクは追従したが**散文 forward 参照**が漏れた（learning #70 / #76 / #50 / #104 の「散文中の素番号」反復）。
- **同一番号が新旧で意味変化するケースの選別コスト**: 本 PR で「Phase 10」は旧=全投入 → 新=ゲームグルーピングへ意味が変わった。`git grep "Phase 10"` の hit を「新 phase の正当参照」と「旧テーマの誤指し」に選別する必要があり、初回の自己追従では完了 phase（phase-03〜06）の誤指しを拾いきれず、レビュー nit を起点に気づいた。
- **完了済み計画（02）からの cross-plan 参照が多い**: 02 は ✅ 完了だが OVERVIEW/README が 03 の renumber 対象 phase を多数参照しており追従コストが高い（#104 と同型・cross-plan dangling の構造的反復）。

## 🚀 Try（次回の改善案）

- planning.md の renumber チェックリストに「**同一 doc 内の導入散文・概要段落・ゴール段落の forward 参照**（README 冒頭 / OVERVIEW 概要）も `git grep` hit として必ず追従。表・mermaid・リンクだけで満足しない」を明記。
- renumber で**同一番号の指示対象が別 phase に変わる**場合、`git grep "Phase <番号>"` の全 hit を「新番号の正当参照 / 旧テーマの誤指し」に**選別する手順**を planning.md に明文化。
- 「完了済み phase doc の forward 参照は凍結可」の例外として、**renumber で番号が別 phase を誤指しする場合は凍結せず是正**を planning.md に追記。
- implementation-workflow Phase 4（セルフ検証）に「renumber を含む plan PR は harness-review 前に自前で `git grep "Phase <旧番号>"` 全 hit 選別を済ませる」を組み込み、blocking を Evaluator 前に潰す。

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit |
| カバレッジ | 100% | 100% | ±0 | Vitest（docs-only・コード不変） |
| Biome 違反 | 0 | 0 | ±0 | biome check |
| CI | pass | pass | — | server-side pnpm verify（19s） |
| 差分 files/行 | — | 13 files | +324/-39 | gh pr diff（docs/plan のみ） |

## 🤖 ハーネス改善提案

- [ ] `[rule]` planning.md renumber チェックリストに「同一 doc 内の導入散文・概要/ゴール段落の forward 参照も `git grep` で追従（表・mermaid・リンクだけで満足しない）」を追記（#70/#76/#50/#104/#118 反復）。
- [ ] `[rule]` planning.md に「renumber で同一番号の指示対象が別 phase へ変わる場合、`git grep "Phase <番号>"` の hit を新番号正当参照 / 旧テーマ誤指しに選別する手順」を明文化。
- [ ] `[rule]` planning.md の「完了済み phase doc forward 参照は凍結可」に、renumber で番号が別 phase を誤指しする場合は凍結せず是正、という例外を追記。
- [ ] `[skill]` implementation-workflow Phase 4 セルフ検証に「renumber を含む plan PR は harness-review 前に `git grep "Phase <旧番号>"` 全 hit 選別を完了させる」を追加。

## 📝 harness-meta フィードバック

### 採用

### 見送り

### 保留
