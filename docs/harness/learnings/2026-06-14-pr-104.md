---
id: learning-pr-104
title: PR #104 レトロスペクティブ (plan 03 へ 3 phase 挿入・renumber + cross-plan 追従)
type: learning
status: draft
related_pr: 104
related_plan: 03-survey-regulation-rework
generated_at: 2026-06-14T12:42:00Z
generator: pr-retrospective skill
---

<!--
この雛形は `.claude/rules/retrospective-format.md`（SoT）に準拠する。
見出し（絵文字・文言）は harness-meta が parse するため変更しないこと。
-->

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/104 / commit a582adc / merge 2026-06-14T12:41:56Z / 差分 12 files +288/-32。survey-regulation 動作確認で判明した 3 是正（per-reg 持ち物 legality・per-reg species name 削除・メガ決定論取り込み）を plan 03 の Phase 7-9 として起票し、M-A 全投入を Phase 10 へ renumber した docs-only PR。

## ✅ Keep（継続したいこと）

- **plan mode で設計を確定 → docs-only PR 化**: 実装前に AskUserQuestion で仕様（メガストーン保持ルール = base 全件 / メガ形態 = 対応ストーンのみ・megaSpecies リンク）を 3 往復で詰め切り、phase doc に落としてから実装を分離した。仕様の取り違えを実装前に潰せた。
- **renumber 追従チェックリストの機械実行**: `git mv` 後に `grep -rn "Phase 7"` を計画ディレクトリ全体へ打ち、散文 bullet・mermaid・表・完了 phase（03-06）の forward 参照・OVERVIEW 総数/直列チェーンまで全 hit を追従。リンク先 10 件と参照 ADR 6 件の実在も機械照合した（[[planning]] のチェックリスト遵守）。
- **harness-review を独立 Agent で起動**: Generator/Evaluator 分離を守り、自己採点でなく独立レビューで cross-plan dangling を検出できた。
- **docs-only でも verify 緑で前進**: worktree に pnpm install → verify（型/カバレッジ100%/Biome/yaml-style）緑を確認してからコミット。pre-commit hook も通過。
- **1 phase = 1 phase doc の粒度**: A/B/C を別 phase に分け、依存（B は A の megaSpecies に依存）を mermaid と直列チェーンに明記した。

## ⚠️ Problem（詰まった点・制約）

- **cross-plan 参照の追従漏れ**: 03 の `phase-07-ma-full-data.md` を `phase-10` へ rename したことで、**完了済み計画 02** が持つ cross-plan リンク 2 件（README）と散文「03 の Phase 7」5 箇所が dangling/誤指しになった。[[planning]] の renumber 追従チェックリストは「**計画ディレクトリ全体**を走査」止まりで、**他計画から当該 phase への参照**を射程に含めていなかったため、初回コミットで見落とし harness-review で検出された（learning #42/#58/#76 の dangling 反復の cross-plan 版）。
- **plan 02 自身の Phase 7 との衝突**: plan 02 にも独自の Phase 7（現ロスター正確化）があり、一括置換だと無関係な番号まで壊す。「03 を指す Phase 7」だけを限定置換する必要があり、機械置換のパターン設計に注意を要した。
- **Workflow ツールの args 不達（別件・関連）**: survey-regulation 動作確認で `self-heal.workflow` を scriptPath+args 起動すると roster が空（early-return）。層2-3 が駆動できず cross-agent 逐次フォールバックに切替えた。Phase 9 実装時に args 取り込み（文字列化疑い）の調査が要る。

## 🚀 Try（次回の改善案）

- [[planning]] の「phase の insert / renumber 追従チェックリスト」に **cross-plan 参照**の項を追加する（他計画 doc から当該 phase doc への相対リンク・散文 phase 番号を `git grep` で全リポジトリ走査）。
- renumber を伴う plan PR の harness-review 観点に「cross-plan dangling」を明示する。
- Workflow `scriptPath` + `args` の不達は Phase 9（メガ決定論）着手前に再現調査し、必要なら survey-regulation workflow の args 取り込みを修正 or skill 手順へ注記。

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit（docs-only） |
| カバレッジ | 100% | 100% | ±0 | Vitest（src 変更なし） |
| Biome 違反 | 0 | 0 | ±0 | biome check |
| CI | pass | pass | — | server-side pnpm verify 23s |
| 差分 files/行 | — | 12 files | +288/-32 | docs/plan のみ（03 phase 追加 + renumber + 02 cross-plan 追従） |

## 🤖 ハーネス改善提案

- [ ] `[rule]` `.claude/rules/planning.md` の renumber 追従チェックリストに「**cross-plan 参照の全リポジトリ走査**」を追加（他計画 doc から rename/renumber した phase への相対リンク・散文 phase 番号を `git grep -rn` で検出し追従）。複数 PR で dangling が反復しており（#42/#58/#76）その cross-plan 拡張。
- [ ] `[skill]` `harness-review` の checklist に「renumber PR は cross-plan dangling を点検」を明記（計画ディレクトリ内だけでなく他計画からの参照も）。
