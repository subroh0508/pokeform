---
id: learning-pr-102
title: PR #102 レトロスペクティブ (03 Phase 6 survey-regulation SKILL 全面改訂 + cross-agent パリティ)
type: learning
status: draft
related_pr: 102
related_plan: 03-survey-regulation-rework/phase-06
generated_at: 2026-06-14T05:18:56Z
generator: pr-retrospective skill
---

<!--
この雛形は `.claude/rules/retrospective-format.md`（SoT）に準拠する。
見出し（絵文字・文言）は harness-meta が parse するため変更しないこと。
書き出し前に `.claude/rules/redaction.md` の正規表現で Secrets / PII を [REDACTED-*] 置換すること。
-->

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/102 / commit a6436b9 / merge 2026-06-14T05:18:56Z / 差分 2 files +232/-198。`survey-regulation` skill を「LLM WebFetch 目視抽出」から「決定論スクレイパー（層1）+ Workflow 自己修復（層2-3）」へ全面改訂し、`allowed-tools` を Bash 中心 + Workflow/Agent へ整理、cross-agent フォールバックを明記。M-A roster 26 種を層1 決定論経路で実証（25 ok / 1 escalated）し HTML を LLM に載せずトークン大幅減を確認。

## ✅ Keep（継続したいこと）

- **実証を先に走らせて SKILL 改訂と並行させた**: 26 種の層1 end-to-end をバックグラウンド実行しつつ doc を書いたため、改訂内容（25/26 成功・rotom-wash escalation）を実データで裏付けられた。受け入れ基準「数十種で end-to-end 緑・トークン大幅減」を空論でなく数値で満たせた。
- **SKILL から references への詳細移譲（progressive disclosure）**: 層2-3 の冗長な block-quote 機構説明を `references/serebii-sourcing.md` へ移し、SKILL を 235→217 行へ圧縮（≤500 行・description 892 字 ≤1024）。二重記述を避けつつ trigger 精度を保てた。
- **cross-agent フォールバックを「正しさは層1 に宿る」で一本化**: 層2-3（Claude 固有 Workflow）と Codex（層1 逐次 + 人手修正）の非対称を、共有 npm script + テスト済み純関数 = 正しさの核、という一文で SKILL/references 双方に整合させた。設計の必須条件を doc に明示できた。
- **独立レビュー（harness-review）を Agent で起動し PR コメントに記録**: Generator/Evaluator 分離を守り、blocking 0・nit 1 の判定を PR #102 上に残した。nit（表記混在）を Draft 解除前に取り込んでからマージ同期点（最終 HEAD ea800c0）を満たした。
- **decision を doc に明示（2 ソース件数検証の縮退）**: phase 備考の「Game8 等を機械的件数比較へ縮退」を SKILL/references/ PR body に判断として記録。後続が「なぜ目視突き合わせを止めたか」を辿れる。

## ⚠️ Problem（詰まった点・制約）

- **end-to-end 実証がライブネットワーク依存**: serebii キャッシュは gitignore + worktree 非共有のため、実証に 26 種のライブ fetch が必要だった。`timeout` が macOS に無く（GNU coreutils 前提のスクリプトが exit 127）、Bash ツールの timeout 引数へ切り替える手戻りが出た。
- **フォルム slug の取得失敗が層1 で吸収できない**: rotom-wash は Serebii の URL がフォルム別で `fetch-serebii` が exit 2（取得失敗）になった。決定論パーサの守備範囲外（取得段の slug マッピング）で、最終 WebFetch fallback / 手動 slug 解決に頼る構造が残る。
- **plan-phase 実装 PR と harness.md テンプレの噛み合わせ**: harness.md テンプレは learning 採用駆動（harness-meta-criteria 対応）の節を持つが、本 PR は plan phase 実装で「対象 learning」が無い。テンプレを phase 実装向けに読み替えて流用したが、節の意味が一部空転した。
- **README 進捗更新が feature PR に同梱できなかった**: オーケストレーター主導 auto-merge で #102 を先にマージしたため、サブ README の Phase 6 チェックは follow-up（本 retro PR）へ切り出すことになった。`implementation-workflow.md` の「同梱 or follow-up」指針どおりだが、同梱漏れの判断を都度要する。

## 🚀 Try（次回の改善案）

- **`survey-regulation` の実証手順にネットワーク前提とフォルム slug 注意を明記**: end-to-end 実証はライブ fetch が要る／GNU `timeout` を前提にしない／フォルム種（rotom 等）は最終 fallback 対象、を SKILL か references の「実証」記述に一文追加し、次レギュ（M-B）investigation での手戻りを防ぐ。
- **フォルム slug マッピングの SoT 化を検討**: Serebii フォルム URL ↔ PokeAPI slug の差異（rotom-wash 等）を references の slug 正規化節へ既知ケースとして列挙し、層1 `fetch-serebii` のフォルム解決 or 手動 slug 表で吸収する余地を Phase 7（全186種投入）前に判断する。
- **harness PR テンプレに「plan phase 実装」レーンを設けるか判断**: learning 採用駆動でない plan-phase 実装 PR 用に、harness.md の「採用根拠/対象 learning」節を任意化する注記、または phase 実装向けの軽量チェック節を追加するか harness-meta で検討する。
- **finish-phase の README 進捗を feature PR 同梱する運用の徹底**: オーケストレーター主導マージ時に README チェック更新を feature ブランチへ同梱できるよう、Phase 4 セルフ検証で「README 進捗を同梱したか」を確認項目化する案を検討。

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit |
| カバレッジ | 100% | 100% | ±0 | Vitest（427 stmts / 242 br / 90 fn / 355 lines） |
| Biome 違反 | 0 | 0 | ±0 | biome check（83 files） |
| CI | pass | pass | — | server-side pnpm verify（auto-merge で緑確認） |
| 差分 files/行 | — | 2 files | +232/-198 | SKILL.md + references/serebii-sourcing.md |
| end-to-end 実証 | — | 26 種 25 ok / 1 escalated | — | 層1 決定論経路・HTML を LLM に載せず exit code 判定 |

## 🤖 ハーネス改善提案

<!-- harness-meta が parse する正規構造。未処理は [ ]、採用後に [x] + PR リンク。 -->

- [x] `[skill]` `survey-regulation` の実証記述にネットワーク前提（ライブ fetch 必須）・GNU `timeout` 非前提・フォルム種は最終 fallback 対象、を一文追記する。 → **採用** PR #109（serebii-sourcing.md 新節）
- [x] `[skill]` Serebii フォルム URL ↔ PokeAPI slug 差異（rotom-wash 等）を `references/serebii-sourcing.md` の slug 正規化節へ既知ケースとして列挙し、Phase 7 前に層1 フォルム解決の要否を判断する。 → **採用** PR #109
- [ ] `[template]` harness.md PR テンプレに「plan phase 実装」レーン（採用根拠/対象 learning 節の任意化注記 or 軽量チェック節）を設けるか harness-meta で検討する。
- [ ] `[skill]` `implementation-workflow` の Phase 4 セルフ検証に「README 進捗を feature PR へ同梱したか」の確認項目を加える案を検討する（オーケストレーター主導マージでの同梱漏れ防止）。

## 📝 harness-meta フィードバック

<!-- 空でも見出しは維持する。harness-meta が後から各表に追記する。 -->

### 採用

<!-- 採用した提案と採用先 PR リンク -->

### 見送り

<!-- 見送り理由（harness-meta-criteria.md の見送り基準に対応） -->

### 保留

<!-- 保留理由 -->
