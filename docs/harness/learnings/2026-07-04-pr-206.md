---
id: learning-pr-206
title: PR #206 レトロスペクティブ (plan10 Phase 6 全件名辞書の基盤・skill rename + workflow + generate superset 緩和 + ADR refine)
type: learning
status: draft
related_pr: 206
related_plan: 10-showdown-first-data/phase-06
generated_at: 2026-07-04T18:09:32Z
generator: pr-retrospective skill
---

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/206 / commit fd1a153b74837c5d662fac2e347c37b4fb098810 / merge 2026-07-04T18:09:32Z / 差分 19 files +411/-172。languages を reg 非依存の全件名辞書にする基盤 = `fetch-pokeapi` 全件化（list endpoint 列挙 + types + en）・`generate` の名前突き合わせを bijection→superset 緩和・`pokeapi-names.yml` workflow 新設・`update-catalog`→`author-static-data` リネーム（cross-agent パリティ）・ADR 0041（0035 の id 集合一致不変条件を refine・非 supersede）。code/harness-review とも blocking 0、non-blocking を即反映してマージ。

## ✅ Keep（継続したいこと）

- **generate 緩和を「orphan チェックのみ」に厳密限定し保護を残した**: `requireNames` から orphan throw だけを撤去し、spec の名前欠落（`!n`）/ ja・en 欠け（`!n.ja || !n.en`）の非0終了保護を維持。ADR 0041 に「限定的な例外・過剰緩和しない」根拠を明記し、検証機構不変（ADR 0039 安全弁）を最小の穴で通した。code-review も「厳密一致」と判定。
- **緩和・全件化を実機で三点確認してからマージ**: orphan 追加で generate exit 0 / spec 名前削除で exit 1 / `sync:ja-names` 再実行 no-op を実際に走らせて受け入れ基準を裏取り（doc 主張でなく実測）。plan 09 系の「失敗コマンドを 1 回実測」運用（#186）と同じ姿勢。
- **skill rename の cross-agent パリティを一対で追従 + frozen history を保護**: canonical `git mv` + 旧 symlink 削除 + 新 symlink 追加を同一 PR で行い `ls .agents/skills` = `.claude/skills` を確認。inbound は live（rule / skill / AGENTS / data README / design doc）を追従し、learnings / completed / archive ADR の歴史言及は凍結。完了 plan doc の 404 化リンクは「脱リンク + 当時記述明記」で処理（learning #174 の既定対応を踏襲）。
- **ADR refine を supersede と区別して処理**: 0035 の中核（3 軸直交・名前 SoT=languages）は不変ゆえ 0035 を Accepted 据え置き・本文 diff ゼロにし、派生不変条件（bijection）だけを 0041 が上書き。[[adr]]「Accepted 本文不変」に反さず、harness-review も規約準拠と判定。「決定の本質 vs 派生不変条件」を切り分けて supersede/refine を選ぶ判断が再現できた。
- **Generator/Evaluator 独立を 2 系統で並行実行**: src/scripts=code-review・ハーネス資産=harness-review を別 Agent で並行起動し、双方 blocking 0。non-blocking（list count assert / codegen ヘッダ）を即コミットして純改善で締めた。

## ⚠️ Problem（詰まった点・制約）

- **受け入れ基準「`git grep update-catalog` = 0」が文字どおり達成不能**: learnings / completed / archive ADR に歴史言及（リネーム文脈・当時記述）が正当に残るため、全消去は frozen history の falsify になる。実質基準は「live dangling ゼロ + 履歴凍結」で、phase doc の基準文言が過剰だった（harness-review も nit 指摘）。全消去型の grep 基準は rename phase で毎回この乖離を生む。
- **coverage 除外ファイルのヘッダが diff 外で陳腐化していた**: `src/codegen/materialize.ts` の「ja 専任」ヘッダが実態（ja/en）とずれたまま。plan 10 Phase 2（#191）で既に「coverage 除外死角のまま」と指摘された系の反復で、名前系 PR で除外ファイルのコメント追従が漏れやすい。
- **`listAllIds` 初版が全件受信を検証していなかった**: `!res.ok` のみ fail-fast で、200 だが `results` 不足（`limit` cap 等）を検出せず、doc の「全件列挙 fail-fast」保証を満たしていなかった（レビューで `count` == `results.length` assert を追加）。「全件取得」系は総数照合を初版から入れるべきだった。
- **設置 ≠ 稼働（ネットワーク検証がローカルで完結しない）**: `pokeapi-names.yml` の実 dispatch・全件取得はネットワーク必須で Phase 6 スコープ外（仕組み検証まで）。差分・冪等ロジックはコードレベルの確認に留まり、実データ挙動は Phase 7 / CI に委ねる前提を明示する必要があった。

## 🚀 Try（次回の改善案）

- **rename phase の受け入れ基準は「全消去 grep」でなく「live dangling ゼロ + 履歴凍結」で書く**: phase doc / planning の insert-renumber チェックリストに、`git grep <old> = 0` 型の基準を避け「live 参照追従 + learnings/completed/archive は凍結」と表現する指針を一文で追記。
- **「全件取得」系スクリプトは総数照合を初版から入れる**: `author-static-data` skill / data-pipeline rule に、list endpoint 全件取得は `count` と受信数の一致を fail-fast する（差分判定の前提保証）ことを一文で明記。
- **skill rename / 用途変更 PR で coverage 除外ファイル（`src/codegen/*.ts` 等）のヘッダ陳腐化を点検対象にする**: harness-review or code-review checklist に「除外ファイルのドキュメントコメントが責務変更に追従しているか」を加える（#191 反復の解消）。

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit |
| カバレッジ | 100% | 100% | ±0 | Vitest（182 tests・scripts は coverage 対象外） |
| Biome 違反 | 0 | 0 | ±0 | biome check |
| CI | pass | pass | — | server-side pnpm verify（21s） |
| 差分 files/行 | — | 19 files | +411/-172 | gh pr diff #206 |

## 🤖 ハーネス改善提案

- [x] `[rule]` `planning`（insert/renumber チェックリスト）: rename/廃止 phase の受け入れ基準を「`git grep <old> = 0`」型で書かず「live dangling ゼロ + learnings/completed/archive 凍結」で表現する指針を追記（frozen history との乖離を毎回生むのを防ぐ）。
- [x] `[skill]` `author-static-data`: list endpoint 全件取得は `count` と受信数の一致を fail-fast する（差分・冪等判定の前提保証）ことを手順に一文で明記。
- [x] `[rule]` `data-pipeline`: list endpoint 全件取得は総数（`count`）と受信数の一致を fail-fast する前提を記す（`author-static-data` と対）。
- [x] `[skill]` `harness-review` / `code-review`: skill rename・用途変更 PR で coverage 除外ファイル（`src/codegen/*.ts` 等）のヘッダ / ドキュメントコメントが責務変更に追従しているかを点検対象に加える（#191 の「除外死角」反復の解消）。

## 📝 harness-meta フィードバック

### 採用

- 4 提案すべて採用 → PR #212（`harness/plan10-retro-learnings`）。planning（rename 基準 wording）・author-static-data / data-pipeline（count fail-fast）・code-review / harness-review checklist（coverage 除外ヘッダ点検・#191→#206 反復）。

### 見送り

### 保留
