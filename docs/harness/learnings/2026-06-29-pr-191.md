---
id: learning-pr-191
title: PR #191 レトロスペクティブ (PokeAPI を ja 専任へ縮小・構造取得廃止)
type: learning
status: draft
related_pr: 191
related_plan: 10-showdown-first-data/phase-02
generated_at: 2026-06-29T08:00:00Z
generator: pr-retrospective skill
---

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/191 / commit 229ccb246f6da3f1661a8f1d7f684b4ac91d3d5d / merge 2026-06-28T22:34:42Z / 差分 6 files +172/-461。plan 10 Phase 2 = 構造取得を Phase 1 の pokemon-showdown 経路へ委ね、PokeAPI を日本語名 ja 取得専任へ縮小。`fetch:data`→`fetch:ja-names` / `materialize`→`sync:ja-names` 改名、`extractSpeciesStructural`/`extractItemCategory` 等を削除、update-catalog skill を ja 専任へ改訂。

## ✅ Keep（継続したいこと）

- **構造系コードを死蔵させず削除した**: `extractSpeciesStructural` / `extractItemCategory` / `STAT_KEY` / raw 構造型を残さず除去し、ja 系（`extractJaName` / `extractEnName` / `extractNames` / `planFields`）のみへ縮小。責務縮小 PR で「使わないが残す」を避け diff が -461/+172 と純減になった。
- **4 データセットを 1 関数へ一般化**: `backfillNames(file, mapKey, category, extract, needs)` へ `extract`/`needs` を注入し species/items/moves/abilities を 1 ループに集約。fetch 段も `DATASETS` 配列で同型化し、データセット軸（showdown と対称）が読み取りやすい。
- **受け入れ基準を機械的に確認した**: `sync:ja-names` が raw 不在で no-op（filled 0）・`git diff data/champions/*-specs.yaml` 空・`generate:data` 緑・`check:regulation` 整合を実行ログで確認し、「構造 specs に差分が出ない」を実証してから PR を出した。
- **phase スコープを厳守し越境改訂を後続へ委譲**: data-pipeline.md（Phase 5）/ survey-regulation 廃止（Phase 4）/ ADR 0039-0040（Phase 3/4）を本 PR で触らず、phase doc のスコープ（scripts/codegen + update-catalog + package.json）に限定。レビュー双方が「スコープ外として blocking にしない」と判断できた。
- **独立レビューを対象別に 2 系統並列実行**: ソース（code-review）と skill（harness-review）を別 Agent で並列起動し、双方 blocking 0 を PR コメントに記録。レビュー文脈に phase 情報（旧 script 名残置は後続 phase 追従）を渡し誤検出を抑えた。
- **正当な nit を著述ファイル内で即是正**: `DATASETS` コメントの「5 軸」不正確さ（types 不取得に未言及）を自分の著述ファイルなので follow-up commit で是正し、再レビュー往復を省いた。

## ⚠️ Problem（詰まった点・制約）

- **npm script 改名が他ソースのコメントを陳腐化させた**: `fetch:data`→`fetch:ja-names` / `materialize`→`sync:ja-names` 改名で、`scripts/serebii-to-catalog.ts:21` と `src/codegen/serebii/catalog-fields.ts:50` のコメントが存在しない script 名を指す状態に。これらは Phase 4（serebii 廃止）の追従範囲だが、改名 PR 時点で「docs だけでなく `scripts/**`/`src/**` コメントまで grep する」観点が必要だった。
- **`src/codegen/materialize.ts` が coverage 除外パスのまま**: `src/codegen/*.ts` は coverage.exclude 対象（learning #73 の既知ギャップ）。`planFields`/`extractJaName` 等の判断分岐を持つ純関数が 100% ゲートの死角に残る。phase doc がパス維持を指示したため本 PR では動かさなかったが、testing.md の「除外パスへ意味あるロジックを置かない」と緊張が残る。
- **fetch がすべて単発 best-effort に統一された**: 旧構造取得の 3 回リトライが消え、一過性 5xx も warn+skip。ja が silent partial fill になりうる（最終的に `generate:data` が hard-fail するため安全側だが、再実行前提の設計が暗黙）。
- **dataset 対応が fetch 段と transcribe 段で二重定義**: `(file, mapKey, category, ja/ja-en 別)` の対応が `fetch-pokeapi.ts` と `materialize.ts` に重複。別スクリプトゆえ分離自体は自然だが追加時の片肺更新リスクが残る（scripts は coverage 除外で test も無い）。

## 🚀 Try（次回の改善案）

- **npm script 改名 PR の追従チェックを「全文 grep（docs + コード comment）」へ拡張**: 改名前後の script 名を `git grep` でリポジトリ全体（`*.ts` のコメント含む）走査し陳腐化を点検する観点を code-review checklist の「正本変更時の全文同期」へ明示（→ `[rule]`）。
- **coverage 除外と純関数の緊張を testing.md / phase 計画で先に解消**: `src/codegen/materialize.ts` のような判断分岐純関数を 100% ゲートへ載せる（serebii と同様 `src/codegen/*.ts` 除外を狭める or サブツリー移設）方針を、後続の data-pipeline 改訂 phase（Phase 5）で扱うか learning #73 と束ねて harness-meta で判断（→ `[rule]`/`[skill]`）。
- **取得スクリプトのリトライ方針を一語明文化**: 「404=正常 skip / 5xx・ネットワーク=リトライ後 warn」の二段方針を採るか、現行の単発 best-effort + `generate:data` hard-fail 前提を許容するかを data-pipeline.md の取得段記述で明示（→ `[rule]`）。

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit |
| カバレッジ | 100% | 100% | ±0 | Vitest（除外パス据え置き） |
| Biome 違反 | 0 | 0 | ±0 | biome check |
| CI | pass | pass | — | server-side pnpm verify |
| 差分 files/行 | — | 6 files +172/-461 | 純減 -289 | gh pr diff |

## 🤖 ハーネス改善提案

- [ ] `[rule]` `code-review.md` の AI 生成コード観点「正本変更時の全文同期」に、**npm script / 識別子の改名 PR は改名前後の名前を `git grep` でリポジトリ全体（docs だけでなく `scripts/**`/`src/**` のコメント含む）走査して陳腐化参照を点検する**を追記（PR #191 で serebii 系コメントが旧 script 名残置になった反復防止）。
- [ ] `[rule]` `testing.md` の「除外パスへ意味あるロジックの純関数を置かない」観点に沿って、`src/codegen/materialize.ts`（`planFields`/`extractJaName` 等の判断分岐純関数）を 100% ゲートへ載せる（`src/codegen/*.ts` 除外を狭める or サブツリー移設）方針を learning #73 と束ねて harness-meta で採否判断。
- [ ] `[rule]` `data-pipeline.md`（Phase 5 の本格改訂時）に **PokeAPI 取得段のリトライ方針**（404=正常 skip / 5xx・ネットワーク=リトライ or 単発 best-effort + `generate:data` hard-fail 前提）を一文明文化する。

## 📝 harness-meta フィードバック
### 採用
### 見送り
### 保留
