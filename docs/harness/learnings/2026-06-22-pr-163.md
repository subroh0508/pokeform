---
id: learning-pr-163
title: PR #163 レトロスペクティブ (レビュー skill 共通フレームを code-review rule へ抽出)
type: learning
status: actioned
related_pr: 163
related_plan: 07-rules-skills-simplify/phase-02
generated_at: 2026-06-22T16:53:02Z
generator: pr-retrospective skill
---

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/163 / commit 1c04030f2865e20a7a1c8bdedd5c43f8dcb76a43 / merge 2026-06-22T16:53:02Z / 差分 6 files +53/-87
> plan07 Phase 2。`code-review` / `harness-review` skill の逐語 5 ステップ手順と auto-merge ゲートを `code-review.md` rule の「共通レビュー手順」「auto-merge ゲート」節へ抽出し DRY 化。両 skill は対象範囲・checklist・差分 3 点に圧縮、references は rule へのポインタ化。description（trigger）は不変で住み分け精度を維持。

## ✅ Keep（継続したいこと）

- **共通フレームを rule に一本化し skill を差分集中に圧縮**: 両 skill の逐語 5 ステップ（diff 収集 → paths 絞込 → checklist 評価 → 重大度 → PR コメント）を `code-review.md` rule の「共通レビュー手順」へ抽出。skill 本文は「対象範囲 / 観点 checklist / auto-merge ゲート」の差分 3 点のみに減り、+53/-87 の純減（差分の正味削減 = 重複削減）を達成。
- **抽出粒度の判断が的確**: 「手順骨格（両 skill で同一の 5 ステップ）」を共通化し、「対象 paths / checklist / 最重要観点（harness は cross-agent 整合）」を skill 側に残した。共通化しすぎて skill の個性（trigger 住み分け・観点の重み）を潰さない粒度を選べている。
- **SoT 一元化で auto-merge ゲートの二重持ちを解消**: 旧構造では両 checklist が auto-merge 条件 2 つを逐語で重複保持していた。rule へ一本化し references は要約ポインタ（「CI 緑 + ブロッキング 0」+ リンク）のみとし、ドリフト面を縮小。
- **description 不変で trigger 精度を保持**: src 用 / ハーネス用の住み分け文言を一切変更せず（diff に `description:` が現れない）、共通化のリファクタが trigger の核を壊していない。
- **dangling ゼロのリンク張り替え**: SKILL.md → `../../rules/code-review.md`、checklist → `../../../rules/code-review.md`、ADR 0017 への相対パスを全て実ファイルで解決。抽出に伴う参照の付け替えを漏れなく完了。
- **stale な plan 参照の同時除去**: 抽出ついでに references 内の `phase-03 doc` / `Phase 11` の可変 plan 参照を除去。不変であるべき手順 SoT から陳腐化しやすい phase 番号を抜いた（ADR 参照のみ残置）。
- **独立レビューが受け入れ基準 (a)-(e) を逐条で検証**: harness-review が description 不変・SoT 一本化・dangling ゼロ・auto-merge 条件の保持・wikilink 解決を機械的根拠付き（grep 0 hit / 相対パス照合）で確認し、ブロッキングなしで通した。

## ⚠️ Problem（詰まった点・制約）

- **rule への手順抽出が「機械ゲート非再実行」原則と隣接して肥大化リスク**: `code-review.md` rule は既に基準・原則・出力先・effort・redaction を持ち、そこへ手順 5 ステップ + auto-merge ゲートが加わった。rule が「規約 SoT」と「手順 SoT」を兼ねる形になり、将来 rule 単独の見通しが悪くなる懸念がある（今回は許容範囲だが境界が曖昧）。
- **`[[implementation-workflow]]` wikilink が後続フェーズ依存**: auto-merge ゲート節が「予約は implementation-workflow が担う」と参照するが、本 plan の時点で当該 skill/rule の成熟度は phase 進行に依存。参照先の解決は確認済みだが、概念参照の前方依存が残る。
- **`/tmp/<prefix>-review.md` の prefix 一意性が暗黙**: 共通手順 step 5 の固定プレースホルダ表記は、同一マシンで連続レビューすると上書きの可能性（nit 指摘）。実運用は呼び出し側が一意 prefix を付ける前提で、手順 SoT には明記されていない。
- **抽出の正しさが逐語照合に依存**: 「skill から逐語ブロックが消え rule に同内容が移ったか」「auto-merge 条件が緩んでいないか」の検証が grep / 目視照合に依存。意味を保ったまま移送できたかは機械ゲートでは捕れず、レビュアーの逐条確認頼み。
- **README 進捗更新を同 PR に同梱**: Phase 2 を `[x]` に更新する README 変更を本 PR に含めた。進捗チェックの更新は finish-phase 責務でもあり、どの PR で進捗を更新するかの規約が暗黙（同梱の是非が PR 間で揺れうる）。

## 🚀 Try（次回の改善案）

- **rule 内の「規約 SoT」と「手順 SoT」の見出し階層を明示**: `code-review.md` rule で「レビュー基準・原則（不変規約）」と「共通レビュー手順（手続き）」を視覚的に区切る節構造（例: `## 規約` / `## 手順`）を導入し、肥大化時の見通しを保つ。→ `[rule]`
- **共通手順 step 5 の出力ファイル名一意性を明文化**: `/tmp/<prefix>-review.md` の prefix は PR/skill 単位で一意にする旨を `code-review.md` rule の step 5 に一言追記する。→ `[rule]`
- **skill 共通フレーム抽出パターンを skill-authoring に汎用 tip 化**: 「複数 skill が同一手順骨格を持つとき、骨格を rule に抽出し skill は差分（paths/checklist/最重要観点）に集中させる」抽出粒度の判断指針を `skill-authoring.md` rule に Gotcha として残し、Phase 4/5 の他 skill 圧縮で再利用する。→ `[rule]`
- **進捗 README 更新を実装 PR に同梱する規約を明文化**: phase 実装 PR で対応 README の `[x]` 更新を同梱するのが標準か、finish-phase が別途行うかを `planning.md` か `finish-phase` skill に明記し PR 間のブレを無くす。→ `[skill]`

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit |
| カバレッジ | 100% | 100% | ±0 | Vitest |
| Biome 違反 | 0 | 0 | ±0 | biome check |
| CI | pass | pass | — | server-side pnpm verify |
| 差分 files/行 | — | 6 files | +53/-87 | gh pr diff |

## 🤖 ハーネス改善提案

- [ ] `[rule]` `code-review.md` rule の「規約・原則（不変）」と「共通レビュー手順（手続き）」を見出し階層で視覚的に区切り、手順 SoT 兼任による肥大化の見通し低下を防ぐ。
- [ ] `[rule]` `code-review.md` rule の共通手順 step 5 に「`/tmp/<prefix>-review.md` の prefix は PR/skill 単位で一意にする」を追記し、連続レビュー時の出力上書きを防ぐ（nit 由来）。
- [ ] `[rule]` `skill-authoring.md` rule に「複数 skill が同一手順骨格を持つとき、骨格を rule へ抽出し skill は差分（対象 paths / checklist / 最重要観点）に集中させる」抽出粒度の判断指針を Gotcha として追加し、Phase 4/5 の他 skill 圧縮で再利用する。
- [ ] `[skill]` phase 実装 PR に対応 README の `[x]` 進捗更新を同梱するのが標準か finish-phase が別途行うかを `planning.md` か `finish-phase` skill に明記し、PR 間の進捗更新タイミングのブレを無くす。

## 📝 harness-meta フィードバック
### 採用
（なし。本 learning の提案は下記理由で見送り）

### 見送り
- `[rule]` code-review.md の規約/手順の見出し階層区切り → 現状可読で構造的問題なし。単発の organization nit のため見送り。
- `[rule]` step5 prefix 一意化の追記 → nit。連続レビューの実害が観測されてから対処で十分。
- `[rule]` skill-authoring に骨格抽出粒度 Gotcha → phase-02（PR #163 自身）で code-review.md への共通フレーム抽出として**実例実現済**。明文化の限界価値が低く見送り（必要時に再評価）。
- `[skill]` README `[x]` 同梱が標準か明記 → **既出**。`implementation-workflow.md`（Phase 4 セルフ検証 / Phase 8）が「README 進捗を feature PR に同梱」を、`finish-phase` 手順4 が二層更新の SoT を既に規定済。

### 保留
