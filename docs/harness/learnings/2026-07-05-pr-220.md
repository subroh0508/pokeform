---
id: learning-pr-220
title: PR #220 レトロスペクティブ (plan 11 起票 + plan 10 completed 集約 + cross-plan move)
type: learning
status: draft
related_pr: 220
related_plan: 11-static-restore-hardening
generated_at: 2026-07-05T08:11:34Z
generator: pr-retrospective skill
---

<!--
この雛形は `.claude/rules/retrospective-format.md`（SoT）に準拠する。
見出し（絵文字・文言）は harness-meta が parse するため変更しないこと。
書き出し前に `.claude/rules/redaction.md` の正規表現で Secrets / PII を [REDACTED-*] 置換すること。
-->

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/220 / commit 1429e5d14ac4 / merge 2026-07-05T08:11:34Z / 差分 21 files +272/-33。author-static-data 動作確認で判明した課題を計画群 11-static-restore-hardening（Phase 1-3）として起票。plan 10 Phase 9（全件本投入）を plan 11 Phase 3 へ cross-plan move、plan 10 を completed/ へ集約。broken main 下で roadmap 整理を先行（赤 PR 許容をユーザー合意）。

## ✅ Keep（継続したいこと）

- **動作確認の発見を計画群へ体系化した**: 「3 前提 + 非 third-party 化」を場当たり修正でなく plans-new 経由で OVERVIEW→6 基準分割し、Phase 1(機構)/Phase 2(PR 作成)/Phase 3(全件投入) の直列計画にした。実装前に構造を固めた。
- **cross-plan move を planning チェックリストに沿い機械追従した**: plan 10 Phase 9→plan 11 Phase 3 の `git mv` + 見出し更新 + 移動元/移動先双方への移動注記、completed 集約の相対リンク深度 +1 補正、completed/09 の旧「10 Phase 9」参照 9 箇所の張り替えを全て追い、`python` によるリンク解決チェックで新規 dangling ゼロを確認。
- **broken main でも plan-first を維持した**: 撤去 PR #218 で main が赤の中、roadmap 整理を先行 PR に切り出し（赤 PR 許容をユーザーに確認）、計画→実装の順序を守った。
- **freezing 判断を正しく適用した**: learnings / completed 内の歴史記述は凍結し、dangling 化するクリッカブルリンク（phase-09→phase-03 移動 / plan 10 パス変更）のみ張り替えた。「番号が別 phase を誤指しする場合は凍結せず是正」を rotom 本投入の chain（09→10→11）に適用。
- **harness-review（blocking 0）で番号整合・テンプレ準拠・リンク実在を独立点検**し、mermaid の orphan ノード除去・ロールアップ整合を確認してからマージした。

## ⚠️ Problem（詰まった点・制約）

- **cross-plan move が 09→10→11 と多段化し、参照張り替えが連鎖した**: 本投入 phase が plan 09 Phase 4 → plan 10 Phase 9 → plan 11 Phase 3 と 3 度移動し、completed/09 の forward 参照 9 箇所を張り替える必要が生じた。#200 の「中間 renumber 累積コスト」と同根で、移動のたびに inbound 参照面が広がる。
- **broken main が plan doc すら緑 merge できなくした**: roadmap doc のみの PR でも main の撤去状態を継承して verify 赤。required status checks 未設定ゆえマージは可能だったが、「赤 PR を先行マージするか / Phase 1 に bundle するか」の判断をユーザーに委ねる必要が生じた（環境要因が計画運用に波及）。
- **plan 10 の完了 phase docs（phase-08）内の「Phase 9」forward 参照は凍結扱いにした**が、Phase 9 が plan 10 から消えたため、厳密には「存在しない phase への言及」が残る。README/OVERVIEW の移動注記で補ったが、frozen prose と現状の乖離は残る。

## 🚀 Try（次回の改善案）

- planning rule の「移動 / 廃止 phase 番号を再利用しない + 末尾採番」を再確認する（多段 cross-plan move の誤指し防止・既存規約の反復徹底）。
- cross-plan move / completed 集約後の dangling 検証を機械化する導線（リンク解決チェックスクリプト）を harness の検証ステップに常設する候補（今回は都度 python one-liner で確認）。

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | fail(broken main) | fail(broken main) | — | roadmap doc のみ・緑化は Phase 1 |
| カバレッジ | n/a | n/a | — | doc 変更 |
| Biome 違反 | 0 | 0 | ±0 | biome check |
| CI | fail(broken main) | fail(broken main) | — | 緑化は後続 Phase 1（ユーザー合意の赤先行） |
| 新規 dangling | 0 | 0 | ±0 | roadmap md リンク解決チェック |
| 差分 files/行 | — | 21 files +272/-33 | — | plan 11 新設 + plan 10 rename + 参照追従 |

## 🤖 ハーネス改善提案

<!-- harness-meta が parse する正規構造。未処理は [ ]、採用後に [x] + PR リンク。 -->

- [ ] `[skill]` planning / harness-review: cross-plan move / completed 集約 PR の受け入れに「リンク解決チェック（相対 md リンクが実在ファイルへ解決）」を機械ステップとして明記（都度 one-liner でなく定型化）。
- [ ] `[rule]` planning: 本投入 phase のように 3 度移動した対象の「移動 chain」を plan doc に 1 行で残す運用を追認（09→10→11 の追跡可能性）。
- [ ] `[skill]` plans-new: broken main（先行 PR で壊れた状態）下で計画 doc を起こすとき「赤 PR 先行 / bundle / 復元先行」の選択肢を提示する導線を検討（環境要因の定型化）。

## 📝 harness-meta フィードバック

<!-- 空でも見出しは維持する。harness-meta が後から各表に追記する。 -->

### 採用

<!-- 採用した提案と採用先 PR リンク -->

### 見送り

<!-- 見送り理由（harness-meta-criteria.md の見送り基準に対応） -->

### 保留

<!-- 保留理由 -->
