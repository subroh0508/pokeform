---
id: learning-pr-44
title: PR #44 レトロスペクティブ (02 Phase 3 M-A情報源確定 + survey-regulation skill + 20匹サンプル)
type: learning
status: draft
related_pr: 44
related_plan: 02-data-model-redesign/phase-03
generated_at: 2026-06-07T17:20:36Z
generator: pr-retrospective skill
---

<!--
この雛形は `.claude/rules/retrospective-format.md`（SoT）に準拠する。
見出し（絵文字・文言）は harness-meta が parse するため変更しないこと。
書き出し前に `.claude/rules/redaction.md` の正規表現で Secrets / PII を [REDACTED-*] 置換すること。
-->

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/44 / commit 5b3ecb2 / merge 2026-06-07T17:20:36Z / 差分 10 files +996/-2。
> 02-data-model-redesign Phase 3。M-A 解禁情報を Game8（権威・186種）+ Serebii（全リスト）で突き合わせ doc 化、
> 解禁情報取得を survey-regulation skill に切り出し（canonical+symlink）、無作為20匹を catalog/per-reg へ投入し
> 新構造を end-to-end 検証。code-review / harness-review とも blocking 0。

## ✅ Keep（継続したいこと）

- **単一ソース依存を避け突き合わせた**: Serebii 自動抽出の「391種」誤カウントを、Game8 の権威的 186 種 + 検索コンセンサスで是正。矛盾と採用根拠を doc に明記し、判断過程を追跡可能にした。
- **既存データが情報源で裏取りされた**: 未解禁デモ種 mewtwo/salamence/metagross が M-A リストに非在であることを確認でき、現行の「未解禁」設定の正しさが情報源側から補強された。
- **解禁情報取得を再利用 skill 化**: survey-regulation を canonical + `.agents` symlink（mode 120000）で作成。WebSearch 調査→突き合わせ→doc→catalog/per-reg 反映を定型化し M-B 以降で再利用可能にした。機械ゲートは verify/generate へ委譲。
- **abilities を raw から機械収集**: 20匹追加時、`data/raw/pokemon/<slug>.json` の abilities を node で集約して catalog/abilities.yaml へ漏れなく追記。catalog 整合 throw を未然に回避（skill にも手順化）。
- **再現可能な決定論的サンプル選定**: 「無作為」より「再現可能」を優先し、単一フォルム最終進化・既存除外・昇順抽出の手順を doc 化。20匹を後から検証できる形にした。
- **多面的な end-to-end 検証**: ①runtime（regulationDex に20匹・未解禁除外）②型レベル（ConstrainParty が新種族を解禁判定・mewtwo を @ts-expect-error で弾く）③CLI 回帰（standard/broken）を独立に確認。
- **append-only を実データで守った**: abilities.yaml は base 16→44 の superset（削除0）。レビューが superset 性を独立検証。

## ⚠️ Problem（詰まった点・制約）

- **abilities.yaml 更新と fetch の順序ミス**: 先に fetch:data → 後で abilities.yaml に新特性追記、の順だったため初回 generate が `ENOENT: ability/anger-point.json` で失敗。fetch を再実行して解消。catalog 追記 → fetch → generate の順序が要。
- **skill allowed-tools の不足**: survey-regulation の allowed-tools に手順で必要な Read/Write/Edit が無く、code-review が non-blocking 指摘。skill は WebSearch だけでなく doc/YAML 書込も行う。
- **WebFetch の件数誤カウント**: 小型モデル抽出で Serebii が 391 種と出た。全リストの厳密確定（特に複数フォルム・メガ59種）は Phase 4 へ送らざるを得なかった。
- **worktree raw 再 fetch（再々発）**: Phase 1/2 と同様、worktree に raw が無く fetch:data が必要。learning #40/#42 の改善提案が未反映で 3 度目の発生。
- **20匹サンプルの moves が sparse**: 既存 catalog 技との積集合のため新種族の moves はほぼ空。構造検証には十分だが、実用的な解禁技網羅は Phase 4。

## 🚀 Try（次回の改善案）

- **survey-regulation skill に「catalog 追記 → fetch → generate の順序」を強調済み**だが、abilities は fetch 後に raw から集めて追記する 2 段が要る点を skill Gotchas で更に明確化する。
- **worktree raw 手順の implementation-workflow.md 反映を harness-meta で必ず採用**（#40/#42/#44 で 3 回反復・採用基準①「複数 PR 反復」を満たす）。
- **新規 skill の allowed-tools は手順で使うツールから逆算**するチェックを skill-creator/harness-review 観点に追加（書込が要る手順 skill で read-only にしない）。

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit |
| カバレッジ | 100% | 100% | ±0 | Vitest 70 tests |
| Biome 違反 | 0 | 0 | ±0 | biome check（63 files） |
| CI | pass | pass | — | server-side pnpm verify（auto-merge） |
| 差分 files/行 | — | 10 files +996/-2 | +994 | gh pr diff（大半は doc + 生成物） |
| 生成種族数 | 10 | 30 | +20 | M-A 無作為20匹サンプル |
| レビュー blocking | — | 0 | ±0 | code-review / harness-review 双方 |

## 🤖 ハーネス改善提案

<!-- harness-meta が parse する正規構造。未処理は [ ]、採用後に [x] + PR リンク。 -->

- [x] `[rule]` `implementation-workflow.md` Phase 0 に「worktree は data/raw 非共有。メイン側 raw を cp するか fetch:data を回す」を反映（#40/#42/#44 で 3 回反復・**優先採用**）。→ 採用（#40/#42/#44/#47 で 4 回反復・harness-meta PR `harness/meta-workflow-raw`）。
- [ ] `[skill]` `skill-creator` / harness-review 観点に「手順 skill の allowed-tools は本文で使うツール（Write/Edit/WebSearch 等）から逆算して過不足なく宣言する」を追記。
- [x] `[skill]` `survey-regulation` の Gotchas に「abilities は fetch:data 後に data/raw から集約して catalog へ追記する（catalog 追記→fetch→集約追記→generate の順）」を明確化。→ 採用（SKILL.md の abilities Gotcha に (1)catalog追記→(2)fetch:data→(3)raw から集約追記→(4)generate の順序を明記・harness-meta PR `harness/meta-workflow-raw`）。

## 📝 harness-meta フィードバック

<!-- 空でも見出しは維持する。harness-meta が後から各表に追記する。 -->

### 採用

- `[rule]` worktree の `data/raw` 非共有 → `implementation-workflow.md` Phase 0 に追記（4 回反復の最優先候補）。harness-meta PR `harness/meta-workflow-raw`。
- `[skill]` survey-regulation の abilities 集約順序 → SKILL.md の abilities Gotcha に「(1)catalog 追記→(2)`fetch:data`→(3)`data/raw` から abilities 集約追記→(4)`generate:data`」の順序を明記。

### 見送り

<!-- 見送り理由（harness-meta-criteria.md の見送り基準に対応） -->

### 保留

<!-- 保留理由 -->
