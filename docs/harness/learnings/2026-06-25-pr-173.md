---
id: learning-pr-173
title: PR #173 レトロスペクティブ (inbound 参照張り替え + 旧 architecture.md 撤去)
type: learning
status: actioned
related_pr: 173
related_plan: 08-docs-restructure/phase-03
generated_at: 2026-06-26T00:00:00Z
generator: pr-retrospective skill
---

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/173 / commit 6a82a92937bab6631d8148ad9cfc70e7e5bd6d7b / merge 2026-06-25T16:43:26Z / 差分 41 files +62/-484。plan 08 Phase 3。旧 `docs/plan/01-mvp/architecture.md`（規約 spec 正本）の inbound 参照を知識種別ごとの SoT（規約=rules / 型・数式=src / 俯瞰=design）へ全張り替えしてから撤去し、design ↔ rule の二重管理を rule 側へ一本化。

## ✅ Keep（継続したいこと）

- **撤去前に inbound 参照を全走査して張り替え**: 架空になる正本（architecture.md）への参照を 41 ファイル横断で先に張り替えてから削除。learning #104 / #117 / #122 の「参照追従漏れ反復」を予防的に踏まえ、dangling ゼロを達成。
- **クリッカブルリンクと歴史言及を明確に区別**: `git grep '\](...architecture)'` でクリッカブル/import リンク = ゼロを確認し、完了 plan doc・learnings の散文（非リンクの歴史記録）は意図的に残置。falsify しない原則を守った。
- **active ADR 本文の機械的追従を「意味保存」に限定**: 削除パスへの「詳細は…」ポインタを `[[rule]]` / `src/` へ向け直すだけに留め、decision / 理由 / Alternatives を改変せず。[[adr]] が Accepted ADR に許す機械的整備の範囲内に収めた。
- **archive ADR を不触（diff ゼロ）**: archive 本文不変方針を守り、active のみ追従。harness-review が diff ゼロを確認。
- **二重管理の解消を構造的に**: rule の「正本は architecture.md」宣言を「本 rule が規約 SoT・実体は src・俯瞰は design」へ転換し、ADR 0038 の責務境界と一致させた。
- **cross-agent パリティを symlink で自動維持**: 6 skills を canonical 編集し `.agents/skills` 相対 symlink で自動反映、copy ドリフトなし。

## ⚠️ Problem（詰まった点・制約）

- **大量 inbound（41 files）の張り替えは漏れ検出が grep パターン依存**: クリッカブルリンク・`@import`・素の番号参照を別々のパターンで走査する必要があり、1 パターンの取りこぼしが dangling を生む。学習 #104/#117/#122 の反復領域。
- **「意図的に残す歴史言及」と「追従漏れ」の線引きが judgment**: 完了 plan doc・archive ADR・learnings の architecture.md 言及をどれも残すが、判断基準（クリッカブルか / 歴史記録か）を都度適用する必要があり機械化しにくい。
- **完了 phase doc に旧パス（architecture.md）が残置**: `phase-07-rules-and-claude-md.md:29,33` 等の受け入れ基準チェックリストに旧パスが残り、Phase 4 の `docs/plan`→`docs/roadmap` 改名でパス追従の要否判断が必要に（nit で先送り）。

## 🚀 Try（次回の改善案）

- inbound 参照の全張り替えを伴う撤去 PR 向けに、「クリッカブルリンク / import / 素の番号 / wikilink」を 1 コマンドで横断 grep する dangling 検出ヘルパを [[adr]] または harness-review-checklist の手順として固定する（learning #104/#117/#122 反復の機械化）。
- 「歴史言及ゆえ残置」の判断基準（クリッカブルリンクか・完了 doc/archive/learnings か）を [[code-review]] harness-review-checklist の「撤去・改名 PR の凍結対象」観点として 1 箇所に明文化する。
- 完了 phase doc に残る旧パスの棚卸しを Phase 4 改名時に一度実施する旨を引き継ぐ（nit 由来・実際に Phase 4 で凍結判断された）。

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit |
| カバレッジ | 100% | 100% | ±0 | Vitest（185 tests） |
| Biome 違反 | 0 | 0 | ±0 | biome check |
| CI | pass | pass | — | server-side pnpm verify 緑 |
| 差分 files/行 | — | 41 files +62/-484 | -422 | architecture.md 撤去で大幅減 |

## 🤖 ハーネス改善提案

- [x] `[skill]` inbound 全張り替えを伴う撤去 PR 向けに「クリッカブルリンク / import / 素の番号 / wikilink」横断 dangling 検出ヘルパ（1 コマンド）を harness-review 手順に固定（learning #104/#117/#122 反復の機械化）。
- [x] `[rule]` [[code-review]] harness-review-checklist に「撤去・改名 PR の凍結対象（クリッカブルか / 完了 doc・archive・learnings の歴史記録か）」の判断基準を 1 箇所明文化。
- [x] `[rule]` [[adr]] の「archive 番号への inbound 参照追従」手順に、`.md` / `.workflow` / `.ts` を横断する grep ワンライナを例示として補強（learning #104/#117/#122 由来）。

## 📝 harness-meta フィードバック

### 採用
- `[skill]` 横断 dangling 検出（クリッカブル/import/番号/wikilink）→ script 化でなく **grep ワンライナ**として harness-review-checklist「撤去・改名 PR の参照追従」観点 + adr.md archive 追従手順に明記（PR #179）。
- `[rule]` 撤去・改名 PR の凍結対象判断 → harness-review-checklist に同観点を追加（#174 と反復・PR #179）。
- `[rule]` adr.md archive 参照追従に grep ワンライナ例 → 追記（PR #179）。

### 見送り


### 保留
