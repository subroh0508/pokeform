---
id: learning-pr-170
title: PR #170 レトロスペクティブ (ADR 0038 配置・front matter 規約の起票)
type: learning
status: actioned
related_pr: 170
related_plan: 08-docs-restructure/phase-00
generated_at: 2026-06-26T00:00:00Z
generator: pr-retrospective skill
---

> **要約(5行以内)**: 対象PR https://github.com/subroh0508/pokeform/pull/170 / commit 0e211b0f64a3b1658ddf1cd49968d6076c857114 / merge 2026-06-25T15:50:48Z / 差分 3 files +105/-1。plan 08（docs 構成再編）Phase 0。後続の不可逆な配置・命名・front matter 規約を 1 本の ADR（0038）に先行確定し、design 新設 / roadmap 改名 / 責務境界を断定形で言い切って後続 phase の設計根拠とした。

## ✅ Keep（継続したいこと）

- **不可逆な決定を実装より先に ADR で固める**: design 新設・`docs/plan`→`docs/roadmap` 改名・front matter 規約という後戻りしにくい構造変更を、Phase 1 以降の実装に入る前に ADR 0038 1 本へ集約。後続 5 phase が同じ設計根拠を参照でき、phase ごとに方針が揺れない。
- **ADR 採番をアクティブ + archive 両走査で確定**: active 最大 0037 + archive 最大 0034 → max+1=0038 を `adr-new` skill が機械的に算出。退避番号の再利用を避ける [[adr]] 規約を skill が担保した。
- **可変 plan ファイル非参照を grep で自己検証**: `grep -nE 'phase-[0-9]+|Phase [0-9]+|OVERVIEW\.md'` hit なしを受け入れ基準に組み込み、[[adr]] の「不変ログが可変 plan を引かない」を機械確認した。
- **front matter スキーマ例を design / rules 双方ぶん本文に明記**: 後続 phase が実適用するときの体裁（`last_modified` + `adr` 配列・`"ADR NNNN"` 文字列形式）を ADR に固定し、phase 1-6 の付与作業が一貫した。
- **Alternatives を 4 案・却下理由つきで残した**: 配置の代替案を比較検討して捨てた根拠を ADR に保存し、半年後の再評価コストを下げた（[[adr]] の Alternatives Considered）。

## ⚠️ Problem（詰まった点・制約）

- **ADR 本文に改名前パス（`docs/plan/...`）が歴史的記述として残る**: decision #4 で改名を決めた当の ADR が本文で旧パスを言及するため、改名後は「決定時点の歴史記述」になる。harness-review が nit として指摘（ADR は不変ログゆえ正しいが、読み手が一瞬迷う構造）。
- **front matter `adr` 値の表記形式（`"ADR 0001"` 文字列 vs bare `"0001"`）が ADR 制定時に選択を迫られた**: 規約として一貫させたが、選択の是非は後続 phase の実運用まで検証できなかった。
- **plan phase 実装は learning 起点でないため harness-meta 採用基準が非該当**: PR テンプレの採用根拠チェックが「複数 PR 反復 / 人間明示」など learning 集約前提の項目で、計画駆動 PR では半分が N/A になり記入の意味が薄い。

## 🚀 Try（次回の改善案）

- 改名を決める ADR では、本文の旧パス言及に「（改名前の歴史的記述）」と一言添えるか、決定の主題以外の旧パス参照を最小化する運用を [[adr]] の用語 rename 例外節に補足する。
- 計画駆動（非 learning 起点）PR 向けに、PR テンプレの「採用根拠（harness-meta-criteria）」節を learning 起点 PR と区別し、計画 phase では受け入れ基準充足に寄せるテンプレ分岐を検討する。
- front matter `adr` 値の表記形式（`"ADR NNNN"` 文字列）を [[redaction]] でなく front matter 規約 SoT（ADR 0038 / 各 rule）側で 1 箇所に明記し、phase ごとの揺れを防ぐ。

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| 型チェック | pass | pass | — | tsc --noEmit（docs-only・非干渉） |
| カバレッジ | 100% | 100% | ±0 | Vitest |
| Biome 違反 | 0 | 0 | ±0 | biome check |
| CI | pass | pass | — | server-side pnpm verify 緑 |
| 差分 files/行 | — | 3 files +105/-1 | +104 | gh pr diff（ADR 1 + README 2） |

## 🤖 ハーネス改善提案

- [ ] `[rule]` [[adr]] に「改名・撤去を決める ADR が本文で旧パスを言及する場合、決定の主題以外は最小化し歴史的記述である旨を添える」運用ノートを追加（PR #170 の nit 由来）。
- [ ] `[template]` 計画駆動（非 learning 起点）phase の PR では「採用根拠（harness-meta-criteria）」節を簡略化し受け入れ基準充足に寄せるテンプレ分岐を検討（PR #170 で半数が N/A）。
- [ ] `[rule]` front matter の `adr` 値表記（`"ADR NNNN"` 文字列形式）を ADR 0038 / front matter 規約 SoT に 1 箇所明記し、phase 1-6 の付与で参照させる。

## 📝 harness-meta フィードバック

### 採用
（なし。本 learning の提案は下記理由で見送り）

### 見送り
- `[rule]` adr.md の旧パス言及最小化の運用ノート → 単発 nit。反復シグナルなく見送り。
- `[template]` 計画駆動 phase の採用根拠節テンプレ分岐 → 単発・テンプレ複雑化のコストが効果を上回る。見送り。
- `[rule]` front matter `adr` 値表記の SoT 明記 → **ADR 0038 が現行 SoT**（`"ADR NNNN"` 文字列配列形式を明示済）。front matter 細部の live rule 新設は規約確立直後で時期尚早（#176 と同主題でまとめて見送り）。

### 保留
